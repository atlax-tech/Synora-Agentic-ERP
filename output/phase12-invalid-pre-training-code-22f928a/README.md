# Phase 12 superseded training metadata

These artifacts were generated with training metadata inheriting the dataset-generation code version. They are retained as superseded evidence; active artifacts record the current training implementation revision while preserving the same dataset digest and seeds.
