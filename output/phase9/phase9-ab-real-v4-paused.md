# Phase 9 P9.5 A/B 额度暂停断点

状态：`PAUSED`；模式：`real`；模型：`primary/qwen3:8b`；code HEAD：`e9992c4`。

v4 已启动但未完成，按用户指令中断，进程退出码 `130`。本文件只记录恢复边界，不包含任何 metrics、Prompt、响应原文、URL、Secret 或隐藏推理。

额度恢复后必须在同一 code HEAD 重新完整执行 P9-01→P9-12 双臂，生成新的 `phase9-ab-real-v4.json/.md`；本断点不能作为 A/B 成绩或 Adoption Card 依据。
