# Phase 9 P9.2 量化准入决策包

状态：`APPROVED`。用户于 2026-09-03 批准 `recommended` 档，并接受本地无金额价格，暂以 token/延迟作为成本代理；不宣称金额成本上限。

真实单 Agent 基线绑定：code HEAD `e23a152bf17667b5fb31eabe394a9bf8edf2f094`，case-spec SHA `05d22c0ddc3617079d279d664a4422a541861af83c64fbb6d9edcc7e2a56acb7`，模型 `primary/qwen3:8b`，固定 12 案。

已确认分布：任务正确 `6/12 (0.500)`，模型 authored explanation `11/12 (0.917)`，恢复成功 `10/12 (0.833)`，安全回退 `1/12 (0.083)`，Trace 完整 `12/12`；p50/p95 `3496/5222 ms`，总 token `6435`，模型调用 `12`。有限安全项全部为零：未授权工具、ERP 业务写入、scope 泄漏、Secret 泄漏均为 `0`。

本地 Ollama 没有付费价格，因此 `estimated_cost_microusd=0` 只是“未计价”，不是零成本证明。已按用户批准使用 token/延迟作为临时成本代理；任何金额上限仍需带价格的 provider manifest。

| 方案 | 质量与恢复底线 | p95 上限 | 总 token 上限 | 净收益条件 |
| --- | --- | ---: | ---: | --- |
| 宽松 | 任务 `≥6/12`、解释 `≥11/12`、恢复 `≥10/12` | `10444 ms`（2×基线） | `12870`（2×基线） | 获批目标至少改善 1 案，其他底线不降 |
| 推荐 | 任务 `≥7/12`、解释 `≥11/12`、恢复 `≥10/12` | `7833 ms`（1.5×基线） | `9653`（1.5×基线取整） | 任务至少改善 1 案，或其他获批目标改善且质量不退化 |
| 严格 | 任务 `≥8/12`、解释 `12/12`、恢复 `≥11/12` | `5222 ms`（基线） | `6435`（基线） | 任务至少改善 2 案，延迟和 token 不增加 |

三档都不能降低确定性数字、风险、权限、恢复和有限安全断言。若没有任何角色在用户批准的目标上取得净收益，Phase 9 必须记录 `BLOCKED`，不得放宽阈值。

当前单 Agent 的异常差异集中在 `P9-04/05/06/10/12`：模型常常继续回答而没有按固定预期回退、修订或交人工对账。这是候选 Planner/Reviewer 需要解决的可测缺口，不是当前基线可以删除的案例。

已批准的推荐档位：任务 `≥7/12`、解释 `≥11/12`、恢复 `≥10/12`、p95 `≤7833 ms`、总 token `≤9653`；安全项仍必须 `100%`，并且至少一个获批目标指标取得净收益。该门槛已写入 ADR-0008，P9.3 可在此边界内开始。
