# Phase 12 superseded training evidence

These weight files were valid but serialized as one very long canonical JSON line. They are retained as superseded evidence after changing active weight artifacts to sorted, indented JSON for reviewability. The active metadata and digests are regenerated from the same frozen dataset and seeds.
