# Phase 12 invalidated report archive

The report files in `reports/` were generated before the second independent
adversarial review. They are retained for audit history, but are not canonical
Phase 12 evidence and must not be used for adoption decisions.

- `2e10b56`: report set generated before the first review fixes.
- `aca94ab`: report set generated before the second review fixes.

The active `output/phase12/` directory must contain at most one complete report
set whose summary matches the current manifest, records, and training metadata.
