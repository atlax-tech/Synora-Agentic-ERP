# Invalidated Phase 12 report set

- These three files were generated before implementation commit `4026fe4` and are preserved for audit.
- They are not the current report entry because their filename and summary binding use parent HEAD `fd3beea`.
- No experiment or provider call was rerun; the set is superseded by the report regenerated from the same immutable records at the final implementation HEAD.
