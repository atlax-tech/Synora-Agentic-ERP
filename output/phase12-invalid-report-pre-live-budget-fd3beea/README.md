# Superseded Phase 12 report set

This set used the old rubric cost aggregation and reported replay simulation
calls as Provider budget. It is retained unchanged; the active report counts
only live Provider calls and keeps replay/local evidence in separate rows.
