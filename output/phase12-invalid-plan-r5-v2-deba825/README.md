# Invalid Phase 12 plan archive

`phase12-plan-r5-v2` was frozen before the implementation-version compatibility fix.
The active output directory was later changed by source code, so this plan cannot bind
new live, training, or held-out evidence. It is retained for audit and is not included
in the active Phase 12 matrix.
