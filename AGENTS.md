# Synora Agentic ERP

Synora 是“Agent 开发岗位学习仓库 + 真实 ERP 实践载体”，在同一仓库内以业务应用层验证真实 ERP 价值，以教学实验层学习和对比 Agent 模式。这不代表两个仓库或两条长期分支。ERPNext/Frappe 仍是只读上游和事务事实源；不得修改上游核心、直写 ERP 数据库，或绕过权限、校验、Workflow 与审计。

## 阅读顺序

1. `docs/PLAN.md`：当前阶段、工作方式与停止条件；
2. `docs/项目方向纠偏.md`：已批准的学习定位、同仓双层结构与 Phase 4–13 路线；
3. 当前任务直接涉及的 `docs/PRD.md`、`docs/ARCHITECTURE.md`、`docs/DESIGN.md`、`docs/SPEC.md`；
4. 对应阶段的 `docs/development-log/`、ADR、源码和测试。

只读完整仓库不是默认动作。普通增量只加载当前业务链路所需上下文；ERP 行为不明确时再查固定上游源码和官方测试。

## 不可简化的边界

- 模型输出、检索内容、ERP 字段和用户输入都不可信。
- 业务写入必须经过 typed validation、policy、当前状态重检、明确授权、幂等和执行回执。
- 不得为实现方便删除已批准需求；延期项写入 Roadmap/SPEC。
- Mock 只能是明确允许的 test double，不能替代真实 ERP 集成完成度。
- 不得声称生产部署、客户采用或未被证据支持的收益。

## 文件所有权与保留边界

- 权威事实来源：`docs/PRD.md`、`docs/ARCHITECTURE.md`、`docs/DESIGN.md`、`docs/SPEC.md`、`docs/PLAN.md`；上游 ERP 行为以 `docs/source-maps/`、`docs/erp-baselines/` 的取证记录为准。
- 用户维护、禁止覆盖或未经确认不得改写的文件：`.env*`（含 `env/dev/.env`，API Key 只存在此处）、`README.md`/`README.zh-CN.md`（公开事实由用户确认后更新）、`.harness/`（由 harness-armor 流程管理，不手工改）。
- 未提交的修改、未合并的决策与未完成的验收不得写入权威文档冒充事实；延期与未决项必须显式标记。
- 工作区里未被本文件声明为"用户维护"的源文件，Agent 修改后必须保留 Git 历史并记录到开发日志，不得静默覆盖任何既有证据。

## 精简工作流

- 编码、修复、重构、代码审查和依赖选择前读取 `.agents/skills/ponytail/SKILL.md`，默认 `full`。
- 普通代码、页面和文案：执行 Agent 自测 + `ponytail-review`；不强制子 Agent。
- 身份、权限、金额、状态机、ERP 写入、幂等、审计和安全边界：增加一个独立 Test 或 Review；风险同时涉及多个边界时才两者都用。
- 阶段出口、发布、Tag、产品版本、依赖基线或固定 ERP/Frappe 版本变更：运行完整验证，并在阶段实现完成后自动安排一次独立对抗审查；审查结果不是 `PASS` 时不得写阶段通过报告或进入下一阶段。
- 先跑最相关检查，提交前再运行该增量必要的较宽检查；失败后只重跑受影响检查，阶段出口才全量运行。
- Harness 只在权威事实、命令、阶段状态或管理文件实际变化时同步；README 只在公开事实变化或阶段出口更新。
- 每个 commit 前只在当前阶段的一份日志顶部新增一轮记录，格式见 `docs/development-log/README.md`；不再为小修复创建独立日志文件。
- 默认把同一用户结果所需的页面/API/测试放进一个主业务提交；不把骨架、契约、Harness 和报告拆成多个无用户结果的提交。
- L2 默认最多调用一个独立角色；阶段出口的独立对抗审查必须在最终 diff 和全量证据之后执行，发现问题后由 Execute 修正并复验，最多两轮；第三轮仍失败时停止重复调用并以 `BLOCKED` 记录根因。
- 连续 30 分钟没有产生业务代码、可运行测试、已确认决定或明确阻塞证据时，立即停止并检查是否在重复读文档、重复审查或扩大范围。
- 文档改动明显多于业务代码和测试时必须说明原因；普通实现不得为了过程完整而扩写权威文档。
- 使用小而完整的 Conventional Commit；不混入用户文件，不推送，不改写历史。

## 实习生协作

默认把用户视为 Agent 开发实习生。适合学习的小任务应先作为练习交给用户，说明业务背景、代码入口、完成标准和测试设计；除非任务超出其能力、安全风险过高或用户明确要求接手，否则 Agent 只提示，不代写。

每个阶段步骤都必须先生成一个面向 Agent 开发岗位的 `Assignment`：说明业务背景、为什么需要、代码入口、输入/输出、完成标准、建议测试、不应修改的边界、提示梯度和面试追问。用户先尝试，Agent 以导师身份讲解、提示和审查；只有用户明确要求接手、任务超出当前能力，或涉及安全门禁/生产缺陷/阶段关键路径时才代写，并在日志中说明原因。

开发日志不按对话轮次更新。只在准备提交、阶段出口或用户明确要求留下 checkpoint 时，按 `docs/development-log/README.md` 记录该增量的业务结果、真实验证、风险和必要的 Assignment 状态。用户对话、问题、疑惑、阻塞和协作反馈默认不逐字写入开发日志；只有用户明确要求记录时才保留原文。阶段结束前必须根据本阶段真实改动定制项目/技术栈/Agent 开发问答，并给出作答提示；用户尚未作答时标记“待练习”，不得声称用户已经掌握。

必要时可在练习入口添加清晰的 `TODO(learning)` 提示，但不得把安全门禁、生产缺陷或阶段关键路径整体交给用户。用户明确要求记录的疑问、Agent 解释和最终结论，可在下一次正常日志更新时放入“大白话讲解”或“面试追问”部分。

只有用户明确要求记录某个问题、作答、答疑或阶段复盘内容时，才触发学习笔记流程。触发后必须先定位并读取 `docs/learning-notes/README.md`，按其中的命名、事实口径、回答结构和原文记录规则执行。未获得这种明确要求时，任务指令、规划偏好、问题、疑惑、对 Agent 工作方式的纠正、进度确认、执行交接和命令/测试输出都不得因对话发生而自动写入学习笔记或开发日志。对外面试表达默认业务价值优先，不主动暴露仓库学习定位、学习属性或实验分层；内部事实只作 Agent 校验依据，不在笔记中额外展开定位说明。纠偏或权威事实发生变化时，直接覆盖已经失效的回答，保留用户要求记录的原始问题，不把旧答案继续作为当前口径。学习笔记必须通过 `git ls-files` 确认已追踪；若本地 exclude 仍忽略目录，使用 `git add -f` 纳入 Git，禁止让学习证据停留在未追踪状态。

## 变更说明

每次交付先用大白话说明：解决什么业务问题、用户能看到什么、数据怎样流动、最重要的三个文件、怎样手工验证。随后再报告实际命令、退出码、限制和未运行检查。

验证入口：

```bash
make format-check
make lint
make type
make unit
make integration
python3 .agents/skills/harness-build/scripts/validate_harness_structure.py .
```

只运行与当前增量和风险相称的命令；不得把未运行命令写成通过。
