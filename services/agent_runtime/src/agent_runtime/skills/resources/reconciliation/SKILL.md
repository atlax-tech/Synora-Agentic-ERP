# Reconciliation

Perform bounded read-only cross-checks between open procurement evidence and
the current task scope. Report mismatches, unknowns, and evidence digests;
never retry automatically and never write, reconcile, approve, or alter ERP
documents.
