## 2026-09-12 · Phase 12 · 第 82 轮 · 阶段出口全量门禁

### 1. 本轮任务列表

- 执行 `make format-check`、`make lint`、`make type`、`make unit`、`make integration`。
- 执行 Phase 12 专项 pytest/mypy、artifact verify 和 Harness structure/manifest/reference/drift/health 只读检查。
- 在最终代码和 evidence 冻结后运行 ponytail-review、ponytail-audit、ponytail-debt，只处理本阶段相关阻塞。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 代码、数据、训练权重、回放、选择、报告和手册均已冻结；未新增功能。

### 4. 测试方式

- `make format-check`、`make lint`、`make type`、`make unit`、`make integration`
- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests -k phase12`
- `uv run --frozen --python 3.14 --group retrieval-lab mypy labs/self_improvement`
- `python3 .agents/skills/harness-build/scripts/validate_harness_structure.py .`
- `python3 .agents/skills/harness-check/scripts/validate_manifest.py .`
- `python3 .agents/skills/harness-check/scripts/check_references.py .`
- `python3 .agents/skills/harness-check/scripts/detect_drift.py .`
- `python3 .agents/skills/harness-check/scripts/score_harness_health.py .`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `git diff --check`

### 5. 测试结果

- format/lint/type/unit 全部退出 0；unit `1053 passed, 55 warnings`。
- integration 退出 0，隔离 bench 执行 `248 tests`。
- Phase 12 专项 `79 passed, 213 deselected`；专项 mypy 退出 0。
- Harness structure、manifest、references、drift 全部有效；references 检查 921 项无 broken reference；health `B / 87`；artifact verify `PASS`（records 600、weights 9、candidates 2、selections 2）。
- ponytail-review 对 Phase 12 diff 未发现可安全删除的复杂度；全仓 audit 结论为 `Lean already. Ship.`；debt 仅命中既有 `synora_agentic_erp/agent/service.py:56`，无新增 marker。

### 6. 用户如何进行手工测试

- 按 runbook 先运行 replay/训练短流程，再打开当前 report 和 Adoption Card，最后运行 `verify-artifacts`。

### 7. 风险预估与规避

- Harness 健康 B/87 不是阶段通过；独立对抗审查和 Harness 写同步仍未闭合。
- integration 只证明隔离开发 bench 的业务回归，不证明生产部署。

### 8. 下轮建议

- 以冻结 diff、原始测试输出、活动 evidence、风险登记和报告草稿启动第二轮独立对抗审查。

### 9. 本轮任务大白话讲解

- 全量门禁同时覆盖业务回归、实验专项、证据完整性和 Harness 一致性；所有检查都通过，但这只说明交付候选可审查，还要等独立审查。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 81 轮 · 干净进程 CLI 全链路验收

### 1. 本轮任务列表

- 在临时空实验根目录复制四个显式 Phase 11 失败源，按手册顺序复跑审核、数据、候选、回放、选择/回滚、训练、报告和验收。
- 捕获每条 CLI 命令的退出码、输出摘要和最终 artifact 统计。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 共 24 条命令在干净临时根目录执行；覆盖 `audit-data`、`prepare-data`、`make-candidates`、8 组 replay、`select-lab`、`rollback-lab`、9 组训练、`report` 和 `verify-artifacts`。

### 4. 测试方式

- 每条命令均使用 `uv run --frozen --python 3.14` 和独立 `--root`。
- 最终运行 `verify-artifacts` 并检查返回 JSON。

### 5. 测试结果

- 24/24 命令退出码为 0；最终统计为 8 个 record files、576 条 replay 记录、2 个 selection、9 个训练权重，状态 `PASS`。
- 临时数据集 digest 与仓库活动 digest 不同是因为 clean run 绑定当时 HEAD，分组和数量仍为 72/24/24。

### 6. 用户如何进行手工测试

- 按 `docs/phase12-cli-runbook.md` 从新的临时目录重跑；不配置 live provider 时不会产生网络调用。

### 7. 风险预估与规避

- clean run 只复制明确列出的 Phase 11 JSON，不递归吸入 output；训练和 replay 继续使用本地合成环境。

### 8. 下轮建议

- 执行阶段出口全量门禁，并把退出码逐项记录到冻结证据。

### 9. 本轮任务大白话讲解

- 这次不是在已有结果上点验，而是从一个几乎空的实验目录重新走完整流程；能全部返回 0，说明用户可以照手册复跑。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 80 轮 · 冻结统一 summary

### 1. 本轮任务列表

- 提交当前 HEAD 的统一 Phase 12 summary JSON。
- 保留方法聚合、held-out bootstrap、训练 metadata、奖励黑客负例、风险和约束。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `phase12-summary-aca94ab.json` 绑定当前代码版本 `aca94ab`，覆盖 600 条 replay、24 条 live、9 份训练 artifact 和 2 份 selection。

### 4. 测试方式

- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- JSON 解析、dataset digest、held-out 方向和训练权重 digest 核对。
- `git diff --check`

### 5. 测试结果

- summary 可读且与活动 evidence 一致；artifact 校验返回 `PASS`。

### 6. 用户如何进行手工测试

- 运行 `python3 -m json.tool output/phase12/phase12-summary-aca94ab.json`，再对照 report 中的方法表。

### 7. 风险预估与规避

- summary 只引用已生成 artifact，不创建新试验，也不把旧版本 report 覆盖掉。

### 8. 下轮建议

- 复跑干净临时目录全链路并记录每个 CLI 退出码。

### 9. 本轮任务大白话讲解

- summary 是机器可读的“证据目录”：它把数据、方法、训练和风险串在一起，但真正的权重和逐条记录仍保留在各自文件。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 79 轮 · 生成当前 HEAD 阶段报告

### 1. 本轮任务列表

- 从当前活动 replay、live、candidate、selection 和 training artifact 生成阶段报告与 Adoption Card。
- 将差值方向、Skill 退化、active pointer 和 live reservation ledger 写入报告，保持 `DRAFT / LAB_ONLY`。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 生成 `phase12-stage-report-draft-aca94ab.md`、`phase12-adoption-card-aca94ab.md` 和对应 summary；报告代码版本为当前 HEAD `aca94ab`，证据记录版本显式列出 `13872f1`、`41bcf93`。

### 4. 测试方式

- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli report`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- 报告 JSON 解析和 held-out 方向核对。
- `git diff --check`

### 5. 测试结果

- report 命令成功；summary 包含 600 条 replay 记录、9 份训练权重、2 份 selection，verify 返回 `PASS`。
- Prompt held-out 为 `INCONCLUSIVE`；baseline 相对 Skill 候选为正差值，报告明确解释为候选退化/拒绝。

### 6. 用户如何进行手工测试

- 打开当前 HEAD 后缀的阶段报告，先看方法表，再按 `method_a`/`method_b` 解释 bootstrap，最后运行 `verify-artifacts`。

### 7. 风险预估与规避

- 报告只汇总已有证据，不把 replay、loss 或预设奖励黑客负例写成生产收益；旧版本报告仍按版本号保留。

### 8. 下轮建议

- 复跑干净临时目录 CLI 全链路，随后进入停止新增功能的全量门禁。

### 9. 本轮任务大白话讲解

- 报告把“谁比谁好”写清楚：baseline 比 Skill 候选好，说明候选退化；Prompt 没有统计优势，所以保留基线。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 78 轮 · 冻结 REINFORCE seed 43 权重

### 1. 本轮任务列表

- 提交 REINFORCE seed 43 权重与 metadata，完成三 seed 环境训练证据。
- 核对其回报、成功率、步数和 SFT 起点绑定。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- artifact `phase12-train-reinforce-seed-43` 平均回报 `0.3774666666666666`，成功率 `0.7166666666666667`，平均步数 `1.96`，无效动作 0。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reinforce.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `git diff --check`

### 5. 测试结果

- seed 43 权重来自真实环境采样，参数更新和 JSON round-trip 均通过；九份训练 artifact 全部可验证。

### 6. 用户如何进行手工测试

- 查看 `weights-reinforce-43.metadata.json`，确认 `initial_weight_sha256` 对应 SFT seed 43，且最终 digest 不同。

### 7. 风险预估与规避

- 三个 seed 的成功率和回报有波动；任何回报上升但任务成功下降的情况都按副作用报告。

### 8. 下轮建议

- 重新生成并提交当前 HEAD 的 summary、stage report 和 Adoption Card，再执行完整 CLI 验收。

### 9. 本轮任务大白话讲解

- 最后一个 RL seed 也完成后，SFT、DPO、RL 三条本地训练链都有参数变化和环境指标，下一步只做证据汇总和收口。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 77 轮 · 冻结 REINFORCE seed 29 权重

### 1. 本轮任务列表

- 提交 REINFORCE seed 29 权重与环境统计。
- 保持从对应 SFT 起点开始，避免把 DPO 链串入 RL 对照。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- artifact `phase12-train-reinforce-seed-29` 平均回报 `0.43073333333333336`，成功率 `0.7433333333333333`，平均步数 `2.03`，无效动作 0。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reinforce.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `git diff --check`

### 5. 测试结果

- seed 29 环境更新和权重读回通过；累计 artifact 校验保持 `PASS`。

### 6. 用户如何进行手工测试

- 对照 SFT seed 29 metadata 的 digest 与 REINFORCE metadata 的初始 digest，再查看 episode 和成功率字段。

### 7. 风险预估与规避

- 移动 baseline 只使用此前 episode 的回报，不读取未来或 test 回报；训练结果按 seed 全部保留。

### 8. 下轮建议

- 提交 REINFORCE seed 43，完成 RL 三 seed 证据。

### 9. 本轮任务大白话讲解

- RL 训练中的 baseline 如果偷看当前或未来回报，会让指标失真；这里把它限定为历史移动平均，并在 metadata 里保留实际 episode 统计。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 76 轮 · 冻结 REINFORCE seed 17 权重

### 1. 本轮任务列表

- 提交从 SFT seed 17 冻结起点开始的 REINFORCE 权重与环境指标。
- 核对 episode 上限、gamma、平均回报、成功率、无效动作和步数。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- artifact `phase12-train-reinforce-seed-17` 使用 300 episodes、gamma `0.95`；平均回报 `0.39793333333333325`，成功率 `0.7266666666666667`，无效动作为 0。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reinforce.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `git diff --check`

### 5. 测试结果

- 环境真实采样产生梯度和参数更新；seed 17 权重可读回，artifact 校验通过。

### 6. 用户如何进行手工测试

- 查看 metadata 的 `initial_weight_sha256` 是否等于 SFT seed 17 digest，再核对 `episodes=300` 和成功率。

### 7. 风险预估与规避

- 回报函数与任务 verifier 分开，任何安全违规都不能被正向回报抵消；这是本地策略实验。

### 8. 下轮建议

- 提交 REINFORCE seed 29，比较不同 seed 的回报波动。

### 9. 本轮任务大白话讲解

- RL 的证据必须包含环境交互、梯度更新和任务指标；只把固定策略跑一遍不能叫 RL。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 75 轮 · 冻结 DPO seed 43 权重

### 1. 本轮任务列表

- 提交 DPO seed 43 权重与 metadata，完成三 seed DPO 证据组。
- 保留 seed 间 loss 波动和 reference 绑定信息。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- artifact `phase12-train-dpo-seed-43` 最终 dev DPO loss 为 `0.42355555295944214`；reference、初始 SFT 和最终 policy digest 均可追溯。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `git diff --check`

### 5. 测试结果

- seed 43 DPO 参数更新且 reference 未变；训练专项和活动 artifact 校验通过。

### 6. 用户如何进行手工测试

- 查看三份 DPO metadata 的 `reference_weight_sha256`，确认分别指向对应 SFT 起点而不是 test 或未来 checkpoint。

### 7. 风险预估与规避

- DPO 偏好准确率和任务成功率可能不同步；最终报告保持两者分开，不宣称通用提升。

### 8. 下轮建议

- 提交 REINFORCE 三个 seed，确认从冻结 SFT 起点开始并记录环境回报。

### 9. 本轮任务大白话讲解

- 三个 DPO seed 让我们看到偏好适配的稳定性边界；即使目标函数改善，也必须回到 verifier 看任务是否变好。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 74 轮 · 冻结 DPO seed 29 权重

### 1. 本轮任务列表

- 提交 DPO seed 29 权重与 metadata。
- 核对其 SFT 起点、reference 冻结和 dev 偏好 loss。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- artifact `phase12-train-dpo-seed-29` 最终 dev DPO loss 为 `0.41726866364479065`；reference digest 等于对应 SFT seed 29 digest。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `git diff --check`

### 5. 测试结果

- seed 29 DPO 参数更新、reference 不变和文件 digest 校验均通过。

### 6. 用户如何进行手工测试

- 读取 DPO seed 29 metadata，比较三项 weight digest；再运行 DPO equal-policy 测试确认标准目标的基准损失。

### 7. 风险预估与规避

- DPO 只使用同状态安全偏好对；无差异或不安全候选不进入训练。

### 8. 下轮建议

- 提交 DPO seed 43，完成三 seed 偏好适配证据。

### 9. 本轮任务大白话讲解

- DPO 的重点不是“loss 变小”四个字，而是偏好对安全、reference 固定、policy 参数确实更新，这三个条件同时成立。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 73a 轮 · 规范 DPO 日志标题

### 1. 本轮任务列表

- 修正上一轮开发日志标题的阶段/轮次格式，使其符合统一日志模板。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 仅修正文档标题，不改变 DPO artifact、代码或实验结果。

### 4. 测试方式

- `git diff --check`

### 5. 测试结果

- 日志标题格式检查通过。

### 6. 用户如何进行手工测试

- 查看日志顶部，确认阶段、日期和轮次字段统一。

### 7. 风险预估与规避

- 纯日志修正，不影响实验行为和证据 digest。

### 8. 下轮建议

- 继续提交 DPO seed 29。

### 9. 本轮任务大白话讲解

- 日志标题只是定位信息；修正它能让每个提交更容易和对应实验结果对上。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 73 轮 · 冻结 DPO seed 17 权重

### 1. 本轮任务列表

- 提交从 SFT seed 17 起步、冻结 reference 的标准 DPO 权重和 metadata。
- 核对偏好对数量、beta、dev loss 以及 reference digest 未变。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- artifact `phase12-train-dpo-seed-17` 使用 beta `0.1`，最终 dev DPO loss `0.40642958879470825`；reference 与 SFT seed 17 digest 相同。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `git diff --check`

### 5. 测试结果

- DPO 权重参数已更新，reference digest 保持不变，artifact 校验通过。

### 6. 用户如何进行手工测试

- 对照 metadata 中 `initial_weight_sha256`、`reference_weight_sha256` 和 `weight_sha256`，确认 reference 冻结而 policy 改变。

### 7. 风险预估与规避

- 偏好对先过安全门禁；DPO loss 下降不替代 verifier 或任务成功率。

### 8. 下轮建议

- 冻结 DPO seed 29，保留相同偏好目标。

### 9. 本轮任务大白话讲解

- DPO 比较同一状态下两个安全动作的偏好；关键证据是 reference 不动、policy 动，并且不能把 chosen 单边交叉熵冒充 DPO。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 72 轮 · 冻结 SFT seed 43 权重

### 1. 本轮任务列表

- 提交 SFT seed 43 的冻结权重和 metadata，完成三 seed SFT 证据组。
- 核对该 seed 的 dev loss、权重更新和有限 JSON 读回。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- artifact `phase12-train-sft-seed-43` 最终 dev loss 为 `0.16406655311584473`；参数更新、数据绑定和 digest 校验均记录。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `git diff --check`

### 5. 测试结果

- 三 seed SFT 权重均可加载；seed 43 专项测试和活动 artifact 校验通过。

### 6. 用户如何进行手工测试

- 查看三个 `weights-sft-*.metadata.json` 的 `initial_weight_sha256`、`weight_sha256` 与 dev loss，确认每个 seed 都有实际更新。

### 7. 风险预估与规避

- seed 43 loss 高于另外两个 seed，保留该波动，不通过挑选最好 seed 伪造稳定性。

### 8. 下轮建议

- 冻结 DPO seed 17/29/43，reference 固定对应 SFT 权重。

### 9. 本轮任务大白话讲解

- SFT 三个 seed 的差异本身就是证据：模型能学到动作，但训练结果有波动，所以结论只能是本地策略实验。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 71 轮 · 冻结 SFT seed 29 权重

### 1. 本轮任务列表

- 提交 SFT seed 29 的冻结权重和 metadata。
- 保持 seed、学习率、epoch 上限和早停规则与固定实验契约一致。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- artifact `phase12-train-sft-seed-29` 的最终 dev loss 为 `0.13240975141525269`，权重 digest 与文件一致，数据仍为冻结 manifest。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `git diff --check`

### 5. 测试结果

- seed 29 权重可读回且数值有限；训练专项测试和 artifact 校验通过。

### 6. 用户如何进行手工测试

- 对照 seed 17/29 metadata 的 loss 与 weight digest，确认重复 seed 之外的初始化和结果均有独立记录。

### 7. 风险预估与规避

- 单 seed 不能代表稳定提升；报告会汇总三个 seed，且不把 loss 下降直接写成业务成功。

### 8. 下轮建议

- 提交 SFT seed 43 后，再冻结从 SFT 起点派生的 DPO 和 REINFORCE artifact。

### 9. 本轮任务大白话讲解

- 多个 seed 是为了看结果是否依赖一次幸运初始化；这里每个 seed 单独留档，后面报告会把它们一起看。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 70 轮 · 冻结 SFT seed 17 权重

### 1. 本轮任务列表

- 提交当前无 oracle 初始状态下重新训练的 SFT seed 17 权重及 metadata。
- 核对初始化 digest、最终 digest、train/dev 数量和有限数值约束。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- artifact `phase12-train-sft-seed-17` 绑定数据集 digest 和 feature/action 版本；最终 dev loss `0.1315225064754486`，权重 digest 与文件一致。

### 4. 测试方式

- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- 新进程 `load_weights`/digest round-trip 检查。
- `uv run --frozen --python 3.14 pytest tests/test_phase12_training.py`
- `git diff --check`

### 5. 测试结果

- seed 17 权重可读回、参数已改变、所有数值有限；活动 artifact 校验返回 `PASS`。

### 6. 用户如何进行手工测试

- 读取 `weights-sft-17.metadata.json`，再用 CLI 训练测试或 `load_weights` 比较 `initial_weight_sha256` 与 `weight_sha256`。

### 7. 风险预估与规避

- 该权重是本地两层 MLP，不是业务语言模型；有限 JSON 校验阻止任意 pickle/脚本加载。

### 8. 下轮建议

- 提交 SFT seed 29，保持同一固定超参数和数据版本。

### 9. 本轮任务大白话讲解

- SFT 的证据不只是一条 loss，而是“初始参数不同、训练后参数真的变了、重启后还能读回同一组参数”三件事。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 69 轮 · 冻结 LAB 选择与回滚指针

### 1. 本轮任务列表

- 重新写入 Prompt 候选选择和回滚 receipt，绑定当前 dev evidence 与完整内容 digest。
- 固化 `active-version.json`，确认回滚后的实际内容是 native profile。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- SELECT receipt `phase12-selection-421cd7a2378be791` 和 ROLLBACK receipt `phase12-selection-7d455070e89d04a5` 均不可变；active 指针最终为 `native-agent/A`。

### 4. 测试方式

- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- 逐文件读取 receipt、比较 version digests 和 active pointer。
- `git diff --check`

### 5. 测试结果

- 两个 receipt、两个版本 digest 和 active 内容一致；artifact 校验返回 `PASS`。

### 6. 用户如何进行手工测试

- 运行 `rollback-lab` 后读取 `output/phase12/active-version.json`，再用 `load_active_policy` 回放，确认 native 内容恢复。

### 7. 风险预估与规避

- 选择与回滚只作用于 LAB 版本；receipt 使用不可覆盖创建，业务 Runtime 和 Registry 不加载 active 指针。

### 8. 下轮建议

- 按方法拆分提交九个训练权重和元数据，随后提交当前 HEAD 报告。

### 9. 本轮任务大白话讲解

- 回滚不只是把文件名改回去，而是重新核对父版本 digest 并更新 active 指针；这样重启后加载的确实是旧内容。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 68 轮 · 冻结 Skill 候选退化证据

### 1. 本轮任务列表

- 重新生成 Skill 候选 dev/test 三重复证据。
- 保留候选加载后的 verifier 退化，用于说明上下文指导不是自动收益。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 两个 Skill 候选 JSONL 各 72 条，绑定 `41bcf93`；dev/test verifier 通过数均为 24/72，失败记录保留。

### 4. 测试方式

- JSONL 逐行解析、失败分类、split 和候选来源核对。
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `git diff --check`

### 5. 测试结果

- Skill 候选安全率仍为 1.0，但 verifier 率约 0.333；活动 artifact 校验返回 `PASS`。

### 6. 用户如何进行手工测试

- 查看 Skill 候选记录中的 `VERIFIER_MISMATCH`，再对照 held-out bootstrap，确认基线相对候选为正差值。

### 7. 风险预估与规避

- Skill 只通过显式实验加载器进入策略，task profile 和工具白名单不变；业务 Skill 文件未修改。

### 8. 下轮建议

- 冻结候选选择、回滚指针及 active version 证据。

### 9. 本轮任务大白话讲解

- 这次故意保留了 Skill 候选的退化结果：它没有越过安全门禁，却降低了任务完成率，说明“更长的指导”可能带来副作用。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 67 轮 · 冻结 Prompt 候选对照

### 1. 本轮任务列表

- 重新生成 Prompt 候选 dev/test 三重复证据。
- 核对候选内容摘要已通过实验副本进入 policy，边界层和工具白名单未变化。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `evaluation-replay-prompt-candidate-dev.jsonl` 和 `evaluation-replay-prompt-candidate-test.jsonl` 各 72 条，均绑定 `41bcf93`；候选回放 verifier 全部通过。

### 4. 测试方式

- JSONL 逐行解析、候选 ID、split、代码版本和 verifier 统计。
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `git diff --check`

### 5. 测试结果

- dev/test 各 72 条且没有读取 held-out oracle；活动 artifact 校验返回 `PASS`。

### 6. 用户如何进行手工测试

- 对照候选 JSON 的 decision 层摘要和回放记录，确认只有实验副本使用候选内容，业务 Registry 文件哈希不变。

### 7. 风险预估与规避

- Prompt 候选仅可改 decision 层；boundary、recovery、output contract 和工具能力由边界 digest 保护。

### 8. 下轮建议

- 冻结 Skill 候选 dev/test，保留其预设退化结果作为无收益/副作用证据。

### 9. 本轮任务大白话讲解

- Prompt 候选的内容现在真正影响实验策略，但它仍被限制在决策指导层；这样可以把“提示写得更好”和“偷偷放宽权限”区分开。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 66 轮 · 冻结 Reflection 与 Best-of-3 对照

### 1. 本轮任务列表

- 重新生成 Reflection 和 Best-of-3 的 dev 三重复证据。
- 验证 Reflection 最多两次调用、Best-of-3 三次候选及统一 verifier 统计。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 两类方法各 72 条 dev 记录，代码版本为 `41bcf93`；Reflection 记录 144 次调用，Best-of-3 记录 216 次调用，均保留调用代价。

### 4. 测试方式

- JSONL 逐行解析、调用数和 verifier 统计。
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `git diff --check`

### 5. 测试结果

- 两类方法 verifier 均为 1.0；活动 artifact 校验返回 `PASS`。

### 6. 用户如何进行手工测试

- 查看两个 JSONL 的 `calls` 字段，确认 Reflection/Best-of-3 的额外调用与记录数量分开体现。

### 7. 风险预估与规避

- replay 结果仅说明确定性环境中的流程行为；候选选择仍由可观察门禁和后验 verifier 约束。

### 8. 下轮建议

- 冻结 Prompt 候选 dev/test 记录，并确认候选内容实际进入决策指导。

### 9. 本轮任务大白话讲解

- Reflection 是一次修订，Best-of-3 是三次候选再筛选；把调用次数和结果同时记下来，才能避免只看成功率而忽略成本。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 65 轮 · 冻结 baseline replay 证据

### 1. 本轮任务列表

- 依据修复后的 oracle-blind replay 代码重新生成 baseline 的 dev/test 三重复 JSONL。
- 检查记录数量、split、方法、代码版本和 verifier 结果，保留每条原始记录。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `evaluation-replay-baseline-dev.jsonl` 和 `evaluation-replay-baseline-test.jsonl` 各 72 条，均绑定代码版本 `41bcf93` 和当前数据集 digest；两组 verifier 全部通过。

### 4. 测试方式

- JSONL 逐行解析、记录数量和字段核对。
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `git diff --check`

### 5. 测试结果

- dev/test 各 72 条、三重复、split 正确；活动 artifact 校验保持 `PASS`。

### 6. 用户如何进行手工测试

- 查看两个 JSONL 的首尾记录，确认 `method=baseline`、`model=deterministic-replay`、`split` 和 `code_version` 一致。

### 7. 风险预估与规避

- 该回放只使用本地合成环境，不代表真实 Provider 质量；oracle 仍只在 verifier/评分侧使用。

### 8. 下轮建议

- 冻结 reflection 与 Best-of-3 的 dev 对照记录。

### 9. 本轮任务大白话讲解

- 基线记录是后面所有方法比较的尺子；每个场景重复三次并保留失败分母，才能看出候选究竟改变了什么。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 64 轮 · 修正预留账本状态类型

### 1. 本轮任务列表

- 修正 reservation ledger `states()` 的返回契约，使 CLI 能按 key/state 检查未收口预留。
- 增加状态形状回归测试，消除专项 mypy 对字典 items 类型的报错。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `states()` 现在只暴露 `(reservation_key, state)`，不泄露内部 batch 元组；实现与 CLI 账本检查一致。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests -k phase12`
- `uv run --frozen --python 3.14 --group retrieval-lab mypy labs/self_improvement`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement tests/test_phase12_*.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement tests/test_phase12_*.py`
- `git diff --check`

### 5. 测试结果

- 修正前专项 mypy 在 `evaluation.py:134` 失败；修正后专项 Phase 12 测试和 mypy 均通过。

### 6. 用户如何进行手工测试

- 在临时目录写入一个 `RESERVED` reservation，调用 CLI artifact 校验，确认状态仍按未收口处理。

### 7. 风险预估与规避

- 账本内部 batch 绑定仍由 ledger 校验；本轮只修复公开状态投影，不改变预算或重试策略。

### 8. 下轮建议

- 先提交本轮类型修复，再按类别冻结活动 evidence。

### 9. 本轮任务大白话讲解

- 之前状态查看函数把“批次信息”和“状态”一起返回，CLI 却按两个字符串使用，类型检查提前发现了这个契约错位；现在只返回 CLI 真正需要的键和状态。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 53 轮 · 全量验证与证据冻结

### 1. 本轮任务列表

- 停止新增功能，按阶段出口清单重跑格式、lint、类型、unit、integration、Phase 12 专项训练、artifact 和 Harness 检查。
- 执行 Phase 12 diff 的 ponytail-review、仓库 ponytail-audit 和 ponytail-debt 只读扫描，冻结最终代码/数据/报告/风险证据。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 源码实现冻结在最后代码提交 `2e10b56`；后续提交只增加活动/失效 evidence 和本轮日志，不改变业务代码。
- 活动 evidence 仍为 `DRAFT / LAB_ONLY`；独立对抗审查尚未启动，Harness 写同步尚未授权。

### 4. 测试方式

- `make format-check`
- `make lint`
- `make type`
- `make unit`
- `make integration`
- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests -k phase12`
- `uv run --frozen --python 3.14 --group retrieval-lab mypy labs/self_improvement`
- `python3 .agents/skills/harness-build/scripts/validate_harness_structure.py .`
- `python3 .agents/skills/harness-check/scripts/validate_manifest.py .`
- `python3 .agents/skills/harness-check/scripts/check_references.py .`
- `python3 .agents/skills/harness-check/scripts/detect_drift.py .`
- `python3 .agents/skills/harness-check/scripts/score_harness_health.py .`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `git diff --check`、`git status --short`

### 5. 测试结果

- format、lint、type 均返回 0；unit `1045 passed, 55 warnings`；integration `248 tests OK`。
- Phase 12 专项 `71 passed, 213 deselected`，专项 mypy 通过；artifact verify 为 `PASS`（records 600、weights 9、candidates 2、selections 2）。
- Harness structure、manifest、references、drift 均返回 0；引用检查 917 项无 broken reference；health 只读评分 B/87。
- ponytail-review 未发现 Phase 12 可安全删除的复杂度；debt 扫描仅命中既有 `synora_agentic_erp/agent/service.py:56`，未新增 Phase 12 marker。

### 6. 用户如何进行手工测试

- 从干净进程按 `docs/phase12-cli-runbook.md` 顺序执行 CLI；再打开 `output/phase12/phase12-stage-report-draft-2e10b56.md` 和 Adoption Card，最后运行 `verify-artifacts`。

### 7. 风险预估与规避

- 当前无已知 P0/P1；Provider 三重复 live 重跑仍是未完成限制，旧 24 条记录和 8 条 UNKNOWN 保留在分母。
- 统计成功选择、oracle/输入泄漏、奖励黑客、训练起点、回滚内容和预算均有活动/失效 evidence；风险登记中采用结论仍开放到独立 Review。
- 不把 Harness 健康 B/87 或全量业务回归通过解释为 Phase 12 PASS。

### 8. 下轮建议

- 以本轮冻结的完整 diff、原始输出、artifact、风险登记和报告草稿启动唯一一次独立对抗审查；根据结果最多两轮小步修复。

### 9. 本轮任务大白话讲解

- 全量测试说明现有业务没有被这批实验代码破坏；artifact 检查说明实验结果没有被篡改或超预算。复杂度扫描没有给出可安全删除的 Phase 12 代码，唯一债务是仓库原有固定上限注释。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 52 轮 · 归档失效与中间批次

### 1. 本轮任务列表

- 将候选来源未绑定、oracle 可见、随机起点 RL、旧 baseline 时序、旧权重格式、旧 usage/report 和最终冻结前的中间 artifact 归档。
- 为历史上缺少说明的完整 checkout 与 pre-final-freeze 目录补充失效原因 README。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 所有失效批次均位于 `output/phase12-invalid-*`、`output/phase12-pre-*` 或 `output/phase12-live-*`，活动目录未被覆盖；每个批次保留原始 JSON/JSONL 和原因说明。
- 失效证据不参与 `report` 或 `verify-artifacts` 的活动统计。

### 4. 测试方式

- `find output/phase12-invalid-* output/phase12-pre-* output/phase12-live-* -type f`
- 对活动目录运行 `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- 运行输出敏感模式、符号链接和业务 Runtime/`.env*` 变更扫描。
- `git diff --check`

### 5. 测试结果

- 活动 verify 返回 `PASS`；活动和失效输出均未发现符号链接或敏感模式命中。
- 代码 diff 仅限 Phase 12 实验模块、专项测试、契约/手册、开发日志和 output 证据；无业务 Runtime、ERP/Frappe 核心或 `.env*` 变更。

### 6. 用户如何进行手工测试

- 查看任一失效目录 README，确认原因与对应修复提交；再运行活动目录 `verify-artifacts`，确认失效文件不会被纳入记录统计。

### 7. 风险预估与规避

- 归档目录明确标记为不可用，避免把修复前结果当作当前结论；保留它们用于审查“失败如何被发现和修复”的链路。
- 不删除失败记录、不放宽校验、不将历史中间结果伪装成成功实验。

### 8. 下轮建议

- 停止新增功能，绑定最终 HEAD 冻结全量命令退出码、artifact digest 和风险登记，然后启动独立对抗审查。

### 9. 本轮任务大白话讲解

- 修复前的结果没有被擦掉，而是放进写明原因的“失效档案”。活动目录只放当前可复核证据，审查者仍能看到每次失败怎样被隔离。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 51 轮 · 冻结阶段报告与 Adoption Card

### 1. 本轮任务列表

- 以最终代码 `2e10b56` 读取活动 JSONL、训练 metadata 和固定 manifest，生成阶段报告、Adoption Card 与 JSON summary。
- 固化真实 assist 失败/UNKNOWN、replay 对照、2000 次 grouped bootstrap、训练指标、奖励黑客负例、风险登记和限制。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 活动报告明确标记 `DRAFT / LAB_ONLY`，不会把 replay、策略模型或一次 live 批次解释为生产能力或通用提升。
- Prompt/Skill held-out 差值均为 0、区间跨 0，结论为 `INCONCLUSIVE`；故意重复调用在错误奖励下得分 1.14 高于安全奖励 -1.36，但 verifier 失败，作为预设副作用负例。

### 4. 测试方式

- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli report`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_reporting.py`
- `git diff --check`

### 5. 测试结果

- `report` 和 `verify-artifacts` 均返回 0；verify 报告 manifest `5abe5145…19eb`、records 600、weights 9、candidates 2、selections 2。
- 报告写入 `phase12-stage-report-draft-2e10b56.md`、`phase12-adoption-card-2e10b56.md` 和对应 summary JSON；报告专项 4 个测试通过。

### 6. 用户如何进行手工测试

- 运行 `python -m labs.self_improvement.cli report` 后打开活动阶段报告，核对方法表、unknown usage、bootstrap 和 reward-hacking 字段，再运行 `verify-artifacts`。

### 7. 风险预估与规避

- 报告将 provider 记录与 replay usage n/a 分开，失败仍在分母；一次三重复 Provider 重跑中断及未写半批的事实保留在限制段。
- 统计与采用状态保持开放/草稿，独立对抗审查和 Harness 文件级同步授权未完成前不写 PASS。

### 8. 下轮建议

- 归档所有失效批次，运行最终全量验证和 ponytail 审计；证据冻结后启动唯一一次独立对抗 Review。

### 9. 本轮任务大白话讲解

- 报告把“分数变好”“调用成本”“模型是否真的完成任务”分开写。没有统计优势时就保留基线，奖励能骗分时也把 verifier 失败展示出来。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 50 轮 · 冻结 SFT、DPO 与 REINFORCE 权重

### 1. 本轮任务列表

- 在最终代码 `2e10b56` 下按 seed 17、29、43 重跑 SFT、标准 DPO 和 REINFORCE。
- 固化有限数值 JSON 权重、训练元数据、数据 digest、起始/最终权重 digest 和训练指标。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 活动目录包含 9 个权重与 9 个 metadata；SFT 和 DPO 均从固定 CPU MLP/SFT 起点更新，REINFORCE 使用同 seed SFT 起点、300 episode、gamma 0.95。
- metadata 的 `code_version` 为 `2e10b56`，权重可在新进程读回，旧版本权重已移入失效归档。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab python -m labs.self_improvement.cli train --method sft|dpo|reinforce --seed 17|29|43`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_training.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 --group retrieval-lab mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 9 次训练命令均返回 0；活动 `verify-artifacts` 返回 `PASS` 且权重数为 9。
- 训练与 REINFORCE 专项 11 个测试通过；训练损失、参数和 JSON 读回保持有限。

### 6. 用户如何进行手工测试

- 新进程运行 `train --method sft --seed 17`、随后 `train --method dpo --seed 17` 和 `train --method reinforce --seed 17`，再运行 `verify-artifacts`；应看到同一 dataset digest 和不同的 final weight digest。

### 7. 风险预估与规避

- 权重只接受有限 JSON envelope，拒绝 symlink、NaN、错误 shape、未知 key 和覆盖已有 artifact；不加载 pickle、脚本或远程代码。
- loss 下降与任务收益分开记录，REINFORCE 只使用本地合成环境，不能进入业务语言模型。

### 8. 下轮建议

- 冻结最终报告和 Adoption Card，归档全部失效批次，再进行最终出口证据与独立对抗审查。

### 9. 本轮任务大白话讲解

- 每个训练结果都带着“从哪份数据、哪个 seed、哪组初始参数来”的凭证。重新打开 JSON 后还能得到同样的模型，才算真实更新，不是只把 loss 写进报告。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 49 轮 · 冻结数据与评测记录证据

### 1. 本轮任务列表

- 冻结显式历史失败审核结果、120 案例的 grouped train/dev/test manifest、候选和选择回执。
- 在最终代码 `2e10b56` 下重生成 replay 对照，保留一轮真实 assist held-out 记录及其失败/UNKNOWN。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 活动目录包含审核 4 条、数据 120 条、候选 2 个、选择回执 2 个、评测 JSONL 9 个共 600 条记录；replay 文件均为三次重复。
- 候选评测带 `candidate_id`，选择回执带父/新版本 digest；live 记录保留旧代码版本和一次重复限制说明。

### 4. 测试方式

- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests -k phase12`
- 干净临时根目录完整 CLI 顺序复跑：`audit-data`、`prepare-data`、`make-candidates`、replay、`select-lab`、`rollback-lab`、三种训练、`report`、`verify-artifacts`。
- `git diff --check`

### 5. 测试结果

- 活动 evidence `verify-artifacts` 返回 `PASS`（manifest digest `5abe5145…19eb`、records 600、weights 9、candidates 2、selections 2）。
- Phase 12 专项 70 passed；干净 CLI 顺序复跑返回 `CLEAN_CLI_SEQUENCE=PASS`。

### 6. 用户如何进行手工测试

- 按 `docs/phase12-cli-runbook.md` 的 replay 命令复跑，确认每个活动 JSONL 为 72 行（24 案例 × 3 repeats），再运行 `verify-artifacts`。

### 7. 风险预估与规避

- 所有活动记录绑定同一 dataset digest；test 只保存冻结基线和候选，失败、UNKNOWN、调用数和 usage 未从分母删除。
- 失效或中间批次不覆盖活动文件，后续单独归档并标注无效原因。

### 8. 下轮建议

- 冻结最终代码版本下的 SFT、DPO、REINFORCE 权重和元数据，随后提交报告及失效批次归档。

### 9. 本轮任务大白话讲解

- 先把“用什么数据、跑了什么方法、结果对应哪个版本”锁成只读证据。以后即使修代码，也不会悄悄改写旧结果。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 48 轮 · 校验候选父版本一致性

### 1. 本轮任务列表

- 在 `select-lab` 写入选择回执前校验候选的固定父版本与命令指定的 previous version 一致。
- 增加父版本错配的 CLI 负向测试，防止跨 Prompt/Skill 信任边界选择。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 候选只有在自身 parent、命令 previous、边界 digest 和评测证据同时一致时才可进入 LAB 选择；业务 Registry 不受影响。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 --group retrieval-lab mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- CLI 专项 10 个测试通过；ruff、format、mypy 和 diff 检查均通过。

### 6. 用户如何进行手工测试

- 将 Prompt 候选以 `--previous-id skill-registry/v1` 选择，命令应返回 2，且不产生 selection JSON。

### 7. 风险预估与规避

- 选择前增加父版本一致性门禁，避免把候选内容挂到错误的 Prompt/Skill registry；旧回执仍不可覆盖。

### 8. 下轮建议

- 以当前代码、数据和训练结果冻结阶段 evidence，提交活动 artifact 与失效批次归档。

### 9. 本轮任务大白话讲解

- 候选文件自己说的“我从哪个版本改来”必须和选择命令的父版本对得上。对不上就拒绝，避免把 Skill 指导误挂到 Prompt 版本上。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 47 轮 · 对齐 live 只读动作白名单

### 1. 本轮任务列表

- 让 live baseline 与本地 replay 共用完整的六种只读工具、`ASK_INPUT` 和 `FINISH` 动作白名单。
- 增加评测入口回归检查，避免模型被错误缩窄到单一工具后把能力问题误判为模型失败。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `run_live_baseline` 与 `run_live_baselines` 现在均从 `READ_ACTIONS` 生成排序稳定的允许动作；未改变业务 Runtime 或 ERP 工具注册。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_evaluation.py tests/test_phase12_methods.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py tests/test_phase12_methods.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py tests/test_phase12_methods.py`
- `uv run --frozen --python 3.14 --group retrieval-lab mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 评测与方法专项 13 个测试通过；ruff、format、mypy 和 diff 检查均通过。

### 6. 用户如何进行手工测试

- 使用合成 live provider 记录请求 payload，确认 `allowed_actions` 包含六个只读工具、`ASK_INPUT` 和 `FINISH`，且排序稳定。

### 7. 风险预估与规避

- 白名单仍只包含只读动作和两个控制动作；不暴露写入口，不改变权限、policy 或工具能力。

### 8. 下轮建议

- 提交本轮修复后，归档旧训练/报告证据并按最终代码版本重新生成 artifact，再进入阶段全量验证。

### 9. 本轮任务大白话讲解

- live 实验如果只告诉模型一个工具，它即使想调查其他只读信息也会被门禁拦住，结果看起来像模型不行。现在 live 和 replay 使用同一份只读动作清单，比较才公平。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 46 轮 · 统一损坏权重错误

### 1. 本轮任务列表

- 捕获权重 JSON 转换中的底层张量类型/形状异常，统一返回契约级 `ValueError`。
- 增加字符串权重值的负向读回测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 任意损坏权重都在模型加载边界被拒绝，不会以未分类异常进入 CLI 或报告。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 训练专项 8 个测试通过；非法 schema、字符串值、NaN 和维度损坏均返回 `ValueError`。

### 6. 用户如何进行手工测试

- 改写任一权重 `state_dict` 值为字符串或错误维度，运行 `verify-artifacts`，应返回 2 并指出权重契约错误。

### 7. 风险预估与规避

- 只在 JSON→tensor 边界捕获已知转换异常，不放宽有限值、shape 或未知 key 检查；不执行任意 artifact 内容。

### 8. 下轮建议

- 停止功能改动，归档并重生成当前代码版本 report，然后执行 P12.16 全量验证和证据冻结。

### 9. 本轮任务大白话讲解

- 坏权重不能因为底层库抛了别的异常就绕过实验门禁。统一成契约错误后，CLI 能给出稳定退出码，审计也能按同一类问题统计。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 45 轮 · 拒绝非法预算起点

### 1. 本轮任务列表

- 为 `CallBudget` 增加 maximum、used 和连续阻塞计数的初始化校验。
- 增加超过 1,200 次及已用量超过上限的负向测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- live 批次重启或手工构造非法预算状态会在 Provider 调用前失败，累计预算不会被绕过。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 评测专项 9 个测试通过；非法 maximum/used 状态返回 `ValueError`，静态检查通过。

### 6. 用户如何进行手工测试

- 构造 `CallBudget(maximum=1201)` 或 `CallBudget(maximum=1, used=2)`，确认不会创建可运行的批次。

### 7. 风险预估与规避

- 校验只限制内存中的批次状态，不自动重置历史调用；已有 JSONL 仍由 `verify_records` 汇总。

### 8. 下轮建议

- 停止新增功能，进行最终全量测试、artifact/报告重算和出口证据冻结。

### 9. 本轮任务大白话讲解

- 预算像一个总账，账本一开始就超支时不能先开工再补解释。现在批次创建就检查上限和已用量。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 44 轮 · 区分 usage 未知与不适用

### 1. 本轮任务列表

- 将 Provider 记录的未知 usage 与 deterministic replay 的 usage 不适用分开统计。
- 扩展阶段报告表格和 reporting 测试，避免把本地零调用误报为外部用量缺失。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `unknown_usage_records` 只计实际 Provider 调用；replay 记录新增 `usage_not_applicable_records`。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- reporting 专项通过；replay usage unknown=0、usage n/a=24，live 仍按实际已知/未知 usage 统计。

### 6. 用户如何进行手工测试

- 运行 `report`，检查 deterministic 方法的 usage n/a 等于记录数，`live-baseline` 的 unknown usage 等于未报告 usage 的记录数。

### 7. 风险预估与规避

- 只改汇总口径和表格，不改变原始记录、预算或 Provider 调用；原始 usage 字段保持不可变。

### 8. 下轮建议

- 重新生成带当前 reporting 代码版本的 report，再进行全量阶段门禁。

### 9. 本轮任务大白话讲解

- 没调用 Provider 时没有“未知用量”，而是“用量不适用”。分开后，报告不会让人误以为 replay 有隐藏成本或缺失计费数据。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 43 轮 · 绑定训练实现版本

### 1. 本轮任务列表

- 训练元数据改为记录当前 Git 实现版本，同时继续绑定冻结 dataset digest。
- 保持 SFT、DPO、REINFORCE 的 seed、权重和指标配置不变。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 训练 artifact 能区分“训练实现版本”和“数据生成版本”；旧元数据后续归档，活动权重需重写。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_training.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 训练专项测试、静态检查和类型检查通过；元数据代码版本由当前 Git HEAD 生成。

### 6. 用户如何进行手工测试

- 运行任一 `train` 命令，检查 metadata 的 `code_version` 等于命令执行时 HEAD，`dataset_digest` 仍等于冻结 manifest。

### 7. 风险预估与规避

- 版本记录只读 Git revision，不读取 secret；重写权重前先归档旧 metadata，避免覆盖历史证据。

### 8. 下轮建议

- 归档当前 9 份训练 artifact，按新元数据绑定重跑，再生成最终阶段报告。

### 9. 本轮任务大白话讲解

- 数据版本回答“拿哪批题训练”，代码版本回答“用哪套训练逻辑”。两者分开记录，出现问题时才知道该复现哪一个。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 42 轮 · 改为可审阅的权重 JSON

### 1. 本轮任务列表

- 将训练权重从巨大单行 JSON 改为排序稳定、缩进可审阅的 JSON。
- 保持权重 digest 基于 canonical payload 不变，并增加多行格式断言。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 权重 artifact 不再通过单行压缩隐藏变更；读回、有限值和 digest 校验保持一致。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 训练专项 7 个测试通过；权重文件包含可审阅的多行 JSON，digest round-trip 和非有限值拒绝通过。

### 6. 用户如何进行手工测试

- 查看任一 `output/phase12/weights-*.json`，确认 `state_dict` 分层换行；用 `verify-artifacts` 确认读回 digest 仍一致。

### 7. 风险预估与规避

- 格式改变不改变 canonical digest；旧单行权重归档为失效证据，新的权重必须重新写入且不覆盖。

### 8. 下轮建议

- 归档当前训练权重并按固定 seed 重跑，再生成最终 report/summary 和出口命令证据。

### 9. 本轮任务大白话讲解

- 实验 artifact 既要能读回，也要方便审查。把权重展开成多行后，reviewer 能看到每层结构，摘要哈希仍保持可复现。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 41 轮 · 修正 REINFORCE baseline 时序

### 1. 本轮任务列表

- 使用当前 episode 更新前的移动 baseline 计算 REINFORCE advantage。
- 增加每个 seed 120 秒墙钟门禁，超时不保存成功 checkpoint。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- RL 训练不再把当前或未来回报混入 baseline；每个 seed 的 episode 上限与墙钟上限均有代码门禁。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_reinforce.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- REINFORCE 和训练专项测试通过，固定 seed 仍可复现；静态检查通过。

### 6. 用户如何进行手工测试

- 用短 episode 运行 `train_reinforce`，在第一轮检查 advantage 等于折扣回报（初始 baseline 为 0），第二轮才使用上一轮更新后的 baseline。

### 7. 风险预估与规避

- 超时或异常发生在写 artifact 前，既有权重不覆盖；本轮修复后的 RL 权重必须重新生成。

### 8. 下轮建议

- 归档当前 RL 权重并按修复后的代码重跑三个 seed，再更新 report/summary。

### 9. 本轮任务大白话讲解

- baseline 是用来降低方差的“过去成绩均值”，不能偷看本轮甚至测试集成绩。现在先用旧均值算梯度，再把本轮成绩纳入下一轮。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 40 轮 · 固化 CLI 复跑手册

### 1. 本轮任务列表

- 新增 `docs/phase12-cli-runbook.md`，列出数据审核、候选、replay/live、选择回滚、SFT/DPO/REINFORCE 和 artifact 验收命令。
- 明确默认命令不联网、错误码、预算占用、Provider 中断和 LAB_ONLY 边界。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 阶段 CLI 入口具备可复制的端到端操作说明；手册不把当前 DRAFT 证据写成生产能力。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- CLI 专项、ruff、format 和 diff 检查通过；手册只引用已实现的命令和当前限制。

### 6. 用户如何进行手工测试

- 按手册顺序在干净临时目录执行 replay 短流程；最后运行 `verify-artifacts`，应返回结构化 `PASS`。

### 7. 风险预估与规避

- live 命令单独标为显式联网入口，不在默认复跑中触发；环境变量只从用户维护文件读取，不复制或打印。

### 8. 下轮建议

- 归档当前证据状态，执行完整 Phase 12 专项/仓库验证和 Harness 只读检查，冻结最终 diff。

### 9. 本轮任务大白话讲解

- 复现实验最怕“少了一步”或误把默认命令跑到线上。手册把每条命令、输入目录、错误码和联网开关写清楚，别人可以照着重跑。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 39 轮 · 内容级版本回滚门禁

### 1. 本轮任务列表

- 为 LAB 选择回执增加父版本和新版本的内容 digest。
- 选择、回滚和 `verify-artifacts` 重新读取当前版本内容，拒绝 digest 已变化的版本。
- 增加篡改候选后回滚失败的测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- A→B→A 的回滚证据现在包含可重算的内容摘要；旧选择文件仍不可覆盖。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_skill_selection.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/candidates.py labs/self_improvement/contracts.py labs/self_improvement/cli.py tests/test_phase12_skill_selection.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/candidates.py labs/self_improvement/contracts.py labs/self_improvement/cli.py tests/test_phase12_skill_selection.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 选择、回滚、来源和 CLI 验收测试通过；候选内容被篡改时回滚返回非零错误。

### 6. 用户如何进行手工测试

- 运行 `select-lab` 后修改候选 JSON 内容，再运行 `rollback-lab`；命令应报告版本内容 digest 变化并拒绝写回滚回执。

### 7. 风险预估与规避

- Native Prompt 和 Skill registry 也计算当前内容摘要；摘要仅用于一致性验证，不代表签名或授权。

### 8. 下轮建议

- 按当前代码版本归档 superseded replay/report 文件，重跑候选和训练的短流程并更新阶段报告。

### 9. 本轮任务大白话讲解

- 真正的回滚要确认恢复的是同一份内容，单看版本名字不够。现在回执保存摘要，内容被改过就不能假装回滚成功。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 38 轮 · 绑定候选评测 artifact

### 1. 本轮任务列表

- 为 `ExperimentRecord` 增加可选 `candidate_id` 追踪字段。
- 候选 replay 评测前强制读取对应唯一候选，`verify-artifacts` 校验方法、候选类型和来源一致。
- 更新 CLI 测试覆盖缺少候选和未绑定来源的失败路径。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- Prompt/Skill 评测结果现在可回溯到具体候选内容；缺失或错配 artifact 会被拒绝。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_cli.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/cli.py labs/self_improvement/contracts.py labs/self_improvement/evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/cli.py labs/self_improvement/contracts.py labs/self_improvement/evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- CLI、评测、ruff、format 和 mypy 均退出 0；候选 replay 记录写入对应 `candidate_id`。

### 6. 用户如何进行手工测试

- 先运行 `audit-data`、`make-candidates`、候选 `evaluate`，检查 JSONL 每条候选记录的 ID 与候选文件名一致；删掉候选后 `verify-artifacts` 应失败。

### 7. 风险预估与规避

- 候选文件只读自 `output/phase12/candidates`，不会加载业务 Registry；历史来源仍经 audit 重新核对。

### 8. 下轮建议

- 归档 superseded report，按最终代码版本重新生成 replay、训练与阶段报告，再冻结出口证据。

### 9. 本轮任务大白话讲解

- 实验表里写着“Prompt 候选”不代表真的加载了候选。现在每条记录都带具体候选 ID，验收还会检查这个文件和来源确实存在。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 37 轮 · 固定标准 DPO 损失

### 1. 本轮任务列表

- 抽取标准 DPO 偏好损失并让 SFT 初始化的 DPO 训练路径复用该实现。
- 增加策略与 reference 相同情况下损失接近 `log(2)` 的数值断言。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- DPO 的 chosen/rejected 对数概率差、冻结 reference 和 beta=0.1 目标均由同一函数实现并可独立验证。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 训练专项 7 个测试通过；相同 policy/reference 的 DPO loss 与 `ln(2)` 差值小于 `1e-6`，静态检查通过。

### 6. 用户如何进行手工测试

- 用同一份本地 SFT 模型作为 policy 和 reference 调用 `dpo_loss`，检查输出约为 0.693147；改变 chosen/rejected 后再观察更新方向。

### 7. 风险预估与规避

- reference logits 在训练前置于无梯度路径并记录 digest；不安全偏好对仍由既有安全门禁排除。

### 8. 下轮建议

- 让候选评测强制读取对应候选 artifact，并在记录中保留 candidate ID，防止只改变方法标签而未绑定候选内容。

### 9. 本轮任务大白话讲解

- DPO 比较的是同一状态下“更好答案”和“较差答案”相对 reference 的偏好差。两者完全一样时没有偏好，损失应正好落在 `log(2)` 附近。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 36 轮 · 隔离模型输入标签

### 1. 本轮任务列表

- 增加 provider 可见任务文本投影，移除内部 `Scenario` 类别标签。
- 让单次和批量 live 调用统一使用投影，并验证期望动作、状态不会进入模型输入。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 模型输入与评分侧标签的边界已显式化；环境仍保留类别用于本地状态转移和 verifier。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_data.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/data.py labs/self_improvement/evaluation.py tests/test_phase12_data.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/data.py labs/self_improvement/evaluation.py tests/test_phase12_data.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 六类数据的投影均不含 `Scenario=`、期望动作或期望状态；live 调用与既有评测测试保持通过。

### 6. 用户如何进行手工测试

- 用任一合成案例调用 `model_input_text`，检查输出仍保留任务描述但没有内部场景标签和评分答案。

### 7. 风险预估与规避

- 投影只影响 provider 文本，不修改冻结数据 manifest、oracle 或本地 verifier；输入仍受 4,000 字符上限约束。

### 8. 下轮建议

- 暴露并测试标准 DPO 损失的 reference 等价性，再重新生成报告。

### 9. 本轮任务大白话讲解

- 数据分类是实验人员用来组织场景的标签，不能偷偷变成模型的答案提示。现在发给 provider 前会去掉这段内部标签。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 35 轮 · 校验候选历史来源

### 1. 本轮任务列表

- 在 `verify-artifacts` 中核对候选的 `source_case_ids` 是否属于当前已复核历史失败清单。
- 增加篡改候选来源的负向 CLI 测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 候选来源追溯门禁已补齐；未知历史来源现在会使 artifact 验收非零退出。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 候选来源被替换为未审核 ID 时，`verify-artifacts` 返回 2；正向 CLI、ruff、format 和 mypy 均应退出 0。

### 6. 用户如何进行手工测试

- 先运行 `audit-data`、`make-candidates`，再改写候选 JSON 的 `source_case_ids`；运行 `verify-artifacts` 应报告未审核历史失败来源。

### 7. 风险预估与规避

- 复核清单仍由显式 Phase 11 来源白名单重新计算，篡改 audit 或候选都不会被 artifact 验收接受。

### 8. 下轮建议

- 再补 DPO 标准损失的 reference 等价性断言，并审查候选评测是否加载了实际候选 artifact。

### 9. 本轮任务大白话讲解

- 候选说自己来自某次失败还不够，验收必须能在当前复核清单里找到这个失败。现在来源 ID 对不上就直接拒绝。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

# Phase 12 开发日志

## 2026-09-12 · Phase 12 · 第 34 轮 · 拒绝非有限训练权重

### 1. 本轮任务列表

- 在 JSON 权重写出前检查每个张量的有限性。
- 增加 NaN 权重写入失败测试，防止损坏模型 artifact 被保存为“训练成功”。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.10/P12.13 权重安全门禁已闭合；合法权重读回路径保持不变。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_training.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 9 个训练测试通过；NaN 写入返回 `ValueError`，ruff、format 和 mypy 均退出 0。

### 6. 用户如何进行手工测试

- 构造一个参数含 NaN 的本地模型并调用 `save_weights`，命令应在写文件前失败且目标文件不存在。

### 7. 风险预估与规避

- 有限性检查只作用于实验 JSON，不加载 pickle、脚本或远程代码；现有已生成权重仍由 verify-artifacts 逐个读回。

### 8. 下轮建议

- 重新执行当前代码版本的报告和 artifact verify，之后进入阶段末独立审查准备。

### 9. 本轮任务大白话讲解

- NaN 会让模型看似有文件、实际上无法稳定预测。现在保存前就拦截，坏权重不会进入可复现证据目录。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 33 轮 · 完善 Provider 阻塞分类

### 1. 本轮任务列表

- 将 `TRANSPORT_ERROR`、`TIMEOUT` 和单调用超时纳入连续阻塞失败门禁。
- 用真实 Provider 常见的 `TRANSPORT_ERROR` 注入测试连续三次后停止批次。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.4 的连接/认证/协议故障分类覆盖了 Provider 实际返回的传输错误；预算仍在请求前占用。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 8 个评测测试通过；连续第三次传输失败后第四次不再调用 Provider，ruff、format 和 mypy 均退出 0。

### 6. 用户如何进行手工测试

- 使用故障注入 Provider 连续返回 `TRANSPORT_ERROR`，确认批次状态为 `MODEL_BATCH_BLOCKED` 且已用调用数保持 3。

### 7. 风险预估与规避

- 只扩充错误分类集合，不重试、不切换 Provider、不重置累计预算。

### 8. 下轮建议

- 重新生成当前代码绑定的报告，随后冻结最终 artifact 并执行一次独立对抗审查。

### 9. 本轮任务大白话讲解

- Provider 报“传输失败”时，和连接断开一样都说明继续请求没有意义。把它纳入三次熔断，既省预算，也不会把环境故障误算成模型能力。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 32 轮 · 显式记录证据代码版本

### 1. 本轮任务列表

- 在 summary/report 中列出所有评测记录实际绑定的代码版本。
- 说明修复重跑可能产生多版本证据，避免报告标题版本被误读为每条记录版本。
- 增加报告字段回归断言。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.14/P12.16 证据版本追踪已补齐；数据 digest、评测记录版本、训练 metadata 和报告版本可以分别核对。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 4 个报告测试通过；报告包含 `evidence_code_versions` 行，ruff、format 和 mypy 均退出 0。

### 6. 用户如何进行手工测试

- 打开 `phase12-summary-<code>.json`，对照 `evidence_code_versions` 与各 JSONL 的 `code_version` 字段。

### 7. 风险预估与规避

- 只增加透明度字段，不改写旧记录或把不同版本的结果合并为同一 trial。

### 8. 下轮建议

- 重新生成当前报告，继续冻结输出目录和独立审查输入。

### 9. 本轮任务大白话讲解

- 报告文件名只代表生成报告的代码版本，里面的记录可能来自修复前后的不同批次。把记录版本列出来，审查者不用猜每一行是哪次运行。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 31 轮 · 隔离 rerank 的 oracle 门禁

### 1. 本轮任务列表

- 将 Reflection 和 Best-of-N 的候选门禁改为只读取安全、终止和观察证据。
- rerank 排序改为观察数量、步数和生成顺序，oracle 只在最终选出后评分。
- 增加“可观察候选被 oracle 判错仍可进入候选集”的回归测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.5/P12.6 的 oracle 泄漏风险已修复；旧 Reflection/Best-of-N 原始 JSONL 需要归档并按新门禁重跑。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py tests/test_phase12_methods.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 12 个评测/方法测试通过；错误期望动作的 `ASK_INPUT` 候选仍能被 rerank 返回，但其最终 verifier 结果保持失败，证明筛选没有读答案。

### 6. 用户如何进行手工测试

- 对同一案例运行 `rerank_candidates`，传入一个结构安全但期望动作错误的候选；候选应先被保留，再由最终 verifier 标记失败。

### 7. 风险预估与规避

- rerank 不读取 `expected_action`、`expected_status` 或 `oracle`；失败原因 `VERIFIER_MISMATCH` 也不会被当作结构失败。

### 8. 下轮建议

- 提交本轮修复后重建 Reflection/Best-of-N dev/test 证据，再继续 Provider 故障分类审计。

### 9. 本轮任务大白话讲解

- 选候选时只能看候选自己带来的证据，不能偷看标准答案。现在即使候选最后答错，也会先按安全结构参与选择，选完后才由 verifier 告诉实验统计它是否正确。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 30 轮 · 记录 live 重复批次中断限制

### 1. 本轮任务列表

- 将三次重复 assist held-out 重跑的中断事实写入阶段报告限制区。
- 保留已完成的一次重复 24 条 JSONL，明确中断批次没有半批结果可用于评分。
- 增加报告文字回归断言，避免把一次重复误写成完整三次重复证据。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.14 的真实 Provider 证据保持可追溯；由于长连接无响应，中断重跑不计入成功或失败分母，阶段采用结论仍为限制项。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`

### 5. 测试结果

- 4 个报告测试通过；ruff、format、mypy 和 artifact verify 均退出 0。
- 当前真实 evidence 是 24 条一次重复记录（3 SUCCEEDED、13 REJECTED、8 UNKNOWN）；中断的三次重复命令没有生成 JSONL。

### 6. 用户如何进行手工测试

- 查看阶段报告“限制与未运行项”，确认它同时写出一次重复的真实结果和三次重复重跑未完成的原因。

### 7. 风险预估与规避

- 不把被中止批次的未知预算或响应猜成 usage；原始一次重复文件保持不可变，未生成伪造补齐行。

### 8. 下轮建议

- 归档旧报告并生成当前代码绑定的限制口径，继续完成候选/训练 artifact 冻结后再启动独立 Review。

### 9. 本轮任务大白话讲解

- 一次请求卡在网络里时，最危险的做法是把它当失败或补一条默认结果。这里明确记成“批次未完成”，只用已经写完的 24 条，证据边界才不会被扩大。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 29 轮 · 固定 REINFORCE 的 SFT 起点

### 1. 本轮任务列表

- 将 REINFORCE API 改为显式接收同 seed 的 SFT 模型。
- CLI 缺少 SFT checkpoint 时拒绝启动 RL，避免随机起点冒充可比实验。
- 增加初始权重哈希一致性测试并更新测试替身调用。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.13 的训练起点约束已闭合；REINFORCE 仍独立于 DPO，参数在本地环境中真实更新。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reinforce.py tests/test_phase12_training.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py labs/self_improvement/cli.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py labs/self_improvement/cli.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 16 个训练/CLI 测试通过；初始 REINFORCE 权重哈希与 SFT 哈希一致，训练后哈希改变；ruff、format 和 mypy 均退出 0。

### 6. 用户如何进行手工测试

- 先对同一 seed 执行 `train --method sft --seed 17`，再执行 `train --method reinforce --seed 17`；查看两个 metadata 的 `initial_weight_sha256` 和 SFT `weight_sha256`。

### 7. 风险预估与规避

- RL 不会隐式训练或覆盖 SFT 文件；缺失起点直接报错，旧权重不可覆盖。

### 8. 下轮建议

- 归档旧的随机起点 RL 权重，按冻结 SFT 起点重建三 seed 权重，并重新验证 artifact。

### 9. 本轮任务大白话讲解

- 如果 RL 从随机权重开始，就无法知道提升来自 SFT 还是来自 RL。现在两者从同一个已保存模型出发，差异才有可解释性。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 28 轮 · 重算审核 artifact 来源哈希

### 1. 本轮任务列表

- 让 `verify-artifacts` 重新执行显式历史来源审核，并逐条比对导出的 ReviewedCase。
- 让候选生成复用同一审核结果，防止篡改审核 JSON 后伪造来源 ID。
- 增加审核记录篡改的失败路径测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.1/P12.15 来源完整性门禁已闭合；原始来源、sha256、脱敏状态和审核枚举必须一致。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 8 个 CLI 测试通过；篡改 `review_reason` 后 `verify-artifacts` 返回 2；ruff、format 和 mypy 均退出 0。

### 6. 用户如何进行手工测试

- 修改 `output/phase12/audited-failures.json` 任一审核字段后执行 `verify-artifacts`，命令应拒绝并保留原文件，不生成新的候选。

### 7. 风险预估与规避

- 校验只读取明确的 Phase 11 allowlist，不递归读取 output，也不把哈希当作签名或授权。

### 8. 下轮建议

- 归档旧候选/选择回执后重建审核来源绑定 artifact，重新生成报告并做最终 verify。

### 9. 本轮任务大白话讲解

- 审核结果本身也是输入，不能因为它位于 output 就自动可信。现在每次验收都会回到四个明确源文件重算，文件被改过就立刻停在门禁处。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 27 轮 · 绑定失败审核来源到候选

### 1. 本轮任务列表

- 让 `make-candidates` 只从已写入的审核 artifact 读取来源。
- 没有可接受或背景可追溯失败时返回非零，不再使用无来源的 `phase12-synthetic` 占位。
- 删除不再生效的 `--source` CLI 参数，并补充成功与阻塞路径测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.1/P12.7 的失败驱动链路已收紧；候选现在引用 `phase12-historical-*` ID，仍只写实验目录。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py tests/test_phase12_candidates.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 11 个 CLI/candidate 测试通过；ruff、format 和 mypy 均退出 0。
- 未审核来源时命令退出 2；写入最小可追溯历史失败后候选生成成功且来源 ID 通过断言。

### 6. 用户如何进行手工测试

- 先执行 `python -m labs.self_improvement.cli audit-data`，再执行 `make-candidates`；打开候选 JSON，确认 `source_case_ids` 是历史审核记录 ID。

### 7. 风险预估与规避

- 候选作者只能读取审核后的脱敏摘要；没有通过审核的输入不会进入候选生成，test 答案仍不可见。

### 8. 下轮建议

- 归档旧的无来源候选与选择回执，按新的审核来源重建候选并更新 report。

### 9. 本轮任务大白话讲解

- 失败驱动不是给候选随便贴一个标签。现在 CLI 必须先找到审核记录，再把那条记录的 ID 带进候选，这样以后才能沿着 ID 回到原始哈希和审核结论。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 26 轮 · 全量门禁与复杂度审计

### 1. 本轮任务列表

- 执行仓库格式、lint、类型、unit、integration 和 Phase 12 专项训练检查。
- 执行 Harness 结构、manifest、引用和 drift 四项只读检查。
- 完成 ponytail-review、ponytail-audit 和 ponytail-debt 扫描，冻结当前实现风险清单。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.16 技术门禁已通过；阶段报告仍为 `DRAFT / LAB_ONLY`，独立对抗审查和 Harness 写入授权尚未开始。
- 复杂度扫描未发现本阶段可安全删除的重复抽象；Phase 12 无新增 `ponytail:` debt，仓库仅有一条既有服务层标记且含升级触发条件。

### 4. 测试方式

- `make format-check`
- `make lint`
- `make type`
- `make unit`
- `make integration`
- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests -k phase12`
- `uv run --frozen --python 3.14 --group retrieval-lab mypy labs/self_improvement`
- `python3 .agents/skills/harness-build/scripts/validate_harness_structure.py .`
- `python3 .agents/skills/harness-check/scripts/validate_manifest.py .`
- `python3 .agents/skills/harness-check/scripts/check_references.py .`
- `python3 .agents/skills/harness-check/scripts/detect_drift.py .`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- 干净临时根目录顺序复跑 `audit-data`、`prepare-data`、`make-candidates`、replay 五方法、held-out replay、三种训练、`select-lab`、`rollback-lab`、`report`、`verify-artifacts`。

### 5. 测试结果

- format、lint、type、unit、integration、专项测试和专项 mypy 均退出 0；unit 为 1034 passed，integration 为 248 passed，Phase 12 为 60 passed。
- Harness 四项均 `valid: true` 且无 broken reference；artifact verify 为 `PASS`（10 个评测文件、672 条记录、2 个候选、2 个 selection、9 个权重）。
- 干净进程 CLI 全链路每条命令退出 0；默认 replay 不触发 Provider。

### 6. 用户如何进行手工测试

- 在仓库根目录运行 `python -m labs.self_improvement.cli verify-artifacts`，再查看 `output/phase12/phase12-stage-report-draft-<code>.md` 的方法表、held-out 区间和风险登记。

### 7. 风险预估与规避

- 全量验证未改变 ERP/Frappe、业务 Runtime、`.env*` 或 `.harness/`；真实模型证据仍按 Provider 失败、UNKNOWN、usage 未知分别统计。
- 阶段报告不写 PASS；后续独立 Review 若发现证据失真，按缺陷小步修复并重跑受影响检查。

### 8. 下轮建议

- 将最终 output artifact 按数据、评测、训练和报告边界冻结提交；完成后启动唯一一次独立对抗审查。

### 9. 本轮任务大白话讲解

- 这一轮像交付前总检：先确认代码能运行，再确认实验结果没有越界引用，最后确认文件里没有多余复杂层。所有检查都绿了，但“是否值得采用”仍要等独立审查和证据门禁。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 25 轮 · 修正 Harness 引用边界

### 1. 本轮任务列表

- 处理 Harness 结构检查发现的单个 broken-reference。
- 将旧日志中的解释器路径改为已核实的 Python 版本描述，避免把本地虚拟环境路径当作项目文件引用。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.16 Harness 结构检查的引用错误已修复；没有改变代码、数据、模型权重或实验结论。

### 4. 测试方式

- `python3 .agents/skills/harness-build/scripts/validate_harness_structure.py .`
- `python3 .agents/skills/harness-check/scripts/validate_manifest.py .`
- `python3 .agents/skills/harness-check/scripts/check_references.py .`
- `python3 .agents/skills/harness-check/scripts/detect_drift.py .`

### 5. 测试结果

- 本轮先修正导致失败的路径引用，随后重跑四项 Harness 检查；结果将在下一轮阶段证据中记录。

### 6. 用户如何进行手工测试

- 在仓库根目录运行 `validate_harness_structure.py .`，确认不再出现 `escapes-root`。

### 7. 风险预估与规避

- 仅改写日志中的路径文字，不覆盖 `.harness/`，不触发 Harness 写入，也不暴露 Secret。

### 8. 下轮建议

- 完成 Harness 四项只读检查和复杂度审计，再冻结阶段 diff。

### 9. 本轮任务大白话讲解

- Harness 会把 Markdown 里的路径当作可点击证据入口。虚拟环境路径不属于仓库证据，所以换成版本事实，既保留信息又不制造越界链接。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 24 轮 · 补齐阶段测试类型边界

### 1. 本轮任务列表

- 修正 Phase 12 测试 helper 的返回类型和 pytest fixture 注解。
- 让 fake Provider 使用正式 `ProviderMessage` 输入类型，恢复仓库级 `make type`。
- 按 ruff 规范化测试格式。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.16 全量类型门禁的测试侧问题已修复；未改变实验逻辑和原始 artifact。

### 4. 测试方式

- `make type`
- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py tests/test_phase12_methods.py tests/test_phase12_rl.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check tests/test_phase12_rl.py tests/test_phase12_methods.py tests/test_phase12_cli.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format --check tests/test_phase12_rl.py tests/test_phase12_methods.py tests/test_phase12_cli.py tests/test_phase12_evaluation.py`

### 5. 测试结果

- `make type` 检查 146 个文件并退出 0；专项测试 21 个通过；ruff 和 format 均退出 0。

### 6. 用户如何进行手工测试

- 在仓库根目录执行 `make type`，应看到 `Success: no issues found in 146 source files`。

### 7. 风险预估与规避

- 修改仅限测试类型声明和格式，生产代码、Provider 协议和实验数据均未变更。

### 8. 下轮建议

- 继续执行 `make unit`、`make integration` 和 Phase 12 专项全量测试。

### 9. 本轮任务大白话讲解

- 类型检查发现的不是算法错误，而是测试替身没有声明自己遵守真实 Provider 的消息接口。把替身声明准确，才能让类型系统真正检查调用契约。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 23 轮 · 更新真实模型限制口径

### 1. 本轮任务列表

- 修正阶段报告中把真实 Provider held-out 误列为未运行项的静态说明。
- 在报告回归测试中锁定“已记录但仍有限制”的事实口径。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.14/P12.15 报告文字已与 `evaluation-live-baseline-test.jsonl` 的 24 条实际记录一致；报告仍保持 `DRAFT / LAB_ONLY`。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 4 个报告测试通过；ruff、format 和 mypy 均退出 0。

### 6. 用户如何进行手工测试

- 打开生成的 `phase12-stage-report-draft-<code>.md`，确认真实 assist held-out 被列为已记录证据，make integration、Harness 授权和独立审查仍列为未闭合门禁。

### 7. 风险预估与规避

- 只更新报告说明，不修改任何原始模型响应、失败记录、训练权重或业务 Runtime。

### 8. 下轮建议

- 归档旧报告并生成绑定当前代码的最终报告，随后进入 P12.16 全量验证。

### 9. 本轮任务大白话讲解

- 真实模型请求已经执行，但失败和未知结果很多，所以报告既要承认“跑过”，也要保留“证据有限”。这两件事同时成立，不能把已运行误写成已证明。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 22 轮 · 分离真实模型与 replay 基线聚合

### 1. 本轮任务列表

- 将报告中的真实 Provider 基线与确定性 replay 基线分开统计。
- 保持 held-out 配对 bootstrap 只比较同类 replay 方法，避免把不同执行引擎混入同一分母。
- 增加 live/replay 聚合回归测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.14/P12.15 的证据聚合边界已修复；现有 24 条真实 assist 记录保留原始 method 和 usage 状态，由报告层标记为 `live-baseline`。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 4 个报告测试通过；ruff、format、mypy 和 diff check 均退出 0。
- 真实 Provider 记录不再改变 replay 基线的 24 条 held-out 分母或配对组。

### 6. 用户如何进行手工测试

- 在已有 `output/phase12/` 上执行 `python -m labs.self_improvement.cli report`，检查方法表同时出现 `baseline` 与 `live-baseline`，held-out bootstrap 不把二者配对。

### 7. 风险预估与规避

- 报告层按模型标识隔离执行引擎，不改写原始 JSONL；真实失败、UNKNOWN、延迟和未知 usage 继续进入 live 分母。

### 8. 下轮建议

- 重新生成绑定当前实现的报告，然后冻结输出 artifact 并执行阶段全量验证。

### 9. 本轮任务大白话讲解

- replay 是本地确定性回放，assist 是真实 Provider，它们即使都叫 baseline 也不是同一次实验。现在报表把它们分成两行，质量比较不会把实时失败率混进本地对照。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 21 轮 · live 批次事件循环修复

### 1. 本轮任务列表

- 发现逐案例 `asyncio.run` 关闭 Provider 事件循环导致批次第二次调用失败。
- 新增同一批次单事件循环的 live baseline runner，保持并发为 1。
- 保留单案例 API 供测试使用，并让批量记录继续绑定 manifest digest。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.4 的真实批量运行缺陷已修复；第一次不完整 live 尝试未当作质量证据，后续会归档为无效批次。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py labs/self_improvement/cli.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py labs/self_improvement/cli.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 13 个评测/CLI 测试通过；ruff、format 和 mypy 均退出 0。
- fake Provider 在同一个批次四次请求均完成，未再出现 event loop 关闭；每次实际调用数为 1。

### 6. 用户如何进行手工测试

- 显式执行 `evaluate --engine live --split test --method baseline --repeats 3`，确认同一输出 JSONL 中批次记录完整；若 Provider 失败，保留失败分类并检查 `calls` 与预算。

### 7. 风险预估与规避

- 真实 Provider 仍只在显式 live 命令中调用；本次修复没有重试、并发或 Provider 替换行为。

### 8. 下轮建议

- 重新执行真实 assist 探测；成功后冻结 live test 结果，失败则明确写入阻塞报告。

### 9. 本轮任务大白话讲解

- 异步客户端通常要一直挂在同一个事件循环上。之前每个案例都开关一次循环，第二个案例就拿到了已关闭的连接；现在一整批请求共享一个循环，同时仍按顺序发出，不会增加并发或重试。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 20 轮 · 干净进程 CLI 入口修复

### 1. 本轮任务列表

- 让 `python -m labs.self_improvement.cli` 在未预先设置 `PYTHONPATH` 的干净进程中加载 workspace runtime。
- 保持 runtime 路径固定为仓库内 `services/agent_runtime/src`，不自动安装依赖或访问网络。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.15 手工 CLI 入口已修复；临时隔离根目录完整顺序复跑通过。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/__init__.py labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/__init__.py labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 6 个 CLI 测试通过；ruff、format、mypy 和 diff check 均退出 0。
- 子进程移除 `PYTHONPATH` 后仍能成功执行 `prepare-data`；隔离复跑共 648 条评测记录、564 次 replay 记账调用，预算未超限。

### 6. 用户如何进行手工测试

- 在仓库根目录执行 `python -m labs.self_improvement.cli prepare-data`，不设置 `PYTHONPATH`，确认命令返回 0 并写入 `output/phase12/`。

### 7. 风险预估与规避

- 入口只补本地 workspace import 路径，不改变 Provider、业务 Runtime、ERP/Frappe 或网络策略。

### 8. 下轮建议

- 在仓库根目录生成冻结的最终 Phase 12 原始 artifact，并绑定当前实现 HEAD。

### 9. 本轮任务大白话讲解

- 用户按文档复制命令时不应该先猜环境变量。CLI 现在会从仓库里找到实验依赖的只读 runtime，仍然不会因此触发远程模型或业务接口。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 19 轮 · 输出路径与版本文件名安全收紧

### 1. 本轮任务列表

- 输出路径逐级拒绝符号链接组件，保留路径穿越和实验目录边界检查。
- 候选与 selection 读回时核对文件名和内容中的 ID。
- 增加 symlink 逃逸负向测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.9/P12.15 的 artifact 路径安全缺陷已修复；没有删除旧 artifact，也没有放宽实验目录。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_artifacts.py tests/test_phase12_candidates.py tests/test_phase12_skill_selection.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/contracts.py labs/self_improvement/candidates.py tests/test_phase12_artifacts.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/contracts.py labs/self_improvement/candidates.py tests/test_phase12_artifacts.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 14 个 artifact/candidate/selection 测试通过；ruff、format、mypy 和 diff check 均退出 0。
- 输出目录、候选文件和 selection 文件的符号链接、改名和重复写入均被拒绝。

### 6. 用户如何进行手工测试

- 在临时根目录将 `output` 或 `candidates` 指向另一个目录后执行 `prepare-data`/`make-candidates`，命令应返回非零且不产生逃逸文件。

### 7. 风险预估与规避

- 文件哈希仍只证明内容一致，不当作签名或授权；业务 Runtime 和 Prompt/Skill Registry 不会被写入。

### 8. 下轮建议

- 运行完整实验批次并冻结 held-out 原始证据，随后执行全量阶段门禁。

### 9. 本轮任务大白话讲解

- 只检查最终路径还不够，因为中间目录可能是链接。现在每一级目录都检查，候选文件里的 ID 也必须和文件名一致，避免实验回执被偷换或写到目录外。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 18 轮 · DPO 偏好与早停有效性修复

### 1. 本轮任务列表

- 从同一可观察状态生成 chosen/rejected 偏好对，过滤不安全动作和无质量差异对。
- 冻结 DPO reference，记录 reference 权重哈希，并保留标准 beta=0.1 偏好损失。
- 将 DPO 早停指标改为 dev 偏好 loss；补充 REINFORCE 平均步数指标。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.11 的安全偏好适配和 dev 早停已完成；没有把 chosen 单边交叉熵命名为 DPO，也没有使用 held-out 标签。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_training.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 8 个训练测试通过；ruff、format、mypy 和 diff check 均退出 0。
- 偏好对来自同一状态且 chosen/rejected 不相同；DPO reference 哈希与 SFT 起点一致，训练后权重发生变化。

### 6. 用户如何进行手工测试

- 运行 `train --method sft --seed 17` 后 `train --method dpo --seed 17`，查看 metadata 中的 `reference_weight_sha256`、`dev_preference_pairs` 和 `final_dpo_loss`。

### 7. 风险预估与规避

- 偏好准确率不作为业务成功替代；若 held-out 任务回归，报告必须标记副作用并保留 SFT/基线。

### 8. 下轮建议

- 生成真实回放与训练 artifact，执行冻结后的 held-out 评测、配对 bootstrap 和完整验收。

### 9. 本轮任务大白话讲解

- DPO 要比较“同一个情况里，哪个安全动作更好”，不能只把正确动作当成普通分类标签。现在参考模型被冻结，训练只根据安全的成对差异调整，并用开发集决定何时停止，避免偷看测试集。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 17 轮 · 统一报告与 Adoption Card

### 1. 本轮任务列表

- 新增阶段报告生成器和 `report` CLI，读取已绑定 manifest 的评测 JSONL 与训练 metadata。
- 汇总 verifier/safety/score/calls/usage/延迟，并执行 2,000 次按组 held-out bootstrap。
- 生成风险登记、暂定九维 Rubric、奖励黑客预设负例和 `LAB_ONLY` Adoption Card。
- 报告、卡片和机器摘要均使用不可覆盖 artifact 写入。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.15 的统一证据输出已完成；报告状态固定为 `DRAFT / LAB_ONLY`，不把 replay、训练或 RAG 写成业务采用。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reporting.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/reporting.py tests/test_phase12_reporting.py labs/self_improvement/cli.py labs/self_improvement/artifacts.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/reporting.py tests/test_phase12_reporting.py labs/self_improvement/cli.py labs/self_improvement/artifacts.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 8 个报告/CLI 测试通过；ruff、format、mypy 和 diff check 均退出 0。
- 报告会保留失败和未知 usage；零差值比较输出 `INCONCLUSIVE`，故意错误奖励明确标记为预设负例。

### 6. 用户如何进行手工测试

- 运行 `prepare-data`、若干 `evaluate`/`train` 后执行 `python -m labs.self_improvement.cli report`，查看 `output/phase12/phase12-stage-report-draft-*.md`、`phase12-adoption-card-*.md` 和 `phase12-summary-*.json`。

### 7. 风险预估与规避

- 报告生成不改变业务 Registry；若数据、记录或训练 metadata 绑定不一致，命令在写报告前失败。

### 8. 下轮建议

- 在临时根目录按顺序生成完整 dev/test、候选、选择/回滚和三算法权重，冻结原始证据并做全量门禁。

### 9. 本轮任务大白话讲解

- 现在实验结果可以被同一份报告复核：它把每种方法做对多少、是否安全、花了多少调用和多久写在一起，还单独标出“奖励分数变高但任务没完成”的反例。没有证据时报告会保留基线，不会自动升级线上能力。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 16 轮 · CLI artifact 完整校验

### 1. 本轮任务列表

- 将审计和训练 metadata 写入统一的一次性 artifact API。
- `verify-artifacts` 逐个校验候选、selection、权重、metadata、数据集绑定和内容哈希。
- 选择/回滚前校验父版本和已有评测证据（没有评测文件的最小演示目录仍可建立选择回执）。
- 评测 CLI 支持三次重复以及 Prompt/Skill 候选方法，live 模式限定为 baseline。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.15 的 artifact 验收入口已补齐；发现权重或 metadata 篡改时返回非零退出，不覆盖旧文件。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py tests/test_phase12_artifacts.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/cli.py labs/self_improvement/evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/cli.py labs/self_improvement/evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 17 个专项测试通过；ruff、format、mypy 和 diff check 均退出 0。
- 新进程读回权重和 metadata 通过；篡改权重哈希、引用缺失、候选方法超出三次重复均被发现。

### 6. 用户如何进行手工测试

- 先 `prepare-data`，再 `evaluate --method prompt-candidate --split dev --repeats 3`，最后 `verify-artifacts`；把 metadata 的 `weight_sha256` 改成全零后再次验证应返回 2。

### 7. 风险预估与规避

- 选择证据不可凭文件名冒充；已有 JSONL 时引用必须真实存在，权重和 metadata 必须互相绑定。

### 8. 下轮建议

- 生成统一评测/训练/奖励黑客结果报告和 Adoption Card，并保留 INCONCLUSIVE/副作用结论。

### 9. 本轮任务大白话讲解

- `verify-artifacts` 像实验室的收货验单：不只看“文件在不在”，还核对它属于哪份数据、文件名是否对应内容、训练权重是否和说明里的哈希一致，以及选择回执引用的证据是否真的存在。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 15 轮 · 安全奖励与奖励黑客边界修复

### 1. 本轮任务列表

- 将安全奖励固定为每步 -0.02、成功 +1、错误 -1、无进展 -0.1，重复调用不获得 bonus。
- 保留单独的故意错误奖励，使重复调用可以产生可复现的高分负例。
- 拒绝 NaN 和无穷奖励参数，避免奖励污染训练指标。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.12 的安全奖励与错误奖励已分离；没有改变环境的安全门禁和最终任务 verifier。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_rl.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/rl.py tests/test_phase12_rl.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/rl.py tests/test_phase12_rl.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 7 个环境和 REINFORCE 测试通过；lint、format、mypy 和 diff check 均退出 0。
- 相同重复轨迹在错误奖励下仍高于安全奖励，但安全奖励没有重复收益；非有限奖励被拒绝。

### 6. 用户如何进行手工测试

- 对 `purchase_order.open` 重复三次再 `FINISH`，分别使用 `safe_reward_config()` 和 `intentionally_bad_reward_config()`，比较总奖励和最终 verifier 状态。

### 7. 风险预估与规避

- 训练算法不能通过调大正向奖励抵消安全或任务失败；奖励和 verifier 继续独立计算。

### 8. 下轮建议

- 加固 CLI 的一次性 artifact 写入、候选/选择校验和权重 metadata 绑定。

### 9. 本轮任务大白话讲解

- 安全评分只奖励完成正确任务，重复查同一件事不会加分。另一套错误评分故意让重复有利，这样可以清楚展示“分数变高但任务仍失败”的奖励黑客风险。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 14 轮 · live Provider 批次边界修复

### 1. 本轮任务列表

- 为 live Provider 增加单次 60 秒墙钟上限。
- 在累计预算内记录真实发出的调用数；预算耗尽时不把未发出的请求记为调用。
- 连续三次连接、认证或协议失败后阻断当前批次，并保留已消耗预算。
- 让 live 结果显式绑定冻结数据集 ID 和 manifest digest。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.4 的批次控制和数据绑定缺陷已修复；没有自动重试，也没有切换 Provider 伪造结果。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 6 个评测测试通过；ruff、format、mypy 和 diff check 均退出 0。
- 故障注入确认三次阻塞性错误后第四次不再触发 Provider；超时和预算不足均保留可审计状态。

### 6. 用户如何进行手工测试

- 使用一个会抛出 `CONNECTION_ERROR` 的 Provider 连续执行四次，确认第四次 `MODEL_BATCH_BLOCKED` 且 Provider 实际只收到三次请求。

### 7. 风险预估与规避

- 中断和批次阻断不会重置累计预算或覆盖旧记录；未知 usage 仍保持为空，不写成 0。

### 8. 下轮建议

- 加固统一 artifact 验证，并生成阶段报告、Adoption Card 和可复跑证据。

### 9. 本轮任务大白话讲解

- 远程模型可能卡住或连续不可用。现在每次调用都有硬截止，连续三次基础设施错误就停止这一批，避免无限等待和重复花费；没真正发出的请求也不会被算进去。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 13 轮 · Phase 12 CLI 与 artifact 收口入口

### 1. 本轮任务列表

- 增加 `audit-data`、`prepare-data`、`make-candidates`、`evaluate`、`select-lab`、`rollback-lab`、`train` 和 `verify-artifacts` CLI。
- 让默认 evaluate 只跑本地回放，live Provider 只有显式 `--engine live` 才启用。
- 绑定 manifest、记录、候选、选择和权重的复跑检查。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.14/P12.15 基础 CLI 完成；训练和评测 artifact 可以从新进程读取并验证。
- `verify-artifacts` 会跳过 metadata 描述文件，只读取严格 JSON 权重；候选和 selection 继续执行契约校验。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 3 个 CLI 端到端测试通过；mypy 10 个实验源文件通过。
- 临时目录完整跑通 prepare/audit/replay/train/verify 和候选选择/回滚；默认回放不需要 Provider。
- live Provider 尚未在当前环境批量执行；不把 fake provider 测试写成真实质量证据。

### 6. 用户如何进行手工测试

- 在仓库根目录依次运行 `python -m labs.self_improvement.cli prepare-data`、`evaluate --split dev`、`train --method sft`、`verify-artifacts`，再查看 `output/phase12/` 中的 manifest、JSONL 和权重 metadata。

### 7. 风险预估与规避

- 训练/评测默认不触网；live 模式显式选择且共享累计 1,200 次调用预算。
- 所有写入使用一次性文件；重复运行不会覆盖旧证据，篡改会在 verify 阶段失败。

### 8. 下轮建议

- 完成真实/回放 held-out 结果冻结、按组 bootstrap、Adoption Card 和统一阶段报告。

### 9. 本轮任务大白话讲解

- 现在有一套可以从命令行重复运行的实验流程：先准备数据，再比较方法或训练模型，最后检查所有文件是否仍和数据、代码版本一致。默认不会意外调用远程模型。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 12 轮 · 评测记录数据绑定修复

### 1. 本轮任务列表

- 发现 replay evaluator 使用单案例摘要哈希而非冻结 manifest 哈希。
- 修正 evaluator 可显式传入 dataset digest，并让 artifact verifier 拒绝错绑定。
- 增加 manifest、JSONL、重复 ID、预算和篡改校验。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.14/P12.15 的 artifact 基础完成；记录现在可以被精确绑定到数据集版本。
- 该修复未放宽任何数据、模型或安全门禁。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_artifacts.py tests/test_phase12_heldout.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py labs/self_improvement/artifacts.py`

### 5. 测试结果

- 6 个 artifact/held-out 测试通过；mypy 9 个实验源文件通过。
- 篡改 manifest、重复实验 ID、错路径和空配对 bootstrap 均被发现。
- 真实模型 held-out 尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_artifacts.py`，核对 JSON manifest 和 JSONL 读回后仍通过 dataset digest 检查。

### 7. 风险预估与规避

- 不同数据版本的实验不能混合验证；所有结果必须携带 manifest digest。
- artifact 写入使用一次性文件，禁止覆盖和输出目录逃逸。

### 8. 下轮建议

- 完成 CLI：audit/prepare/evaluate/select/rollback/train/verify，并生成最小阶段证据。

### 9. 本轮任务大白话讲解

- 发现一个会影响可信度的问题：实验记录之前可能只绑定单条案例的哈希。现在每条记录都必须绑定整份冻结数据集，数据换了就不能假装沿用旧结果。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 11 轮 · 训练实验类型边界修复

### 1. 本轮任务列表

- 根据专项 mypy 结果修正 replay/evaluation/data/training/rl 的严格类型边界。
- 保持失败分类、动作白名单和延迟加载语义不变。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 修复了 12 个静态类型诊断；没有新增业务路径或放宽运行时校验。

### 4. 测试方式

- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- mypy `Success: no issues found in 8 source files`，diff check 退出 0。
- 受影响专项 pytest 将在本轮提交后与下一步训练验收一并复跑。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 mypy labs/self_improvement`，确认实验目录没有类型错误。

### 7. 风险预估与规避

- 修复只收紧类型标注和移除无效 ignore；没有改动模型、环境或权限行为。

### 8. 下轮建议

- 冻结数据、候选和权重后实现 held-out 统计与 artifact 校验。

### 9. 本轮任务大白话讲解

- 训练代码先通过静态检查，避免把“看起来能跑”的宽松类型错误带到最终实验。修复只处理编译器发现的问题，不改变实验结果规则。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 10 轮 · REINFORCE 参数更新

### 1. 本轮任务列表

- 在模拟采购环境中实现固定折扣因子 0.95 的 REINFORCE。
- 使用 episode 内真实状态、采样动作、奖励和移动 baseline 更新 MLP 参数。
- 保存 episode 回报、成功率、无效动作和权重读回证据。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.13 完成；episode 上限固定为 300、每回合最多 8 步，训练只使用 train 场景。
- 训练每个 seed 都是真实采样和梯度更新，不把固定回放称为 RL。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_reinforce.py`

### 5. 测试结果

- 3 个 REINFORCE 测试通过；权重真实改变、JSON 新进程读回一致、相同 seed 可复现。
- 超过 300 episode 会拒绝；有限回报和梯度未出现 NaN。
- 阶段全量训练三 seed 和 held-out 评测尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_reinforce.py`，核对 `weight_sha256` 变化和 12 episode 的回报长度。

### 7. 风险预估与规避

- baseline 只使用此前 episode 的回报，不读取未来或 test；训练异常不会保存隐式成功状态。
- RL 结果必须与最终业务 verifier 分开报告，避免高 reward 被误读为高任务质量。

### 8. 下轮建议

- 固定代码、数据和候选后执行 dev/test held-out 评测、按组 bootstrap 和结果分类。

### 9. 本轮任务大白话讲解

- 现在小模型会在采购沙盒里自己试动作：查、问、停都会产生奖励，然后用这些真实经历调整参数。它仍然只在本地实验环境行动，线上 Agent 不会加载这套权重。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 9 轮 · 采购模拟环境与奖励黑客负例

### 1. 本轮任务列表

- 实现本地 `reset/step` 采购调查环境和 8 步 episode 上限。
- 将安全门禁、业务终态 oracle 和奖励计算分开。
- 增加安全奖励与故意重复调用可骗分的错误奖励对照。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.12 完成；环境不访问网络、不连接 ERP、不保留跨 episode 状态。
- 已提供可复现的预设 reward-hacking 负例，作为风险演示而非自然生产发现。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_rl.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/rl.py tests/test_phase12_rl.py`
- `git diff --check`

### 5. 测试结果

- 3 个环境、越界动作和奖励黑客测试通过。
- 终态后继续 step 会拒绝；非法动作立即 `REJECTED` 且安全失败。
- 重复调用在错误奖励下总分高于安全奖励，但最终业务 verifier 仍不通过。
- REINFORCE 尚未接入；全量 unit/integration 尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_rl.py`，查看相同重复轨迹在两种 reward config 下的总分和最终状态。

### 7. 风险预估与规避

- 正向 reward 不能抵消安全失败；任务 oracle 只由环境使用，策略只获得观察状态。
- 错误奖励明确标记为故意负例，不能拿高分推导业务质量提升。

### 8. 下轮建议

- 在同一环境接入 REINFORCE，固定三 seed、移动 baseline、300 episode 上限，并保存真实参数更新。

### 9. 本轮任务大白话讲解

- 这个沙盒让 Agent 真正经历“查、遇到问题、停”的过程。我们故意给一套错误奖励，让重复查同一个东西反而得分，证明奖励分数可能被钻空子，所以必须单独看业务成功和安全结果。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 8 轮 · SFT 与 DPO 小模型更新

### 1. 本轮任务列表

- 实现固定 7 维可观察特征、8 个动作的两层 MLP。
- 实现 CPU SFT、dev 早停、有限 loss 和真实参数更新。
- 实现 reference 冻结的标准 DPO 偏好损失。
- 实现有限 JSON 权重保存、版本/形状/有限值检查和新进程读回。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.10、P12.11 基础实现完成；训练输入只含可观察状态，oracle/奖励不进入 feature。
- PyTorch 仅在训练模块实际调用时加载；不使用 pickle、远程代码或新依赖。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_training.py`

### 5. 测试结果

- 4 个训练测试通过；SFT/DPO 权重均真实改变且有限。
- 同 seed SFT 结果一致；DPO artifact 的初始哈希等于 SFT 权重哈希。
- JSON 权重新进程读回一致；版本损坏被拒绝。
- RL 尚未实现；全量 unit/integration 尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_training.py`，确认日志中的 `initial_weight_sha256` 与 `weight_sha256` 不同，并检查读回摘要一致。

### 7. 风险预估与规避

- 权重 schema、feature/action version、形状和有限值均由加载器检查，任意损坏不会被执行。
- loss 下降不等于采购任务改善；业务指标必须在 held-out verifier 中单独核对。

### 8. 下轮建议

- 实现采购模拟环境的 reset/step、固定安全奖励和预设 reward-hacking 负例，再接入 REINFORCE。

### 9. 本轮任务大白话讲解

- 训练模型只学“看到了什么之后该查、该问还是该停”，不会接触正确答案或 ERP 写权限。SFT 学安全动作，DPO 学安全候选之间的偏好；权重用普通 JSON 保存，坏文件直接拒绝。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 7 轮 · Skill 候选与实验回滚

### 1. 本轮任务列表

- 增加只读 Skill 候选构造与能力扩张拒绝。
- 增加实验选择回执读取和不可覆盖的父版本回滚。
- 增加候选/选择文件的路径、符号链接和重复写保护。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.8、P12.9 完成；Skill 候选只写实验副本，回滚只产生新的 `ROLLBACK` receipt。
- 业务 Prompt/Skill registry 的内容和边界哈希没有被修改。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_candidates.py tests/test_phase12_skill_selection.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/candidates.py tests/test_phase12_skill_selection.py`
- `git diff --check`

### 5. 测试结果

- 10 个候选、Skill、选择和回滚测试通过。
- 路径穿越、远程/写工具提示、受保护层标记、陈旧候选和二次回滚均被拒绝。
- 全量 unit/integration 和 live held-out 尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_skill_selection.py`，确认 A→B→A 的回滚只新增回执，旧文件仍可读取。

### 7. 风险预估与规避

- Skill 内容不能增加工具或执行能力；选择记录不能覆盖既有 artifact。
- 当前“选择”仍是 LAB_ONLY 文件记录，不代表业务 Runtime 已晋级候选。

### 8. 下轮建议

- 实现本地 MLP 的 SFT、DPO 和权重安全读回，先用合成回放建立真实参数更新证据。

### 9. 本轮任务大白话讲解

- Skill 候选像一张实验用操作提示卡，只能帮助 Agent 更好地查和停，不能让它多出工具。选错版本时不删除旧文件，而是写一张新回执把指针退回父版本。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 6 轮 · Prompt 候选版本化

### 1. 本轮任务列表

- 实现失败来源驱动的 Prompt 候选构造、内容哈希和固定父版本。
- 固定 boundary/recovery/output contract/只读动作的联合边界哈希。
- 增加候选安全校验、限定目录写入、重复写保护和读取复验。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.7 的候选版本基础完成；候选只能作为实验副本，不会注入 `PromptRegistry`。
- 修改受保护层、工具、权限或远程执行能力的内容会在加载前拒绝。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_candidates.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/candidates.py tests/test_phase12_candidates.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/candidates.py tests/test_phase12_candidates.py`

### 5. 测试结果

- 4 个候选哈希、边界篡改、重复写和选择证据测试通过。
- 候选写入后可读回且内容相等；陈旧边界和无证据选择被拒绝。
- 全量 unit/integration 和 dev 比较尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_candidates.py`，确认候选文件只会出现在实验输出目录，并核对 `boundary_sha256` 未变。

### 7. 风险预估与规避

- 候选内容不能改变保护层、输出契约、权限或工具集合；边界摘要变化即 fail closed。
- 文件写入拒绝覆盖既有 artifact，避免回滚链被静默改写。

### 8. 下轮建议

- 使用同一实验副本生成并评测只读 Skill 候选，同时接入显式选择/回滚回执。

### 9. 本轮任务大白话讲解

- 失败轨迹只能影响 Agent 的“判断提示”，不能借机改安全规则或增加权限。每个候选都有父版本和哈希，后面可以知道它从哪来、是否被改过。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 5 轮 · Reflection 与 Best-of-3 对照

### 1. 本轮任务列表

- 增加最多一次修订的 Reflection 回放。
- 增加最多三个候选的 Best-of-N 回放和稳定 reranker。
- 先执行安全/业务 verifier 硬门禁，再按可见分数、步数和生成顺序排序。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.5、P12.6 的离线对照实现完成；失败、退化和调用次数均保留。
- reranker 没有接触 case oracle；只有 verifier 已经确认的候选进入排序。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_methods.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_methods.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py tests/test_phase12_methods.py`

### 5. 测试结果

- 8 个方法和基线专项测试通过。
- Reflection 成功修复一次失败且不会对已验证结果额外调用；Best-of-2 能过滤错误候选；无有效候选时不伪造成功。
- 全量 unit/integration 和真实远程方法比较尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_methods.py`，核对 `calls` 和 `verifier_passed`，确认流畅但错误的候选不会被排序选中。

### 7. 风险预估与规避

- Reflection 反馈只来自模型可见失败状态；Best-of-N 不能使用期望答案或隐藏标签。
- 多调用成本仍由外层 CallBudget 统计，后续 live runner 不得绕过预算。

### 8. 下轮建议

- 生成只改变 decision 层的 Prompt 候选和只读 Skill 候选，并做边界哈希校验。

### 9. 本轮任务大白话讲解

- Reflection 是“先答一次，发现不合格再改一次”；Best-of-3 是“最多写三份，先剔除不安全和不合格的，再选最好的一份”。两者都不能靠模型自我感觉判断好坏。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 4 轮 · 有限预算 Provider 基线

### 1. 本轮任务列表

- 增加统一 `ExperimentRecord` 回放评测和汇总指标。
- 增加累计调用预算、输入/输出上限和不重试的 Provider 适配。
- 分类记录预算耗尽、传输/Provider 错误、空响应和 JSON schema 错误。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.4 基础实现完成；`run_live_baseline` 的模型动作仍需通过本地回放 verifier。
- provider 未报告正用量时记录为空，不把默认 0 宣称为实测 usage。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`

### 5. 测试结果

- 4 个回放汇总、预算阻断、Provider JSON 解析和无重试测试通过。
- 预算为 0 时 provider 调用次数保持 0；无效响应只消耗一次已登记调用并保留失败码。
- 全量 unit/integration 和真实远程 Provider 批次尚未运行。

### 6. 用户如何进行手工测试

- 使用 fake provider 运行专项测试；确认 `MODEL_CALL_BUDGET` 和 `MODEL_RESPONSE_SCHEMA` 都出现在结构化记录中。

### 7. 风险预估与规避

- 每次请求前占用预算，失败不会被静默重试；跨进程持久化预算将在 CLI artifact 层完成。
- live prompt 只包含合成任务和允许动作，当前没有真实 ERP 字段外发路径。

### 8. 下轮建议

- 实现 Reflection 与 Best-of-3，保持相同 verifier 和预算记录。

### 9. 本轮任务大白话讲解

- 现在每次模型调用都有一笔账：发送前先扣额度，返回后记模型结果或明确失败。这样网络超时不会悄悄重发，也不会把“模型没回答”写成业务失败已解决。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 3 轮 · 只读回放与确定性 verifier

### 1. 本轮任务列表

- 建立不暴露 oracle 的本地采购回放状态和六类场景终态。
- 增加只读工具/ASK_INPUT/FINISH 白名单、步数预算和安全拒绝。
- 增加确定性 verifier，独立检查最终动作、业务状态和安全状态。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.3 完成；后续方法可以共用 `run_replay` 和 `verify_result`。
- 回放 policy 只能读取观察、失败、冲突和剩余步数；场景 oracle 只在环境和 verifier 侧使用。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_replay.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/replay.py tests/test_phase12_replay.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/replay.py tests/test_phase12_replay.py`

### 5. 测试结果

- 4 个回放、越界动作、伪成功和未知/冲突终态测试通过。
- 业务写工具、模型口头成功和未验证证据均不能绕过 verifier。
- 全量 unit/integration 尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_replay.py`，观察确定性 policy 在六类场景均通过，非法动作返回 `ACTION_NOT_ALLOWED`。

### 7. 风险预估与规避

- 回放环境不访问网络、不持有 ERP 会话、不提供 writer；模型状态不直接包含场景 kind、期望动作或奖励。
- verifier 与奖励侧分离；后续 reranker 和 RL 不能把模型自评当作事实。

### 8. 下轮建议

- 在不改变回放安全门禁的前提下，接入有限预算的 Provider 基线和统一实验记录。

### 9. 本轮任务大白话讲解

- 先造一个很小但严格的“采购调查沙盒”：Agent 只能查和停，环境会明确告诉它遇到未知、冲突或无进展。最后由独立检查器判断是否真的完成，模型自己说成功没有用。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 2 轮 · 历史失败审核与数据契约

### 1. 本轮任务列表

- 新增严格的 Phase 12 JSON 契约、内容哈希和实验输出路径边界。
- 实现显式 Phase 11 失败 artifact 审核、脱敏摘要和来源完整性检查。
- 实现六类合成采购场景及按组固定的 72/24/24 数据划分。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.1、P12.2 的基础实现和负面测试完成；真实历史记录只保留脱敏背景，不进入模型输入。
- 代码和测试变更集中在 `labs/self_improvement/` 与 `tests/test_phase12_data.py`。

### 4. 测试方式

- `uv run --frozen --python 3.14 ruff check labs/self_improvement tests/test_phase12_data.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement tests/test_phase12_data.py`
- `uv run --frozen --python 3.14 pytest tests/test_phase12_data.py`
- `git diff --check`

### 5. 测试结果

- 6 个数据审核/隔离测试通过；lint、format、diff check 均退出 0。
- 已验证缺失源、Secret-like 源、重复 ID 和跨 split 场景会拒绝。
- 仅运行专项检查；全量 unit/integration 尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_data.py`，核对历史源为显式 allowlist，合成 manifest 为 60 组、120 条、train/dev/test=72/24/24。

### 7. 风险预估与规避

- 真实 Phase 11 artifact 的字段和内容不送往模型；导出只使用固定任务摘要。
- 数据集的 oracle 仍保存在评分侧，后续 evaluator 必须继续阻止它进入输入投影。

### 8. 下轮建议

- 实现本地采购回放和确定性 verifier，使所有后续方法共享同一安全门禁。

### 9. 本轮任务大白话讲解

- 先检查哪些旧失败真的能用，再把同类采购问题按组分到训练、开发和测试。这样后面即使模型分数变好，也能确定它没有提前见过测试答案。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 1 轮 · 冻结离线实验契约

### 1. 本轮任务列表

- 核对 Phase 11 出口、当前 HEAD 和工作区状态。
- 建立 Phase 12 离线自进化、后训练与 Agentic RL 的边界、预算、数据隔离和出口契约。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记；阶段结束后再按用户触发进行答疑。

### 3. 本轮任务完成度

- P12.0 契约已冻结；实现尚未开始。
- 起始 HEAD 为 `ffb5b81`，工作区在编辑前干净。
- 业务代码、ERP/Frappe、业务 Runtime、`.env*` 和 README 未修改。

### 4. 测试方式

- 读取 `docs/PLAN.md`、Phase 11 最终报告、Phase 7 Prompt/Skill 实现和当前测试入口。
- 使用项目虚拟环境完成 Python/PyTorch CPU autograd 探测。

### 5. 测试结果

- `git status --short`：编辑前为空。
- 本地 Python 3.14.5，PyTorch 2.13.0；有限张量反向传播通过。
- 本轮未运行 `make unit`、`make integration` 或真实 Provider 批次；不把历史结果冒充本轮结果。

### 6. 用户如何进行手工测试

- 查看 `docs/phase12-execution-contract.md`，确认所有实验只写入 `output/phase12/`，并核对 1,200 次调用上限和 72/24/24 数据划分。

### 7. 风险预估与规避

- 初始高风险为训练/测试泄漏、候选扩权、真实轨迹外发、奖励黑客、伪训练和不可回滚。后续每步必须用结构化校验、边界哈希、隔离目录、参数读回和失败保留来关闭。
- 真实 Provider 尚未调用；当前不对模型质量或成本做结论。

### 8. 下轮建议

- 实现历史失败 artifact 的显式审核和导出准入。

### 9. 本轮任务大白话讲解

- 先把实验规则写死，后面所有训练和比较才不会因为看到了测试答案而临时改规则。这个文件只规定实验室怎么工作，不会给线上 Agent 增加任何权限。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 54 轮 · 修复策略初始状态的 oracle 泄漏

### 1. 本轮任务列表

- 根据阶段末独立对抗审查，修复 `initial_state` 在首个动作前从 `case.kind` 注入 `no_progress`、`needs_input` 和 `untrusted_content` 的 P1 缺陷。
- 让六类合成场景在首个动作前只暴露相同的空可观察状态，在只读环境返回对应结果后再更新状态标志。
- 增加 oracle 改动不影响初始策略特征的负向测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `initial_state` 不再读取场景类型；缺失条件、重复无进展和不可信内容均由首个只读观察产生。
- 确定性回放和本地训练示例继续使用相同 verifier，未引入业务 Runtime 或 ERP 写入路径。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_replay.py tests/test_phase12_methods.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff format labs/self_improvement/replay.py labs/self_improvement/cli.py tests/test_phase12_replay.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/replay.py labs/self_improvement/cli.py tests/test_phase12_replay.py`
- `git diff --check`

### 5. 测试结果

- 18 个回放、方法和训练测试通过；ruff 检查通过，格式化仅调整了本次修改文件，`git diff --check` 通过。
- 新负例证明不同场景及改变 oracle 后的首状态特征一致。

### 6. 用户如何进行手工测试

- 运行 `python -m labs.self_improvement.cli --root <临时目录> prepare-data` 后，用回放评测逐类检查首个动作均为受限只读动作；再运行 `pytest tests/test_phase12_replay.py -k oracle`。

### 7. 风险预估与规避

- 已关闭审查指出的 P1 oracle 初始状态泄漏；环境仍可在只读动作后返回故障观察，verifier 继续独立使用评分侧 oracle。
- 训练 artifact 需在全部审查修复完成后重新生成，旧权重保留为待失效证据，不在本提交中覆盖。

### 8. 下轮建议

- 单独接入候选内容策略，补充候选实际影响实验输入的测试；随后处理跨进程 live 预算账本。

### 9. 本轮任务大白话讲解

- 以前模型在还没调查前就被告知“这是缺资料/重复/不可信”的标签；现在必须先做一次受限调查，环境返回观察后模型才看到对应信号。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 55 轮 · 让候选内容真正进入回放

### 1. 本轮任务列表

- 根据独立对抗审查，修复 Prompt/Skill 候选只绑定 `candidate_id`、实际仍调用基线策略的 P1 缺陷。
- 增加受限 `candidate_policy`，仅从候选文字解析 ask-before-read 和 finish-after-observation 两个决策偏好。
- CLI 回放将候选内容传入策略；安全动作集合、verifier、边界和工具能力保持不变。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- Prompt/Skill 候选的内容摘要现在会影响回放首动作或观察后的停止动作；候选内容不能新增工具、权限或执行路径。
- 增加测试证明两个不同候选文字在相同可观察状态产生不同的受限策略偏好。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_replay.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format labs/self_improvement/replay.py labs/self_improvement/cli.py tests/test_phase12_replay.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/replay.py labs/self_improvement/cli.py tests/test_phase12_replay.py`
- `git diff --check`

### 5. 测试结果

- 16 个回放和 CLI 测试通过；ruff 检查、格式检查和 diff 检查通过。
- CLI 候选方法的短流程可运行，候选 ID 与内容同时绑定到实验记录。

### 6. 用户如何进行手工测试

- 在临时目录运行 `make-candidates`，分别编辑实验副本候选内容后执行 `evaluate --engine replay --method prompt-candidate --split dev`，比较结果中的动作和失败记录。

### 7. 风险预估与规避

- 候选内容只进入实验策略解释器，仍由 `ALL_ACTIONS` 和 verifier 双重约束；业务 Prompt/Skill Registry 不写入。
- 活动候选 evidence 需在本轮审查修复完成后重新生成，旧记录保留并标记为 superseded，不覆盖原始证据。

### 8. 下轮建议

- 实现跨进程 live 调用 reservation ledger，确保中断调用仍占用全局预算且不会被静默重发。

### 9. 本轮任务大白话讲解

- 候选文本以前只是结果上的标签；现在会经过一个很小的白名单解释器，确实影响“先问还是先查、查到后是否结束”，但不能改变能用哪些工具。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 56 轮 · 持久化 live 调用预算预留

### 1. 本轮任务列表

- 根据独立对抗审查，修复 live 调用只在内存计数、进程中断后可重复占用或超出 1,200 次预算的 P1 缺陷。
- 增加 append-only reservation ledger：请求发出前 fsync 预留，完成/失败/未知后记录终态，结果写入后标记 `RECORDED`。
- 将批次 ID、重复编号和 case ID 绑定到每条预留；重启读取未物化预留并拒绝复用同一 key。
- 将 `ConnectionError` 归类为可阻塞传输故障，并让 artifact/report 扫描跳过预算账本文件。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- CLI live 评测现在显式创建 `output/phase12/live-reservations.jsonl` 和唯一批次 ID；默认 replay 不创建账本、不触发远程请求。
- ledger 使用追加和 fsync，不覆盖历史预留；未完成调用在新进程中继续占用预算，网络恢复需新批次。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format labs/self_improvement/evaluation.py labs/self_improvement/cli.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py labs/self_improvement/cli.py tests/test_phase12_evaluation.py`
- `git diff --check`

### 5. 测试结果

- 21 个 evaluation 和 CLI 测试通过；ruff 检查、格式检查和 diff 检查通过。
- 新测试覆盖中断后重启计数、重复 reservation 不再调用 provider、已记录调用不被二次计数。

### 6. 用户如何进行手工测试

- 在临时目录运行 live fake-provider 测试，检查 ledger 先出现 `RESERVED` 再出现终态；中断后用同一 reservation key 重启，确认 provider 调用数为 0 且预算仍被占用。

### 7. 风险预估与规避

- 预留账本是预算事实源的一部分，异常退出留下 `RESERVED` 会保守占用额度，避免静默重发；记录失败不删除原始事件。
- 旧活动 live 记录尚未绑定新账本，后续活动证据更新时需保留旧批次并显式区分，不把旧 usage 当作新预留。

### 8. 下轮建议

- 实现 LAB active-version 指针和实际回滚消费路径，并把选择 evidence 限定到同一候选、dev split、数据与代码条件。

### 9. 本轮任务大白话讲解

- 以前预算只记在进程内存里，程序被杀后就忘了；现在每次请求前先在磁盘留下不可变预留，恢复时能知道额度已经用掉，也不会悄悄把同一调用再发一次。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 57 轮 · 绑定可回滚的 LAB active 版本

### 1. 本轮任务列表

- 根据独立对抗审查，修复 rollback 只写 receipt、没有恢复任何可消费实验版本的 P1 缺陷。
- 增加 `active-version.json` 指针，选择和回滚均校验父/子版本内容摘要，并通过临时文件 + fsync + replace 更新指针。
- 增加 active policy 读取入口，让实验回放可以消费当前 LAB 版本；业务 Prompt/Skill Registry 仍不写入。
- 收紧 `LabSelection`：必须包含 previous/selected 两个版本的完整摘要，禁止同版本选择。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `select-lab` 生成 immutable receipt 后激活候选；`rollback-lab` 生成新 receipt 并把 active 指针恢复到精确父版本。
- `verify-artifacts` 检查 active 指针、receipt、当前内容和摘要四者一致；篡改或陈旧指针会失败。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_candidates.py tests/test_phase12_skill_selection.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format labs/self_improvement/contracts.py labs/self_improvement/candidates.py labs/self_improvement/cli.py tests/test_phase12_candidates.py tests/test_phase12_skill_selection.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/contracts.py labs/self_improvement/candidates.py labs/self_improvement/cli.py tests/test_phase12_candidates.py tests/test_phase12_skill_selection.py`
- `git diff --check`

### 5. 测试结果

- 21 个候选、选择和 CLI 测试通过；ruff、格式和 diff 检查通过。
- 新测试确认选择后 active 指针指向候选，回滚后指向父版本且可加载对应 active policy。

### 6. 用户如何进行手工测试

- 在临时目录生成候选并执行带真实 dev evidence 的 `select-lab`；查看 `output/phase12/active-version.json`，再执行 `rollback-lab` 并比较回放动作序列。

### 7. 风险预估与规避

- 指针只位于 LAB 输出目录，内容哈希每次重算；旧候选、selection 和 rollback receipt 不覆盖。
- 跨文件更新仍可能在进程被杀时留下旧指针或新 receipt，但 `verify-artifacts` 会拒绝不一致组合，随后可按最后一致 receipt 恢复。

### 8. 下轮建议

- 收紧 selection evidence：要求同一候选、dev split、同一数据摘要/实验代码条件，并让 verify 对历史 receipt 执行同样门禁。

### 9. 本轮任务大白话讲解

- 以前回滚只是记了一张“我要回去”的票据；现在有一个只服务实验室的活动版本指针，选择和回滚后实际回放会读取它，并核对文件内容没有被换掉。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 58 轮 · 收紧 selection evidence 相关性

### 1. 本轮任务列表

- 根据独立对抗审查，修复选择门禁只检查 experiment ID 存在、可用任意 split/method/live 失败记录作为 evidence 的 P1 缺陷。
- 解析 evidence 记录并要求同一候选、dev split、冻结数据摘要、单一实验代码版本及已完成的成功/拒绝状态。
- 对 rollback 使用被回滚的候选版本作为 evidence 绑定；`verify-artifacts` 对历史 selection 复用相同门禁。
- 保留实验记录原始失败，未改变候选或业务 Registry。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `select-lab` 和 `rollback-lab` 现在拒绝跨候选、跨 split、跨数据集、UNKNOWN/未完成或混合代码版本的 evidence。
- 新的负向 CLI 测试用 baseline dev 记录尝试选择 Prompt 候选，命令返回 2 并不生成 selection。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py tests/test_phase12_candidates.py tests/test_phase12_skill_selection.py`
- `uv run --frozen --python 3.14 ruff format labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `git diff --check`

### 5. 测试结果

- 22 个候选、选择和 CLI 测试通过；ruff 和 diff 检查通过。
- 正向 CLI 选择/回滚流程使用真实 dev candidate evidence 通过；无关 evidence 负例返回非零。

### 6. 用户如何进行手工测试

- 先生成并评测 Prompt 候选，再把同一目录中的 baseline、test 或 live 记录 ID 传给 `select-lab`，确认命令拒绝；传入候选 dev 记录后确认生成 selection 和 active pointer。

### 7. 风险预估与规避

- evidence 仍是可追溯引用，不把摘要文本当授权；代码版本只要求同一批条件，实际是否为当前代码由报告和 artifact 绑定共同说明。
- 历史 selection 若缺 evidence 或使用旧批次会在 verify 阶段暴露，旧文件保留，不静默修正。

### 8. 下轮建议

- 运行受影响的全量 Phase 12 测试并重新生成受代码策略/契约影响的 active evidence；随后检查独立 Review 的剩余意见。

### 9. 本轮任务大白话讲解

- 以前只要知道一串实验 ID 就能声称“有证据”；现在系统会打开这些记录，核对它们确实来自这个候选、dev 数据、同一代码条件，而且没有把未知失败冒充结论。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 59 轮 · 隔离不同 live 批次的 reservation

### 1. 本轮任务列表

- 复核 reservation ledger 的恢复路径，发现不同批次使用相同 `repeat/case` key 会被错误视为重复调用。
- 将 CLI live reservation key 命名空间绑定到 batch ID；同一 case 只有同一批次禁止复用，网络恢复新批次可显式重跑。
- 收紧 ledger 状态机，禁止多个终态、从终态回到 RESERVED 或跳过终态直接 RECORDED。
- 让 artifact verify 在存在未物化 reservation 时失败，避免把未完成批次当作完整证据。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 账本状态现在严格为 RESERVED → COMPLETED/FAILED/UNKNOWN → RECORDED；新批次使用自己的 key 前缀。
- 增加不同批次相同 case 可运行的正向测试，保持同批次重启拒绝重复调用。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format labs/self_improvement/evaluation.py labs/self_improvement/cli.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py labs/self_improvement/cli.py tests/test_phase12_evaluation.py`
- `git diff --check`

### 5. 测试结果

- 23 个 evaluation 和 CLI 测试通过；ruff、格式和 diff 检查通过。
- 账本恢复、终态校验及新批次重跑场景均通过。

### 6. 用户如何进行手工测试

- 为两次 live 命令分别传 `--batch-id batch-a` 与 `--batch-id batch-b`，确认 ledger 中 key 前缀不同；同一 batch 重启传同 key 时 provider 不再被调用。

### 7. 风险预估与规避

- 未完成预留按已占预算保守处理；新批次不会复用旧批次 key，但仍受累计 1,200 次上限约束。
- ledger 文件是 Phase 12 专用输出，verify/report 明确跳过其 Record 解析并单独校验状态。

### 8. 下轮建议

- 重新生成本轮策略和候选修复后的活动 evidence；再冻结全量检查，准备第二轮独立对抗审查。

### 9. 本轮任务大白话讲解

- 同一批次的“第几次、哪个 case”不能重发，但网络恢复后用户明确开启的新批次可以使用新的账本编号重跑；账本还会阻止状态倒退或伪造完成。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 60 轮 · 修正 reservation 终态物化

### 1. 本轮任务列表

- 复查 live ledger 收口逻辑，修复匹配多个批次 key 时边遍历边更新状态的潜在运行时错误。
- 让 `mark_recorded_matching` 先冻结匹配 key，再逐条追加 `RECORDED` 事件。
- 保持批次命名空间、终态状态机和 artifact 未物化门禁不变。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- live 批次在结果写入后可稳定标记当前批次 reservation；历史批次同一 case 不会被误写或重复消费。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `git diff --check`

### 5. 测试结果

- 12 个 evaluation 测试通过；ruff、格式和 diff 检查通过。
- 新批次重跑和匹配物化路径均实际执行。

### 6. 用户如何进行手工测试

- 在 ledger 中写入两个批次相同 case 的终态，调用 `mark_recorded_matching(("repeat:1:case:...",))`，确认两条状态均可安全收口且不报字典迭代错误。

### 7. 风险预估与规避

- 状态更新仍是追加事件，不删除或覆盖历史；异常中断保持保守占用并由 verify 拦截。

### 8. 下轮建议

- 运行全量 Phase 12 测试，确认修复集合通过后归档旧活动候选/训练证据并重新生成当前 evidence。

### 9. 本轮任务大白话讲解

- 账本收尾时先把要更新的条目列出来，再逐条追加完成状态，避免一边遍历一边改表导致收尾过程自己崩掉。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 61 轮 · 清理失败的版本激活事务

### 1. 本轮任务列表

- 复查 active-version 写入顺序，避免 pointer 激活失败后留下没有对应 active 状态的孤立 selection receipt。
- 封装 `apply_selection`：先写新 receipt，激活失败时删除本次新 receipt；成功后返回 receipt 和 active pointer 路径。
- rollback 复用同一事务封装，保留旧 receipt 和旧 pointer，不覆盖历史版本。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- CLI 选择和回滚均使用统一事务路径；正常错误不会留下半更新 selection，文件系统异常仍由 verify 检查一致性。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_candidates.py tests/test_phase12_skill_selection.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format labs/self_improvement/candidates.py labs/self_improvement/cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/candidates.py labs/self_improvement/cli.py`
- `git diff --check`

### 5. 测试结果

- 21 个候选、选择和 CLI 测试通过；ruff、格式和 diff 检查通过。
- 现有回滚内容恢复测试继续通过，active policy 可读回。

### 6. 用户如何进行手工测试

- 令 active pointer 指向旧版本后重复提交同 selection，确认命令返回错误且 selection 目录没有新增半条 receipt；再按正常流程选择并回滚。

### 7. 风险预估与规避

- 事务清理只删除本次刚写入的 receipt；不删除历史证据。进程硬中断的跨文件窗口仍由 active/selection 一致性校验发现。

### 8. 下轮建议

- 重新跑全量专项检查和 `verify-artifacts`，确认修复后活动记录、权重和 selection evidence 仍保持绑定。

### 9. 本轮任务大白话讲解

- 如果版本指针更新失败，系统现在会把刚写的那张新票据撤掉，避免目录里出现一张说已生效、实际却没有生效的票。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 62 轮 · 对齐账本恢复与 selection 原子写入

### 1. 本轮任务列表

- 复核跨进程预算恢复，补上结果文件已落盘但尚未标记 `RECORDED` 时的启动 reconciliation，避免重复计数或错误重发。
- 让不同批次的 reservation key 保持命名空间隔离，同时只把有终态的匹配项标记为已记录；中断的 RESERVED 项继续占用额度。
- 将 selection receipt 改为 `O_EXCL + fsync` 的单文件原子创建，并在 active 激活失败时清理本次 receipt。
- 增加 ledger 父路径符号链接门禁。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- CLI live 启动会先根据已存在的 live 记录物化对应终态 reservation，再计算累计预算；硬中断留下的 RESERVED 不会被清理。
- selection/rollback 的新 receipt 不会出现半写 JSON；active 指针和 receipt 不一致时由 verify 拦截。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_candidates.py tests/test_phase12_evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format labs/self_improvement/evaluation.py labs/self_improvement/cli.py labs/self_improvement/candidates.py tests/test_phase12_candidates.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py labs/self_improvement/cli.py labs/self_improvement/candidates.py tests/test_phase12_candidates.py`
- `git diff --check`

### 5. 测试结果

- 28 个候选、ledger 和 CLI 测试通过；ruff、格式和 diff 检查通过。
- 覆盖中断预留、跨批次同 case、终态状态机、selection 激活失败清理。

### 6. 用户如何进行手工测试

- 在 live 结果已写但 ledger 未 `RECORDED` 的临时目录重启 CLI，确认 reconciliation 只收口已有终态；保留 RESERVED 行时 `verify-artifacts` 返回 2。

### 7. 风险预估与规避

- 对结果文件与 ledger 不一致采取保守策略；不会删除旧事件或自动重发同一批次调用。
- selection 原子创建只覆盖单文件写入，跨 receipt/pointer 的硬中断仍通过一致性校验和人工恢复处理。

### 8. 下轮建议

- 重新生成当前 HEAD 的报告（报告代码版本随最后代码提交更新），然后冻结完整 evidence 和专项测试。

### 9. 本轮任务大白话讲解

- 程序重启时会把已经有结果的账本条目补记为已落盘，但不会碰仍停在 RESERVED 的中断调用；selection 文件也会一次性写完整，避免半个 JSON。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 63 轮 · 澄清采用方向与 live 账本手册

### 1. 本轮任务列表

- 修正文档报告中 held-out 差值方向表述，明确 `method_a` 相对 `method_b`，避免把“基线优于 Skill 候选”误读为候选提升。
- 更新 CLI 复跑手册，补充 active-version 指针、reservation ledger、批次 ID 和 evidence 相关性门禁。
- 不改变评分器、数据、模型或业务运行时，仅同步已实现的实验操作事实。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- Adoption Card 和阶段报告将在当前代码提交后重新生成，报告会列出 Prompt `INCONCLUSIVE` 与 Skill 退化/拒绝的方向。
- 手册现在能指导用户从新批次安全恢复 live 调用，并检查 active pointer 和 selection evidence。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff format labs/self_improvement/reporting.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/reporting.py`
- `git diff --check`

### 5. 测试结果

- 报告测试通过（4 个）；ruff、格式和 diff 检查通过。
- 新文案继续保留 `DRAFT / LAB_ONLY`，没有把差值方向升级成生产采用结论。

### 6. 用户如何进行手工测试

- 阅读 `docs/phase12-cli-runbook.md` 的 LAB 选择/回滚和 Live Provider 段落；对照 summary 中 `method_a`/`method_b` 字段解释 bootstrap 结论。

### 7. 风险预估与规避

- 仅改报告和操作说明，不会改变已冻结的实验结果；报告文件必须随新 HEAD 重新生成并绑定代码版本。

### 8. 下轮建议

- 提交本轮文档代码修正后，重新生成当前 HEAD 报告，再分批提交活动 evidence 和失效归档。

### 9. 本轮任务大白话讲解

- 表格里的“改善”现在明确写成谁相对谁变好；手册也写清楚 live 预算预留在哪个文件、怎样换批次、怎样回滚实验版本。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 83 轮 · 精确绑定 live reservation key

### 1. 本轮任务列表

- 将 live `ExperimentRecord` 绑定完整批次 reservation key。
- 将账本恢复从后缀匹配改为完整 key 精确匹配，补充跨批次同 case 回归测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `run_live_baseline(s)` 及持久化记录保存完整 key；旧记录没有 key 时不推导后缀，避免误消费其他批次预算。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py tests/test_phase12_artifacts.py`
- `uv run --frozen --python 3.14 ruff check ...`、`ruff format --check ...`、`mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 18 个评估/工件测试通过；mypy、ruff 和 diff 检查退出 0。
- 新回归证明 batch-a 的记录不会把 batch-b 同一 repeat/case 的 reservation 标为 RECORDED。

### 6. 用户如何进行手工测试

- 新建 live ledger 后为两个 batch 写入相同 repeat/case，调用精确 key 对账，检查另一 batch 仍为 `UNKNOWN` 或 `RESERVED`。

### 7. 风险预估与规避

- 旧 live artifact 缺少 reservation key，继续按 legacy evidence 保留且不自动推导；后续启动恢复会优先处理其余账本状态。

### 8. 下轮建议

- 处理 CancelledError 和启动时 stale RESERVED 的显式恢复，确保中断调用不会永久留下不可解释的 RESERVED。

### 9. 本轮任务大白话讲解

- 每条真实模型调用现在带着“哪个批次、哪次重复、哪个案例”的完整凭据回写；恢复时只认完全相同的凭据，避免两个实验互相把预算结清。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 84 轮 · 中断 reservation 显式收敛

### 1. 本轮任务列表

- 增加启动时 stale `RESERVED` 的显式 `UNKNOWN` 对账。
- 捕获 provider `CancelledError`，落盘未知结果并保留已占预算。
- 增加取消调用和重启对账测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `ReservationLedger.reconcile_reserved()` 将遗留预留安全终结为 `UNKNOWN`；CLI live 启动先执行该步骤。
- `_call_provider` 的取消路径写入终态后返回 bounded `LiveCall`，不自动重发。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check ...`、`ruff format --check ...`、`mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 27 个评估/CLI 测试通过；mypy、ruff 和 diff 检查退出 0。
- 故障注入证明取消后账本为 `UNKNOWN`，新进程仍把该调用计入预算；stale reservation 不会永久阻塞新批次。

### 6. 用户如何进行手工测试

- 在 ledger 写入一条 `RESERVED` 后运行 live CLI，检查它先转为 `UNKNOWN`，随后 `verify-artifacts` 在结果未物化前保持非零，避免伪造完整证据。

### 7. 风险预估与规避

- 未知结果仍保留预算并阻止同一完整 key 重用；只有新批次可在剩余预算内继续，旧未知证据必须显式处理。

### 8. 下轮建议

- 绑定候选内容/边界 digest，并拒绝 candidate ID 与内容摘要不一致，防止结果在 artifact 之间被替换。

### 9. 本轮任务大白话讲解

- 调用被取消时，系统现在明确记账为“结果未知但额度已用”，重启只会承认这个事实，不会偷偷再打一次请求。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 85 轮 · 候选内容 digest 绑定

### 1. 本轮任务列表

- 强制 Candidate ID 摘要后缀等于候选内容 SHA-256 前 16 位。
- 在候选实验记录写入内容和边界 digest，并在验收时和候选 artifact 交叉核对。
- 增加 ID 篡改及缺少候选 digest 的负向测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- Prompt/Skill 评估调用现在保存候选内容与边界摘要；普通 replay 记录保持无候选绑定。
- 同一 candidate ID 不能承载被替换的内容，候选记录也不能脱离其内容/边界摘要通过校验。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_candidates.py tests/test_phase12_evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check ...`、`ruff format --check ...`、`mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 34 个候选、评估和 CLI 测试通过；mypy、ruff 和 diff 检查退出 0。
- 非匹配 ID 后缀和没有内容/边界 digest 的候选实验均在构造阶段被拒绝。

### 6. 用户如何进行手工测试

- 修改 `output/phase12/candidates/*.json` 的 candidate ID 或内容后运行 `verify-artifacts`，应得到非零退出并指出摘要绑定错误。

### 7. 风险预估与规避

- 摘要只证明当前文件内容一致，不承担签名或授权；业务 Registry 仍不读取实验候选。

### 8. 下轮建议

- 移除 policy 可见的 `ReplayState.case_id`，把案例追踪 ID 留在环境记录侧；该变更后重生成回放和训练 evidence。

### 9. 本轮任务大白话讲解

- 候选文件名、内容和评测结果现在互相“对得上号”；只改其中一处，验收就会发现，而不是把替换后的内容当成原候选继续统计。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 86 轮 · 隔离 ReplayState 案例标签

### 1. 本轮任务列表

- 从 policy 可见的 `ReplayState` 移除 `case_id`。
- 保留 `ReplayResult.case_id` 作为环境侧证据追踪，并让兼容 helper 不再按案例标签分派。
- 增加 policy 读取案例标签的负向回归测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 初始状态与每次环境更新只暴露观察、失败/冲突标志、剩余步数等允许特征。
- `policy_from_actions` 对旧式多案例映射拒绝，单值只作为固定动作，避免借 helper 重新引入 oracle 标签。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests -k phase12`
- `uv run --frozen --python 3.14 ruff check ...`、`ruff format --check ...`、`mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- Phase 12 专项 85 个测试通过，mypy、ruff 和 diff 检查退出 0。
- 回归 policy 只能看到无标签状态；尝试读取 `case_id` 时不能获得案例类别。

### 6. 用户如何进行手工测试

- 在 replay policy 中打印或读取 `state.case_id`，应发现该属性不存在；查看生成的 ExperimentRecord 仍保留环境侧 `case_id` 追踪。

### 7. 风险预估与规避

- 状态表示改变会使旧 replay/training evidence 绑定旧代码版本；下一步将整批重生成相关证据，不混用旧结果。

### 8. 下轮建议

- 增加冻结 replay 方法的完整 case/repeat/group 覆盖校验，并让 report/verify 拒绝裁剪或不等长配对结果。

### 9. 本轮任务大白话讲解

- 策略只能看到已经调查到的事实和安全标志，不能从案例编号猜到答案；编号仍保留在结果账本里供审计和配对统计。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 87 轮 · 冻结矩阵完整性门禁

### 1. 本轮任务列表

- 为 baseline、Reflection、Best-of-3、Prompt candidate 和 Skill candidate 固定 dev/test 三次重复的完整 case 矩阵。
- 让 canonical report 存在时 `verify-artifacts` 校验每条 case、repeat 和 group；`report` 写出前也执行同一门禁。
- 让 grouped bootstrap 拒绝两侧不等长的配对重复。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 新增冻结覆盖校验和 canonical report 文件组探测；缺失、额外或裁剪记录均明确报错。
- 完整性检查按 experiment ID 覆盖 8 个预注册 method/split 组合，天然覆盖 12 个场景组。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_heldout.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check ...`、`ruff format --check ...`、`mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 16 个 held-out/CLI 测试通过；mypy、ruff 和 diff 检查退出 0。
- 删除一个冻结 baseline 记录后，覆盖门禁按预期拒绝；配对 bootstrap 的不等长输入也被拒绝。

### 6. 用户如何进行手工测试

- 从完整回放 JSONL 删除任意一行，再运行 `report` 或 `verify-artifacts`；错误应包含 method/split 的 coverage mismatch，而不是生成新 summary。

### 7. 风险预估与规避

- 不把 live provider 的旧 24 条记录当作 replay 冻结矩阵；live evidence 继续单独统计并保留 UNKNOWN。

### 8. 下轮建议

- 为 selection 强制完整 dev 证据集，并扩大 receipt 的 evidence ID 容量；单条 cherry-pick 证据不得代表整批候选。

### 9. 本轮任务大白话讲解

- 评测文件像一张固定考场座位表：每个场景组、每次重复都必须有成绩，删掉哪怕一条，系统也不会把残缺文件当完整实验。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 88 轮 · selection 完整对照证据

### 1. 本轮任务列表

- 将 LabSelection 的 evidence 上限扩大到完整 dev 对照所需容量。
- 要求候选选择/回滚包含候选与 baseline 两侧的全部 24 case × 3 repeat 记录。
- 校验 selection evidence 的数据、代码版本、候选内容/边界 digest 和完整 experiment ID 集。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `select-lab` 与 `rollback-lab` 不再接受单条成功样本充当批量比较；必须绑定预注册的两侧 dev 矩阵。
- CLI 回归改为使用 144 条完整 baseline + Prompt candidate 证据。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py tests/test_phase12_skill_selection.py tests/test_phase12_candidates.py`
- `uv run --frozen --python 3.14 ruff check ...`、`ruff format --check ...`、`mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 25 个 selection/candidate/CLI 测试通过；mypy、ruff 和 diff 检查退出 0。
- 单条或裁剪 evidence 会先因 coverage mismatch 拒绝；候选记录 digest 不匹配也会被阻断。

### 6. 用户如何进行手工测试

- 对 dev baseline 和 candidate 各运行 `--repeats 3`，把两份 JSONL 的全部 `experiment_id` 传给 `select-lab`；删一条后命令应返回 2。

### 7. 风险预估与规避

- 选择回执只引用同一 dataset/code 版本的可复核记录；任何跨批次、跨候选或 partial cherry-pick 都不会成为采用证据。

### 8. 下轮建议

- 处理当前活动目录中旧报告与新报告冲突，建立唯一 canonical report 并校验 summary 与实际 evidence 一致。

### 9. 本轮任务大白话讲解

- 选版本像评审一整张成绩单：候选和基线的所有场景、所有重复都要在回执里出现，不能拿一个表现好的案例代替完整比较。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 89 轮 · canonical report 一致性门禁

### 1. 本轮任务列表

- 让活动目录只接受一套同代码后缀的 stage report、Adoption Card 和 summary。
- 由 manifest、全部 ExperimentRecord 和 TrainingArtifact 重新构造 summary，并比较 canonical JSON digest。
- 将第一轮和第二轮审查前的旧报告移入明确的 invalid archive，保留历史但不再作为活动证据。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `verify-artifacts` 在存在报告时检查唯一文件组、统一后缀、冻结覆盖和 summary/evidence 一致性；`report` 仍先通过完整覆盖门禁。
- 活动目录暂时没有报告，等待状态表示变更后的 replay/training/selection evidence 重新生成。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py tests/test_phase12_reporting.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check ...`、`ruff format --check ...`、`mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 33 个 CLI/report/evaluation 测试通过；canonical 后缀不一致的伪报告会被拒绝；mypy、ruff 和 diff 检查退出 0。
- 旧 `2e10b56` 与 `aca94ab` 报告已通过 Git rename 方式归档，活动目录不再存在相互矛盾的 summary。

### 6. 用户如何进行手工测试

- 在 `output/phase12/` 放入两套不同后缀报告后运行 `verify-artifacts`，应返回 canonical report set 错误；篡改 summary 后应返回 evidence 不一致。

### 7. 风险预估与规避

- 报告归档不删除历史证据；只有活动目录当前唯一 summary 才能被验收读取，旧结果必须从 invalid archive 语义上排除。

### 8. 下轮建议

- 重新生成与无 case_id ReplayState 绑定的新 replay records，并逐个 seed 重训/读回权重；旧 artifacts 保留归档。

### 9. 本轮任务大白话讲解

- 现在不能同时摆两张互相矛盾的成绩单：活动区只认一套报告，而且报告里的数字必须由当前原始记录重新算出来。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 90 轮 · 隔离旧 replay/training evidence

### 1. 本轮任务列表

- 归档 case-label 隔离修复前的 8 份 replay JSONL、9 份训练权重及 metadata、2 份 selection receipt 和 active pointer。
- 保留旧 live baseline 作为 legacy provider evidence，并标注其没有持久化 reservation key。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `output/phase12/` 已清理为可重新生成的活动证据区；历史文件通过 Git rename 移入 `output/phase12-invalid-post-review-8091274/`。
- 归档 README 明确旧证据受新 ReplayState/schema 约束影响，不可参与当前报告或采用结论。

### 4. 测试方式

- 检查 active/invalid 文件清单、Git rename 状态和工作区路径边界。
- 后续新证据生成前运行专项测试，避免把旧结果当作新代码通过依据。

### 5. 测试结果

- 归档只发生路径移动和说明新增，无业务代码变化；所有移动文件仍可从 Git 历史追溯。

### 6. 用户如何进行手工测试

- `find output/phase12 -maxdepth 1 -type f` 应只显示 manifest、audit 和 legacy live baseline；新 replay/training 需由 CLI 重新生成。

### 7. 风险预估与规避

- 不删除旧 artifact，避免失去审计链；活动验收仅扫描 `output/phase12/` 直接文件，不递归吸入 invalid archive。

### 8. 下轮建议

- 重新运行 8 个冻结 replay 方法/split 的三次重复，确认新 ExperimentRecord 带候选 digest 且不含 case label 状态。

### 9. 本轮任务大白话讲解

- 代码换了输入接口，旧成绩就像用旧题目规则考出的分数；先把它们放进带原因的历史柜，再用新规则完整重考。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 91 轮 · 重生成 baseline dev 回放

### 1. 本轮任务列表

- 用无 `case_id` policy 状态和当前代码版本重跑 baseline dev 三次重复。
- 输出 24 case × 3 repeat 的完整 JSONL，覆盖所有 dev 场景组。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 新文件 `output/phase12/evaluation-replay-baseline-dev.jsonl` 写入 72 条记录，保持 deterministic verifier 和数据 digest 绑定。

### 4. 测试方式

- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli --root . evaluate --engine replay --method baseline --split dev --repeats 3`
- `pytest tests/test_phase12_replay.py tests/test_phase12_evaluation.py tests/test_phase12_artifacts.py`
- `ruff check labs/self_improvement tests/test_phase12_*.py`、`ruff format --check ...`、`mypy labs/self_improvement`、`git diff --check`

### 5. 测试结果

- CLI 返回 0 并写入 72 条；相关 28 个测试通过，ruff/mypy/diff 检查退出 0。

### 6. 用户如何进行手工测试

- 检查该 JSONL 的 `repeat` 集合为 1/2/3、每个 repeat 有 24 个 case，并运行 `verify-artifacts` 的 coverage 检查。

### 7. 风险预估与规避

- 旧 baseline dev 已归档；新记录不携带 oracle 标签，只引用允许的回放状态和当前代码版本。

### 8. 下轮建议

- 继续重生成 baseline test，保持 held-out 的三次重复与 dev 结构一致。

### 9. 本轮任务大白话讲解

- 基线 dev 重新按新规则完整跑了一遍，每个场景组的三次成绩都补齐，旧规则下的成绩不会混入新表。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 92 轮 · 重生成 baseline held-out 回放

### 1. 本轮任务列表

- 用当前隔离后的 ReplayState 重跑 baseline held-out test 三次重复。
- 保留 test 场景组边界和 verifier 结果，供后续冻结 bootstrap 使用。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `output/phase12/evaluation-replay-baseline-test.jsonl` 生成 72 条记录，绑定 dataset digest 和当前代码版本。

### 4. 测试方式

- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli --root . evaluate --engine replay --method baseline --split test --repeats 3`
- `pytest tests/test_phase12_replay.py tests/test_phase12_heldout.py tests/test_phase12_artifacts.py`
- `ruff check labs/self_improvement tests/test_phase12_*.py`、`mypy labs/self_improvement`、`git diff --check`

### 5. 测试结果

- CLI 返回 0 并写入 72 条；15 个回放/held-out/工件测试通过，ruff/mypy/diff 检查退出 0。

### 6. 用户如何进行手工测试

- 检查 baseline test JSONL 每个 repeat 有 24 条且 `split` 全为 `test`，不要将它与 dev 记录合并。

### 7. 风险预估与规避

- test 只由冻结 manifest 驱动；oracle 不进入 policy，旧 test 记录已归档，不在本批次重复使用。

### 8. 下轮建议

- 重生成 Reflection dev 三次重复，记录一次修订调用的总 calls 和 verifier 结果。

### 9. 本轮任务大白话讲解

- held-out 基线也按新状态重新跑齐，之后所有候选只能在这套固定 test 上一次性比较，不能边看边改。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 93 轮 · 重生成 Reflection dev 回放

### 1. 本轮任务列表

- 用当前 policy 状态和 verifier 重跑 Reflection dev 三次重复。
- 保留首轮、修订轮和失败/退化记录的 calls 统计。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `output/phase12/evaluation-replay-reflection-dev.jsonl` 生成 72 条记录，记录 144 次有界 replay 调用。

### 4. 测试方式

- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli --root . evaluate --engine replay --method reflection --split dev --repeats 3`
- `pytest tests/test_phase12_methods.py tests/test_phase12_evaluation.py tests/test_phase12_replay.py`
- `ruff check labs/self_improvement tests/test_phase12_*.py`、`mypy labs/self_improvement`、`git diff --check`

### 5. 测试结果

- CLI 返回 0，records=72、calls=144；28 个方法/评估/回放测试通过，ruff/mypy/diff 检查退出 0。

### 6. 用户如何进行手工测试

- 检查 Reflection JSONL 每个 repeat 有 24 条、`calls` 只为 1 或 2，并确认最终结果都再次经过 verifier。

### 7. 风险预估与规避

- 修订策略只读首轮可观察输出，不读取 oracle；旧 Reflection evidence 保留在 invalid archive。

### 8. 下轮建议

- 重生成 Best-of-3 dev 三次重复，并单独核对三倍调用成本与无合格候选处理。

### 9. 本轮任务大白话讲解

- Reflection 的每个案例最多多看一轮反馈，新的 72 条记录把修订成本和结果一起留下，不只挑成功修复的案例。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 94 轮 · 重生成 Best-of-3 dev 回放

### 1. 本轮任务列表

- 用当前隔离状态重跑 Best-of-3 dev 三次重复。
- 记录最多三次候选调用、安全过滤、可观察 rerank 和最终 verifier 结果。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `output/phase12/evaluation-replay-best-of-3-dev.jsonl` 生成 72 条记录，calls=216，覆盖完整 dev 矩阵。

### 4. 测试方式

- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli --root . evaluate --engine replay --method best-of-3 --split dev --repeats 3`
- `pytest tests/test_phase12_methods.py tests/test_phase12_evaluation.py tests/test_phase12_replay.py`
- `ruff check labs/self_improvement tests/test_phase12_*.py`、`mypy labs/self_improvement`、`git diff --check`

### 5. 测试结果

- CLI 返回 0，records=72、calls=216；28 个方法/评估/回放测试通过，ruff/mypy/diff 检查退出 0。

### 6. 用户如何进行手工测试

- 检查每个 Best-of-3 record 的 calls 不超过 3，越界或无效候选不会被 reranker 选中。

### 7. 风险预估与规避

- 选择排序只使用候选可观察证据；test 答案和 oracle 不参与生成或排序。

### 8. 下轮建议

- 重生成 Prompt candidate dev，确认候选内容与边界 digest 随每条记录绑定。

### 9. 本轮任务大白话讲解

- Best-of-3 把每个场景最多三次尝试的成本完整记账，再先过安全门禁、后按可见证据选结果，不能用答案偷排。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 95 轮 · 重生成 Prompt candidate dev 回放

### 1. 本轮任务列表

- 用当前 Prompt candidate 内容和新 ExperimentRecord schema 重跑 dev 三次重复。
- 为每条 candidate record 写入内容 digest 与边界 digest。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `output/phase12/evaluation-replay-prompt-candidate-dev.jsonl` 生成 72 条记录，候选内容真正参与 bounded policy。

### 4. 测试方式

- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli --root . evaluate --engine replay --method prompt-candidate --split dev --repeats 3`
- `pytest tests/test_phase12_candidates.py tests/test_phase12_cli.py tests/test_phase12_evaluation.py tests/test_phase12_replay.py`
- `ruff check labs/self_improvement tests/test_phase12_*.py`、`mypy labs/self_improvement`、`git diff --check`

### 5. 测试结果

- CLI 返回 0 并写入 72 条；43 个候选/CLI/评估/回放测试通过，ruff/mypy/diff 检查退出 0。
- 抽查记录中的 candidate content/boundary digest 与候选文件一致。

### 6. 用户如何进行手工测试

- 对该 JSONL 任意一条修改 candidate digest 后运行 `verify-artifacts`，应返回 `candidate experiment digest binding mismatch`。

### 7. 风险预估与规避

- Prompt candidate 仍只改变 decision 指导，boundary/recovery/output contract 和工具白名单由边界 digest 固定。

### 8. 下轮建议

- 重生成 Prompt candidate held-out test，保持与 baseline test 同一 24 case × 3 repeat 结构。

### 9. 本轮任务大白话讲解

- Prompt 候选现在不只是挂名：它的内容和边界指纹会跟每条成绩绑定，换内容或换边界都会被验收拦住。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 96 轮 · 重生成 Prompt candidate held-out

### 1. 本轮任务列表

- 用相同 Prompt candidate 和当前 verifier 重跑 held-out test 三次重复。
- 保持 test 场景组不变，供冻结比较与 bootstrap 使用。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `output/phase12/evaluation-replay-prompt-candidate-test.jsonl` 生成 72 条记录，candidate content/boundary digest 均绑定。

### 4. 测试方式

- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli --root . evaluate --engine replay --method prompt-candidate --split test --repeats 3`
- `pytest tests/test_phase12_candidates.py tests/test_phase12_cli.py tests/test_phase12_evaluation.py tests/test_phase12_replay.py`
- `ruff check labs/self_improvement tests/test_phase12_*.py`、`mypy labs/self_improvement`、`git diff --check`

### 5. 测试结果

- CLI 返回 0 并写入 72 条；43 个候选/CLI/评估/回放测试通过，ruff/mypy/diff 检查退出 0。

### 6. 用户如何进行手工测试

- 对 Prompt test JSONL 检查 24 个 test case 各出现三次，并与 baseline test 的 case/repeat 清单逐项配对。

### 7. 风险预估与规避

- held-out 只使用冻结候选，不根据 test 输出调整内容；如评分器改变，旧 test 证据必须作废并新建批次。

### 8. 下轮建议

- 重生成 Skill candidate dev，预期保留其已知退化表现，不能只保存成功候选。

### 9. 本轮任务大白话讲解

- Prompt 候选在未见过的 test 场景上也跑齐三次，之后报告会把它和 baseline 成对统计，不能临时挑样本。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 97 轮 · 重生成 Skill candidate dev 回放

### 1. 本轮任务列表

- 用 Skill candidate 实验加载器和新记录 schema 重跑 dev 三次重复。
- 保留 Skill 候选的安全门禁与已知质量退化，不删去失败记录。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `output/phase12/evaluation-replay-skill-candidate-dev.jsonl` 生成 72 条记录，candidate 内容/边界 digest 已绑定。

### 4. 测试方式

- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli --root . evaluate --engine replay --method skill-candidate --split dev --repeats 3`
- `pytest tests/test_phase12_skill_selection.py tests/test_phase12_candidates.py tests/test_phase12_cli.py tests/test_phase12_evaluation.py tests/test_phase12_replay.py`
- `ruff check labs/self_improvement tests/test_phase12_*.py`、`mypy labs/self_improvement`、`git diff --check`

### 5. 测试结果

- CLI 返回 0 并写入 72 条；50 个 Skill/candidate/CLI/评估/回放测试通过，ruff/mypy/diff 检查退出 0。
- 退化记录仍被保留，后续 held-out 结论可以明确拒绝该候选。

### 6. 用户如何进行手工测试

- 检查 Skill dev JSONL 的 candidate digest 与文件一致，并确认任何工具越界内容不会进入 policy 加载。

### 7. 风险预估与规避

- Skill 内容不能追加工具、远程引用或可执行脚本；业务 Skill Registry 文件保持不变。

### 8. 下轮建议

- 重生成 Skill candidate held-out test，保留与 baseline 的配对矩阵和真实退化证据。

### 9. 本轮任务大白话讲解

- Skill 候选按同样规则完整跑 dev，效果不好的成绩也留在账本里，后面会用 test 证据决定拒绝而不是删掉它。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 98 轮 · 重生成 Skill candidate held-out

### 1. 本轮任务列表

- 用冻结 Skill candidate 重跑 held-out test 三次重复。
- 保留 Skill 相对 baseline 的失败与退化，供最终 bootstrap 和采用结论引用。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `output/phase12/evaluation-replay-skill-candidate-test.jsonl` 生成 72 条记录，内容/边界 digest 与当前候选一致。

### 4. 测试方式

- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli --root . evaluate --engine replay --method skill-candidate --split test --repeats 3`
- `pytest tests/test_phase12_skill_selection.py tests/test_phase12_cli.py tests/test_phase12_evaluation.py tests/test_phase12_replay.py tests/test_phase12_heldout.py`
- `ruff check labs/self_improvement tests/test_phase12_*.py`、`mypy labs/self_improvement`、`git diff --check`

### 5. 测试结果

- CLI 返回 0 并写入 72 条；48 个 Skill/CLI/评估/回放/held-out 测试通过，ruff/mypy/diff 检查退出 0。
- Skill 退化结果完整保留，后续 summary 的比较方向会明确 baseline 相对 Skill 的回归。

### 6. 用户如何进行手工测试

- 将 Skill test 与 baseline test 按 `case_id/repeat` 配对，检查失败比例和安全率，不只看平均分。

### 7. 风险预估与规避

- test 结果不反馈给候选内容；若 verifier 或候选改变，当前 held-out 证据立即失效并需新批次。

### 8. 下轮建议

- 重新生成 3 个 SFT seed 权重；新 ReplayState 特征变更后必须验证参数更新与新进程读回。

### 9. 本轮任务大白话讲解

- Skill 在未见 test 上的成绩也完整跑完，效果变差的记录会留在报告里，确保最终拒绝是基于整批证据。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
## 2026-09-12 · Phase 12 · 第 99 轮 · 校正 selection 证据边界

### 1. 本轮任务列表

- 复核完整 selection 证据与原子提交代码版本绑定的交互。
- 保留完整 candidate dev case/repeat 矩阵、method/candidate/dataset/code 校验；移除会把不同日志提交误当不同实现的额外 baseline 嵌入要求。
- 更新 CLI 回归为完整 72 条候选 evidence。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- selection receipt 仍不能用单条 cherry-pick 证据通过；baseline 对照由预注册 held-out/report 比较单独绑定。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py tests/test_phase12_candidates.py tests/test_phase12_skill_selection.py`
- `uv run --frozen --python 3.14 ruff check ...`、`ruff format --check ...`、`mypy labs/self_improvement`、`git diff --check`

### 5. 测试结果

- 26 个 selection/candidate/CLI 测试通过；mypy、ruff 和 diff 检查退出 0。
- 完整候选矩阵可在不同日志提交生成的同一实验阶段内被验收，单条 evidence 仍被 coverage mismatch 拒绝。

### 6. 用户如何进行手工测试

- 传入 Prompt candidate dev JSONL 的全部 72 个 `experiment_id` 可创建 receipt；只传第一条会返回 2。

### 7. 风险预估与规避

- 证据仍绑定每条记录自身的 code_version；报告阶段统一按当前 summary 重算，避免用额外拼接记录制造虚假统一版本。

### 8. 下轮建议

- 生成新的完整 Prompt selection/rollback receipt，并检查 active pointer 内容哈希与回放版本一致。

### 9. 本轮任务大白话讲解

- 回执要求的是候选完整成绩单和明确身份指纹；基线对照已经在实验报告中固定，不再把两个不同提交时间的文件硬塞成一张回执。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
