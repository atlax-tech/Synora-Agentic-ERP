## 2026-09-12 · Phase 12 · 第 47 轮 · 对齐 live 只读动作白名单

### 1. 本轮任务列表

- 让 live baseline 与本地 replay 共用完整的六种只读工具、`ASK_INPUT` 和 `FINISH` 动作白名单。
- 增加评测入口回归检查，避免模型被错误缩窄到单一工具后把能力问题误判为模型失败。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `run_live_baseline` 与 `run_live_baselines` 现在均从 `READ_ACTIONS` 生成排序稳定的允许动作；未改变业务 Runtime 或 ERP 工具注册。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_evaluation.py tests/test_phase12_methods.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py tests/test_phase12_methods.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py tests/test_phase12_methods.py`
- `uv run --frozen --python 3.14 --group retrieval-lab mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 评测与方法专项 13 个测试通过；ruff、format、mypy 和 diff 检查均通过。

### 6. 用户如何进行手工测试

- 使用合成 live provider 记录请求 payload，确认 `allowed_actions` 包含六个只读工具、`ASK_INPUT` 和 `FINISH`，且排序稳定。

### 7. 风险预估与规避

- 白名单仍只包含只读动作和两个控制动作；不暴露写入口，不改变权限、policy 或工具能力。

### 8. 下轮建议

- 提交本轮修复后，归档旧训练/报告证据并按最终代码版本重新生成 artifact，再进入阶段全量验证。

### 9. 本轮任务大白话讲解

- live 实验如果只告诉模型一个工具，它即使想调查其他只读信息也会被门禁拦住，结果看起来像模型不行。现在 live 和 replay 使用同一份只读动作清单，比较才公平。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 46 轮 · 统一损坏权重错误

### 1. 本轮任务列表

- 捕获权重 JSON 转换中的底层张量类型/形状异常，统一返回契约级 `ValueError`。
- 增加字符串权重值的负向读回测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 任意损坏权重都在模型加载边界被拒绝，不会以未分类异常进入 CLI 或报告。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 训练专项 8 个测试通过；非法 schema、字符串值、NaN 和维度损坏均返回 `ValueError`。

### 6. 用户如何进行手工测试

- 改写任一权重 `state_dict` 值为字符串或错误维度，运行 `verify-artifacts`，应返回 2 并指出权重契约错误。

### 7. 风险预估与规避

- 只在 JSON→tensor 边界捕获已知转换异常，不放宽有限值、shape 或未知 key 检查；不执行任意 artifact 内容。

### 8. 下轮建议

- 停止功能改动，归档并重生成当前代码版本 report，然后执行 P12.16 全量验证和证据冻结。

### 9. 本轮任务大白话讲解

- 坏权重不能因为底层库抛了别的异常就绕过实验门禁。统一成契约错误后，CLI 能给出稳定退出码，审计也能按同一类问题统计。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 45 轮 · 拒绝非法预算起点

### 1. 本轮任务列表

- 为 `CallBudget` 增加 maximum、used 和连续阻塞计数的初始化校验。
- 增加超过 1,200 次及已用量超过上限的负向测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- live 批次重启或手工构造非法预算状态会在 Provider 调用前失败，累计预算不会被绕过。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 评测专项 9 个测试通过；非法 maximum/used 状态返回 `ValueError`，静态检查通过。

### 6. 用户如何进行手工测试

- 构造 `CallBudget(maximum=1201)` 或 `CallBudget(maximum=1, used=2)`，确认不会创建可运行的批次。

### 7. 风险预估与规避

- 校验只限制内存中的批次状态，不自动重置历史调用；已有 JSONL 仍由 `verify_records` 汇总。

### 8. 下轮建议

- 停止新增功能，进行最终全量测试、artifact/报告重算和出口证据冻结。

### 9. 本轮任务大白话讲解

- 预算像一个总账，账本一开始就超支时不能先开工再补解释。现在批次创建就检查上限和已用量。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 44 轮 · 区分 usage 未知与不适用

### 1. 本轮任务列表

- 将 Provider 记录的未知 usage 与 deterministic replay 的 usage 不适用分开统计。
- 扩展阶段报告表格和 reporting 测试，避免把本地零调用误报为外部用量缺失。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- `unknown_usage_records` 只计实际 Provider 调用；replay 记录新增 `usage_not_applicable_records`。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- reporting 专项通过；replay usage unknown=0、usage n/a=24，live 仍按实际已知/未知 usage 统计。

### 6. 用户如何进行手工测试

- 运行 `report`，检查 deterministic 方法的 usage n/a 等于记录数，`live-baseline` 的 unknown usage 等于未报告 usage 的记录数。

### 7. 风险预估与规避

- 只改汇总口径和表格，不改变原始记录、预算或 Provider 调用；原始 usage 字段保持不可变。

### 8. 下轮建议

- 重新生成带当前 reporting 代码版本的 report，再进行全量阶段门禁。

### 9. 本轮任务大白话讲解

- 没调用 Provider 时没有“未知用量”，而是“用量不适用”。分开后，报告不会让人误以为 replay 有隐藏成本或缺失计费数据。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 43 轮 · 绑定训练实现版本

### 1. 本轮任务列表

- 训练元数据改为记录当前 Git 实现版本，同时继续绑定冻结 dataset digest。
- 保持 SFT、DPO、REINFORCE 的 seed、权重和指标配置不变。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 训练 artifact 能区分“训练实现版本”和“数据生成版本”；旧元数据后续归档，活动权重需重写。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_training.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 训练专项测试、静态检查和类型检查通过；元数据代码版本由当前 Git HEAD 生成。

### 6. 用户如何进行手工测试

- 运行任一 `train` 命令，检查 metadata 的 `code_version` 等于命令执行时 HEAD，`dataset_digest` 仍等于冻结 manifest。

### 7. 风险预估与规避

- 版本记录只读 Git revision，不读取 secret；重写权重前先归档旧 metadata，避免覆盖历史证据。

### 8. 下轮建议

- 归档当前 9 份训练 artifact，按新元数据绑定重跑，再生成最终阶段报告。

### 9. 本轮任务大白话讲解

- 数据版本回答“拿哪批题训练”，代码版本回答“用哪套训练逻辑”。两者分开记录，出现问题时才知道该复现哪一个。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 42 轮 · 改为可审阅的权重 JSON

### 1. 本轮任务列表

- 将训练权重从巨大单行 JSON 改为排序稳定、缩进可审阅的 JSON。
- 保持权重 digest 基于 canonical payload 不变，并增加多行格式断言。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 权重 artifact 不再通过单行压缩隐藏变更；读回、有限值和 digest 校验保持一致。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 训练专项 7 个测试通过；权重文件包含可审阅的多行 JSON，digest round-trip 和非有限值拒绝通过。

### 6. 用户如何进行手工测试

- 查看任一 `output/phase12/weights-*.json`，确认 `state_dict` 分层换行；用 `verify-artifacts` 确认读回 digest 仍一致。

### 7. 风险预估与规避

- 格式改变不改变 canonical digest；旧单行权重归档为失效证据，新的权重必须重新写入且不覆盖。

### 8. 下轮建议

- 归档当前训练权重并按固定 seed 重跑，再生成最终 report/summary 和出口命令证据。

### 9. 本轮任务大白话讲解

- 实验 artifact 既要能读回，也要方便审查。把权重展开成多行后，reviewer 能看到每层结构，摘要哈希仍保持可复现。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 41 轮 · 修正 REINFORCE baseline 时序

### 1. 本轮任务列表

- 使用当前 episode 更新前的移动 baseline 计算 REINFORCE advantage。
- 增加每个 seed 120 秒墙钟门禁，超时不保存成功 checkpoint。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- RL 训练不再把当前或未来回报混入 baseline；每个 seed 的 episode 上限与墙钟上限均有代码门禁。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_reinforce.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- REINFORCE 和训练专项测试通过，固定 seed 仍可复现；静态检查通过。

### 6. 用户如何进行手工测试

- 用短 episode 运行 `train_reinforce`，在第一轮检查 advantage 等于折扣回报（初始 baseline 为 0），第二轮才使用上一轮更新后的 baseline。

### 7. 风险预估与规避

- 超时或异常发生在写 artifact 前，既有权重不覆盖；本轮修复后的 RL 权重必须重新生成。

### 8. 下轮建议

- 归档当前 RL 权重并按修复后的代码重跑三个 seed，再更新 report/summary。

### 9. 本轮任务大白话讲解

- baseline 是用来降低方差的“过去成绩均值”，不能偷看本轮甚至测试集成绩。现在先用旧均值算梯度，再把本轮成绩纳入下一轮。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 40 轮 · 固化 CLI 复跑手册

### 1. 本轮任务列表

- 新增 `docs/phase12-cli-runbook.md`，列出数据审核、候选、replay/live、选择回滚、SFT/DPO/REINFORCE 和 artifact 验收命令。
- 明确默认命令不联网、错误码、预算占用、Provider 中断和 LAB_ONLY 边界。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 阶段 CLI 入口具备可复制的端到端操作说明；手册不把当前 DRAFT 证据写成生产能力。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- CLI 专项、ruff、format 和 diff 检查通过；手册只引用已实现的命令和当前限制。

### 6. 用户如何进行手工测试

- 按手册顺序在干净临时目录执行 replay 短流程；最后运行 `verify-artifacts`，应返回结构化 `PASS`。

### 7. 风险预估与规避

- live 命令单独标为显式联网入口，不在默认复跑中触发；环境变量只从用户维护文件读取，不复制或打印。

### 8. 下轮建议

- 归档当前证据状态，执行完整 Phase 12 专项/仓库验证和 Harness 只读检查，冻结最终 diff。

### 9. 本轮任务大白话讲解

- 复现实验最怕“少了一步”或误把默认命令跑到线上。手册把每条命令、输入目录、错误码和联网开关写清楚，别人可以照着重跑。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 39 轮 · 内容级版本回滚门禁

### 1. 本轮任务列表

- 为 LAB 选择回执增加父版本和新版本的内容 digest。
- 选择、回滚和 `verify-artifacts` 重新读取当前版本内容，拒绝 digest 已变化的版本。
- 增加篡改候选后回滚失败的测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- A→B→A 的回滚证据现在包含可重算的内容摘要；旧选择文件仍不可覆盖。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_skill_selection.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/candidates.py labs/self_improvement/contracts.py labs/self_improvement/cli.py tests/test_phase12_skill_selection.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/candidates.py labs/self_improvement/contracts.py labs/self_improvement/cli.py tests/test_phase12_skill_selection.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 选择、回滚、来源和 CLI 验收测试通过；候选内容被篡改时回滚返回非零错误。

### 6. 用户如何进行手工测试

- 运行 `select-lab` 后修改候选 JSON 内容，再运行 `rollback-lab`；命令应报告版本内容 digest 变化并拒绝写回滚回执。

### 7. 风险预估与规避

- Native Prompt 和 Skill registry 也计算当前内容摘要；摘要仅用于一致性验证，不代表签名或授权。

### 8. 下轮建议

- 按当前代码版本归档 superseded replay/report 文件，重跑候选和训练的短流程并更新阶段报告。

### 9. 本轮任务大白话讲解

- 真正的回滚要确认恢复的是同一份内容，单看版本名字不够。现在回执保存摘要，内容被改过就不能假装回滚成功。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 38 轮 · 绑定候选评测 artifact

### 1. 本轮任务列表

- 为 `ExperimentRecord` 增加可选 `candidate_id` 追踪字段。
- 候选 replay 评测前强制读取对应唯一候选，`verify-artifacts` 校验方法、候选类型和来源一致。
- 更新 CLI 测试覆盖缺少候选和未绑定来源的失败路径。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- Prompt/Skill 评测结果现在可回溯到具体候选内容；缺失或错配 artifact 会被拒绝。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_cli.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/cli.py labs/self_improvement/contracts.py labs/self_improvement/evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/cli.py labs/self_improvement/contracts.py labs/self_improvement/evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- CLI、评测、ruff、format 和 mypy 均退出 0；候选 replay 记录写入对应 `candidate_id`。

### 6. 用户如何进行手工测试

- 先运行 `audit-data`、`make-candidates`、候选 `evaluate`，检查 JSONL 每条候选记录的 ID 与候选文件名一致；删掉候选后 `verify-artifacts` 应失败。

### 7. 风险预估与规避

- 候选文件只读自 `output/phase12/candidates`，不会加载业务 Registry；历史来源仍经 audit 重新核对。

### 8. 下轮建议

- 归档 superseded report，按最终代码版本重新生成 replay、训练与阶段报告，再冻结出口证据。

### 9. 本轮任务大白话讲解

- 实验表里写着“Prompt 候选”不代表真的加载了候选。现在每条记录都带具体候选 ID，验收还会检查这个文件和来源确实存在。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 37 轮 · 固定标准 DPO 损失

### 1. 本轮任务列表

- 抽取标准 DPO 偏好损失并让 SFT 初始化的 DPO 训练路径复用该实现。
- 增加策略与 reference 相同情况下损失接近 `log(2)` 的数值断言。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- DPO 的 chosen/rejected 对数概率差、冻结 reference 和 beta=0.1 目标均由同一函数实现并可独立验证。

### 4. 测试方式

- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 训练专项 7 个测试通过；相同 policy/reference 的 DPO loss 与 `ln(2)` 差值小于 `1e-6`，静态检查通过。

### 6. 用户如何进行手工测试

- 用同一份本地 SFT 模型作为 policy 和 reference 调用 `dpo_loss`，检查输出约为 0.693147；改变 chosen/rejected 后再观察更新方向。

### 7. 风险预估与规避

- reference logits 在训练前置于无梯度路径并记录 digest；不安全偏好对仍由既有安全门禁排除。

### 8. 下轮建议

- 让候选评测强制读取对应候选 artifact，并在记录中保留 candidate ID，防止只改变方法标签而未绑定候选内容。

### 9. 本轮任务大白话讲解

- DPO 比较的是同一状态下“更好答案”和“较差答案”相对 reference 的偏好差。两者完全一样时没有偏好，损失应正好落在 `log(2)` 附近。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 36 轮 · 隔离模型输入标签

### 1. 本轮任务列表

- 增加 provider 可见任务文本投影，移除内部 `Scenario` 类别标签。
- 让单次和批量 live 调用统一使用投影，并验证期望动作、状态不会进入模型输入。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 模型输入与评分侧标签的边界已显式化；环境仍保留类别用于本地状态转移和 verifier。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_data.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/data.py labs/self_improvement/evaluation.py tests/test_phase12_data.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/data.py labs/self_improvement/evaluation.py tests/test_phase12_data.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 六类数据的投影均不含 `Scenario=`、期望动作或期望状态；live 调用与既有评测测试保持通过。

### 6. 用户如何进行手工测试

- 用任一合成案例调用 `model_input_text`，检查输出仍保留任务描述但没有内部场景标签和评分答案。

### 7. 风险预估与规避

- 投影只影响 provider 文本，不修改冻结数据 manifest、oracle 或本地 verifier；输入仍受 4,000 字符上限约束。

### 8. 下轮建议

- 暴露并测试标准 DPO 损失的 reference 等价性，再重新生成报告。

### 9. 本轮任务大白话讲解

- 数据分类是实验人员用来组织场景的标签，不能偷偷变成模型的答案提示。现在发给 provider 前会去掉这段内部标签。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 35 轮 · 校验候选历史来源

### 1. 本轮任务列表

- 在 `verify-artifacts` 中核对候选的 `source_case_ids` 是否属于当前已复核历史失败清单。
- 增加篡改候选来源的负向 CLI 测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 候选来源追溯门禁已补齐；未知历史来源现在会使 artifact 验收非零退出。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 候选来源被替换为未审核 ID 时，`verify-artifacts` 返回 2；正向 CLI、ruff、format 和 mypy 均应退出 0。

### 6. 用户如何进行手工测试

- 先运行 `audit-data`、`make-candidates`，再改写候选 JSON 的 `source_case_ids`；运行 `verify-artifacts` 应报告未审核历史失败来源。

### 7. 风险预估与规避

- 复核清单仍由显式 Phase 11 来源白名单重新计算，篡改 audit 或候选都不会被 artifact 验收接受。

### 8. 下轮建议

- 再补 DPO 标准损失的 reference 等价性断言，并审查候选评测是否加载了实际候选 artifact。

### 9. 本轮任务大白话讲解

- 候选说自己来自某次失败还不够，验收必须能在当前复核清单里找到这个失败。现在来源 ID 对不上就直接拒绝。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

# Phase 12 开发日志

## 2026-09-12 · Phase 12 · 第 34 轮 · 拒绝非有限训练权重

### 1. 本轮任务列表

- 在 JSON 权重写出前检查每个张量的有限性。
- 增加 NaN 权重写入失败测试，防止损坏模型 artifact 被保存为“训练成功”。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.10/P12.13 权重安全门禁已闭合；合法权重读回路径保持不变。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_training.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 9 个训练测试通过；NaN 写入返回 `ValueError`，ruff、format 和 mypy 均退出 0。

### 6. 用户如何进行手工测试

- 构造一个参数含 NaN 的本地模型并调用 `save_weights`，命令应在写文件前失败且目标文件不存在。

### 7. 风险预估与规避

- 有限性检查只作用于实验 JSON，不加载 pickle、脚本或远程代码；现有已生成权重仍由 verify-artifacts 逐个读回。

### 8. 下轮建议

- 重新执行当前代码版本的报告和 artifact verify，之后进入阶段末独立审查准备。

### 9. 本轮任务大白话讲解

- NaN 会让模型看似有文件、实际上无法稳定预测。现在保存前就拦截，坏权重不会进入可复现证据目录。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 33 轮 · 完善 Provider 阻塞分类

### 1. 本轮任务列表

- 将 `TRANSPORT_ERROR`、`TIMEOUT` 和单调用超时纳入连续阻塞失败门禁。
- 用真实 Provider 常见的 `TRANSPORT_ERROR` 注入测试连续三次后停止批次。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.4 的连接/认证/协议故障分类覆盖了 Provider 实际返回的传输错误；预算仍在请求前占用。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 8 个评测测试通过；连续第三次传输失败后第四次不再调用 Provider，ruff、format 和 mypy 均退出 0。

### 6. 用户如何进行手工测试

- 使用故障注入 Provider 连续返回 `TRANSPORT_ERROR`，确认批次状态为 `MODEL_BATCH_BLOCKED` 且已用调用数保持 3。

### 7. 风险预估与规避

- 只扩充错误分类集合，不重试、不切换 Provider、不重置累计预算。

### 8. 下轮建议

- 重新生成当前代码绑定的报告，随后冻结最终 artifact 并执行一次独立对抗审查。

### 9. 本轮任务大白话讲解

- Provider 报“传输失败”时，和连接断开一样都说明继续请求没有意义。把它纳入三次熔断，既省预算，也不会把环境故障误算成模型能力。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 32 轮 · 显式记录证据代码版本

### 1. 本轮任务列表

- 在 summary/report 中列出所有评测记录实际绑定的代码版本。
- 说明修复重跑可能产生多版本证据，避免报告标题版本被误读为每条记录版本。
- 增加报告字段回归断言。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.14/P12.16 证据版本追踪已补齐；数据 digest、评测记录版本、训练 metadata 和报告版本可以分别核对。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 4 个报告测试通过；报告包含 `evidence_code_versions` 行，ruff、format 和 mypy 均退出 0。

### 6. 用户如何进行手工测试

- 打开 `phase12-summary-<code>.json`，对照 `evidence_code_versions` 与各 JSONL 的 `code_version` 字段。

### 7. 风险预估与规避

- 只增加透明度字段，不改写旧记录或把不同版本的结果合并为同一 trial。

### 8. 下轮建议

- 重新生成当前报告，继续冻结输出目录和独立审查输入。

### 9. 本轮任务大白话讲解

- 报告文件名只代表生成报告的代码版本，里面的记录可能来自修复前后的不同批次。把记录版本列出来，审查者不用猜每一行是哪次运行。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 31 轮 · 隔离 rerank 的 oracle 门禁

### 1. 本轮任务列表

- 将 Reflection 和 Best-of-N 的候选门禁改为只读取安全、终止和观察证据。
- rerank 排序改为观察数量、步数和生成顺序，oracle 只在最终选出后评分。
- 增加“可观察候选被 oracle 判错仍可进入候选集”的回归测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.5/P12.6 的 oracle 泄漏风险已修复；旧 Reflection/Best-of-N 原始 JSONL 需要归档并按新门禁重跑。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py tests/test_phase12_methods.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 12 个评测/方法测试通过；错误期望动作的 `ASK_INPUT` 候选仍能被 rerank 返回，但其最终 verifier 结果保持失败，证明筛选没有读答案。

### 6. 用户如何进行手工测试

- 对同一案例运行 `rerank_candidates`，传入一个结构安全但期望动作错误的候选；候选应先被保留，再由最终 verifier 标记失败。

### 7. 风险预估与规避

- rerank 不读取 `expected_action`、`expected_status` 或 `oracle`；失败原因 `VERIFIER_MISMATCH` 也不会被当作结构失败。

### 8. 下轮建议

- 提交本轮修复后重建 Reflection/Best-of-N dev/test 证据，再继续 Provider 故障分类审计。

### 9. 本轮任务大白话讲解

- 选候选时只能看候选自己带来的证据，不能偷看标准答案。现在即使候选最后答错，也会先按安全结构参与选择，选完后才由 verifier 告诉实验统计它是否正确。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 30 轮 · 记录 live 重复批次中断限制

### 1. 本轮任务列表

- 将三次重复 assist held-out 重跑的中断事实写入阶段报告限制区。
- 保留已完成的一次重复 24 条 JSONL，明确中断批次没有半批结果可用于评分。
- 增加报告文字回归断言，避免把一次重复误写成完整三次重复证据。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.14 的真实 Provider 证据保持可追溯；由于长连接无响应，中断重跑不计入成功或失败分母，阶段采用结论仍为限制项。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`

### 5. 测试结果

- 4 个报告测试通过；ruff、format、mypy 和 artifact verify 均退出 0。
- 当前真实 evidence 是 24 条一次重复记录（3 SUCCEEDED、13 REJECTED、8 UNKNOWN）；中断的三次重复命令没有生成 JSONL。

### 6. 用户如何进行手工测试

- 查看阶段报告“限制与未运行项”，确认它同时写出一次重复的真实结果和三次重复重跑未完成的原因。

### 7. 风险预估与规避

- 不把被中止批次的未知预算或响应猜成 usage；原始一次重复文件保持不可变，未生成伪造补齐行。

### 8. 下轮建议

- 归档旧报告并生成当前代码绑定的限制口径，继续完成候选/训练 artifact 冻结后再启动独立 Review。

### 9. 本轮任务大白话讲解

- 一次请求卡在网络里时，最危险的做法是把它当失败或补一条默认结果。这里明确记成“批次未完成”，只用已经写完的 24 条，证据边界才不会被扩大。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 29 轮 · 固定 REINFORCE 的 SFT 起点

### 1. 本轮任务列表

- 将 REINFORCE API 改为显式接收同 seed 的 SFT 模型。
- CLI 缺少 SFT checkpoint 时拒绝启动 RL，避免随机起点冒充可比实验。
- 增加初始权重哈希一致性测试并更新测试替身调用。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.13 的训练起点约束已闭合；REINFORCE 仍独立于 DPO，参数在本地环境中真实更新。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reinforce.py tests/test_phase12_training.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py labs/self_improvement/cli.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py labs/self_improvement/cli.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 16 个训练/CLI 测试通过；初始 REINFORCE 权重哈希与 SFT 哈希一致，训练后哈希改变；ruff、format 和 mypy 均退出 0。

### 6. 用户如何进行手工测试

- 先对同一 seed 执行 `train --method sft --seed 17`，再执行 `train --method reinforce --seed 17`；查看两个 metadata 的 `initial_weight_sha256` 和 SFT `weight_sha256`。

### 7. 风险预估与规避

- RL 不会隐式训练或覆盖 SFT 文件；缺失起点直接报错，旧权重不可覆盖。

### 8. 下轮建议

- 归档旧的随机起点 RL 权重，按冻结 SFT 起点重建三 seed 权重，并重新验证 artifact。

### 9. 本轮任务大白话讲解

- 如果 RL 从随机权重开始，就无法知道提升来自 SFT 还是来自 RL。现在两者从同一个已保存模型出发，差异才有可解释性。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 28 轮 · 重算审核 artifact 来源哈希

### 1. 本轮任务列表

- 让 `verify-artifacts` 重新执行显式历史来源审核，并逐条比对导出的 ReviewedCase。
- 让候选生成复用同一审核结果，防止篡改审核 JSON 后伪造来源 ID。
- 增加审核记录篡改的失败路径测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.1/P12.15 来源完整性门禁已闭合；原始来源、sha256、脱敏状态和审核枚举必须一致。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 8 个 CLI 测试通过；篡改 `review_reason` 后 `verify-artifacts` 返回 2；ruff、format 和 mypy 均退出 0。

### 6. 用户如何进行手工测试

- 修改 `output/phase12/audited-failures.json` 任一审核字段后执行 `verify-artifacts`，命令应拒绝并保留原文件，不生成新的候选。

### 7. 风险预估与规避

- 校验只读取明确的 Phase 11 allowlist，不递归读取 output，也不把哈希当作签名或授权。

### 8. 下轮建议

- 归档旧候选/选择回执后重建审核来源绑定 artifact，重新生成报告并做最终 verify。

### 9. 本轮任务大白话讲解

- 审核结果本身也是输入，不能因为它位于 output 就自动可信。现在每次验收都会回到四个明确源文件重算，文件被改过就立刻停在门禁处。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 27 轮 · 绑定失败审核来源到候选

### 1. 本轮任务列表

- 让 `make-candidates` 只从已写入的审核 artifact 读取来源。
- 没有可接受或背景可追溯失败时返回非零，不再使用无来源的 `phase12-synthetic` 占位。
- 删除不再生效的 `--source` CLI 参数，并补充成功与阻塞路径测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.1/P12.7 的失败驱动链路已收紧；候选现在引用 `phase12-historical-*` ID，仍只写实验目录。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py tests/test_phase12_candidates.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 11 个 CLI/candidate 测试通过；ruff、format 和 mypy 均退出 0。
- 未审核来源时命令退出 2；写入最小可追溯历史失败后候选生成成功且来源 ID 通过断言。

### 6. 用户如何进行手工测试

- 先执行 `python -m labs.self_improvement.cli audit-data`，再执行 `make-candidates`；打开候选 JSON，确认 `source_case_ids` 是历史审核记录 ID。

### 7. 风险预估与规避

- 候选作者只能读取审核后的脱敏摘要；没有通过审核的输入不会进入候选生成，test 答案仍不可见。

### 8. 下轮建议

- 归档旧的无来源候选与选择回执，按新的审核来源重建候选并更新 report。

### 9. 本轮任务大白话讲解

- 失败驱动不是给候选随便贴一个标签。现在 CLI 必须先找到审核记录，再把那条记录的 ID 带进候选，这样以后才能沿着 ID 回到原始哈希和审核结论。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 26 轮 · 全量门禁与复杂度审计

### 1. 本轮任务列表

- 执行仓库格式、lint、类型、unit、integration 和 Phase 12 专项训练检查。
- 执行 Harness 结构、manifest、引用和 drift 四项只读检查。
- 完成 ponytail-review、ponytail-audit 和 ponytail-debt 扫描，冻结当前实现风险清单。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.16 技术门禁已通过；阶段报告仍为 `DRAFT / LAB_ONLY`，独立对抗审查和 Harness 写入授权尚未开始。
- 复杂度扫描未发现本阶段可安全删除的重复抽象；Phase 12 无新增 `ponytail:` debt，仓库仅有一条既有服务层标记且含升级触发条件。

### 4. 测试方式

- `make format-check`
- `make lint`
- `make type`
- `make unit`
- `make integration`
- `uv run --frozen --python 3.14 --group retrieval-lab pytest tests -k phase12`
- `uv run --frozen --python 3.14 --group retrieval-lab mypy labs/self_improvement`
- `python3 .agents/skills/harness-build/scripts/validate_harness_structure.py .`
- `python3 .agents/skills/harness-check/scripts/validate_manifest.py .`
- `python3 .agents/skills/harness-check/scripts/check_references.py .`
- `python3 .agents/skills/harness-check/scripts/detect_drift.py .`
- `uv run --frozen --python 3.14 python -m labs.self_improvement.cli verify-artifacts`
- 干净临时根目录顺序复跑 `audit-data`、`prepare-data`、`make-candidates`、replay 五方法、held-out replay、三种训练、`select-lab`、`rollback-lab`、`report`、`verify-artifacts`。

### 5. 测试结果

- format、lint、type、unit、integration、专项测试和专项 mypy 均退出 0；unit 为 1034 passed，integration 为 248 passed，Phase 12 为 60 passed。
- Harness 四项均 `valid: true` 且无 broken reference；artifact verify 为 `PASS`（10 个评测文件、672 条记录、2 个候选、2 个 selection、9 个权重）。
- 干净进程 CLI 全链路每条命令退出 0；默认 replay 不触发 Provider。

### 6. 用户如何进行手工测试

- 在仓库根目录运行 `python -m labs.self_improvement.cli verify-artifacts`，再查看 `output/phase12/phase12-stage-report-draft-<code>.md` 的方法表、held-out 区间和风险登记。

### 7. 风险预估与规避

- 全量验证未改变 ERP/Frappe、业务 Runtime、`.env*` 或 `.harness/`；真实模型证据仍按 Provider 失败、UNKNOWN、usage 未知分别统计。
- 阶段报告不写 PASS；后续独立 Review 若发现证据失真，按缺陷小步修复并重跑受影响检查。

### 8. 下轮建议

- 将最终 output artifact 按数据、评测、训练和报告边界冻结提交；完成后启动唯一一次独立对抗审查。

### 9. 本轮任务大白话讲解

- 这一轮像交付前总检：先确认代码能运行，再确认实验结果没有越界引用，最后确认文件里没有多余复杂层。所有检查都绿了，但“是否值得采用”仍要等独立审查和证据门禁。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 25 轮 · 修正 Harness 引用边界

### 1. 本轮任务列表

- 处理 Harness 结构检查发现的单个 broken-reference。
- 将旧日志中的解释器路径改为已核实的 Python 版本描述，避免把本地虚拟环境路径当作项目文件引用。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.16 Harness 结构检查的引用错误已修复；没有改变代码、数据、模型权重或实验结论。

### 4. 测试方式

- `python3 .agents/skills/harness-build/scripts/validate_harness_structure.py .`
- `python3 .agents/skills/harness-check/scripts/validate_manifest.py .`
- `python3 .agents/skills/harness-check/scripts/check_references.py .`
- `python3 .agents/skills/harness-check/scripts/detect_drift.py .`

### 5. 测试结果

- 本轮先修正导致失败的路径引用，随后重跑四项 Harness 检查；结果将在下一轮阶段证据中记录。

### 6. 用户如何进行手工测试

- 在仓库根目录运行 `validate_harness_structure.py .`，确认不再出现 `escapes-root`。

### 7. 风险预估与规避

- 仅改写日志中的路径文字，不覆盖 `.harness/`，不触发 Harness 写入，也不暴露 Secret。

### 8. 下轮建议

- 完成 Harness 四项只读检查和复杂度审计，再冻结阶段 diff。

### 9. 本轮任务大白话讲解

- Harness 会把 Markdown 里的路径当作可点击证据入口。虚拟环境路径不属于仓库证据，所以换成版本事实，既保留信息又不制造越界链接。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 24 轮 · 补齐阶段测试类型边界

### 1. 本轮任务列表

- 修正 Phase 12 测试 helper 的返回类型和 pytest fixture 注解。
- 让 fake Provider 使用正式 `ProviderMessage` 输入类型，恢复仓库级 `make type`。
- 按 ruff 规范化测试格式。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.16 全量类型门禁的测试侧问题已修复；未改变实验逻辑和原始 artifact。

### 4. 测试方式

- `make type`
- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py tests/test_phase12_methods.py tests/test_phase12_rl.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check tests/test_phase12_rl.py tests/test_phase12_methods.py tests/test_phase12_cli.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format --check tests/test_phase12_rl.py tests/test_phase12_methods.py tests/test_phase12_cli.py tests/test_phase12_evaluation.py`

### 5. 测试结果

- `make type` 检查 146 个文件并退出 0；专项测试 21 个通过；ruff 和 format 均退出 0。

### 6. 用户如何进行手工测试

- 在仓库根目录执行 `make type`，应看到 `Success: no issues found in 146 source files`。

### 7. 风险预估与规避

- 修改仅限测试类型声明和格式，生产代码、Provider 协议和实验数据均未变更。

### 8. 下轮建议

- 继续执行 `make unit`、`make integration` 和 Phase 12 专项全量测试。

### 9. 本轮任务大白话讲解

- 类型检查发现的不是算法错误，而是测试替身没有声明自己遵守真实 Provider 的消息接口。把替身声明准确，才能让类型系统真正检查调用契约。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 23 轮 · 更新真实模型限制口径

### 1. 本轮任务列表

- 修正阶段报告中把真实 Provider held-out 误列为未运行项的静态说明。
- 在报告回归测试中锁定“已记录但仍有限制”的事实口径。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.14/P12.15 报告文字已与 `evaluation-live-baseline-test.jsonl` 的 24 条实际记录一致；报告仍保持 `DRAFT / LAB_ONLY`。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 4 个报告测试通过；ruff、format 和 mypy 均退出 0。

### 6. 用户如何进行手工测试

- 打开生成的 `phase12-stage-report-draft-<code>.md`，确认真实 assist held-out 被列为已记录证据，make integration、Harness 授权和独立审查仍列为未闭合门禁。

### 7. 风险预估与规避

- 只更新报告说明，不修改任何原始模型响应、失败记录、训练权重或业务 Runtime。

### 8. 下轮建议

- 归档旧报告并生成绑定当前代码的最终报告，随后进入 P12.16 全量验证。

### 9. 本轮任务大白话讲解

- 真实模型请求已经执行，但失败和未知结果很多，所以报告既要承认“跑过”，也要保留“证据有限”。这两件事同时成立，不能把已运行误写成已证明。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 22 轮 · 分离真实模型与 replay 基线聚合

### 1. 本轮任务列表

- 将报告中的真实 Provider 基线与确定性 replay 基线分开统计。
- 保持 held-out 配对 bootstrap 只比较同类 replay 方法，避免把不同执行引擎混入同一分母。
- 增加 live/replay 聚合回归测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.14/P12.15 的证据聚合边界已修复；现有 24 条真实 assist 记录保留原始 method 和 usage 状态，由报告层标记为 `live-baseline`。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/reporting.py tests/test_phase12_reporting.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 4 个报告测试通过；ruff、format、mypy 和 diff check 均退出 0。
- 真实 Provider 记录不再改变 replay 基线的 24 条 held-out 分母或配对组。

### 6. 用户如何进行手工测试

- 在已有 `output/phase12/` 上执行 `python -m labs.self_improvement.cli report`，检查方法表同时出现 `baseline` 与 `live-baseline`，held-out bootstrap 不把二者配对。

### 7. 风险预估与规避

- 报告层按模型标识隔离执行引擎，不改写原始 JSONL；真实失败、UNKNOWN、延迟和未知 usage 继续进入 live 分母。

### 8. 下轮建议

- 重新生成绑定当前实现的报告，然后冻结输出 artifact 并执行阶段全量验证。

### 9. 本轮任务大白话讲解

- replay 是本地确定性回放，assist 是真实 Provider，它们即使都叫 baseline 也不是同一次实验。现在报表把它们分成两行，质量比较不会把实时失败率混进本地对照。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 21 轮 · live 批次事件循环修复

### 1. 本轮任务列表

- 发现逐案例 `asyncio.run` 关闭 Provider 事件循环导致批次第二次调用失败。
- 新增同一批次单事件循环的 live baseline runner，保持并发为 1。
- 保留单案例 API 供测试使用，并让批量记录继续绑定 manifest digest。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.4 的真实批量运行缺陷已修复；第一次不完整 live 尝试未当作质量证据，后续会归档为无效批次。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py labs/self_improvement/cli.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py labs/self_improvement/cli.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 13 个评测/CLI 测试通过；ruff、format 和 mypy 均退出 0。
- fake Provider 在同一个批次四次请求均完成，未再出现 event loop 关闭；每次实际调用数为 1。

### 6. 用户如何进行手工测试

- 显式执行 `evaluate --engine live --split test --method baseline --repeats 3`，确认同一输出 JSONL 中批次记录完整；若 Provider 失败，保留失败分类并检查 `calls` 与预算。

### 7. 风险预估与规避

- 真实 Provider 仍只在显式 live 命令中调用；本次修复没有重试、并发或 Provider 替换行为。

### 8. 下轮建议

- 重新执行真实 assist 探测；成功后冻结 live test 结果，失败则明确写入阻塞报告。

### 9. 本轮任务大白话讲解

- 异步客户端通常要一直挂在同一个事件循环上。之前每个案例都开关一次循环，第二个案例就拿到了已关闭的连接；现在一整批请求共享一个循环，同时仍按顺序发出，不会增加并发或重试。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 20 轮 · 干净进程 CLI 入口修复

### 1. 本轮任务列表

- 让 `python -m labs.self_improvement.cli` 在未预先设置 `PYTHONPATH` 的干净进程中加载 workspace runtime。
- 保持 runtime 路径固定为仓库内 `services/agent_runtime/src`，不自动安装依赖或访问网络。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.15 手工 CLI 入口已修复；临时隔离根目录完整顺序复跑通过。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/__init__.py labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/__init__.py labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 6 个 CLI 测试通过；ruff、format、mypy 和 diff check 均退出 0。
- 子进程移除 `PYTHONPATH` 后仍能成功执行 `prepare-data`；隔离复跑共 648 条评测记录、564 次 replay 记账调用，预算未超限。

### 6. 用户如何进行手工测试

- 在仓库根目录执行 `python -m labs.self_improvement.cli prepare-data`，不设置 `PYTHONPATH`，确认命令返回 0 并写入 `output/phase12/`。

### 7. 风险预估与规避

- 入口只补本地 workspace import 路径，不改变 Provider、业务 Runtime、ERP/Frappe 或网络策略。

### 8. 下轮建议

- 在仓库根目录生成冻结的最终 Phase 12 原始 artifact，并绑定当前实现 HEAD。

### 9. 本轮任务大白话讲解

- 用户按文档复制命令时不应该先猜环境变量。CLI 现在会从仓库里找到实验依赖的只读 runtime，仍然不会因此触发远程模型或业务接口。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 19 轮 · 输出路径与版本文件名安全收紧

### 1. 本轮任务列表

- 输出路径逐级拒绝符号链接组件，保留路径穿越和实验目录边界检查。
- 候选与 selection 读回时核对文件名和内容中的 ID。
- 增加 symlink 逃逸负向测试。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.9/P12.15 的 artifact 路径安全缺陷已修复；没有删除旧 artifact，也没有放宽实验目录。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_artifacts.py tests/test_phase12_candidates.py tests/test_phase12_skill_selection.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/contracts.py labs/self_improvement/candidates.py tests/test_phase12_artifacts.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/contracts.py labs/self_improvement/candidates.py tests/test_phase12_artifacts.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 14 个 artifact/candidate/selection 测试通过；ruff、format、mypy 和 diff check 均退出 0。
- 输出目录、候选文件和 selection 文件的符号链接、改名和重复写入均被拒绝。

### 6. 用户如何进行手工测试

- 在临时根目录将 `output` 或 `candidates` 指向另一个目录后执行 `prepare-data`/`make-candidates`，命令应返回非零且不产生逃逸文件。

### 7. 风险预估与规避

- 文件哈希仍只证明内容一致，不当作签名或授权；业务 Runtime 和 Prompt/Skill Registry 不会被写入。

### 8. 下轮建议

- 运行完整实验批次并冻结 held-out 原始证据，随后执行全量阶段门禁。

### 9. 本轮任务大白话讲解

- 只检查最终路径还不够，因为中间目录可能是链接。现在每一级目录都检查，候选文件里的 ID 也必须和文件名一致，避免实验回执被偷换或写到目录外。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 18 轮 · DPO 偏好与早停有效性修复

### 1. 本轮任务列表

- 从同一可观察状态生成 chosen/rejected 偏好对，过滤不安全动作和无质量差异对。
- 冻结 DPO reference，记录 reference 权重哈希，并保留标准 beta=0.1 偏好损失。
- 将 DPO 早停指标改为 dev 偏好 loss；补充 REINFORCE 平均步数指标。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.11 的安全偏好适配和 dev 早停已完成；没有把 chosen 单边交叉熵命名为 DPO，也没有使用 held-out 标签。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_training.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 8 个训练测试通过；ruff、format、mypy 和 diff check 均退出 0。
- 偏好对来自同一状态且 chosen/rejected 不相同；DPO reference 哈希与 SFT 起点一致，训练后权重发生变化。

### 6. 用户如何进行手工测试

- 运行 `train --method sft --seed 17` 后 `train --method dpo --seed 17`，查看 metadata 中的 `reference_weight_sha256`、`dev_preference_pairs` 和 `final_dpo_loss`。

### 7. 风险预估与规避

- 偏好准确率不作为业务成功替代；若 held-out 任务回归，报告必须标记副作用并保留 SFT/基线。

### 8. 下轮建议

- 生成真实回放与训练 artifact，执行冻结后的 held-out 评测、配对 bootstrap 和完整验收。

### 9. 本轮任务大白话讲解

- DPO 要比较“同一个情况里，哪个安全动作更好”，不能只把正确动作当成普通分类标签。现在参考模型被冻结，训练只根据安全的成对差异调整，并用开发集决定何时停止，避免偷看测试集。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 17 轮 · 统一报告与 Adoption Card

### 1. 本轮任务列表

- 新增阶段报告生成器和 `report` CLI，读取已绑定 manifest 的评测 JSONL 与训练 metadata。
- 汇总 verifier/safety/score/calls/usage/延迟，并执行 2,000 次按组 held-out bootstrap。
- 生成风险登记、暂定九维 Rubric、奖励黑客预设负例和 `LAB_ONLY` Adoption Card。
- 报告、卡片和机器摘要均使用不可覆盖 artifact 写入。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.15 的统一证据输出已完成；报告状态固定为 `DRAFT / LAB_ONLY`，不把 replay、训练或 RAG 写成业务采用。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reporting.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/reporting.py tests/test_phase12_reporting.py labs/self_improvement/cli.py labs/self_improvement/artifacts.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/reporting.py tests/test_phase12_reporting.py labs/self_improvement/cli.py labs/self_improvement/artifacts.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 8 个报告/CLI 测试通过；ruff、format、mypy 和 diff check 均退出 0。
- 报告会保留失败和未知 usage；零差值比较输出 `INCONCLUSIVE`，故意错误奖励明确标记为预设负例。

### 6. 用户如何进行手工测试

- 运行 `prepare-data`、若干 `evaluate`/`train` 后执行 `python -m labs.self_improvement.cli report`，查看 `output/phase12/phase12-stage-report-draft-*.md`、`phase12-adoption-card-*.md` 和 `phase12-summary-*.json`。

### 7. 风险预估与规避

- 报告生成不改变业务 Registry；若数据、记录或训练 metadata 绑定不一致，命令在写报告前失败。

### 8. 下轮建议

- 在临时根目录按顺序生成完整 dev/test、候选、选择/回滚和三算法权重，冻结原始证据并做全量门禁。

### 9. 本轮任务大白话讲解

- 现在实验结果可以被同一份报告复核：它把每种方法做对多少、是否安全、花了多少调用和多久写在一起，还单独标出“奖励分数变高但任务没完成”的反例。没有证据时报告会保留基线，不会自动升级线上能力。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 16 轮 · CLI artifact 完整校验

### 1. 本轮任务列表

- 将审计和训练 metadata 写入统一的一次性 artifact API。
- `verify-artifacts` 逐个校验候选、selection、权重、metadata、数据集绑定和内容哈希。
- 选择/回滚前校验父版本和已有评测证据（没有评测文件的最小演示目录仍可建立选择回执）。
- 评测 CLI 支持三次重复以及 Prompt/Skill 候选方法，live 模式限定为 baseline。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.15 的 artifact 验收入口已补齐；发现权重或 metadata 篡改时返回非零退出，不覆盖旧文件。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py tests/test_phase12_artifacts.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/cli.py labs/self_improvement/evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/cli.py labs/self_improvement/evaluation.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 17 个专项测试通过；ruff、format、mypy 和 diff check 均退出 0。
- 新进程读回权重和 metadata 通过；篡改权重哈希、引用缺失、候选方法超出三次重复均被发现。

### 6. 用户如何进行手工测试

- 先 `prepare-data`，再 `evaluate --method prompt-candidate --split dev --repeats 3`，最后 `verify-artifacts`；把 metadata 的 `weight_sha256` 改成全零后再次验证应返回 2。

### 7. 风险预估与规避

- 选择证据不可凭文件名冒充；已有 JSONL 时引用必须真实存在，权重和 metadata 必须互相绑定。

### 8. 下轮建议

- 生成统一评测/训练/奖励黑客结果报告和 Adoption Card，并保留 INCONCLUSIVE/副作用结论。

### 9. 本轮任务大白话讲解

- `verify-artifacts` 像实验室的收货验单：不只看“文件在不在”，还核对它属于哪份数据、文件名是否对应内容、训练权重是否和说明里的哈希一致，以及选择回执引用的证据是否真的存在。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 15 轮 · 安全奖励与奖励黑客边界修复

### 1. 本轮任务列表

- 将安全奖励固定为每步 -0.02、成功 +1、错误 -1、无进展 -0.1，重复调用不获得 bonus。
- 保留单独的故意错误奖励，使重复调用可以产生可复现的高分负例。
- 拒绝 NaN 和无穷奖励参数，避免奖励污染训练指标。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.12 的安全奖励与错误奖励已分离；没有改变环境的安全门禁和最终任务 verifier。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_rl.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/rl.py tests/test_phase12_rl.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/rl.py tests/test_phase12_rl.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 7 个环境和 REINFORCE 测试通过；lint、format、mypy 和 diff check 均退出 0。
- 相同重复轨迹在错误奖励下仍高于安全奖励，但安全奖励没有重复收益；非有限奖励被拒绝。

### 6. 用户如何进行手工测试

- 对 `purchase_order.open` 重复三次再 `FINISH`，分别使用 `safe_reward_config()` 和 `intentionally_bad_reward_config()`，比较总奖励和最终 verifier 状态。

### 7. 风险预估与规避

- 训练算法不能通过调大正向奖励抵消安全或任务失败；奖励和 verifier 继续独立计算。

### 8. 下轮建议

- 加固 CLI 的一次性 artifact 写入、候选/选择校验和权重 metadata 绑定。

### 9. 本轮任务大白话讲解

- 安全评分只奖励完成正确任务，重复查同一件事不会加分。另一套错误评分故意让重复有利，这样可以清楚展示“分数变高但任务仍失败”的奖励黑客风险。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 14 轮 · live Provider 批次边界修复

### 1. 本轮任务列表

- 为 live Provider 增加单次 60 秒墙钟上限。
- 在累计预算内记录真实发出的调用数；预算耗尽时不把未发出的请求记为调用。
- 连续三次连接、认证或协议失败后阻断当前批次，并保留已消耗预算。
- 让 live 结果显式绑定冻结数据集 ID 和 manifest digest。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.4 的批次控制和数据绑定缺陷已修复；没有自动重试，也没有切换 Provider 伪造结果。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- 6 个评测测试通过；ruff、format、mypy 和 diff check 均退出 0。
- 故障注入确认三次阻塞性错误后第四次不再触发 Provider；超时和预算不足均保留可审计状态。

### 6. 用户如何进行手工测试

- 使用一个会抛出 `CONNECTION_ERROR` 的 Provider 连续执行四次，确认第四次 `MODEL_BATCH_BLOCKED` 且 Provider 实际只收到三次请求。

### 7. 风险预估与规避

- 中断和批次阻断不会重置累计预算或覆盖旧记录；未知 usage 仍保持为空，不写成 0。

### 8. 下轮建议

- 加固统一 artifact 验证，并生成阶段报告、Adoption Card 和可复跑证据。

### 9. 本轮任务大白话讲解

- 远程模型可能卡住或连续不可用。现在每次调用都有硬截止，连续三次基础设施错误就停止这一批，避免无限等待和重复花费；没真正发出的请求也不会被算进去。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 13 轮 · Phase 12 CLI 与 artifact 收口入口

### 1. 本轮任务列表

- 增加 `audit-data`、`prepare-data`、`make-candidates`、`evaluate`、`select-lab`、`rollback-lab`、`train` 和 `verify-artifacts` CLI。
- 让默认 evaluate 只跑本地回放，live Provider 只有显式 `--engine live` 才启用。
- 绑定 manifest、记录、候选、选择和权重的复跑检查。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.14/P12.15 基础 CLI 完成；训练和评测 artifact 可以从新进程读取并验证。
- `verify-artifacts` 会跳过 metadata 描述文件，只读取严格 JSON 权重；候选和 selection 继续执行契约校验。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/cli.py tests/test_phase12_cli.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`

### 5. 测试结果

- 3 个 CLI 端到端测试通过；mypy 10 个实验源文件通过。
- 临时目录完整跑通 prepare/audit/replay/train/verify 和候选选择/回滚；默认回放不需要 Provider。
- live Provider 尚未在当前环境批量执行；不把 fake provider 测试写成真实质量证据。

### 6. 用户如何进行手工测试

- 在仓库根目录依次运行 `python -m labs.self_improvement.cli prepare-data`、`evaluate --split dev`、`train --method sft`、`verify-artifacts`，再查看 `output/phase12/` 中的 manifest、JSONL 和权重 metadata。

### 7. 风险预估与规避

- 训练/评测默认不触网；live 模式显式选择且共享累计 1,200 次调用预算。
- 所有写入使用一次性文件；重复运行不会覆盖旧证据，篡改会在 verify 阶段失败。

### 8. 下轮建议

- 完成真实/回放 held-out 结果冻结、按组 bootstrap、Adoption Card 和统一阶段报告。

### 9. 本轮任务大白话讲解

- 现在有一套可以从命令行重复运行的实验流程：先准备数据，再比较方法或训练模型，最后检查所有文件是否仍和数据、代码版本一致。默认不会意外调用远程模型。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 12 轮 · 评测记录数据绑定修复

### 1. 本轮任务列表

- 发现 replay evaluator 使用单案例摘要哈希而非冻结 manifest 哈希。
- 修正 evaluator 可显式传入 dataset digest，并让 artifact verifier 拒绝错绑定。
- 增加 manifest、JSONL、重复 ID、预算和篡改校验。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.14/P12.15 的 artifact 基础完成；记录现在可以被精确绑定到数据集版本。
- 该修复未放宽任何数据、模型或安全门禁。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_artifacts.py tests/test_phase12_heldout.py`
- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py labs/self_improvement/artifacts.py`

### 5. 测试结果

- 6 个 artifact/held-out 测试通过；mypy 9 个实验源文件通过。
- 篡改 manifest、重复实验 ID、错路径和空配对 bootstrap 均被发现。
- 真实模型 held-out 尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_artifacts.py`，核对 JSON manifest 和 JSONL 读回后仍通过 dataset digest 检查。

### 7. 风险预估与规避

- 不同数据版本的实验不能混合验证；所有结果必须携带 manifest digest。
- artifact 写入使用一次性文件，禁止覆盖和输出目录逃逸。

### 8. 下轮建议

- 完成 CLI：audit/prepare/evaluate/select/rollback/train/verify，并生成最小阶段证据。

### 9. 本轮任务大白话讲解

- 发现一个会影响可信度的问题：实验记录之前可能只绑定单条案例的哈希。现在每条记录都必须绑定整份冻结数据集，数据换了就不能假装沿用旧结果。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 11 轮 · 训练实验类型边界修复

### 1. 本轮任务列表

- 根据专项 mypy 结果修正 replay/evaluation/data/training/rl 的严格类型边界。
- 保持失败分类、动作白名单和延迟加载语义不变。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- 修复了 12 个静态类型诊断；没有新增业务路径或放宽运行时校验。

### 4. 测试方式

- `uv run --frozen --python 3.14 mypy labs/self_improvement`
- `git diff --check`

### 5. 测试结果

- mypy `Success: no issues found in 8 source files`，diff check 退出 0。
- 受影响专项 pytest 将在本轮提交后与下一步训练验收一并复跑。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 mypy labs/self_improvement`，确认实验目录没有类型错误。

### 7. 风险预估与规避

- 修复只收紧类型标注和移除无效 ignore；没有改动模型、环境或权限行为。

### 8. 下轮建议

- 冻结数据、候选和权重后实现 held-out 统计与 artifact 校验。

### 9. 本轮任务大白话讲解

- 训练代码先通过静态检查，避免把“看起来能跑”的宽松类型错误带到最终实验。修复只处理编译器发现的问题，不改变实验结果规则。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 10 轮 · REINFORCE 参数更新

### 1. 本轮任务列表

- 在模拟采购环境中实现固定折扣因子 0.95 的 REINFORCE。
- 使用 episode 内真实状态、采样动作、奖励和移动 baseline 更新 MLP 参数。
- 保存 episode 回报、成功率、无效动作和权重读回证据。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.13 完成；episode 上限固定为 300、每回合最多 8 步，训练只使用 train 场景。
- 训练每个 seed 都是真实采样和梯度更新，不把固定回放称为 RL。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_reinforce.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_reinforce.py`

### 5. 测试结果

- 3 个 REINFORCE 测试通过；权重真实改变、JSON 新进程读回一致、相同 seed 可复现。
- 超过 300 episode 会拒绝；有限回报和梯度未出现 NaN。
- 阶段全量训练三 seed 和 held-out 评测尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_reinforce.py`，核对 `weight_sha256` 变化和 12 episode 的回报长度。

### 7. 风险预估与规避

- baseline 只使用此前 episode 的回报，不读取未来或 test；训练异常不会保存隐式成功状态。
- RL 结果必须与最终业务 verifier 分开报告，避免高 reward 被误读为高任务质量。

### 8. 下轮建议

- 固定代码、数据和候选后执行 dev/test held-out 评测、按组 bootstrap 和结果分类。

### 9. 本轮任务大白话讲解

- 现在小模型会在采购沙盒里自己试动作：查、问、停都会产生奖励，然后用这些真实经历调整参数。它仍然只在本地实验环境行动，线上 Agent 不会加载这套权重。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 9 轮 · 采购模拟环境与奖励黑客负例

### 1. 本轮任务列表

- 实现本地 `reset/step` 采购调查环境和 8 步 episode 上限。
- 将安全门禁、业务终态 oracle 和奖励计算分开。
- 增加安全奖励与故意重复调用可骗分的错误奖励对照。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.12 完成；环境不访问网络、不连接 ERP、不保留跨 episode 状态。
- 已提供可复现的预设 reward-hacking 负例，作为风险演示而非自然生产发现。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_rl.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/rl.py tests/test_phase12_rl.py`
- `git diff --check`

### 5. 测试结果

- 3 个环境、越界动作和奖励黑客测试通过。
- 终态后继续 step 会拒绝；非法动作立即 `REJECTED` 且安全失败。
- 重复调用在错误奖励下总分高于安全奖励，但最终业务 verifier 仍不通过。
- REINFORCE 尚未接入；全量 unit/integration 尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_rl.py`，查看相同重复轨迹在两种 reward config 下的总分和最终状态。

### 7. 风险预估与规避

- 正向 reward 不能抵消安全失败；任务 oracle 只由环境使用，策略只获得观察状态。
- 错误奖励明确标记为故意负例，不能拿高分推导业务质量提升。

### 8. 下轮建议

- 在同一环境接入 REINFORCE，固定三 seed、移动 baseline、300 episode 上限，并保存真实参数更新。

### 9. 本轮任务大白话讲解

- 这个沙盒让 Agent 真正经历“查、遇到问题、停”的过程。我们故意给一套错误奖励，让重复查同一个东西反而得分，证明奖励分数可能被钻空子，所以必须单独看业务成功和安全结果。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 8 轮 · SFT 与 DPO 小模型更新

### 1. 本轮任务列表

- 实现固定 7 维可观察特征、8 个动作的两层 MLP。
- 实现 CPU SFT、dev 早停、有限 loss 和真实参数更新。
- 实现 reference 冻结的标准 DPO 偏好损失。
- 实现有限 JSON 权重保存、版本/形状/有限值检查和新进程读回。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.10、P12.11 基础实现完成；训练输入只含可观察状态，oracle/奖励不进入 feature。
- PyTorch 仅在训练模块实际调用时加载；不使用 pickle、远程代码或新依赖。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/training.py tests/test_phase12_training.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/training.py tests/test_phase12_training.py`

### 5. 测试结果

- 4 个训练测试通过；SFT/DPO 权重均真实改变且有限。
- 同 seed SFT 结果一致；DPO artifact 的初始哈希等于 SFT 权重哈希。
- JSON 权重新进程读回一致；版本损坏被拒绝。
- RL 尚未实现；全量 unit/integration 尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_training.py`，确认日志中的 `initial_weight_sha256` 与 `weight_sha256` 不同，并检查读回摘要一致。

### 7. 风险预估与规避

- 权重 schema、feature/action version、形状和有限值均由加载器检查，任意损坏不会被执行。
- loss 下降不等于采购任务改善；业务指标必须在 held-out verifier 中单独核对。

### 8. 下轮建议

- 实现采购模拟环境的 reset/step、固定安全奖励和预设 reward-hacking 负例，再接入 REINFORCE。

### 9. 本轮任务大白话讲解

- 训练模型只学“看到了什么之后该查、该问还是该停”，不会接触正确答案或 ERP 写权限。SFT 学安全动作，DPO 学安全候选之间的偏好；权重用普通 JSON 保存，坏文件直接拒绝。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 7 轮 · Skill 候选与实验回滚

### 1. 本轮任务列表

- 增加只读 Skill 候选构造与能力扩张拒绝。
- 增加实验选择回执读取和不可覆盖的父版本回滚。
- 增加候选/选择文件的路径、符号链接和重复写保护。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.8、P12.9 完成；Skill 候选只写实验副本，回滚只产生新的 `ROLLBACK` receipt。
- 业务 Prompt/Skill registry 的内容和边界哈希没有被修改。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_candidates.py tests/test_phase12_skill_selection.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/candidates.py tests/test_phase12_skill_selection.py`
- `git diff --check`

### 5. 测试结果

- 10 个候选、Skill、选择和回滚测试通过。
- 路径穿越、远程/写工具提示、受保护层标记、陈旧候选和二次回滚均被拒绝。
- 全量 unit/integration 和 live held-out 尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_skill_selection.py`，确认 A→B→A 的回滚只新增回执，旧文件仍可读取。

### 7. 风险预估与规避

- Skill 内容不能增加工具或执行能力；选择记录不能覆盖既有 artifact。
- 当前“选择”仍是 LAB_ONLY 文件记录，不代表业务 Runtime 已晋级候选。

### 8. 下轮建议

- 实现本地 MLP 的 SFT、DPO 和权重安全读回，先用合成回放建立真实参数更新证据。

### 9. 本轮任务大白话讲解

- Skill 候选像一张实验用操作提示卡，只能帮助 Agent 更好地查和停，不能让它多出工具。选错版本时不删除旧文件，而是写一张新回执把指针退回父版本。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 6 轮 · Prompt 候选版本化

### 1. 本轮任务列表

- 实现失败来源驱动的 Prompt 候选构造、内容哈希和固定父版本。
- 固定 boundary/recovery/output contract/只读动作的联合边界哈希。
- 增加候选安全校验、限定目录写入、重复写保护和读取复验。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.7 的候选版本基础完成；候选只能作为实验副本，不会注入 `PromptRegistry`。
- 修改受保护层、工具、权限或远程执行能力的内容会在加载前拒绝。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_candidates.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/candidates.py tests/test_phase12_candidates.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/candidates.py tests/test_phase12_candidates.py`

### 5. 测试结果

- 4 个候选哈希、边界篡改、重复写和选择证据测试通过。
- 候选写入后可读回且内容相等；陈旧边界和无证据选择被拒绝。
- 全量 unit/integration 和 dev 比较尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_candidates.py`，确认候选文件只会出现在实验输出目录，并核对 `boundary_sha256` 未变。

### 7. 风险预估与规避

- 候选内容不能改变保护层、输出契约、权限或工具集合；边界摘要变化即 fail closed。
- 文件写入拒绝覆盖既有 artifact，避免回滚链被静默改写。

### 8. 下轮建议

- 使用同一实验副本生成并评测只读 Skill 候选，同时接入显式选择/回滚回执。

### 9. 本轮任务大白话讲解

- 失败轨迹只能影响 Agent 的“判断提示”，不能借机改安全规则或增加权限。每个候选都有父版本和哈希，后面可以知道它从哪来、是否被改过。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 5 轮 · Reflection 与 Best-of-3 对照

### 1. 本轮任务列表

- 增加最多一次修订的 Reflection 回放。
- 增加最多三个候选的 Best-of-N 回放和稳定 reranker。
- 先执行安全/业务 verifier 硬门禁，再按可见分数、步数和生成顺序排序。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.5、P12.6 的离线对照实现完成；失败、退化和调用次数均保留。
- reranker 没有接触 case oracle；只有 verifier 已经确认的候选进入排序。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_methods.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_methods.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py tests/test_phase12_methods.py`

### 5. 测试结果

- 8 个方法和基线专项测试通过。
- Reflection 成功修复一次失败且不会对已验证结果额外调用；Best-of-2 能过滤错误候选；无有效候选时不伪造成功。
- 全量 unit/integration 和真实远程方法比较尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_methods.py`，核对 `calls` 和 `verifier_passed`，确认流畅但错误的候选不会被排序选中。

### 7. 风险预估与规避

- Reflection 反馈只来自模型可见失败状态；Best-of-N 不能使用期望答案或隐藏标签。
- 多调用成本仍由外层 CallBudget 统计，后续 live runner 不得绕过预算。

### 8. 下轮建议

- 生成只改变 decision 层的 Prompt 候选和只读 Skill 候选，并做边界哈希校验。

### 9. 本轮任务大白话讲解

- Reflection 是“先答一次，发现不合格再改一次”；Best-of-3 是“最多写三份，先剔除不安全和不合格的，再选最好的一份”。两者都不能靠模型自我感觉判断好坏。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 4 轮 · 有限预算 Provider 基线

### 1. 本轮任务列表

- 增加统一 `ExperimentRecord` 回放评测和汇总指标。
- 增加累计调用预算、输入/输出上限和不重试的 Provider 适配。
- 分类记录预算耗尽、传输/Provider 错误、空响应和 JSON schema 错误。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.4 基础实现完成；`run_live_baseline` 的模型动作仍需通过本地回放 verifier。
- provider 未报告正用量时记录为空，不把默认 0 宣称为实测 usage。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/evaluation.py tests/test_phase12_evaluation.py`

### 5. 测试结果

- 4 个回放汇总、预算阻断、Provider JSON 解析和无重试测试通过。
- 预算为 0 时 provider 调用次数保持 0；无效响应只消耗一次已登记调用并保留失败码。
- 全量 unit/integration 和真实远程 Provider 批次尚未运行。

### 6. 用户如何进行手工测试

- 使用 fake provider 运行专项测试；确认 `MODEL_CALL_BUDGET` 和 `MODEL_RESPONSE_SCHEMA` 都出现在结构化记录中。

### 7. 风险预估与规避

- 每次请求前占用预算，失败不会被静默重试；跨进程持久化预算将在 CLI artifact 层完成。
- live prompt 只包含合成任务和允许动作，当前没有真实 ERP 字段外发路径。

### 8. 下轮建议

- 实现 Reflection 与 Best-of-3，保持相同 verifier 和预算记录。

### 9. 本轮任务大白话讲解

- 现在每次模型调用都有一笔账：发送前先扣额度，返回后记模型结果或明确失败。这样网络超时不会悄悄重发，也不会把“模型没回答”写成业务失败已解决。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 3 轮 · 只读回放与确定性 verifier

### 1. 本轮任务列表

- 建立不暴露 oracle 的本地采购回放状态和六类场景终态。
- 增加只读工具/ASK_INPUT/FINISH 白名单、步数预算和安全拒绝。
- 增加确定性 verifier，独立检查最终动作、业务状态和安全状态。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.3 完成；后续方法可以共用 `run_replay` 和 `verify_result`。
- 回放 policy 只能读取观察、失败、冲突和剩余步数；场景 oracle 只在环境和 verifier 侧使用。

### 4. 测试方式

- `uv run --frozen --python 3.14 pytest tests/test_phase12_replay.py`
- `uv run --frozen --python 3.14 ruff check labs/self_improvement/replay.py tests/test_phase12_replay.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement/replay.py tests/test_phase12_replay.py`

### 5. 测试结果

- 4 个回放、越界动作、伪成功和未知/冲突终态测试通过。
- 业务写工具、模型口头成功和未验证证据均不能绕过 verifier。
- 全量 unit/integration 尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_replay.py`，观察确定性 policy 在六类场景均通过，非法动作返回 `ACTION_NOT_ALLOWED`。

### 7. 风险预估与规避

- 回放环境不访问网络、不持有 ERP 会话、不提供 writer；模型状态不直接包含场景 kind、期望动作或奖励。
- verifier 与奖励侧分离；后续 reranker 和 RL 不能把模型自评当作事实。

### 8. 下轮建议

- 在不改变回放安全门禁的前提下，接入有限预算的 Provider 基线和统一实验记录。

### 9. 本轮任务大白话讲解

- 先造一个很小但严格的“采购调查沙盒”：Agent 只能查和停，环境会明确告诉它遇到未知、冲突或无进展。最后由独立检查器判断是否真的完成，模型自己说成功没有用。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 2 轮 · 历史失败审核与数据契约

### 1. 本轮任务列表

- 新增严格的 Phase 12 JSON 契约、内容哈希和实验输出路径边界。
- 实现显式 Phase 11 失败 artifact 审核、脱敏摘要和来源完整性检查。
- 实现六类合成采购场景及按组固定的 72/24/24 数据划分。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.1、P12.2 的基础实现和负面测试完成；真实历史记录只保留脱敏背景，不进入模型输入。
- 代码和测试变更集中在 `labs/self_improvement/` 与 `tests/test_phase12_data.py`。

### 4. 测试方式

- `uv run --frozen --python 3.14 ruff check labs/self_improvement tests/test_phase12_data.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement tests/test_phase12_data.py`
- `uv run --frozen --python 3.14 pytest tests/test_phase12_data.py`
- `git diff --check`

### 5. 测试结果

- 6 个数据审核/隔离测试通过；lint、format、diff check 均退出 0。
- 已验证缺失源、Secret-like 源、重复 ID 和跨 split 场景会拒绝。
- 仅运行专项检查；全量 unit/integration 尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_data.py`，核对历史源为显式 allowlist，合成 manifest 为 60 组、120 条、train/dev/test=72/24/24。

### 7. 风险预估与规避

- 真实 Phase 11 artifact 的字段和内容不送往模型；导出只使用固定任务摘要。
- 数据集的 oracle 仍保存在评分侧，后续 evaluator 必须继续阻止它进入输入投影。

### 8. 下轮建议

- 实现本地采购回放和确定性 verifier，使所有后续方法共享同一安全门禁。

### 9. 本轮任务大白话讲解

- 先检查哪些旧失败真的能用，再把同类采购问题按组分到训练、开发和测试。这样后面即使模型分数变好，也能确定它没有提前见过测试答案。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 1 轮 · 冻结离线实验契约

### 1. 本轮任务列表

- 核对 Phase 11 出口、当前 HEAD 和工作区状态。
- 建立 Phase 12 离线自进化、后训练与 Agentic RL 的边界、预算、数据隔离和出口契约。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记；阶段结束后再按用户触发进行答疑。

### 3. 本轮任务完成度

- P12.0 契约已冻结；实现尚未开始。
- 起始 HEAD 为 `ffb5b81`，工作区在编辑前干净。
- 业务代码、ERP/Frappe、业务 Runtime、`.env*` 和 README 未修改。

### 4. 测试方式

- 读取 `docs/PLAN.md`、Phase 11 最终报告、Phase 7 Prompt/Skill 实现和当前测试入口。
- 使用项目虚拟环境完成 Python/PyTorch CPU autograd 探测。

### 5. 测试结果

- `git status --short`：编辑前为空。
- 本地 Python 3.14.5，PyTorch 2.13.0；有限张量反向传播通过。
- 本轮未运行 `make unit`、`make integration` 或真实 Provider 批次；不把历史结果冒充本轮结果。

### 6. 用户如何进行手工测试

- 查看 `docs/phase12-execution-contract.md`，确认所有实验只写入 `output/phase12/`，并核对 1,200 次调用上限和 72/24/24 数据划分。

### 7. 风险预估与规避

- 初始高风险为训练/测试泄漏、候选扩权、真实轨迹外发、奖励黑客、伪训练和不可回滚。后续每步必须用结构化校验、边界哈希、隔离目录、参数读回和失败保留来关闭。
- 真实 Provider 尚未调用；当前不对模型质量或成本做结论。

### 8. 下轮建议

- 实现历史失败 artifact 的显式审核和导出准入。

### 9. 本轮任务大白话讲解

- 先把实验规则写死，后面所有训练和比较才不会因为看到了测试答案而临时改规则。这个文件只规定实验室怎么工作，不会给线上 Agent 增加任何权限。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
