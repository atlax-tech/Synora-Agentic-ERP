# Phase 12 开发日志

## 2026-09-12 · Phase 12 · 第 2 轮 · 历史失败审核与数据契约

### 1. 本轮任务列表

- 新增严格的 Phase 12 JSON 契约、内容哈希和实验输出路径边界。
- 实现显式 Phase 11 失败 artifact 审核、脱敏摘要和来源完整性检查。
- 实现六类合成采购场景及按组固定的 72/24/24 数据划分。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记。

### 3. 本轮任务完成度

- P12.1、P12.2 的基础实现和负面测试完成；真实历史记录只保留脱敏背景，不进入模型输入。
- 代码和测试变更集中在 `labs/self_improvement/` 与 `tests/test_phase12_data.py`。

### 4. 测试方式

- `uv run --frozen --python 3.14 ruff check labs/self_improvement tests/test_phase12_data.py`
- `uv run --frozen --python 3.14 ruff format --check labs/self_improvement tests/test_phase12_data.py`
- `uv run --frozen --python 3.14 pytest tests/test_phase12_data.py`
- `git diff --check`

### 5. 测试结果

- 6 个数据审核/隔离测试通过；lint、format、diff check 均退出 0。
- 已验证缺失源、Secret-like 源、重复 ID 和跨 split 场景会拒绝。
- 仅运行专项检查；全量 unit/integration 尚未运行。

### 6. 用户如何进行手工测试

- 运行 `uv run --frozen --python 3.14 pytest tests/test_phase12_data.py`，核对历史源为显式 allowlist，合成 manifest 为 60 组、120 条、train/dev/test=72/24/24。

### 7. 风险预估与规避

- 真实 Phase 11 artifact 的字段和内容不送往模型；导出只使用固定任务摘要。
- 数据集的 oracle 仍保存在评分侧，后续 evaluator 必须继续阻止它进入输入投影。

### 8. 下轮建议

- 实现本地采购回放和确定性 verifier，使所有后续方法共享同一安全门禁。

### 9. 本轮任务大白话讲解

- 先检查哪些旧失败真的能用，再把同类采购问题按组分到训练、开发和测试。这样后面即使模型分数变好，也能确定它没有提前见过测试答案。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。

## 2026-09-12 · Phase 12 · 第 1 轮 · 冻结离线实验契约

### 1. 本轮任务列表

- 核对 Phase 11 出口、当前 HEAD 和工作区状态。
- 建立 Phase 12 离线自进化、后训练与 Agentic RL 的边界、预算、数据隔离和出口契约。

### 2. 用户 Assignment 与导师引导

- 用户明确要求 Agent 全程接管，本阶段不创建 Assignment、不生成学习笔记；阶段结束后再按用户触发进行答疑。

### 3. 本轮任务完成度

- P12.0 契约已冻结；实现尚未开始。
- 起始 HEAD 为 `ffb5b81`，工作区在编辑前干净。
- 业务代码、ERP/Frappe、业务 Runtime、`.env*` 和 README 未修改。

### 4. 测试方式

- 读取 `docs/PLAN.md`、Phase 11 最终报告、Phase 7 Prompt/Skill 实现和当前测试入口。
- 使用项目虚拟环境完成 Python/PyTorch CPU autograd 探测。

### 5. 测试结果

- `git status --short`：编辑前为空。
- `.venv/bin/python`：Python 3.14.5，PyTorch 2.13.0；有限张量反向传播通过。
- 本轮未运行 `make unit`、`make integration` 或真实 Provider 批次；不把历史结果冒充本轮结果。

### 6. 用户如何进行手工测试

- 查看 `docs/phase12-execution-contract.md`，确认所有实验只写入 `output/phase12/`，并核对 1,200 次调用上限和 72/24/24 数据划分。

### 7. 风险预估与规避

- 初始高风险为训练/测试泄漏、候选扩权、真实轨迹外发、奖励黑客、伪训练和不可回滚。后续每步必须用结构化校验、边界哈希、隔离目录、参数读回和失败保留来关闭。
- 真实 Provider 尚未调用；当前不对模型质量或成本做结论。

### 8. 下轮建议

- 实现历史失败 artifact 的显式审核和导出准入。

### 9. 本轮任务大白话讲解

- 先把实验规则写死，后面所有训练和比较才不会因为看到了测试答案而临时改规则。这个文件只规定实验室怎么工作，不会给线上 Agent 增加任何权限。

### 10. 面试官可能追问的技术细节以及如何回答

- 按用户要求本阶段不生成问答；阶段结束后用户明确触发答疑时再记录。
