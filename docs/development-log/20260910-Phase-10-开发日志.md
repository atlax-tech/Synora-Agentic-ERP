# Phase 10 开发日志

## 2026-09-10  · Phase 10 · 第 6 轮 · P10.5 受控取消与人工恢复

### 1. 本轮任务列表

- 从固定 ERPNext `11e0ba0a1c45f217e2e73e885f699102d06da325` / Frappe `6a329d068416768ec47ccd3326b9cc95a8d7bf99` 复核 `Document.cancel` 反向链接扫描，以及 Purchase Order、Purchase Receipt、Purchase Invoice、Payment Entry 的取消依赖、库存/GL 逆向和官方负面用例；确认合法顺序为 Payment Entry → Purchase Invoice → Purchase Receipt → Purchase Order。
- 启用 `CANCEL_PO`、`CANCEL_PR`、`CANCEL_PI`、`CANCEL_PAYMENT_ENTRY`；所有取消仍使用 schema v2、独立审批、payload digest、执行前重检、连接级源锁、幂等 reservation 和 ERP 原生 controller。
- 在 deterministic policy 中提前检查当前用户可见的已提交下游单据：PO 被 PR/PI 阻塞、PR 被 PI 阻塞、PI 被 Payment Entry 阻塞；取消请求不会自动级联、回滚数据库或替代创建退货/退款单。
- 增加取消回读：目标必须为 Cancelled；PR 核对 PO 收货量和 Stock Ledger 净数量；PI 核对 PR/PO billed 进度和 GL 借贷净额；Payment Entry 核对发票余额/状态、引用分配和 GL 借贷净额。
- 失败回执增加 side-effect 状态、目标 docstatus、人工介入标记和恢复建议；Runs 页面显示下游取消语义和恢复建议。补充真实 ERP 取消阻塞、合法逆序、重复回放、controller 失败及响应丢失验收。

### 2. 用户 Assignment 与导师引导

本阶段按用户明确指令由 Agent 全程接管代码编写，不触发 Assignment；阶段结束后的答疑由用户另行触发。

### 3. 本轮任务完成度

- P10.5：已完成四类受治理取消和人工恢复证据。提交下游存在时策略先拒绝；按 Payment Entry → PI → PR → PO 逆序取消后，ERP 单据状态、发票 outstanding、PR/PO billing、PO received、库存台账和 GL 读回一致。
- 取消动作的回执保留业务目标和反向结果；同一幂等键回放只读取并核对历史证据。controller 已知失败保持原单据 Submitted，Receipt 标记 `FAILED/NOT_APPLIED`；提交后响应丢失标记 `RECONCILIATION_REQUIRED/APPLIED_BUT_RESPONSE_FAILED`，禁止盲重试并给出人工接管建议。
- 大致耗时：不可见；Token：不可见；子 Agent：1 次独立只读对抗 Review，结论 `PASS`。
- 改动量：取消契约/策略/执行回读/恢复证据/UI 与真实验收约 700 行；Harness/README 未改。

### 4. 测试方式

- `env/dev/scripts/dev/env.sh bash '... bench --site "$FRAPPE_SITE" run-tests --app synora_agentic_erp --module synora_agentic_erp.tests.test_phase10_cancellation'`：固定开发 ERP 真实取消模块，覆盖 blocker、逆序链路、失败回执和响应丢失。
- `UV_CACHE_DIR=/private/tmp/synora-phase10-uv-cache uv run --python 3.14 pytest tests/test_execution_contracts.py tests/test_governance_contracts.py tests/test_governance_policy.py`：纯契约、回执匹配和策略门禁测试。
- `.venv/bin/ruff check synora_agentic_erp/governance synora_agentic_erp/tests/test_phase10_cancellation.py`、`.venv/bin/python -m py_compile ...`、`node --check synora_agentic_erp/synora_agentic_erp/page/runs/runs.js`、`git diff --check`：提交前静态检查。
- 按 ponytail-review 检查取消逻辑是否复用既有治理入口、Frappe 原生 controller 和已存在的 reconciliation；独立 Review 检查依赖门禁、逆向财务/库存证据、权限泄露、幂等和 recovery evidence。

### 5. 测试结果

- P10.5 真实模块最终 **4 tests，退出码 0**：`test_downstream_blockers_and_legal_reverse_order` 证明三类下游阻塞与合法逆序；`test_submitted_purchase_order_without_downstream_can_be_cancelled` 证明无下游 PO 可取消并可回放；两个故障用例分别证明已知 controller 失败不改变 docstatus、响应丢失记录已发生副作用并停在对账。
- 纯测试最终 **31 tests，退出码 0**；Ruff、Python 编译、Runs 页面 Node 语法和 `git diff --check` 均通过。
- 独立只读 Review 结论 **PASS**，未发现可直接证实的 P1/P2：确认 PO→PR/PI、PR→PI、PI→Payment Entry 依赖门禁，取消状态/库存/会计回读，连接锁、幂等/replay、失败和响应丢失恢复证据均闭合。
- 尚未运行阶段出口完整 `make` 门禁、跨单据持久工作流、调度实验、浏览器截图和最终阶段对抗审查；因此不把 P10.5 通过等同 Phase 10 阶段通过。

### 6. 用户如何进行手工测试

1. 准备一条已提交 PO→PR→PI，并可选提交一笔 Payment Entry；用 Buyer 发起 `CANCEL_PO`，确认策略列出下游 PR/PI 并拒绝，不改变 ERP 状态。
2. 由 Payment Operator 发起 `CANCEL_PAYMENT_ENTRY`，Payment Approver 独立审批并执行；确认 Payment Entry 为 Cancelled、PI outstanding 恢复，Receipt 中 GL 净额为 0。
3. 依次独立审批并执行 `CANCEL_PI`、`CANCEL_PR`、`CANCEL_PO`；分别确认 PI/PR/PO 为 Cancelled，PR/PO billed 和 received 回到当前剩余事实，库存台账净数量为 0，GL 借贷平衡。
4. 重放任一已完成取消的幂等键，确认只返回同一目标和历史 Receipt；在 Runs 页面查看每个 Action 的审批、Reservation、Receipt 和恢复建议。
5. 注入 controller 失败或提交后响应丢失，确认失败回执明确标记 `NOT_APPLIED` 或 `APPLIED_BUT_RESPONSE_FAILED`；只读对账或人工接管前不得重新提交取消。

### 7. 风险预估与规避

- P1：先取消上游可能留下库存、应付或已付款残留；policy 显式列出已提交下游，ERP controller 再做原生依赖校验，测试按 Payment Entry→PI→PR→PO 逆序验证。
- P1：取消后成功响应丢失可能被重复取消；reservation、目标 docstatus、库存/GL 净额和发票余额写入 Receipt，未知结果停在只读对账/人工介入。
- P1：取消回读若只看 `docstatus=2` 会遗漏反向影响；PR/PI/Payment Entry 分别读取 Stock Ledger、PR/PO billing、Purchase Invoice outstanding 和 GL 全量净额。
- P2：后续合法取消会改变父单累计进度；回放匹配只放宽明确的父级 received/billed 与 Payment Entry 关联发票状态/余额字段，其余目标、链接和台账证据仍必须稳定。
- P2：ERP 新增 Workflow、冻结期或权限撤销可能使旧审批失效；取消前后均执行当前 scope/permission/deterministic/workflow 五门重检，失败时不调用 controller。

### 8. 下轮建议

进入 P10.6：把已实现的 P2P 动作组合进持久跨单据 Run，修正单动作执行关闭 Run 的旧假设，增加中断恢复、人工修改后的重新调查和完整运营界面回读。

### 9. 本轮任务大白话讲解

这一步补上了“出错后怎么合法撤销”的闭环。系统不会看到上游有问题就一键级联取消，而是先检查有没有已提交的收货、发票或付款；有就明确告诉审批人必须按下游到上游处理。每次取消都要重新独立审批，ERPNext 原生 controller 真正执行，完成后同时核对单据状态、库存或会计冲销、发票余额和来源进度。取消失败时回执会说明副作用没有发生；如果调用方在 ERP 已取消后丢了响应，系统会停在只读对账并把人工恢复入口显示在 Runs 页面。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么不能把取消做成数据库回滚？回答：ERPNext 取消会生成库存和 GL 逆向事实，并更新下游累计状态；数据库回滚无法替代业务冲销，也会破坏审计，所以每个取消是独立 action、独立审批和独立 Receipt。
- 为什么 PI 取消前必须先取消 Payment Entry？回答：固定上游的 Payment Entry 引用会使发票仍有活动付款链接，PI controller 会阻止取消；即使 controller 没有阻止，也必须先恢复 outstanding，再取消发票。
- 如何确认 PR 取消真的恢复库存？回答：不只看 PR docstatus，而是读取该 PR 的全部 Stock Ledger Entry，要求原始与反向数量净额为 0，同时核对 PO Item received_qty 与剩余已提交收货合计。
- 响应丢失时为什么不能重新调用 cancel？回答：取消是不可重复的业务副作用；先用目标状态、库存/GL 和发票余额判断是否已发生，未知时进入对账或人工介入，避免把恢复动作变成第二次业务写入。
- 父单累计进度为什么允许回放时变化？回答：合法后续取消会改变 PO/PR 的 received/billed 汇总；Receipt 固定目标、链接、状态和账务证据，只对明确的累计进度字段做受限历史快照匹配。

## 2026-09-10  · Phase 10 · 第 5 轮 · P10.4 Payment Entry 与部分付款

### 1. 本轮任务列表

- 从固定 ERPNext `11e0ba0a1c45f217e2e73e885f699102d06da325` / Frappe `6a329d068416768ec47ccd3326b9cc95a8d7bf99` 复核 `get_payment_entry` mapper、Payment Entry `validate/on_submit/on_cancel`、引用分配、GL Entry 和 Purchase Invoice outstanding 更新路径；确认测试公司使用 `Cash - SP1`（Cash）与 `Creditors - SP1`（Payable）。
- 启用 `CREATE_PAYMENT_ENTRY_DRAFT` 与 `SUBMIT_PAYMENT_ENTRY`，沿用 schema v2 typed action、独立审批、payload digest、reservation、执行前重检、幂等回放、失败回执和当前 ERP 读回。
- 使用 ERPNext 原生 Payment Entry mapper 生成付款草稿，限制为一个已提交 Purchase Invoice 的当前未付余额；服务端校验供应商、公司、付款/应付科目、币种、金额和引用，禁止客户端指定任意 DocType、执行人或总账分录。
- 为 Purchase Invoice/Payment Entry 增加连接级命名锁，覆盖草稿创建与提交竞争；Payment Entry 提交后的 Receipt 同时记录 Payment Entry 金额/引用、发票状态与 outstanding、GL 数量及借贷平衡。
- 增加部分付款两次结清、超额/错误供应商/科目/币种、同一余额并发竞争、失败提交保留草稿及回执的真实 Frappe 验收；新增具备 `Accounts User + Purchase User` 的隔离 Payment Operator，保持发起人与独立 Payment Approver 分离。
- 保留用户现有 `docs/PLAN.md` 修改、历史 MR/PO/PR/PI 动作和旧 Receipt 可读性；不启用 `codex-with-chatgpt`，不写学习笔记。

### 2. 用户 Assignment 与导师引导

本阶段按用户明确指令由 Agent 全程接管代码编写，不触发 Assignment；阶段结束后的答疑由用户另行触发。

### 3. 本轮任务完成度

- P10.4：已完成实现与隔离开发 ERP 验收。20 元 Purchase Invoice 分两次生成、审批、提交各 10 元 Payment Entry；第一次提交后发票 `Partly Paid/outstanding=10`，第二次后 `Paid/outstanding=0`，每张 Payment Entry 均产生平衡 GL Entry。
- 付款草稿与提交是两份独立 action、approval、reservation 和 Receipt；同一提交 key 回放原 Payment Entry，不重复调用 controller。失败提交保持真实草稿 `docstatus=0`，并把目标名称写入 `FAILED` Receipt。
- 大致耗时：不可见；Token：不可见；子 Agent：1 次独立只读 Review，最终结论 `PASS`。
- 改动量：治理契约/权限/执行与回读约 500 行，真实验收测试约 430 行，测试清理和隔离用户脚本；Harness/README 未改。

### 4. 测试方式

- `bash env/dev/scripts/dev/env.sh p2p-users`：幂等核验并创建 Payment Operator/Payment Approver 隔离身份。
- `bash env/dev/scripts/dev/env.sh bash '... bench --site "$FRAPPE_SITE" run-tests --app synora_agentic_erp --module synora_agentic_erp.tests.test_phase10_payment_entry'`：真实 Frappe P10.4 模块测试；修复权限、Decimal mapper、Run 状态和连接竞争问题后最终运行 1 次为 4/4。
- 同一命令运行 `synora_agentic_erp.tests.test_phase10_purchase_invoice`：验证付款新增的 Receipt/清理和财务回读没有破坏 P10.3，最终 5/5。
- `.venv/bin/ruff check synora_agentic_erp/governance synora_agentic_erp/tests/test_phase10_payment_entry.py synora_agentic_erp/tests/phase10_test_helpers.py`、`.venv/bin/python -m py_compile ...`、`node --check synora_agentic_erp/synora_agentic_erp/page/runs/runs.js`、`git diff --check`：提交前静态检查。
- 按 ponytail-review 检查是否复用既有 P2P 执行器、ERP 原生 mapper 和连接锁；独立只读 Review 检查 Payment Entry mapper、权限/币种/余额、幂等、并发、失败回执和 UI calculation。

### 5. 测试结果

- 定向 P10.4：最终 **4 tests，退出码 0**。过程中发现并修复三类问题：Buyer 提案被错误要求 Payment Entry create 权限；原生 mapper 收到字符串金额；两个 Frappe 自动提交连接只靠 `FOR UPDATE` 仍可同时建草稿。最终用独立 Payment Operator 补齐真实分析权限，并以 Decimal + MariaDB 连接级命名锁解决。
- `test_partial_payments_update_invoice_outstanding_and_gl` 真实核对两次付款、发票状态/余额、Payment Entry 引用和 GL 借贷；`test_two_approved_payment_drafts_cannot_consume_same_outstanding` 两连接只允许一个副作用；`test_failed_payment_submit_preserves_draft_and_receipt` 证明 controller 失败不伪造已付款。
- P10.3 定向 PI：**5 tests，退出码 0**；Ruff、Python 3.14 编译、Runs 页面 Node 语法和 `git diff --check` 均通过。
- 独立只读 Review 首轮结论 **PASS**，确认原生 mapper、引用重建、部分余额、币种/科目约束、独立审批、源单锁、幂等回放和失败 Receipt 无 P1/P2 遗留问题。
- 尚未运行浏览器登录态截图、Payment Entry 取消、故障注入、完整 `make` 门禁和阶段出口对抗审查；因此当前不把 P10.4 通过等同 Phase 10 阶段通过。

### 6. 用户如何进行手工测试

1. 用 `synora-p1-buyer@dev.localhost` 在固定公司/仓库准备一张已提交且 outstanding 为 20 的 Purchase Invoice。
2. Buyer 提交 `CREATE_PAYMENT_ENTRY_DRAFT`，填写 10 元、`Cash - SP1` 到 `Creditors - SP1`、唯一发票引用；由 `synora-p1-accountant@dev.localhost` 独立审批并执行，确认只生成 Payment Entry Draft，不发生银行转账。
3. 用具备 Accounts/Purchase 读取权限的 Payment Operator 提交该草稿的 `SUBMIT_PAYMENT_ENTRY`；由不同的 `synora-p1-payment-approver@dev.localhost` 审批并执行，确认 Payment Entry 为 Submitted、发票为 Partly Paid、outstanding 为 10 且 GL 借贷相等。
4. 按相同流程支付剩余 10 元，确认发票变为 Paid、outstanding 为 0；重复第一次提交 key 应只返回同一 Payment Entry。
5. 尝试超额、错误供应商、Payable 作为付款科目、币种不一致或发票已结清的动作，应在 policy 阶段拒绝；若出现 `UNCERTAIN_RESULT`，只读对账或人工接管，不重发 Submit。

### 7. 风险预估与规避

- P1：并发付款可能重复消耗同一 outstanding；创建草稿在发票父单上、提交在 Payment Entry 父单上持有 MariaDB 连接锁，并在锁后重新做当前状态/余额检查；双连接真实测试只生成一份引用。
- P1：ERP 成功后响应丢失可能被误判为失败；reservation、Payment Entry/发票/GL 读回和稳定 Receipt 证据用于对账，未知结果不允许下游或盲重试。
- P1：发起人、审批人和执行人权限不同；Payment Operator/Approver 使用隔离身份，审批和执行阶段分别重检当前 DocPerm、公司、供应商、科目和发票余额。
- P2：多币种账户需要汇率和会计配置；当前契约没有让模型填写汇率，源发票、付款科目和应付科目币种不一致时 fail closed，留待后续明确支持。
- P2：Payment Entry 取消及逆向 GL/outstanding 尚未实现；下一轮按 ERP 合法依赖顺序增加独立取消动作，不以数据库回滚替代业务取消。

### 8. 下轮建议

进入 P10.5：先固定上游核验 PO、PR、PI、Payment Entry 的取消依赖、库存/会计逆向效果和负面用例，再实现单动作取消、重新审批、回读和人工恢复。

### 9. 本轮任务大白话讲解

这一步把“给供应商付款”接进了受治理闭环。系统先从已提交发票的当前未付余额生成一张 Payment Entry 草稿，付款金额、供应商、公司、付款科目、应付科目和唯一发票引用都被服务端固定；不同用户审批后，ERPNext 原生 controller 负责真正提交和记账。提交完成后，系统同时核对 Payment Entry、发票余额/状态和 GL 借贷，只有三者一致才认为付款记账成功。用户在 Runs 页面能看到付款金额、应付余额、独立审批要求和回执；这里记录的是 ERP 付款记账，不连接银行转账。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么不能根据“Payment Entry 单据存在”就说付款成功？回答：草稿存在没有 GL 副作用，提交成功也可能在响应返回前断连；必须读回 Submitted 状态、引用分配、发票 outstanding/status 和平衡 GL。
- 为什么创建草稿也要独立审批？回答：草稿会锁定一个付款引用并可能被后续提交，创建与提交分别绑定 digest、幂等键和回执，任何一步都不能替另一动作授权。
- 如何避免两个付款同时超过发票余额？回答：审批后执行前重新读取余额，发票父单上的连接级命名锁把两个草稿转换串行化，锁内再检查未结草稿和 ERP 当前余额；输掉的动作返回冲突，不重试 controller。
- 为什么只支持同币种付款？回答：当前 typed payload 没有可信汇率输入，ERPNext 汇率/科目配置由 controller 决定；发现账户币种和发票币种不一致就 fail closed，避免模型猜税额或汇率。
- 付款提交失败时如何恢复？回答：保留真实 Draft 和失败 Receipt，Run/Action 进入可审查失败状态；确认没有提交副作用后才能重新提出新的提交动作，未知结果必须先做只读对账。

## 2026-09-10  · Phase 10 · 第 4 轮 · P10.3 Purchase Invoice 与部分开票

### 1. 本轮任务列表

- 从固定 ERPNext/Frappe 版本核验 `make_purchase_invoice` mapper、PR 行的 `pr_detail`/`purchase_receipt` 关联、已开票数量、退货扣减、`Purchase Invoice.on_submit` 的来源状态更新、GL Entry 与 outstanding 计算，以及官方重复/超开票和取消负面用例。
- 启用 `CREATE_PI_DRAFT` 与 `SUBMIT_PI`，沿用 schema v2 typed action、独立会计审批、reservation、来源行锁、幂等回放、失败回执和当前状态重检。
- 以 ERPNext 原生 PR→PI mapper 生成草稿，只覆盖批准的来源行、数量和交易日期；费率必须与提交 PR 当前行一致，税额、科目、合计和应付余额交由 ERP controller 计算。
- 增加部分开票、超开票、费率/UOM/仓库漂移、同一来源行并发草稿、提交失败保留草稿和失败插入回执的真实 ERP 验收。
- 保留用户现有 `docs/PLAN.md` 修改、P10.1/P10.2 行为和旧动作事实；不启用 `codex-with-chatgpt`，不写学习笔记。

### 2. 用户 Assignment 与导师引导

本阶段按用户明确指令由 Agent 全程接管代码编写，不触发 Assignment；阶段结束后的答疑由用户另行触发。

### 3. 本轮任务完成度

- P10.3：已完成实现与隔离开发 ERP 验收。两张 PI 分别开票 1 件并提交；第一张提交后 PR `billed_amt=10`、`per_billed=50`、PI `Unpaid/outstanding=10` 且产生 GL Entry，第二张后 PR `billed_amt=20`、`per_billed=100`、PO `per_billed=100`。
- 费率、UOM、仓库和超开票候选均在 policy 阶段拒绝；同一 PR 来源行的两个已批准草稿只生成一个未提交 PI，第二个动作返回 `CONFLICT`。
- PI 草稿和提交使用不同 action/approval/receipt；成功提交同一 key 只回放原 PI，不重复调用 controller。
- 大致耗时：不可见；Token：不可见；子 Agent：1 次独立只读 Review，第二轮结论 `PASS`。
- 改动量：契约 allowlist、PI policy/read-back/mapper 约束、真实验收测试、测试清理和本阶段日志；Harness/README 未改。

### 4. 测试方式

- `env/dev/scripts/dev/env.sh bash '... bench --site "$FRAPPE_SITE" run-tests --app synora_agentic_erp --module synora_agentic_erp.tests.test_phase10_purchase_invoice'`：定向真实 Frappe PI 模块测试。
- `env/dev/scripts/dev/env.sh app-test`：提交前固定 site 全量 App Test，包含 P10.1、P10.2、P10.3 真实验收及持久测试数据清理。
- `UV_CACHE_DIR=/private/tmp/synora-phase10-uv-cache uv run --python 3.14 pytest tests/test_phase10_contracts.py tests/test_governance_contracts.py tests/test_execution_contracts.py tests/test_governance_policy.py`：契约、治理和回读纯测试。
- `UV_CACHE_DIR=/private/tmp/synora-phase10-uv-cache uv run --python 3.14 ruff check synora_agentic_erp/governance synora_agentic_erp/api.py synora_agentic_erp/tests/test_phase10_purchase_invoice.py synora_agentic_erp/tests/test_phase10_purchase_receipt.py synora_agentic_erp/tests/test_phase10_po_submit.py tests/test_phase10_contracts.py` 与 `git diff --check`：提交前静态检查。
- 按 ponytail-review 检查是否可复用既有 P2P 执行器和 ERP 原生计算；独立只读 Review 检查 mapper、数量/金额、独立审批、幂等、并发和真实证据。

### 5. 测试结果

- 定向 `TestPhase10PurchaseInvoice`：最初 4/4 因 Buyer 仍被要求持有 PI 写权限及 `SUBMIT_PI` 测试来源 DocType 错误而失败；修复独立审批权限与来源绑定后 4/4 通过。补充税额/总额/GL/来源回读、Buying Settings 两分支和真实双连接并发后，定向最终 **5/5** 通过，退出码 0。
- 并发测试首轮发现两个连接均创建草稿；根因是 Frappe 连接自动提交使单独 `FOR UPDATE` 不能跨事务互斥。改为 MariaDB 连接级命名锁，锁取得后再做状态重检，第二次运行只生成一个 PI、另一动作返回 `CONFLICT`。
- 纯契约/治理测试：38 passed，退出码 0；`env.sh app-test`：**221 tests，退出码 0**；Ruff、Node 语法检查和 `git diff --check` 均通过。
- 独立只读 Review 首轮要求补齐 PI 财务/税会计回读、Buying Settings 规则、UI 财务证据和并发证明；已逐项修复。第二轮按固定上游 `get_pending_qty` 语义复核结论 **PASS**。
- 真实回读依据为 PI `docstatus/status/outstanding_amount`、PR Item `billed_amt`、PR/PO `per_billed`、PI `GL Entry` 及 mapper 来源行；不手工改会计、库存或来源状态。浏览器登录态、真实响应丢失和故障注入留到 P10.8，当前不把 P10.3 通过等同 Phase 10 阶段通过。

### 6. 用户如何进行手工测试

1. 用 Buyer 在固定公司/仓库准备一张已提交且已完整收货的 PR（数量 2）。
2. Buyer 提交 `CREATE_PI_DRAFT`，选择 PR 来源行 1 件；Accountant 作为不同用户在独立审批队列批准并执行，确认 PI 草稿的来源行、仓库、UOM、费率和日期一致。
3. 对该草稿提交 `SUBMIT_PI`，由 Accountant 再次独立批准并执行；确认 PI 为 `Unpaid`、outstanding 等于本次金额，PR `per_billed` 约为 50%，并能看到对应 GL Entry。
4. 再创建并提交余量 1 件的 PI，确认 PR/PO 开票进度为 100%；重复同一提交 key 应回放原 PI。
5. 尝试提交超开票数量、错误费率、错误 UOM 或错误仓库，应在 policy 阶段拒绝；遇到 `UNCERTAIN_RESULT` 只读对账，不重发 Submit。

### 7. 风险预估与规避

- P1：发起人与会计审批人的 DocPerm 不同；policy 在发起阶段只验证读取和来源范围，审批/执行阶段重新验证 PI create/submit、公司/仓库、来源状态和会计权限。
- P1：已开票数量不能从 PR Item 的 `billed_amt` 直接推导；服务端按提交 PI 的 `pr_detail` 聚合数量，并按固定上游 `get_pending_qty` 处理 Buying Settings、退货和拒收数量；MariaDB 命名锁覆盖源行，锁后再次检查未提交 PI 草稿，避免超开票和重复草稿。
- P1：ERP 税模板、科目、会计年度或冻结期变化时不生成默认会计数据；由原生 mapper/controller 计算，读回不完整则失败或进入人工对账。
- P2：多来源行顺序可能与用户输入不同；执行和 Receipt 按来源子表行关联核对，不依赖 mapper 返回顺序。
- P2：未运行真实浏览器、故障注入和全量 `make` 门禁，不能宣称完整 P2P 阶段出口。

### 8. 下轮建议

进入 P10.4：先核验固定版本 Payment Entry 构建、提交、分配、取消与 outstanding 更新，再启用 `CREATE_PAYMENT_ENTRY_DRAFT`/`SUBMIT_PAYMENT_ENTRY`，验证部分付款和会计回读。

### 9. 本轮任务大白话讲解

这一步把“已收的货分批开票”接进治理闭环。系统只从已提交 PR 的指定来源行生成 PI 草稿，费率必须来自 PR，数量不能超过扣除历史发票和退货后的余量；会计用户审批并提交后，ERPNext 自己计算税、科目、总额和应付余额，系统再把 PI、PR/PO 开票进度和 GL Entry 对回来。重复提交只返回原结果，不会再记一笔账。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么不使用 PR Item 的 `billed_amt` 直接算剩余数量？回答：金额受税、折扣和币种精度影响，ERPNext mapper 的权威剩余量按 `Purchase Invoice Item.pr_detail` 聚合数量；`billed_amt` 只作为提交后的结果证据。
- 为什么发起 Buyer 不需要 PI create 权限？回答：发起阶段只提出并冻结来源、数量和费率，真正写入由不同的有权 Accountant 审批并执行，执行前服务器再次做当前权限重检。
- 如何证明开票真的影响会计？回答：PI 提交后同时核对 docstatus/status/outstanding、来源 PR/PO 的 per_billed 和该 PI 的 GL Entry，单看 HTTP 200 或单据存在都不算成功。
- 如果税务配置在审批后变化怎么办？回答：审批绑定摘要和有效期，执行前重新检查当前 ERP 配置；原生 controller 拒绝或回读不一致时保留草稿/回执并停止后续动作，不填默认税率或科目。

## 2026-09-10  · Phase 10 · 第 3 轮 · P10.2 Purchase Receipt 与部分收货

### 1. 本轮任务列表

- 按固定 ERPNext/Frappe 版本核验 PO→Purchase Receipt mapper、来源子表关联、剩余收货数量、UOM、仓库和库存台账路径。
- 启用 `CREATE_PR_DRAFT` 与 `SUBMIT_PR`，为草稿创建和提交分别保留 schema v2 typed action、独立审批、reservation、执行前重检、幂等回放和 Receipt 读回。
- 使用 ERPNext 原生 `make_purchase_receipt` mapper 和 Purchase Receipt controller，支持同一 PO 分次部分收货。
- 补足来源行、父单、交易日期、UOM、仓库、数量和供应商直送约束；增加真实 ERP 超收、非法 UOM 与非法仓库拒绝验收。
- 保留用户现有 `docs/PLAN.md` 修改及旧 schema v1 MR/PO Draft 行为。

### 2. 用户 Assignment 与导师引导

本阶段按用户明确指令由 Agent 全程接管代码编写，不触发 Assignment，不写学习笔记；阶段结束后的答疑由用户另行触发。

### 3. 本轮任务完成度

- P10.2：已完成实现与隔离开发 ERP 验收。第一张 PR 收货 1 件并提交，第二张 PR 再收货 1 件；PO `received_qty=2`、`per_received=100`，两张 PR 的库存台账合计增加 2 件。
- 超收 2 件的候选动作在 policy 阶段被拒绝，未进入审批或 ERP 写入。
- 同一来源行已有未提交 PR 草稿时，新的已批准 `CREATE_PR_DRAFT` 会在来源行 `FOR UPDATE` 锁后被拒绝；避免多个草稿重复预留同一剩余量，已用双 action 竞争测试验证。测试 teardown 只通过 ERP cancel 保留生成单据的取消历史，避免重复 app-test 污染固定分页。
- 每个 PR 创建和提交均使用独立 action/approval/receipt；Run 在单步完成后保持 `EXECUTING`，不提前宣称整条 P2P 完成。
- 大致耗时：不可见；Token：不可见；子 Agent：1 次独立只读 Review，最终结果 `PASS`。
- 改动量：治理契约、policy/read-back 约束、真实验收测试和本阶段日志；Harness/README 未改。

### 4. 测试方式

- `bash scripts/dev/env.sh bash '... bench --site "$FRAPPE_SITE" run-tests --app synora_agentic_erp --module synora_agentic_erp.tests.test_phase10_purchase_receipt ...'`：定向真实 Frappe 模块测试；在补充超收断言前后各运行 1 次。
- `cd env/dev && bash scripts/dev/env.sh app-test`：固定 site 全量 App Test，包含本步新增真实验收。
- `UV_CACHE_DIR=/private/tmp/synora-phase10-uv-cache uv run --python 3.14 pytest tests/test_phase10_contracts.py tests/test_governance_contracts.py tests/test_execution_contracts.py tests/test_governance_policy.py`：提交前运行 1 次。
- `UV_CACHE_DIR=/private/tmp/synora-phase10-uv-cache uv run --python 3.14 ruff check synora_agentic_erp/governance synora_agentic_erp/api.py synora_agentic_erp/tests/test_phase10_purchase_receipt.py tests/test_phase10_contracts.py`：提交前运行 1 次。
- `node --check synora_agentic_erp/synora_agentic_erp/page/runs/runs.js` 与 `git diff --check`：提交前各运行 1 次。
- 按 ponytail-review 检查本步 diff 的可删除复杂度；独立只读 Review 检查来源行、权限、状态、幂等、回读和真实测试证据。

### 5. 测试结果

- 定向 `TestPhase10PurchaseReceipt`：共 7 次运行；前 6 次在逐步补齐断言/门禁时分别为 1/1、1/1、1/1、3/3、4/5、5/5，最新一次加入测试数据清理后为 5/5，均以退出码 0 收口。
- `env.sh app-test`：本轮第一次提交前重跑出现 2 failures + 1 error（退出码 1），根因是持久 site 中前序验收产生的测试 PO 进入固定分页，以及审批队列按最早创建顺序截断，导致旧测试无法看到当前动作；已通过 ERP controller 取消测试 PO/PR 保留取消历史，并把审批队列改为最新动作优先。修复后最终运行 **216 tests，退出码 0**。
- 纯契约/治理测试、Ruff、Node 语法检查和 `git diff --check` 已在提交前执行并通过；尚未运行浏览器登录态验收、完整 `make` 门禁和故障注入。
- 独立只读 Review 首轮为 `CHANGES_REQUIRED`：要求关闭 PO 在 policy 阶段拒绝、失败 PR 插入不得把不存在的 target 写入 Receipt，并指出来源行 UI 和真实并发/恢复证据缺口；已修复前两项及 UI 来源行。二轮先发现竞争测试错误地把 Draft 预期为已收数量，修正为“单一未提交 Draft + received_qty=0”后，定向 5/5 通过；最终复核为 `PASS`，确认来源锁、开放草稿门禁、回读与失败回执无剩余阻塞项。真实多进程故障、响应丢失和浏览器验收保留至 P10.8。
- 真实回读依据为 Purchase Receipt `docstatus`、PO Item `received_qty`/PO `per_received` 和 `tabStock Ledger Entry.actual_qty`；未通过手工修改库存或 received_qty 恢复。

### 6. 用户如何进行手工测试

1. 使用 Buyer 在固定公司/仓库准备一张已提交、数量为 2 的 Purchase Order。
2. Buyer 提交 `CREATE_PR_DRAFT`，只选择来源 PO 子表 1 件；由具有 Stock User 权限的不同用户在独立审批队列批准并执行，确认 PR 保持 Draft、来源行和仓库/UOM 一致。
3. 为该 PR 提交 `SUBMIT_PR`，再次独立审批并执行；确认 PR 为 Submitted、PO 已收数量增加 1、库存台账增加 1。
4. 再按相同流程创建并提交第二张 1 件 PR，确认 PO 收货进度为 100%，两张 PR 各自只有批准的来源行。
5. 尝试提出 2 件超收动作，应在 policy 阶段拒绝；若执行返回 `UNCERTAIN_RESULT`，只读对账，不重复发送 Submit。

### 7. 风险预估与规避

- P1：真实多进程故障尚未在本步注入；服务端以来源行 `FOR UPDATE`、未提交 PR 单一草稿门禁、剩余量重检、唯一 idempotency reservation 和 ERP controller 校验为门禁，双 action 竞争已通过，阶段出口继续做进程重启/响应丢失验证。
- P1：mapper 可能因上游配置变化返回额外行；执行器只接受批准的来源子表行，Receipt 回读会核对父单、来源行、数量、UOM、仓库，无法核对则失败并进入对账。
- P2：供应商直送行不产生普通库存收货，本步已 fail closed；税额、会计和发票余额留在 P10.3/P10.4 由 ERP controller 权威计算。
- P2：未运行真实浏览器和完整 make 门禁，不能把本步通过等同 Phase 10 阶段通过。

### 8. 下轮建议

进入 P10.3：先核验固定版本 PR→Purchase Invoice mapper、可开票数量、税/科目和会计冻结路径，再启用 `CREATE_PI_DRAFT`/`SUBMIT_PI` 并完成分次开票回读。

### 9. 本轮任务大白话讲解

这一步把“收到货”接进了受治理闭环。系统先从已提交 PO 的指定来源行生成一张只包含批准数量的 PR 草稿，收货人必须是与 Buyer 不同且有库存权限的审批人；ERPNext 原生 controller 提交后，系统读回 PR、PO 的已收数量和库存台账，只有三者相互对得上才写成功回执。同一 PO 可以分两次收完，超出剩余数量的请求会在写入前拒绝。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么 PR 草稿必须通过 PO mapper？回答：mapper 保留 ERPNext 的来源行、价格、仓库和必填默认值；Agent 只覆盖审批过的数量，不自己重建业务单据。
- 为什么同时核对 PO `received_qty` 和库存台账？回答：来源进度证明业务关联正确，库存台账证明实际库存副作用发生且数量一致，单看 HTTP 响应或 PR 存在都不够。
- 超收为什么在 policy 阶段拒绝？回答：服务端按当前 PO 子表 `qty-received_qty` 重算剩余量，payload 数量超过剩余量就 fail closed，避免把校验责任交给 UI 或模型。
- 部分收货如何避免不同来源行串行？回答：payload 必须携带唯一来源子表行，mapper 和 Receipt 都核对 `purchase_order_item` 与父 PO，来源行不一致直接冲突。
- 如果提交响应丢失怎么办？回答：reservation 保留动作身份，后续只读对账依据目标单据和关键字段分类成功或人工接管，不能盲目重新提交。

## 2026-09-10  · Phase 10 · 第 2 轮 · P10.1 PO Submit 闭环收口

### 1. 本轮任务列表

- 修复独立审批的当前公司/仓库范围和操作权限重检，并把审批绑定到当前策略规则版本。
- 为 P10.1 只启用 `SUBMIT_PO`；其余 P2P 动作继续保持可解析但 fail closed，等待各自阶段实现。
- 完成 Purchase Order Submit 的 reservation、独立审批、执行前重检、ERP controller submit、读回、幂等回放和发起人 Receipt 读取。
- 修复 reservation 租约过期后的只读对账入口，避免进程退出后永久卡在 `STARTED`；补齐审批队列批准后执行按钮。
- 保留旧 schema v1 MR/PO Draft 行为及用户现有 `docs/PLAN.md` 修改。

### 2. 用户 Assignment 与导师引导

本阶段按用户明确指令由 Agent 全程接管代码编写，不触发 Assignment，不写学习笔记；阶段结束后的答疑由用户另行触发。

### 3. 本轮任务完成度

- P10.1：完成本步实现与真实 Frappe 验收。覆盖发起人自批拒绝、独立审批、错误执行人拒绝、批准后队列可见、PO Submit、重复 key 回放、Run Receipt 读回。
- 独立只读 Review：发现的 P1/P2 问题已逐项修复并复验，未遗留本步阻塞项。
- P10.2 及后续业务写入尚未开始；P2P 未来动作不会在本步获得执行权限。
- 大致耗时：不可见；Token：不可见；子 Agent：1 次独立只读 Review。
- 改动量：本步新增/修改业务代码、契约、UI、测试和日志；Harness/README 未改。

### 4. 测试方式

- `UV_CACHE_DIR=/private/tmp/synora-phase10-uv-cache uv run --python 3.14 pytest tests/test_phase10_contracts.py tests/test_governance_contracts.py tests/test_execution_contracts.py tests/test_governance_policy.py`。
- `UV_CACHE_DIR=/private/tmp/synora-phase10-uv-cache uv run --python 3.14 ruff check synora_agentic_erp/governance synora_agentic_erp/api.py synora_agentic_erp/tests/test_phase10_po_submit.py tests/test_phase10_contracts.py`。
- `node --check synora_agentic_erp/synora_agentic_erp/page/runs/runs.js`。
- `cd env/dev && bash scripts/dev/env.sh app-test`；迁移固定 `dev.localhost` 后运行完整 app test。
- 独立只读 Review 输入最终 P10.1 diff、需求和真实测试结果；修复后重新运行上述受影响检查。

### 5. 测试结果

- 纯 Python 定向测试：38 passed，退出码 0。
- Ruff：退出码 0；JavaScript `node --check`：退出码 0。
- `env.sh app-test`：本轮两次运行均为 211 tests，退出码 0；最新一次包含 P10.1 真实验收扩展。
- 真实验收 `TestPhase10PurchaseOrderSubmit`：隔离 Buyer 创建 Run 和 PO，Approver 读取审批队列、拒绝 Buyer 自批、独立批准并执行；ERP Purchase Order `docstatus=1`，Action=`EXECUTED`，Receipt approver 为 Approver，Run 保持 `EXECUTING`；重复 key 返回同一 PO，Buyer 可读回 Run Receipt。
- Review 发现的范围、租约、Receipt 回读、批准后执行队列、controller 错误分类和未来动作启用问题均已修复；未运行浏览器登录态验收、完整 `make` 门禁、真实丢响应故障注入。

### 6. 用户如何进行手工测试

1. 使用 `synora-p1-buyer@dev.localhost` 登录 Runs 页面，准备一张同公司/仓库的 Purchase Order Draft。
2. Buyer 发起 `SUBMIT_PO` 提议并查看来源单据、digest、独立审批要求；不要使用 Buyer 自批。
3. 切换 `synora-p1-approver@dev.localhost`，在独立审批队列打开该动作，确认来源和风险后批准。
4. 在同一队列执行“提交 Purchase Order”，观察 Receipt、目标单据和 Run 保持执行中；刷新页面确认同一动作不会重复 Submit。
5. 若调用返回 `UNCERTAIN_RESULT`，只使用“只读对账”，不要再次发送 Submit；若显示人工接管，保留 ERP 单据和回执供后续处理。

### 7. 风险预估与规避

- P1：PO Submit 已具备双身份、当前范围重检、策略版本和 ERP 状态读回；审批后撤权、Workflow 漂移和真实进程故障注入留到阶段出口继续验证。
- P1：租约过期的 `STARTED` reservation 现在进入只读对账；无法确认目标时转人工处理，不重发 controller。
- P1：独立审批队列只返回当前用户有权审批或执行的已启用动作，不授予他人 Run 详情。
- P2：P2P Receipt 目前按动作类型做最小字段校验；收货、开票和付款会在后续步骤增加数量、税务、会计和余额证据。
- P2：尚未完成浏览器真实登录态截图和故障注入，因此本步不能宣称完整 Phase 10 通过。

### 8. 下轮建议

进入 P10.2：先固定上游核验 PO→Purchase Receipt mapper、剩余数量、UOM、仓库和库存影响，再扩展启用集合、实现 PR Draft/Submit 双动作及部分收货真实验收。

### 9. 本轮任务大白话讲解

这一步把“提交采购订单”变成一条可追责的业务动作：Buyer 只能提出，Approver 必须在自己的权限和公司/仓库范围内批准，Approver 执行时服务器再次检查 PO 仍是草稿，ERP controller 完成 Submit，随后系统读回 `docstatus=1` 才写成功 Receipt。用户在 Runs 页面能看到独立审批队列；批准后同一个审批人仍能看到执行按钮，提交成功后重复点击只回放同一份结果，不会再提交第二次。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么批准后还要再次检查权限和范围？回答：批准是带有效期的事实，执行前公司、仓库、DocPerm 和来源状态都可能变化，服务器必须以当前 ERP 事实重新决定是否允许写入。
- 租约过期后为什么不能直接重试 Submit？回答：进程可能已经在 ERP 完成 Submit，重试会产生重复副作用；先用来源单据和 Receipt 做只读对账，无法唯一确认就转人工。
- 为什么 Run 没有在一个 PO Submit 完成后关闭？回答：Phase 10 是跨单据长任务，单 Action 完成不代表收货、开票和付款都完成；Run 终态要等后续 PlanStep 的事实确认。

## 2026-09-10  · Phase 10 · 第 1 轮 · P10.0 基线与 P10.1 契约/门禁

### 1. 本轮任务列表

- 恢复并核验固定开发 ERP 环境，确认容器健康、上游版本与现有测试基线。
- 为 Phase 10 增加 schema v2 的 P2P action 类型、来源单据和付款引用契约。
- 扩展服务端 policy 的 P2P 权限、来源范围、状态、数量和余额重检。
- 增加受治理 P2P 执行入口、独立审批绑定、reservation、Receipt、回读和只读对账入口。
- 保留旧 schema v1 MR/PO Draft 行为与用户现有 `docs/PLAN.md` 修改。

### 2. 用户 Assignment 与导师引导

本阶段按用户明确指令由 Agent 全程接管代码编写，不触发 Assignment，不写学习笔记；阶段结束后的答疑由用户另行触发。

### 3. 本轮任务完成度

- P10.0：完成。Docker daemon 已恢复，固定开发容器健康，ERPNext/Frappe 上游未改写。
- P10.1：契约、policy 门禁、通用 P2P 执行/API 入口完成；真实 PO Submit 双身份链路尚未验收，留到下一轮。
- 大致耗时：不可见；Token：不可见；子 Agent：0。
- 改动量：业务代码约 590 行，纯契约测试约 130 行，阶段日志约 40 行；Harness/README 未改。

### 4. 测试方式

- `docker info --format '{{.ServerVersion}}'` 与 `cd env/dev && bash scripts/dev/env.sh up`：恢复并启动固定依赖。
- `cd env/dev && bash scripts/dev/env.sh app-test`：运行 Frappe App Test。
- `UV_CACHE_DIR=/private/tmp/synora-phase10-uv-cache uv run --python 3.14 pytest tests/test_phase10_contracts.py tests/test_governance_contracts.py tests/test_execution_contracts.py tests/test_governance_policy.py`。
- `UV_CACHE_DIR=/private/tmp/synora-phase10-uv-cache uv run --python 3.14 ruff check synora_agentic_erp/governance synora_agentic_erp/api.py tests/test_phase10_contracts.py`。
- `git diff --check`。

### 5. 测试结果

- `docker info` 返回 Server `28.5.1`，四个项目容器均启动，MariaDB/Redis/Bench health check 通过。
- `env.sh app-test`：210 tests，退出码 0。
- 纯 Python 定向测试：37 passed，退出码 0。
- Ruff：退出码 0；`git diff --check`：退出码 0。
- 第一次定向测试误用了不存在的 `tests/test_governance_execution.py` 路径，未收集测试；随后按仓库实际路径修正并通过，未产生代码影响。
- 尚未运行真实 P2P 写入、浏览器验收、完整 `make` 门禁、独立 Review 或 Harness 检查。

### 6. 用户如何进行手工测试

本轮尚无可安全复跑的真实写入手工步骤。下一轮先用隔离 Buyer/Approver 测试账号创建一张低金额 PO Draft，再由不同 Approver 审批并调用 P2P submit 入口；任何返回 `UNCERTAIN_RESULT` 的动作先只读对账，不重试。

### 7. 风险预估与规避

- P1：新动作已加入服务端门禁，但真实 ERP DocPerm/Workflow 和 Payment Entry 配置还未在本轮重新取证；下一轮先跑权限探针，映射缺失则 fail closed。
- P1：P2P 执行器在 ERP 操作开始后统一按未知结果处理异常，避免响应丢失时盲重试；下一轮补充真实 controller 错误和回读测试。
- P1：跨单据 Run 尚未改变既有终态语义；当前通用入口保持 Run `EXECUTING`，P10.6 才允许按所有步骤完成条件收口。
- P2：Receipt 回读字段目前是最小稳定集合；下一轮按 PO Submit 实际字段补足数量、状态和关联证据。

### 8. 下轮建议

先在固定 site 重跑权限/Workflow/会计配置取证，完成 PO Submit 的双身份真实测试、响应丢失/重复 key/撤权测试，再提交 P10.1 增量并进入 Purchase Receipt。

### 9. 本轮任务大白话讲解

这轮先把“谁能批准、谁能执行、批准的内容有没有被改、ERP 最后到底发生了什么”这四道门接到 Phase 10 的新动作上。用户看不到新的业务单据结果，因为真实 P2P 写入还没有开始；现在已经有了提交、收货、开票和付款动作可验证的 typed 入口，所有动作都会绑定来源单据、审批人、幂等键和回执。最重要的文件是 `governance/contracts.py`、`governance/policy.py` 和 `governance/p2p_execution.py`。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么 P2P action 使用 schema v2？回答：保留 v1 Draft 历史可读性，同时让新动作的字段、审批和重检规则有明确版本边界。
- 为什么审批人和执行人必须绑定？回答：避免批准者看到一份内容、另一个会话执行另一份内容；执行前仍会重新验证身份、权限、状态和 digest。
- 为什么 ERP 返回成功但应用仍可能进入对账？回答：网络可能在业务提交后丢失响应，Transport 成功不等于业务回执已被确认。
- 为什么不让 Agent 直接提供税额或总账分录？回答：会计、税务和 Workflow 由 ERP/Frappe controller 权威计算与校验。
- 当前最重要的未完成证据是什么？回答：固定 ERP 上使用隔离发起人与独立审批人的 PO Submit 真实链路及其失败/恢复证据；当前状态为待验收。
