# Phase 2 开发日志

> 本文件归并 Typed 只读 ERP Gateway 的历史记录，最新轮次在上；原始长日志保留在 Git 历史。

## 2026-08-25 17:37 · Phase 2 · 第 10 轮 · 增加开发预算与停止纠偏护栏

### 本轮任务列表
- 增加按用户结果提交、最小阅读包、分层测试、审查轮次上限和 30 分钟停止线。
- 要求阶段日志记录耗时、Token、子 Agent、测试次数和代码/测试/文档改动量。
- 为 P3.2–P3.7 设置一个主业务提交、一次必要 Harness 同步和阶段出口成本复盘的默认目标。

### 本轮任务完成度
计划 `LEAN-WORKFLOW-20260825-v2` 已按批准范围写入；耗时从批准后开始约 10 分钟内，Token 由宿主统计，本轮未调用子 Agent；业务代码 0、测试代码 0，修改范围仅为规则、日志和 Harness 指纹。

### 测试方式
运行 manifest、Harness 结构、引用、drift、`git diff --check`、文件边界与规则冲突检查；产品代码与依赖未变化，不运行产品测试。

### 测试结果
manifest、Harness 结构和 191 个引用检查通过，drift 为零，`git diff --check` 通过；Phase 2 共 10 轮、90 个规定小节完整；变更不含业务代码、测试、依赖和 P3.1 决策包。Harness 机器分仍为 79/100，扣分主要是语义维度上限和嵌套 Skill `AGENTS.md` 识别，不是本轮回归。

### 用户如何进行手工测试
阅读 `AGENTS.md` 精简工作流和 `PLAN.md` §4.5，确认预算只限制重复过程，不允许跳过 ERP 权限、安全、真实集成和阶段出口验证；确认 Git diff 不含业务代码和 P3.1 决策包。

### 风险预估与规避
结构影响评估：阶段顺序、F-001–F-015、L1/L2/L3、停止条件、ERP 权限和真实验收均未删除，工程结构没有被破坏。固定提交数或时间线可能诱发赶工，L2 单角色也可能漏掉跨边界问题；因此数字只作跑偏提醒，复杂风险可升级双角色，阶段出口仍全量对抗审查。30 分钟停止线只要求重新聚焦，不中止正确但耗时的真实测试。实际质量是否保持仍需用 Phase 3 的缺陷、返工和出口证据验证，当前不能只凭规则文件宣称成功。

### 下轮建议
用 P3.2 作为第一组真实样本，记录用户可见结果、耗时、Token、审查与测试次数；Phase 3 出口再判断是否达到预计 45%–65% Token 降幅。

### 本轮任务大白话讲解
这轮给 Agent 加了“别瞎忙”的仪表盘：读太多文档、反复叫子 Agent、拆很多没有用户效果的提交时必须停下来。它省的是重复劳动，不是把权限检查和真实 ERP 测试删掉。

### 面试官可能追问的技术细节以及如何回答
- 问：为什么预算不能是硬上限？答：安全缺陷和真实集成失败不可预测，硬停会鼓励隐藏风险；预算应触发解释和缩小范围。
- 问：如何证明降本没有降质？答：比较相同阶段的缺陷逃逸、测试覆盖、真实 ERP 证据和阶段出口结论，同时记录 Token、耗时和审查次数。
- 问：文档/代码比例有意义吗？答：只作为流程跑偏信号，不能跨功能机械比较，更不能用来删除必要契约和安全证据。

## 2026-08-25 17:18 · Phase 2 · 第 9 轮 · 精简项目工作流并归并阶段日志

### 本轮任务列表
- 将普通开发改为自测 + Ponytail Review，安全关键使用按需独立审查，阶段出口才做完整对抗审查。
- 把 Context Receipt、测试重跑、Harness/README 同步缩减到风险相称的最小范围。
- 建立实习生练习与大白话讲解规则，补充 Phase 7 多 Agent 学习路线。
- 将 Phase 0/1/2 历史日志归并为每阶段一份倒序日志并同步引用和 Harness。

### 本轮任务完成度
计划 `LEAN-WORKFLOW-20260825-v1` 已按用户批准范围完成文档写入；未推进 P3.1，未修改业务代码，未处理用户未跟踪的决策包。

### 测试方式
运行 manifest、Harness 结构、引用、drift、日志结构、旧链接、`git diff --check` 和文件边界检查。

### 测试结果
manifest 有效、Harness 结构有效、drift 为零、191 个仓库引用无断链；日志目录只保留规范和 Phase 0/1/2 三份日志，20 轮记录均包含九个规定部分；`git diff --check` 通过，变更不含业务代码和 P3.1 决策包。产品代码与依赖未变化，因此未运行产品测试。

### 用户如何进行手工测试
阅读 `AGENTS.md` 与 `docs/PLAN.md` 的风险分级；检查 `docs/development-log/` 除规范外每个 Phase 只有一个日志；确认 `git diff` 没有业务代码和 P3.1 决策包。

### 风险预估与规避
归并会删除当前树中的旧日志路径；仓库内引用同步到新文件，原始逐文件内容仍可从 Git 历史恢复。精简不能删除权限、数据安全、真实 ERP 集成和必要测试。

### 下轮建议
用户先批准或调整 P3.1 决策包；后续从 P3.2 开始优先安排一个小型、低风险的 Agent 开发练习。

### 本轮任务大白话讲解
以前 Agent 每做一点代码都要写一份报告、叫多个 Agent 审查。现在改成“小事小检查、大事大检查”，日志也像一本按阶段倒序记账的工作笔记。以后每轮先讲用户能看到什么和数据怎么走，再讲测试与提交。

### 面试官可能追问的技术细节以及如何回答
- 问：精简后如何避免质量下降？答：按风险分级而不是统一降级；身份、权限、金额、写入和阶段出口仍保留独立审查与真实集成测试。
- 问：如何培养多 Agent 经验？答：Phase 3 先测单 Agent 基线，Phase 7 用 Planner → Policy Reviewer 做 typed handoff 和工具隔离 A/B，不用自由 swarm。
- 问：为什么不让实习生写全部业务代码？答：练习应覆盖可局部验证的知识点，安全门禁和端到端关键路径仍由项目负责人控制。

## 2026-08-25 15:42 · Phase 2 · 第 8 轮 · 修正出口超时语义与治理一致性

### 本轮任务列表
- 将“服务端硬超时”纠正为耗时分类，补客户端 deadline 真实边界说明。
- 同步授权未决项、README/身份 Spike 状态，补跨公司拒绝、安全事件日志和内部诊断。

### 本轮任务完成度
已完成；提交 `adaacda`。

### 测试方式
运行静态、单元、集成、真实 HTTP、Harness 检查及安全关键审查。

### 测试结果
Phase 2 出口问题闭环；不再把返回后的耗时比较描述为能中断 ERP 调用。

### 用户如何进行手工测试
检查 Runtime deadline、Gateway TIMEOUT 分类、跨公司无权限场景和安全日志不包含 capability。

### 风险预估与规避
进程内无法安全强杀任意 ERP 调用；由客户端 deadline、Web worker/request timeout 和可观测日志共同限制影响。

### 下轮建议
进入 P3.1 决策，不在 Phase 2 继续扩展写入能力。

### 本轮任务大白话讲解
这轮修正了“代码能发现调用太慢”和“代码能把卡住的调用停掉”之间的区别，并把漏掉的治理状态补齐。

### 面试官可能追问的技术细节以及如何回答
- 问：为什么 `monotonic()` 不是硬超时？答：检查发生在函数返回后，阻塞期间没有机会执行；真正截止需由客户端、worker 或可取消执行边界提供。
- 问：安全日志为什么不能记录 capability？答：它是短期凭据，日志泄露仍可能在过期前被重放，只记录 correlation 和分类信息。

## 2026-08-25 12:25 · Phase 2 · 第 7 轮 · 完成真实 HTTP 验证与阶段出口

### 本轮任务列表
- 准备 P2.6 幂等数据，使用 Runtime client 走真实 Bench HTTP 覆盖 11 个场景。
- 修复 Frappe RPC 注入 `cmd` 导致严格契约误拒绝的问题。
- 完成 Phase 2 出口报告和命令登记。

### 本轮任务完成度
P2.6 与阶段出口完成；提交 `733da89`、`5006db3`、`5e1d169`、`95f8c28`。

### 测试方式
format/lint/type/unit、Bench app-test、P26 数据脚本、真实 HTTP E2E、上游 clean、Harness 检查。

### 测试结果
29 unit、23 integration、11/11 HTTP 场景通过；真实 HTTP 首次暴露 `cmd` 缺陷，修复后通过。Review 首轮发现清理非幂等和跨公司 fixture 缺失，修复后 `PASS`。

### 用户如何进行手工测试
运行 P26 data，再启动 Bench Web，设置 `SYNORA_P2P_USER_PWD` 后执行 `p26_e2e.py`，期望 `P26-E2E-OK`。

### 风险预估与规避
开发 site 为单语言、单会计年度；客户端 deadline 和服务端耗时分类不是同一个边界，已在下一轮诚实化。

### 下轮建议
停止 Phase 2，先做 Phase 3 产品和前端决策包。

### 本轮任务大白话讲解
这轮让请求真正经过网络、Frappe 路由、Gateway、ERP 和 Runtime 返回，而不是只在 Python 里直接调用函数。

### 面试官可能追问的技术细节以及如何回答
- 问：为什么直接函数测试没发现 `cmd`？答：`cmd` 是 Frappe HTTP 路由层注入的字段，只有真实传输链路才出现。
- 问：为什么需要 E2E？答：契约、认证、序列化、路由和环境配置问题无法由单层单测完全证明。

## 2026-08-25 11:26 · Phase 2 · 第 6 轮 · 建立 Runtime Typed Gateway Client

### 本轮任务列表
- 实现固定 origin/path 的 HTTPX client、严格 Pydantic envelope、错误分类、2 MB 响应上限和 secret 防反射。
- 添加 Runtime 无 ERP/数据库依赖的架构测试。

### 本轮任务完成度
P2.5 完成；提交 `fb32bad`。

### 测试方式
29 个 Runtime/unit/architecture 测试，mypy、ruff；独立 Test/Review 针对响应错配、异常和 secret 生命周期。

### 测试结果
最终全部通过；中间修复 mypy 收窄、格式问题和多项 fail-closed 契约测试。

### 用户如何进行手工测试
运行 `make unit`；设置 Gateway origin 后启动 Runtime，访问 `/healthz`，再由 P2.6 E2E 验证真实请求。

### 风险预估与规避
客户端只能严格验证服务端自报字段，不能独立证明 ERP 真值；P2.6 用 issue_run 与真实结果对照。

### 下轮建议
进入真实 Bench HTTP，不再增加客户端抽象。

### 本轮任务大白话讲解
Runtime 像一个只会拨固定号码、说固定格式话的客户端；即使模型想改 URL、带 Cookie 或传未知工具，也会被拒绝。

### 面试官可能追问的技术细节以及如何回答
- 问：为什么 `trust_env=False`？答：避免环境代理把含 capability 的请求发往意外代理。
- 问：为什么重复解析客户端响应？答：服务端响应也属于跨信任边界数据，未知字段、重复 JSON key、NaN 和请求错配都要 fail closed。

## 2026-08-25 01:52 · Phase 2 · 第 5 轮 · 实现六个真实 ERP 只读工具

### 本轮任务列表
- 实现 Item、Supplier、projected stock、open demand、open MR、open PO。
- 保持公司/仓库/用户权限、分页、UOM、禁用与取消语义。

### 本轮任务完成度
P2.4 完成；提交 `032bcbd`。

### 测试方式
22 个 Bench 集成测试加静态/unit，使用真实 ERP DocType 创建并提交 MR/PO。

### 测试结果
工具返回真实 ERP 数据；Accountant 不会获得 PO 权限；跨仓库隔离、分页、混合 UOM、禁用供应商 omissions 通过。

### 用户如何进行手工测试
准备 Phase 1 数据，签发 Buyer Run，逐个调用六个工具并与 Desk 中同一单据比较。

### 风险预估与规避
大范围读取设仓库、文档和需求行上限；被过滤数据用 `PARTIAL/omissions` 表示，不能误当零。

### 下轮建议
在独立 Runtime 中建立唯一 HTTP 调用边界。

### 本轮任务大白话讲解
这轮把 ERPNext 里的库存、需求和在途采购变成 Agent 能安全读取的六种标准问法。

### 面试官可能追问的技术细节以及如何回答
- 问：为什么数量用 Decimal 和字符串？答：避免 float 精度误差，并让跨 JSON 边界的业务数量语义稳定。
- 问：为什么 `limit + 1`？答：多读一条即可判断 `has_more`，无需额外 count 查询。

## 2026-08-25 01:18 · Phase 2 · 第 4 轮 · 建立 Run Capability 与 Gateway 契约

### 本轮任务列表
- 实现最小 Agent Run、五分钟 opaque capability 摘要、严格请求/错误 envelope、固定工具注册表和审计。
- 拒绝身份伪造、混合凭据、未知/过期/撤销 capability 与未知工具。

### 本轮任务完成度
P2.3 完成；提交 `527a0d7`。

### 测试方式
4 unit、13 Bench app tests 起步，覆盖签发、解析、撤销、权限重检、输入、分页、错误脱敏和审计。

### 测试结果
全部通过；Runtime 不能提交 initiator/company/warehouse 覆盖服务端 Run。

### 用户如何进行手工测试
以 Buyer 登录调用 `issue_run`，切换 Guest 使用 capability 调 `execute`，再撤销并确认旧 capability 被拒绝。

### 风险预估与规避
内部 Run/Audit 使用 `ignore_permissions=True`，只允许经过验证的服务端路径创建，DocType 自身不可由普通用户泛化写入。

### 下轮建议
按固定 ERP 源码逐个增加只读工具，不开放任意方法名。

### 本轮任务大白话讲解
用户先在 ERP 里领一张五分钟有效、只能用于这次 Run 的临时通行证；Runtime 只拿通行证，不拿用户密码。

### 面试官可能追问的技术细节以及如何回答
- 问：为什么只存 SHA-256 摘要？答：数据库泄露时不能直接拿到可重放 token；校验时结合 run_id 重新计算并常量时间比较。
- 问：为什么每次还要重检权限？答：用户可能被停用、角色或 User Permission 可能在签发后变化。

## 2026-08-25 00:17 · Phase 2 · 第 3 轮 · 完成身份授权安全 Spike

### 本轮任务列表
- 验证 Frappe Cookie、API key、OAuth、auth_hooks 和当前用户解析行为。
- 比较三种 Runtime 用户绑定方案并批准 ADR-0003。

### 本轮任务完成度
P2.2 完成；提交 `651380b`。

### 测试方式
真实 WSGI/HTTP 认证 Spike、官方 hook 测试函数、临时凭据清理和上游 clean 检查。

### 测试结果
确认不能安全转发用户 Cookie/API key/OAuth；采用 Frappe 登录态签发、服务端 Run 解析的短期 capability。

### 用户如何进行手工测试
按安全取证文档使用固定测试用户重跑六个认证场景，并确认临时凭据清零。

### 风险预估与规避
有效 Cookie 与无效 Bearer、auth hook 改写用户等组合有身份歧义；Gateway 明确拒绝混合身份来源。

### 下轮建议
实现最小 Run 和 capability，不提前实现 Phase 3 Goal/UI。

### 本轮任务大白话讲解
这轮决定 Agent 服务如何代表“当前 ERP 用户”读数据，同时又不把用户长期密码或 Cookie 交给模型环境。

### 面试官可能追问的技术细节以及如何回答
- 问：为什么不用服务账号？答：服务账号不能表达发起人的原始权限，容易形成宽权限代理；服务身份也必须绑定服务端 Run 并重检用户权限。

## 2026-08-24 23:35 · Phase 2 · 第 2 轮 · 同步工程骨架与 Harness 命令

### 本轮任务列表
- 将实际 format/lint/type/unit/integration/runtime 命令登记到开发权威和 Harness。

### 本轮任务完成度
完成；提交 `4707b29`。

### 测试方式
验证 manifest、引用、drift 和已登记命令真实性。

### 测试结果
产品命令未决项关闭；身份授权仍保持未决，未偷跑 Gateway 实现。

### 用户如何进行手工测试
按 `docs/DEVELOPMENT.md` 逐条运行 P2.1 命令并检查退出码。

### 风险预估与规避
命令存在不等于永远通过；日志必须记录具体运行日期和真实结果。

### 下轮建议
先做身份授权 Spike，再写 Gateway。

### 本轮任务大白话讲解
这轮把“项目到底怎么格式化、检查和启动”从猜测变成可执行命令。

### 面试官可能追问的技术细节以及如何回答
- 问：为什么命令要进入权威文档？答：不同 Agent 和开发者使用同一入口，避免 README 与实际脚手架漂移。

## 2026-08-24 23:04 · Phase 2 · 第 1 轮 · 创建 Frappe App 与独立 Runtime 骨架

### 本轮任务列表
- 创建可安装 Frappe App、最小 DocType 包、独立 `services/agent_runtime`、锁定 Python 3.14 工具链和基础健康检查。

### 本轮任务完成度
P2.1 完成；提交 `e10a4dd`。

### 测试方式
运行 format/lint/type/unit/integration/runtime health，并确认 App 可安装到固定 Bench。

### 测试结果
工程骨架和命令通过；此时没有 ERP 工具、授权 Gateway 或 Agent 业务逻辑。

### 用户如何进行手工测试
运行 `make setup`、`make unit`、`make integration`、`make runtime`，访问 `127.0.0.1:8001/healthz`。

### 风险预估与规避
骨架不能冒充产品完成；后续必须连接真实 ERP 和权限测试。

### 下轮建议
调查 Runtime 与 Frappe 的用户绑定，未经批准不实现生产授权。

### 本轮任务大白话讲解
这轮只搭了两个容器式边界：ERP 里的 Synora App 和 ERP 外的 Agent Runtime，还没有真正分析采购问题。

### 面试官可能追问的技术细节以及如何回答
- 问：为什么拆两个 Python 包？答：Frappe App 持 ERP 权限和业务集成，Runtime 持模型依赖；拆分能机械证明 Runtime 不直连 ERP 内部模块或数据库。
