# Phase 1 开发日志

> 本文件归并 ERP 基线与业务考古的历史记录，最新轮次在上；原始长日志保留在 Git 历史。

## 2026-08-24 21:47 · Phase 1 · 第 5 轮 · 冻结 ERP 基线并完成阶段出口

### 本轮任务列表
- 固定 Frappe `6a329d0`（16.31.0）与 ERPNext `11e0ba0`（16.32.3）。
- 建立权限/Workflow 基线、ADR-0002、Harness 同步和 Phase 1 出口结论。

### 本轮任务完成度
P1.5 与 Phase 1 出口完成；提交 `e9aaa12`、`f14659c`、`fc2418e`。

### 测试方式
复跑 seed、P2P、双上游 HEAD/clean、Harness manifest/引用/drift；版本冻结执行四轮独立对抗审查。

### 测试结果
`SEED-OK`、`P2P-USERS-OK`、`P2P-RUN-OK`；上游工作区干净；前三轮审查纠正版本声明与证据问题，终轮 `PASS`。

### 用户如何进行手工测试
运行 `env.sh seed`、`p2p-users`、`p2p-run`，再检查容器内 Frappe/ERPNext HEAD 与 `git status --porcelain`。

### 风险预估与规避
Node/MariaDB/Redis 只固定 major tag；通过固定 ERP commit pair 和真实 P2P 复核缓释。公开发布前仍需处理第三方许可证边界。

### 下轮建议
Phase 2 基于固定上游实现 typed 只读 Gateway，不修改 ERPNext 核心。

### 本轮任务大白话讲解
这轮把后续开发依赖的 ERP 版本钉死，并证明同一套测试数据能真正走完采购、收货和开票。

### 面试官可能追问的技术细节以及如何回答
- 问：为什么固定 commit 而不是版本号？答：commit 不可变，可让源码引用、测试和运行行为完全对应。
- 问：为什么不改 ERPNext？答：升级和权限边界更清晰，Synora 作为 Frappe App 通过官方 controller/API 集成。

## 2026-08-24 20:49 · Phase 1 · 第 4 轮 · 建立 P2P 源码地图

### 本轮任务列表
- 定位 MR、PO、Receipt、Invoice DocType、controller、转换链、权限、官方测试和业务不变量。

### 本轮任务完成度
P1.4 完成；提交 `da2db1a`。

### 测试方式
用固定上游源码、官方测试和 Phase 1 运行观察交叉验证；独立 Test 首轮发现两处行号错误，修正后通过。

### 测试结果
形成带 `[S]/[R]/[P]/[I]/[U]` 证据标签的源码地图，明确源码事实、运行观察和推断。

### 用户如何进行手工测试
打开 `docs/source-maps/phase1-p2p-source-map.md`，任选一个业务不变量跳到固定上游源码或官方测试核对。

### 风险预估与规避
上游升级会让行号和行为变化；只有通过版本升级门禁后才刷新地图。

### 下轮建议
根据真实 P2P 成功结果冻结完整 commit pair。

### 本轮任务大白话讲解
这轮是在回答“ERPNext 自己是怎么处理采购单据的”，避免 Synora 重新发明或绕过原有业务规则。

### 面试官可能追问的技术细节以及如何回答
- 问：源码地图的价值是什么？答：开发每个工具前能找到业务公式、权限入口和官方回归测试，而不是凭经验猜。

## 2026-08-24 20:05 · Phase 1 · 第 3 轮 · 跑通人工采购到付款基线

### 本轮任务列表
- 使用命名 Buyer/Accountant 用户完成 MR → PO → Receipt → Invoice，观察 Payment 状态和失败路径。

### 本轮任务完成度
P1.3 完成；提交 `99b4ca2`，并在 `97ee1fc` 记录阶段断点。

### 测试方式
运行 `env.sh p2p-run`，保存单据名称、状态、权限、金额与四类失败证据。

### 测试结果
输出 `P2P-RUN-OK`；Invoice 为 Unpaid、outstanding 500；业务单据未用 Administrator 代替命名业务用户。

### 用户如何进行手工测试
登录 ERPNext Desk，按 MR、PO、Purchase Receipt、Purchase Invoice 顺序查看脚本生成的单据和状态。

### 风险预估与规避
取消/退货没有在本轮真实跑通，只引用官方测试，不把它写成运行通过。

### 下轮建议
从真实单据反查 controller 和权限实现。

### 本轮任务大白话讲解
这轮不是开发 Agent，而是先当采购员把 ERPNext 原本的流程走一遍，弄清 Agent 将来要辅助的工作。

### 面试官可能追问的技术细节以及如何回答
- 问：为什么先手工跑流程？答：没有真实基线就无法判断 Agent 读取的数据和后续写入结果是否正确。
- 问：Payment 为什么没自动完成？答：Invoice 只形成应付，付款涉及独立会计权限与流程，按 Roadmap 后置。

## 2026-08-24 19:46 · Phase 1 · 第 2 轮 · 建立确定性测试主数据

### 本轮任务列表
- 创建可重复 seed/cleanup：公司、Supplier、Item、Warehouse、FY、Price List 和 Global Defaults。
- 修正采购默认日期、币种和全局币种问题。

### 本轮任务完成度
P1.2 完成；提交 `a040142`、`2269087`、`ec0fbb6`。

### 测试方式
重复运行 seed 和 cleanup，检查对象数量、公司默认值和采购单据前置条件。

### 测试结果
数据准备幂等；修复空 site 上购买价目表、会计年度和全局币种不一致导致的失败。

### 用户如何进行手工测试
运行 `env.sh seed` 两次，确认第二次不重复累积；在 Desk 查看 `SYNORA-P1` 命名空间主数据。

### 风险预估与规避
清理脚本只应作用于测试命名空间；不得用于真实公司数据。

### 下轮建议
使用命名业务用户跑真实 P2P，而不是只检查数据存在。

### 本轮任务大白话讲解
这轮像为实验室准备一套固定样品，确保每次测试都从相同公司、物料和供应商开始。

### 面试官可能追问的技术细节以及如何回答
- 问：为什么 fixture 要幂等？答：测试可重复运行且失败后可恢复，避免旧数据让结果假通过或假失败。

## 2026-08-24 18:06 · Phase 1 · 第 1 轮 · 建立并恢复真实 ERP 候选环境

### 本轮任务列表
- 建立 Bench、Frappe v16、ERPNext v16、MariaDB、Redis 容器环境。
- 处理初始凭据和环境恢复，完成一次空卷重建验证。

### 本轮任务完成度
P1.1 完成；提交 `52a2429`、`ea3a46f`，ADR-0001 记录环境选择。

### 测试方式
从空 volume 启动服务、安装 site/app、检查健康状态和容器时间戳。

### 测试结果
未修改上游的候选 ERP 环境成功运行；凭据只从 gitignored `.env` 注入。

### 用户如何进行手工测试
按 `env/dev` 命令启动环境，访问 `http://127.0.0.1:8000` 并登录开发 site。

### 风险预估与规避
空卷重建是破坏性验证，后续不在已有 site 上随意重跑；固定版本必须等真实 P2P 通过。

### 下轮建议
建立确定性主数据和可重复清理步骤。

### 本轮任务大白话讲解
这轮把真正的 ERPNext 跑起来。ERPNext 源码存在 Bench volume 的 `apps/erpnext`，不复制进 Synora Git 仓库。

### 面试官可能追问的技术细节以及如何回答
- 问：这是插件还是 Fork？答：Synora 是可安装 Frappe App + 独立 Runtime，运行时安装到 ERPNext Bench，不维护 ERPNext Fork。
- 问：为什么用 MariaDB/Redis？答：它们是固定 Frappe/ERPNext 运行基线的真实依赖，不是 Synora 自造的数据库层。
