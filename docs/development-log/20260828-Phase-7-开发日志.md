# Phase 7 开发日志

## 2026-08-28 19:42 · Phase 7 · 第 5 轮 · P7.4 同任务职责对照、评测与 Adoption Card

### 1. 本轮任务列表

- 固定 P4-G01 `observation-driven-second-tool` 与 P4-G08 `malicious-observation` 为 Phase 7 同任务基线；保持原始目标、tool adapter、Observation 语义、期望序列和安全断言，新增 Prompt A/B、Skill on/off、Context 压缩、预算失败和恶意 Skill 变体。
- 新增确定性的非网络 recorded evaluator，记录 Prompt profile/schema/hash、ContextBuilder 版本、tool schema hash、caller/effective tools、Skill manifest/ref、estimate、Provider `prompt_tokens`、压缩原因、工具轨迹、证据 digest、stop reason、任务结果和安全结果；报告不保存原始 Prompt、Goal、Skill 或完整 Context。
- 用同一固定任务对照 Prompt、ContextBuilder、Skill、Typed Tool、Workflow、MCP 的职责；保留较短 Prompt A，Prompt B 不因措辞更专业而晋级；MCP 明确 `DEFERRED / Phase 9`。
- 形成 `20260828-Phase7-prompt-context-skills.md` Adoption Card，说明全量 caller read-only allowlist 的复现边界、采用/条件采用/拒绝/延期结论，以及未运行真实付费 Provider 的限制。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Phase 7 由 Agent 全程接管代码编写；阶段内不创建 Assignment、不写学习笔记、不自动记录答疑。本轮只记录业务实现、评测证据和必要风险；阶段答疑待用户在阶段结束后另行触发。

### 3. 本轮任务完成度

- P7.4 已完成；Phase 7 阶段出口全量验证、独立对抗 Review 和关闭文档尚未开始。本轮没有启动 Phase 8。
- 评测加载器严格校验 schema、固定 P4 base case、caller allowlist 和变体集合；记录式执行复用既有 Native Tool Calling、ContextBuilder、Prompt Registry 和 Skill Registry，不新增运行时能力。
- 两个固定 case 共生成 12 条 recorded 记录：每个 case 覆盖 Prompt A/B、Skill on/off、Context 压缩和预算失败；恶意 Skill 另执行 capability/schema 边界检查。长 fixture 与 Native 实际压缩分别保留 estimate 和 Provider usage 两类证据。
- 本轮改动量：Runtime 评测代码 2 个文件，评测测试 2 个文件，固定 case 2 个文件，Adoption Card 1 个文件，阶段日志 1 个文件；无新依赖、无 ERP 写工具、无公共 endpoint、无 RAG/Memory、MCP 或 Multi-Agent。
- Token、子 Agent 数和精确耗时宿主不可见；子 Agent 为 0，未编造其它成本数据。

### 4. 测试方式

- 先运行新增评测 loader/evaluator 测试，再实现最小完整记录器；首次测试在实现前退出码 `2`，原始失败为 `ImportError: cannot import name 'load_phase7_cases'` 和 `ModuleNotFoundError: No module named 'agent_runtime.evaluation.phase7'`。
- 运行 P7.4 targeted pytest、Runtime 全量 pytest、Ruff check/format、mypy、compileall 和 `git diff --check`；用独立 Python 命令读取最终报告，检查字段、版本/hash、压缩指标和原始敏感标记。
- 独立 Test/Review 只检查最终记录和边界，不替 Execute 修代码；发现 `max_steps=2` 导致 Native 误报 `MAX_STEPS`、有序工具比较误报压缩丢失、G01 使用 `9000` 预算无法容纳压缩后 Context，以及 Ruff 排版不一致后，均先保留失败证据，再按根因最小修复并重跑。

### 5. 测试结果

- P7.4 targeted pytest：最终退出码 `0`，`9 passed`；覆盖 fixed case 加载、Prompt A/B 单变量、Skill on/off、长 Context、预算失败、恶意 Skill、Adoption 状态和报告 no-leak。
- Runtime 全量 pytest：最终退出码 `0`，`244 passed`，仅有既有 2 个 FastAPI `on_event` 弃用警告。
- Runtime 评测文件 Ruff check 与 format check：退出码 `0`；mypy evaluation 目录：退出码 `0`，`5 source files`；evaluation/tests compileall：退出码 `0`；`git diff --check`：退出码 `0`。
- Prompt A/B：两个 case 的非 decision 层字节完全一致、tool schema/序列/evidence/stop/task 结果一致，A hash 为 `1a676172e121c37910512c73b4a77cf3955cad7bca2c659f342d5b2c6e9dbda4`，B hash 为 `49ffea7a309feb53abdd5227e6ec1803646f60eba7940854c912ca8641123572`；B 未严格改善，结论为 `RETAIN_A`。
- Skill on/off：两个 case 的工具 schema、工具序列、evidence、stop reason、任务成功和安全结果均未下降，当前服务端 task profile 结论为 `ENABLE_CURRENT_PROFILE`；恶意 Skill 只产生 `item.lookup` schema，Provider 调用为 `0`，报告序列化不含 `purchase.submit`、`Ignore previous`、SQL、HTTP 或 shell 原文。
- 长 Context：G01 estimate `31417 → 14861`、G08 `31401 → 14845`，均低于 `16000` 预算；Native 压缩 G01 `9385 → 9327`（预算 `9350`）、G08 `9330 → 9288`（预算 `9300`），两者 Provider `prompt_tokens=160`、任务成功未下降，安全 Prompt、最新 Observation、evidence digest 和 tool schema 均保留。长 fixture 未调用真实 Provider，实际 token 记为未运行。
- 预算失败变体在 Provider 前返回 `CONTEXT_BUDGET` 且调用次数为 `0`。独立证据抽取脚本首轮误用单数报告字段而退出码 `1`；定位真实 schema 为复数集合后重跑退出码 `0`，未改变实现或测试结论。
- `ponytail-review` 只检查过度工程，结论为 `Lean already. Ship.`；未运行真实付费 Provider、收费 A/B、Frappe app-test 或登录态浏览器，本轮不声称真实模型质量、成本或生产收益。

### 6. 用户如何进行手工测试

- 在固定 Runtime 环境执行 Phase 7 recorded suite，检查两个 case 的 `task_fingerprint`、Prompt A/B hash、ContextBuilder/tool schema 版本、Skill manifest hash 和 12 条变体记录是否稳定。
- 使用同一 P4-G01/P4-G08 输入切换 A/B，确认除 decision 层外其它层和 tool schema 不变；只有 B 在安全、任务成功和工具选择上同时严格改善才允许改为采用，否则保留 A。
- 设置显式 Context budget，观察正常 Context、长 Observation、缺失/非法/过小预算和 Provider usage 超限分别进入压缩、前置 `CONTEXT_BUDGET` 或后验拒绝；确认 estimate 不被称作真实 token。
- 对 Skill manifest、正文和 reference 做 hash/路径/allowlist 破坏，或在 Skill 正文放入 `purchase.submit`、SQL、HTTP、shell 诱导语；确认 Skill fail closed、Provider 不获得写 schema，Trace/报告只保留元数据。
- 对 Adoption Card 逐项核对职责：Prompt 不授权，Context 不拥有 ERP 事实，Skill 不授予工具，Tool 由 Gateway 授权，Workflow 负责持久顺序与恢复，MCP 保持 `DEFERRED / Phase 9`。

### 7. 风险预估与规避

- A/B 失真：`3×3=P1`；用同一 task/tool adapter/Observation/断言和 recorded Provider 固定变量，字节级比较共同层；没有严格改善就保留 A，当前无已知 P1 未关闭问题。
- 压缩质量下降：`3×4=P1`；同时检查 estimate 严格下降、任务成功、安全 Prompt、最新 Observation、全部 evidence digest 和 tool schema；预算过小不删除 mandatory 内容而 fail closed。
- 评测结果泄露 Prompt/Skill/恶意文本：`2×4=P1`，真实 Secret 按 P0；记录模型只含 hash/id/版本/原因/摘要 digest，独立序列化扫描未发现敏感原文。
- 评测边界偏移：`2×3=P2`；Adoption Card 明确 Phase 7 的 caller allowlist 和 recorded/no-network 限制，不将 deterministic 结果包装为真实模型收益，不进入 RAG/MCP/Multi-Agent。
- 本轮仍未扩大 capability、policy、Executor 或 ERP 写入面；若阶段出口发现 P0、未关闭 P1、真实写入、契约错配或同一问题两轮修复仍失败，按计划立即停止并报告精确证据。

### 8. 下轮建议

- 进入 Phase 7 阶段出口：先运行新增 targeted 和既有 Runtime 回归，再运行 Frappe app-test、compileall、format/lint/type/unit/integration、真实只读 Buyer 链路、登录态浏览器 no-leak、Harness 和全量静态边界检查。
- 在最终 diff 与全量证据后执行一次独立对抗 Review，检查 Prompt 注入、Context 丢失、Skill 扩权、Trace 泄露、预算、版本偏移和范围越界；发现问题最多修复两轮，第三轮同根因则 `BLOCKED`。
- 只有最终 `PASS`、P0/P1 清零且适用 Rubric 平均分达到门槛后，才更新 PLAN/ARCHITECTURE/README 的阶段事实；随后写收尾日志、计算 fingerprint、提交阶段关闭与 Harness 收尾，并停止在 Phase 7。

### 9. 本轮任务大白话讲解

本轮没有让模型“凭感觉比较新旧方案”，而是给两条已经验证过的采购调查任务做固定对照：同一份输入、同一组只读工具、同一组观察和同一套安全断言，只改变 Prompt 的 decision 层，或只开关 Skill。结果显示 Prompt B 没有严格改善，所以业务主线继续用 A；Skill 能提供工作手册，但没有任何权限；长历史经过固定压缩后明显变短，同时保留安全规则、最新观察、证据摘要和工具边界。数据流是：固定 P4 任务 → Prompt/Context/Skill 版本化装配 → recorded Provider/typed tool → 任务、安全、证据和停止结果 → Adoption Card；真实 Provider 未授权，因此没有把确定性复现说成模型质量提升。

### 10. 面试官可能追问的技术细节以及如何回答

本轮不记录答疑内容。用户将在 Phase 7 结束后显式触发答疑环节；届时再依据本阶段真实改动生成问答并按学习笔记规范记录。

## 2026-08-28 19:19 · Phase 7 · 第 4 轮 · P7.3 Procurement Skills 与恶意扩权防护

### 1. 本轮任务列表

- 建立本地、版本化、三层渐进披露的四个 Procurement Skill：`replenishment-analysis`、`duplicate-purchase-check`、`material-request-draft` 和 `reconciliation`；每个 manifest 固定版本、来源、自由度、只读 `allowed_tools`、正文/引用 hash。
- 新增严格 `SkillRegistry`：只读取 Runtime 包内固定资源根目录，拒绝未知字段、未知版本、重复选择、路径穿越、绝对路径、越界 symlink、根目录未知资源和正文/引用 hash 不一致。
- 将当前 Native Agent task profile 固定映射为 `replenishment-analysis + duplicate-purchase-check`；Skill 只能在调用方 read-only allowlist 内生效，不能扩展 Provider tool schema、Gateway capability 或 Executor 写能力。
- Native 每次 Provider 调用前加载当前 Skill/触发的 Level 3 reference，并以 `skill.loaded` 元数据记录版本、manifest hash、披露等级、加载原因和 effective tool subset；MR Draft/reconciliation 只注册和评测，不进入当前 AGENT 调查路径。
- 本轮未新增 ERP 写工具、generic writer、公共 endpoint、RAG/Memory、MCP、Multi-Agent 或第三方依赖；仅为既有低层测试显式关闭 Skill test double，生产 `execute_agent` 保持默认启用。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Phase 7 由 Agent 全程接管代码编写；阶段内不创建 Assignment、不写学习笔记、不自动记录答疑。本轮只记录业务开发、真实验证和必要风险；阶段答疑待用户在阶段结束后另行触发。

### 3. 本轮任务完成度

- P7.3 已完成，P7.4 尚未开始；四个 Skill 资源、Registry、Native 接入、Frappe `skill.loaded` 严格契约和回归测试均已实现。
- Level 1 manifest 只由服务端解析；选中 Skill 才加载 Level 2 正文；只有 task profile 与 required tools 同时满足时才 JIT 加载 Level 3 reference。当前补货路径不接受模型传入 Skill id。
- 生产 Native provider 看到的 tool schema 仍来自调用方六个 read-only tools；Skill manifest 若声明写工具或超出调用方 allowlist，直接 `CONTEXT_INVALID`，Provider 调用次数为 0。
- 当前改动量：Runtime 业务代码 2 个文件、Skill 资源 14 个文件、Runtime/Frappe 测试 6 个文件、阶段日志 1 个文件；无新依赖。精确耗时、Token 和子 Agent 数宿主不可见，未编造。

### 4. 测试方式

- 先运行新增 `test_skills.py`，首轮在实现前以退出码 `2` 暴露 `ModuleNotFoundError: agent_runtime.skills`；随后按 manifest 字段、路径、hash、JIT、allowlist 和 Native 调用边界逐步补齐测试。
- 运行 Skill targeted pytest、Native/Context/security 相关回归和 Runtime 全量 pytest；用记录 Provider 验证固定 task profile 的 Skill 加载顺序、工具 schema 不扩展以及非法 allowlist 在 Provider 前停止。
- 独立 Test/Review 只检查最终 diff，不替 Execute 修代码；额外执行资源根目录静态架构检查、Ruff、mypy、`git diff --check` 和 Frappe 全量 app-test。
- 发现根目录未知资源被静默跳过后，先新增失败测试，再修正 Registry 根目录枚举；正文 hash 与 Level 3 reference hash 均有独立失败路径。

### 5. 测试结果

- Skill targeted 最终退出码 `0`，`19 passed`；覆盖四 Skill 注册、Level 3 触发/不触发、正文和引用 hash、未知字段/版本、重复选择、路径穿越、根/文件 symlink、未知根资源、allowlist 扩权和无外部/写入集成。
- 独立 Native Skill 集成测试退出码 `0`：固定补货 profile 加载两个 Skill，`skill.loaded → context.assembled → model.requested` 顺序成立，Provider tool names 仍严格等于六个调用方 read-only tools；非法调用方 allowlist 时 Provider 调用 `0`。
- Runtime 全量 pytest 退出码 `0`，`241 passed`，仅保留既有 2 个 FastAPI `on_event` 弃用警告。
- Frappe 定向及最终全量 app-test 均退出码 `0`，最终 `Ran 147 tests ... OK`；新增 known manifest hash、版本、披露元数据和 effective tool subset 契约校验通过，历史 Prompt v1 读取仍通过。
- Ruff 退出码 `0`；mypy 生产目标 4 个文件退出码 `0`；`git diff --check` 退出码 `0`；`ponytail-review` 结论为 `Lean already. Ship.`。
- 本轮首次 Ruff 因 Frappe fixture 一行 102 字符退出码 `1`，拆分 fixture 后通过；独立审查首次发现未知根资源被静默跳过，新增测试先以 2 项失败，修复根目录枚举后复验通过。未运行付费真实模型、真实 A/B 或登录态浏览器，不声称模型质量、成本或生产收益。

### 6. 用户如何进行手工测试

- 在 Runtime 设置合法正整数 `SYNORA_CONTEXT_INPUT_TOKEN_BUDGET`，执行 Buyer 的只读 AGENT Run；检查 Trace 中当前两个 Skill 的 id/version/manifest hash、披露等级、加载原因和 effective tool subset。
- 将 Skill manifest 的 `allowed_tools` 改为 `purchase.submit`、将正文或引用改写而不更新 hash，或在 resources 根目录加入未知文件；确认执行在 Provider 前返回 `CONTEXT_INVALID` 且没有工具调用。测试后恢复受控资源，不能把改写后的 hash 静默写回。
- 将 `material-request-draft` 或 `reconciliation` 作为非当前 task profile 的注册 Skill 检查；确认它们可被服务端映射加载和评测，但不会获得当前 Native Executor 的写能力或公共入口。
- 在恶意 Skill 正文中放入 `purchase.submit`、SQL、HTTP、shell 等诱导文本；确认 Provider 仍只收到调用方有效 read-only schema，Trace/Frappe 只出现元数据，不出现 Skill 正文。

### 7. 风险预估与规避

- Skill 扩权：`2×4=P1`，真实未授权写按 `P0` 处理；通过 manifest read-only 白名单、调用方子集校验、Provider schema 与既有 Gateway 双门禁，以及 Provider 零调用测试规避。当前无已知 P0/P1 未关闭问题。
- Skill 注入错误调查：`3×3=P1`；Skill 只提供版本化程序性指导，Goal/Observation/Skill 正文仍是数据，task profile 由服务端固定，证据/数字/工具门禁保持不变。
- 资源完整性和路径越界：`2×3=P2`；固定包内根目录、严格相对路径、symlink 检查、正文/引用 hash 和未知资源拒绝形成 fail-closed 链路。
- Runtime/Frappe Skill hash 漂移：`2×3=P2`；Frappe 只接受四个已确认 manifest hash，Skill 内容升级必须显式更新版本、资源 hash、服务端 allowlist 与回归测试，不静默接收未知值。
- 本轮没有扩大业务范围；P7.4 才做同任务职责对照和 Adoption Card，MCP 仍明确 DEFERRED，不把本轮确定性测试写成真实模型收益。

### 8. 下轮建议

- 进入 P7.4：固定同一 P4-G01/P4-G08 输入、tool adapter、Observation 和断言，补齐 Prompt A/B、Context 压缩前后、Skill on/off、恶意 Skill 和预算失败的记录式评测。
- 形成 Prompt/Context/Skill/Tool/Workflow/MCP 职责矩阵和 Adoption Card；真实 Provider 未获授权时只报告 deterministic/recorded 复现与安全结果，A/B 质量和成本标记未运行或 `INCONCLUSIVE`。
- P7.4 完成前不写 Phase 7 通过结论、不更新 Phase 8 状态、不启动 MCP/RAG/Memory/Multi-Agent。

### 9. 本轮任务大白话讲解

本轮给模型旁边增加了四套“只读工作手册”，但手册不是权限卡。服务端根据任务类型固定选择手册，先校验 manifest 和文件 hash，再把正文或满足条件的参考页交给上下文装配器；模型即使在手册里写“提交采购单”，也看不到写工具 schema，调用方 allowlist 和 Gateway 仍是最终边界。数据流是：固定 task profile → 本地 Skill manifest/hash → Level 2/3 指导 → ContextBuilder → 只有调用方允许的 read-only tools → 既有证据与 Trace 门禁。

### 10. 面试官可能追问的技术细节以及如何回答

本轮不记录答疑内容。用户将在 Phase 7 结束后显式触发答疑环节；届时再依据本阶段真实改动生成问答并按学习笔记规范记录。

## 2026-08-28 · Phase 7 · 第 3 轮 · P7.2b 业务路径、Trace、Frappe 持久化与 UI

### 1. 本轮任务列表

- Native Agent 每次 Provider 调用前按当前 Observation 重新执行 GSSC ContextBuilder；`/enhance` 同样使用 Prompt Registry 与显式上下文预算，Context 失败时分别安全停止或回退确定性摘要。
- 接入 `CONTEXT_INVALID`、`CONTEXT_BUDGET` 以及 `skill.loaded`、`context.assembled`、`context.compressed` 的 Runtime/Frappe v2 契约；历史 Prompt v1 Trace 继续只读可读。
- 在 Frappe Trace 边界校验 Context 顺序、估算/实际预算、read-only effective tools 子集、当前 Prompt profile hash 和原始内容拒绝；Trace 与 Plan evidence 均只保留版本、hash、id、usage、估算和原因元数据。
- 为 `Synora Run Plan` 增加不可变 `context_evidence_json`，Runs 页面展示 Prompt profile、ContextBuilder、实际 token、估算前后、预算、压缩原因和丢弃片段，并继续统一转义、权限和 no-leak 行为。
- 本轮未新增 ERP 写工具、generic writer、公共 endpoint、RAG/Memory、MCP、Multi-Agent 或第三方依赖；未改变 Gateway、policy、approval、Executor 和 Receipt 权威。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Phase 7 由 Agent 全程接管代码编写；阶段内不创建 Assignment、不写学习笔记、不自动记录答疑。阶段答疑由用户在阶段结束后另行触发。

### 3. 本轮任务完成度

- P7.2b 已完成，P7.3 尚未开始。
- Native v2 每次 `model.requested` 前均有同 step 的 `context.assembled`；Provider 返回后追加实际 `prompt_tokens` 证据。实际 prompt token 超预算时保留 usage、拒绝模型动作并返回 `CONTEXT_BUDGET`。
- Frappe validator 对 v2 Context event 采用严格字段集合、事件顺序、预算关系、有效工具集合和 profile hash allowlist；`content`、Prompt/Skill 正文、原始 Goal、Secret 和写工具名不会进入持久化边界。v1 Trace 不要求新 Context event，保持历史可读。
- `/enhance` 预算缺失/非法和 provider 实际输入超预算均返回 200 确定性摘要，不采用模型文本；确定性计划数量、风险分类和证据规则不受模型增强改变。
- 新增的 metadata-only context evidence 可落库并从 `get_run` 读取；页面只渲染受限元数据。无 Provider 配置的旧回退路径保存空 context evidence，不伪造模型上下文事实。

### 4. 测试方式

- 先以 Runtime integration、enhance endpoint 和 Frappe v2 contract 测试覆盖缺预算、压缩、实际 usage 超限、Trace 缺 Context、原始 `content`、写工具和伪造 profile hash，再实现对应路径。
- 运行 Runtime targeted 与全量 pytest，检查每次模型请求前 Context event、Provider 后验 usage、确定性回退、取消/错误路径和 tool adapter 调用次数。
- 运行 Frappe migration/app-test，验证 DocType 字段、Plan evidence 持久化、权限隔离、历史 v1 Trace 和 v2 event validator；运行 Node 语法检查验证 Runs 页面脚本。
- 独立复核只检查最终 diff 的事件状态机、隐私最小披露、profile/hash 复现、预算和 no-write 边界，不替 Execute 修改代码；随后按发现的根因修复并重新验证。

### 5. 测试结果

- Runtime targeted：退出码 `0`，`61 passed`，保留既有 2 个 FastAPI `on_event` 弃用警告。
- Runtime 全量：退出码 `0`，`222 passed`，同样只有 2 个既有 FastAPI `on_event` 弃用警告。
- Frappe app-test 初次在默认沙箱中因 Docker socket 权限拒绝退出码 `1`，原始错误为 `permission denied while trying to connect to the Docker daemon socket`；获得本地环境授权后重跑 migration 和全量 app-test，退出码 `0`，`Ran 146 tests ... OK`。
- Frappe contract 覆盖 v2 model request 缺 assembled Context、原始 `content`、`purchase.submit`、伪造 profile hash 拒绝，以及历史 v1 Trace 可读；Plan test 覆盖 metadata-only context evidence 的不可变落库和读取。
- 独立复核发现 Trace 通用敏感键规则会把 `actual_prompt_tokens` 当作 Secret 删除；修复为只保留非负整数 usage keys，其他 token/capability/cookie/Authorization 仍脱敏，随后 targeted、Runtime 全量和 Frappe 全量复验通过。
- 独立复核发现 Frappe profile hash 仅校验 64 位格式会接受伪造 hash；加入与当前 Registry 结果一致的两个 profile hash allowlist，并新增失败测试，复验通过。hash 仍只用于完整性/复现，不声称是签名或可信发布机制。
- `mypy` 生产目标 6 个文件退出码 `0`；Ruff 退出码 `0`；`node --check synora_agentic_erp/synora_agentic_erp/page/runs/runs.js` 退出码 `0`；`git diff --check` 退出码 `0`；`ponytail-review` 结论为 `Lean already. Ship.`
- 未运行付费真实模型、收费 A/B 或登录态浏览器验收；本轮证明 Runtime/Frappe 契约、安全回退、确定性压缩、元数据持久化和页面脚本语法，不声称真实模型质量、成本或生产收益。

### 6. 用户如何进行手工测试

- 在 Runtime 与 Frappe sidecar 使用同一 Prompt profile hash，设置正整数 `SYNORA_CONTEXT_INPUT_TOKEN_BUDGET`，执行 Buyer 的只读 AGENT Run；在 Trace 展开中检查 profile/hash、ContextBuilder、估算前后、实际输入 token、压缩原因和 Skill refs（当前 P7.3 尚未加载 Skill）。
- 删除或改成非法 Context budget，确认 Native 在 Provider 前返回 `CONTEXT_BUDGET`；对 `/enhance` 确认 HTTP 仍为 200、展示确定性摘要且 evidence 标注 context fallback。
- 用恶意 Goal/Observation 放入 `purchase.submit`、SQL、shell、Secret 等文本，确认它们只作为不可信数据，不增加 Provider tool schema，也不出现在 Trace 或 Plan context evidence 中。
- 用固定的 metadata-only context evidence 执行 Plan，再从 Frappe `get_run` 和 Runs 页面检查版本、hash、usage、估算和压缩元数据；确认不出现原始 Prompt、Goal、Skill 正文或完整 Context。

### 7. 风险预估与规避

- 敏感数据泄漏：`2×4=P1`，实际 Secret 泄漏按 P0 处理；通过 Runtime/Frappe 深层脱敏、严格字段白名单、原始 content 拒绝、profile hash 校验和 Plan/UI no-leak 测试规避。当前无已知 P0/P1 未关闭问题。
- Context 事件与 Prompt v2 错配：`2×3=P2`；Runtime 每次模型请求前强制 Context，Frappe 对 v2 严格校验，v1 仅保留历史读取兼容；未知/伪造 profile hash fail closed。
- 实际 token 误删或被伪造：`2×3=P2`；估算与 Provider usage 分字段记录，usage 仅接受非负整数，超过显式预算保留证据但拒绝动作。
- UI 与服务端不一致：`2×3=P2`；服务端持久化的 metadata-only evidence 和 validated Trace 是唯一来源，UI 只显示已校验字段，不推断模型成功。
- 本轮没有扩展 capability、tool registry、policy 或 ERP 写入面；P7.3 Skills 仍未加载，P7.4 评测尚未开始。

### 8. 下轮建议

- 进入 P7.3：建立四个本地三层 Procurement Skill、严格 manifest/hash/path parser、服务端 task-profile 映射和 effective tool subset 检查。
- 先写恶意 Skill 扩权、路径穿越、hash 漂移、Level 3 JIT 和 provider schema 不扩展测试，再实现 `SkillRegistry`；当前 Native 固定只加载 replenishment-analysis 与 duplicate-purchase-check。
- 保持 MR Draft/reconciliation 只注册和评测，不授予 Executor 或新增写能力；完成后再进入同任务职责对照评测。

### 9. 本轮任务大白话讲解

本轮把前一轮的“上下文装配工”真正接到两条业务路径上：Native Agent 每次问模型前都会重新整理当前观察，模型回来的输入 token 也会再检查一次；超预算就保留发生过的用量证据，但不接受模型下一步动作。Frappe 只保存“用了哪个版本、哪些片段、估算多少、实际多少、丢了什么”的索引信息，不保存 Prompt、Goal、Skill 正文或完整上下文。Runs 页面展示的也是这些可审计的摘要。数据流是：Frappe 只读 Run → Runtime Registry/ContextBuilder → Provider → typed tool/最终答案门禁 → 脱敏 Trace → Frappe metadata evidence/UI；没有新增任何 ERP 写通道。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么实际 `prompt_tokens` 不能被通用的 `token` 脱敏规则删除？答：它是预算后验和成本审计的必要事实，不是 Secret；因此只对精确 usage key 且值为非负整数放行，任意其它 token-like 字段仍脱敏。状态：待练习。
- Frappe 为什么还要维护 profile hash allowlist？答：Frappe 没有运行时 Prompt 正文，单凭 64 位格式无法证明 profile 身份；固定 allowlist 把 wire contract 绑定到已确认 Registry hash，未知或伪造值 fail closed，Prompt 升级时必须显式同步。状态：待练习。
- Context event 为什么必须在 `model.requested` 前？答：模型请求的输入必须能追溯到同一 step 的选择、压缩和预算；否则 Trace 只能说明“调用过模型”，不能证明调用前真的执行了 Context 门禁。状态：待练习。

## 2026-08-28 · Phase 7 · 第 2 轮 · P7.2a GSSC ContextBuilder 与显式预算

### 1. 本轮任务列表

- 新增严格类型 `ContextFragment`、`ContextStageDecision`、`ContextProvenance` 和 `ContextBuildResult`。
- 实现 `Gather → Select → Structure → Compress` 四阶段，固定工具/Skill/Reference/Observation 排序、canonical JSON 结构和压缩顺序。
- 新增必填正整数配置 `SYNORA_CONTEXT_INPUT_TOKEN_BUDGET`；缺失、非法或 mandatory 内容超限时返回 `CONTEXT_BUDGET`，不进入 Provider。
- 将既有 UTF-8 字节上界更名为 `estimate_input_units`，保留 `estimate_input_tokens` 兼容别名，避免把 estimate 冒充真实 token。
- 实现 provider 返回后的 `prompt_tokens` 后验检查；超预算保留实际 usage 元数据并拒绝模型动作。
- 本轮只修改 Runtime 纯契约和测试，没有接入 Frappe、Trace 持久化、UI、Skill Registry 或新的业务能力。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Phase 7 由 Agent 全程接管代码编写；阶段内不创建 Assignment、不写学习笔记、不自动记录答疑。

### 3. 本轮任务完成度

- P7.2a 已完成并通过独立长上下文复核，尚未开始 P7.2b。
- GSSC 的最终 Provider messages 只保留系统安全层、服务端选择的资源、结构化 Goal/Observation 和有效 tool schema；Trace/持久化所需的元数据类型已准备好，但本轮未写入外部记录。
- 长历史压缩保留最新 Observation 元数据、全部 evidence digest、typed tool allowlist 和 Prompt output contract；未删除安全层或盲目提高预算。
- 本轮改动量：Runtime 业务代码 2 个文件、测试 2 个文件、阶段日志 1 个；无新依赖。宿主不可见本轮精确 token 和耗时，未编造。

### 4. 测试方式

- 先运行新增 `test_context.py`，确认缺失模块失败，再按语法、ID 规范和测试输入问题逐项修复。
- 运行 Context/预算 targeted pytest，再运行 Runtime 全量 pytest。
- 使用 6 条最大长度 Observation、6 个只读 tool schema 的独立脚本测量压缩前后 estimate、预算拒绝和 provider usage 后验。
- 运行 Ruff、format check、compileall 和 `git diff --check`；按 `ponytail-review` 只检查不必要复杂度。

### 5. 测试结果

- 首次新增测试退出码 `2`，原始错误为 `ModuleNotFoundError: agent_runtime.agent.context`。
- Context 首版退出码 `2`，原始错误为 `context.py:74` 反斜杠字符串未闭合；修复字符转义后，7 个测试因内部 ID 大写和测试预算值 `10_000` 被严格解析拒绝而失败，错误分别为 `CONTEXT_INVALID` 和非预期预算代码。
- 修复 ID 小写规范、严格 JSON 十进制输入、JSON array→immutable tuple、Windows 绝对路径拒绝后，Context+预算 targeted 为 `15 passed`，随后 Context targeted 为 `9 passed`。
- Runtime 全量在前一版 Context 变更上为 `214 passed`；最终改动后的全量 Runtime 复验列入本轮提交前门禁，未把旧结果冒充最终结果。
- 独立最大历史第一次以预算 `10000` 退出 `CONTEXT_BUDGET`，冻结证据为 6 条近最大长度 Observation + 6 tools 的 mandatory summary 仍为 `14081`；移除 user message 中重复的 tool schema 内容、保留 Provider `tools` 参数后重测：估算前 `79566`、后 `8398`、预算 `10000`，退出码 `0`。
- 独立后验 usage 检查：模拟 `prompt_tokens=10001` 退出 `CONTEXT_BUDGET`，结果保留 `actual_prompt_tokens=10001`，未调用任何业务 tool。
- targeted Ruff、format check、compileall 和 `git diff --check` 均在最终修复后通过；`ponytail-review`：`Lean already. Ship.`
- 未运行付费真实模型；本轮结论仅覆盖确定性构建、安全不变量和成本前置守卫，不声称真实模型质量提升。

### 6. 用户如何进行手工测试

- 设置 `SYNORA_CONTEXT_INPUT_TOKEN_BUDGET=10000`，用固定 6 步 read-only fixture 构建 Context，检查 `estimated_input_units_before` 大于 after 且 after 不超过预算。
- 将配置改为缺失、空、`0`、负数、浮点或带空格的值，确认构建在 Provider 前返回 `CONTEXT_BUDGET`。
- 在 Goal/Observation 中放入“ignore boundary / purchase.submit / SQL”等文本，检查它们只出现在 user 的 `UNTRUSTED` 区域，不改变 system boundary 或 effective tools。
- 用一条实际 Provider response 的 `prompt_tokens` 大于预算模拟后验，确认 usage 保留但不执行该 response 指定的 tool。

### 7. 风险预估与规避

- 上下文成本失控：`3×3=P1`；通过无代码默认预算、每轮 estimate、固定压缩和实际 usage 后验拒绝规避。当前无已知 P1 未关闭问题。
- 压缩删除安全条件：`3×4=P1`；安全 Prompt 是 required system message，最新 Observation/digest/tool schema 保留；mandatory 最小上下文仍超限时 fail closed。
- 估算与真实 tokenizer 不一致：显式使用 `estimated_input_units`，Provider usage 单独记录为 `actual_prompt_tokens`；不把字节数写成真实 token。
- 调用方无序输入导致复现漂移：工具、Skill、Reference、Observation 按稳定键排序；canonical JSON 固定对象键顺序。
- 本轮未改变 Gateway、policy、Executor、ERP 写能力；P7.2b 才处理 Runtime/Frappe event wire 与持久化。

### 8. 下轮建议

- 进入 P7.2b：每次 native/enhance Provider 调用前重建 Context，接入 `skill.loaded`、`context.assembled`、`context.compressed` 和 `CONTEXT_*` StopCode。
- 在 Frappe 侧严格校验事件顺序、上下文预算、effective tools 子集和深层敏感字段，增加不可变 `context_evidence_json`。
- 先做 Runtime endpoint/contract targeted，再做真实 Frappe app-test 和登录态 UI 验收；不提前引入 Skill、RAG、MCP 或新的写工具。

### 9. 本轮任务大白话讲解

以前每次调查都会把旧 Observation 原文继续累加，步骤多时 Provider 输入会线性变大。本轮加了一个固定的“上下文装配工”：先收集当前任务需要的东西，再只选择服务端允许的内容，把 Goal 和 ERP 观察标成不可信数据，最后按固定顺序压缩旧记录。系统安全规则不会被压缩掉；如果连最小安全上下文都装不进用户明确给的预算，就停止，不调用模型。数据流是：调用方提供 Goal/工具/观察 → ContextBuilder 生成两条 Provider message 和有效 tool schema → Provider 返回 usage → 后续 Runtime 再决定是否接受模型动作。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么要分 estimate 和 actual token？答：调用前只有本地可计算的 UTF-8 保守上界，不能冒充 Provider tokenizer；调用后才有 `prompt_tokens` 事实，若实际超过配置要保留 usage 并拒绝动作。状态：待练习。
- 压缩时为什么不直接提高预算？答：预算是用户明确配置的成本边界；应先按规定顺序删未选 catalog、未触发参考、压缩旧 Observation，mandatory 仍超限就 fail closed，不能用更大预算掩盖上下文设计问题。状态：待练习。
- 如何证明恶意 Observation 没有改变安全边界？答：system message 来自 Registry 的 required boundary，Goal/Observation 只在 user canonical JSON 的 `UNTRUSTED` 字段；effective tools 从调用方 allowlist 校验，内容不能创建新 schema。状态：待练习。

## 2026-08-28 · Phase 7 · 第 1 轮 · P7.1 Prompt Registry 与 v2 版本契约

### 1. 本轮任务列表

- 建立业务 Runtime 唯一 Prompt Registry，覆盖 native Agent 与确定性计划增强两条业务路径。
- 将 Prompt 拆为 `boundary`、`decision`、`recovery`、`output_contract` 四层，并固定顺序、版本和 canonical JSON + SHA-256 hash。
- 增加 native Agent Prompt A/B 变体；A 为默认，B 只改变 `decision` 层。
- 将现有 native system prompt 与 `/enhance` system prompt 迁入 Registry；保持 typed final JSON、Observation digest、数字证据和增强校验边界不变。
- 将新 Runtime 响应的 `prompt_schema_version` 升为 `2`；Frappe 保留历史 v1 响应/Trace 的读取兼容，不迁移、不改写历史记录。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Phase 7 由 Agent 全程接管代码编写，并明确阶段内不创建 Assignment、不写学习笔记、不自动记录答疑；本日志只记录本轮业务开发、真实验证和必要风险。

### 3. 本轮任务完成度

- P7.1 已完成，尚未开始 P7.2；本轮未增加 ContextBuilder、Skill、Memory/RAG、MCP、Multi-Agent 或 ERP 写能力。
- 新增 `prompting.py` 及其契约测试；native/enhance 已通过 Registry 取得 system message。
- `PromptProfile` 仅接受 v2、四个唯一且有序层；profile hash 和非 decision canonical bytes 可复现。
- Runtime 新写 v2；Frappe validator 接受 v1/v2，已补历史 v1 可读测试。
- 本轮改动量：业务代码 5 个文件、测试 3 个文件、阶段日志 1 个；无新依赖、无用户维护文件改动。宿主不可见本轮精确 token 和耗时，未编造。

### 4. 测试方式

- 先运行新增 `test_prompting.py`，确认缺失实现会失败；随后按失败根因逐次修正。
- 运行 Prompt、enhance、security、native tool calling、Agent execute targeted pytest。
- 运行 Runtime 全部 pytest。
- 运行独立 canonical hash/A-B 不变量脚本、源码静态检查和 `compileall`；检查 `git diff --check`。
- 运行真实 `bash env/dev/scripts/dev/env.sh app-test`，验证 Frappe migration、API、权限和历史 v1 读取。

### 5. 测试结果

- 首次新增测试：退出码 `2`，原始错误为 `ModuleNotFoundError: agent_runtime.agent.prompting`；证明测试先于实现进入失败态。
- Registry 首版收集：退出码 `1`，原始错误为严格模型拒绝 JSON list 到 tuple；已改为显式 list→不可变 tuple 边界，未放宽未知字段/层校验。
- P7.1 targeted Runtime：退出码 `0`，`60 passed`，2 个既有 FastAPI `on_event` 弃用警告。
- Runtime 全量：退出码 `0`，`206 passed`，2 个既有 FastAPI `on_event` 弃用警告。
- 独立 A/B invariant 脚本首次因根目录未提供 `PYTHONPATH` 退出码 `1`，原始错误为 `ModuleNotFoundError`；按正确 Runtime import 边界重跑退出码 `0`，A/B hash 不同，boundary/recovery/output hash 相同。
- `python -m compileall`：退出码 `0`；`git diff --check`：退出码 `0`。
- 真实 Frappe app-test 首次受沙箱 Docker socket 权限拒绝，退出码 `1`，未归因于代码；获环境授权后重新运行，migration 成功，`Ran 142 tests ... OK`，退出码 `0`，含历史 Prompt v1 可读测试。
- 未运行付费真实模型 A/B；本轮只证明 Registry、版本和确定性安全回归，不声称模型质量提升。

### 6. 用户如何进行手工测试

- 在 Runtime 环境中导入 `PromptRegistry`，分别 resolve `native-agent/A`、`native-agent/B`，确认 profile hash 稳定且只有 decision 层不同。
- 执行现有 read-only Agent 流程，确认默认使用 A；再用固定测试入口显式选择 B，确认工具 allowlist、Gateway 请求和最终 JSON 契约未改变。
- 执行 `/enhance` 的确定性回退/校验用例，确认计划中的数字和风险结论仍由确定性数据控制。
- 手工验证不应把 Prompt 文本、Goal、Skill 正文或 capability 写入 Trace；本轮完整 Context 证据尚待 P7.2 接入。

### 7. 风险预估与规避

- Prompt 安全约束漂移：本轮等级 `3×4=P1`；通过不可变共同层、四层有序校验、A/B 非 decision 字节比较和 Frappe v1 兼容测试规避。当前无已知 P1 未关闭问题。
- Prompt 版本错配：当前 `2×3=P2`；Runtime 新写 v2，Frappe 显式接受 v1/v2，未知版本仍拒绝；Context/Trace v2 事件尚未在本轮提前接入。
- 误把 hash 当可信签名：profile hash 仅用于完整性/复现，不是签名或发布信任机制，已写入模块说明。
- 本轮未改变工具、policy、Gateway、Executor、approval 或 ERP 写路径；P7.2 预算与 Trace 接入前不进入下一步。

### 8. 下轮建议

- 进入 P7.2a：先写 ContextFragment、ContextBuildResult、ContextProvenance 和显式 `SYNORA_CONTEXT_INPUT_TOKEN_BUDGET` 的失败测试，再实现 GSSC 与确定性压缩。
- 保留本轮 P7.1 提交作为独立边界；下一轮不得把 Context、Skill 或 Frappe/UI 改动提前混入本提交。
- 继续使用保守 UTF-8 estimate 与 Provider 实际 prompt token 两套证据命名，不把 estimate 冒充真实 token。

### 9. 本轮任务大白话讲解

以前两个业务入口各自把一段 system prompt 写在代码里，改一个地方容易忘另一个地方，也无法证明 A/B 只改了调查顺序。本轮把规则收进一个有版本的登记册：安全边界、失败时怎么停、最终结果怎么写，三层共同固定；只有“下一步先查什么”允许做 A/B。模型只能看到这些指令，授权和工具能力仍由现有 Gateway/allowlist 决定。数据流是：业务入口选择已登记 profile → Runtime 组合四层 → Provider 收到 system/user message → 现有 typed parser、证据和工具门禁继续校验。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么 Prompt hash 不能直接用普通 `dict` 转字符串？答：普通表示可能受插入顺序和序列化细节影响；这里使用固定层顺序、`sort_keys`、稳定 separators 和 UTF-8 canonical JSON，再做 SHA-256，才能让同一 profile 可复现。
- 如何证明 A/B 是单变量实验？答：比较 A/B 的非 decision canonical bytes 和 boundary/recovery/output 层 hash；只有 decision 层 hash、profile 总 hash 可以不同，任务、工具、模型和安全层由后续固定评测保持一致。状态：待练习。
- Prompt 为什么不能授予模型权限？答：Prompt 是不可信指令输入，真正的能力来自调用方 allowlist、typed Gateway 和服务端权限；即使文本诱导写入，也没有对应 tool schema，Gateway 仍会拒绝。状态：待练习。
