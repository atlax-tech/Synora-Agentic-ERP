# Phase 5 开发日志

## 2026-08-27 17:24 · Phase 5 · 第 31 轮 · 修复独立审查发现的 n8n 证据措辞

### 1. 本轮任务列表

- 接收最终独立对抗审查第一轮结果 `CHANGES_REQUIRED`。
- 按审查证据定位 Adoption Card 第 32 行：仍称 LangGraph/n8n “不可用”，与 n8n 已完成 LAB_ONLY import/execute/audit 证据冲突。
- 只改为“LangGraph/n8n 未纳入该三模式 comparison，也未被填成成功行”，不改变采用边界或实验结论。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- 已完成独立审查要求的最小文字修复；n8n 仍为已验证但仅 `LAB_ONLY`，LangGraph 仍为未纳入该三模式对照的实验。
- 未修改 `.env`、README（根目录）、上游或 `.harness/`。

### 4. 测试方式与实际结果

- 独立审查第一轮：`CHANGES_REQUIRED`，唯一问题为 Adoption Card 过时措辞；未发现新 P0/P1。
- 修复后待重跑 `git diff --check`、Harness 引用校验和独立第二轮审查。

### 5. 风险预估、修复与剩余门禁

- 这是证据表述一致性问题，若不修会让读者误以为 n8n 未运行；已按原始输出校正，不掩盖 HTTP Request audit 风险。
- 阶段仍受 Harness managed/source fingerprint drift 门禁约束；独立审查第二轮未通过前不写 PASS。

### 6. 本轮任务大白话讲解

审查指出 Adoption Card 还有一句旧话，像是在说 n8n 没跑过。实际 n8n 已经跑通，只是没有参加那组三模式离线比较；现在把两件事分开写清楚。

## 2026-08-27 17:08 · Phase 5 · 第 30 轮 · 最终全量复验与 n8n 相对路径证据

### 1. 本轮任务列表

- 在最终 HEAD 上重跑阶段全量命令：format、lint、type、unit、真实 Frappe integration、compileall、Harness structure 和 diff check。
- 按实验 README 的相对路径命令重新启动一次性 recorded Gateway，并在固定 n8n digest 中完成 import、execute、audit。
- 核对工作区边界和提交历史，准备独立对抗审查；阶段状态继续保持 `BLOCKED / P5.5`，不进入 Phase 6。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- 全量检查通过：format 146 files、Ruff lint、mypy 60 files、unit 225 passed、真实 Frappe integration 86 tests OK、compileall、Harness structure（manifest valid / broken references 0）和 `git diff --check` 均退出码 0。
- README 相对路径 n8n 复跑：固定 digest import 退出码 0；execute 退出码 0，`Recorded Gateway`/`Gateway Result Is Readable`/`Safe Result` success，`safe_result=recorded read succeeded`；官方全类别 audit 退出码 0。
- audit 仍原样报告 HTTP Request 官方风险节点；实例 hardened 设置显示 community packages、templates、version notifications、public API 和 diagnostics 均关闭。该证据支持 n8n 仅作 `LAB_ONLY` 对照，不支持业务采用。
- 未修改 `.env`、README（根目录）、上游或 `.harness/`；Frappe/Runtime 服务和 n8n 临时容器均按实验结束清理。

### 4. 测试方式与实际结果

- 沙箱首次运行 `make format-check` 与 `make integration` 的权限错误分别通过 `UV_CACHE_DIR=/tmp/synora-uv-cache` 和宿主 Docker 权限重跑；最终命令均通过。真实 integration 期间执行了项目 migration，未改上游 checkout。
- 既有独立审查最终轮 `PASS` 的修复证据仍在当前历史；本轮文档/实验增量完成后再交独立角色只读复核最终 diff。

### 5. 风险预估、修复与剩余门禁

- n8n HTTP Request 风险是工具能力层面的通用提示，不因 loopback URL 自动消失；已显式记录，不当作安全通过。n8n 不进入业务 Runtime，不接 capability、ERP credential、生产数据或任意外网。
- 代码、权限、恢复、取消/过期、幂等、不重放和 Trace 的 P0/P1 证据在最终全量检查中保持通过；当前唯一未关闭项是 Harness managed/source fingerprint drift，需后续 Harness 管理流程与授权。

### 6. 本轮任务大白话讲解

最后一轮证明：业务代码、真实 ERP 测试、页面/Runtime 证据和 n8n 对照都能在同一最终版本复现。n8n 自己报告的 HTTP 能力风险被留下作为边界提醒；因此阶段还不能越过 Harness 门禁，也不会把实验工具接到生产业务链路。

## 2026-08-27 16:51 · Phase 5 · 第 29 轮 · 修复 n8n README Harness 引用

### 1. 本轮任务列表

- 执行阶段出口 Harness 结构/引用校验。
- 记录真实失败：README 的 Docker bind mount 使用 `$PWD/...`，校验器按字面路径解析，报告 workflow JSON 和 Gateway fixture 两个 broken-reference。
- 将命令改为从仓库根目录可解析的 `./labs/...` 路径，保持运行语义不变。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- 已完成最小文档引用修复；待重跑 Harness 结构、diff 和最终全量检查。
- 未修改 `.env`、README（根目录）、上游或 `.harness/`。

### 4. 测试方式与实际结果

- `python3 -m compileall -q synora_agentic_erp labs tests services/agent_runtime/src`：退出码 0。
- 修复前 `validate_harness_structure.py .`：退出码 1，326 个引用中 2 个因 `$PWD` 字面路径 broken-reference。
- `git diff --check`：修复前未执行到结果；修复后随最终检查重跑。

### 5. 风险预估、修复与剩余门禁

- 这是文档命令可追溯性问题，不改变业务代码或 n8n 能力；必须修复后再把 Harness 结构校验记为通过。
- `.harness/` 仍只读检查；历史 managed/source fingerprint drift 仍待授权的 Harness 管理流程处理。

### 6. 本轮任务大白话讲解

命令本身能运行，但自动检查器看不懂 `$PWD` 变量，于是把它当成不存在的文件。改成明确的仓库相对路径后，命令和检查器都能找到同一个实验文件。

## 2026-08-27 16:42 · Phase 5 · 第 28 轮 · 同步 n8n 出口证据与阶段门禁

### 1. 本轮任务列表

- 将已完成的 n8n 固定镜像、LAB_ONLY import/execute/audit 原始结果同步到 PLAN、Architecture、ADR-0006、Adoption Card 和 n8n 实验说明。
- 明确官方 audit 的 HTTP Request 通用风险提示：不隐藏、不把退出码 0 解释成“无风险”，保持 n8n 不进入业务 Runtime。
- 保持阶段严格状态为 `BLOCKED / P5.5`，唯一未关闭门禁为 Harness managed/source fingerprint drift；不手工修改 `.harness/`。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- n8n blocker 已从“未运行”更新为“真实 LAB_ONLY 证据已形成”：固定 digest pull、import、execute、audit 均成功退出，最终只读结果为 `recorded read succeeded`。
- Adoption Card 继续选择手写引擎作为业务主线；LangGraph 与 n8n 保持 `LAB_ONLY`，没有开放 ERP 写工具。
- 未修改 `.env`、README、上游或 `.harness/`。

### 4. 测试方式与实际结果

- `node --check labs/workflow_comparison/n8n/recorded_gateway.js`：退出码 0。
- `python3 -m json.tool labs/workflow_comparison/n8n/n8n-workflow.json`：退出码 0。
- `git diff --check`：退出码 0。
- 文档仅更新真实证据和剩余门禁；阶段全量检查及 Harness 健康命令留在最终收口复验。

### 5. 风险预估、修复与剩余门禁

- 官方 audit 报告 HTTP Request 的通用风险能力，即使 URL 固定为 loopback 也不会自动消失；因此该工作流只做低代码对照，不能作为业务 Runtime 安全通过或采用依据。
- n8n 网络问题已由宿主 Docker Desktop 代理解决；Codex 沙箱不能访问宿主代理/Docker socket 的边界仍记录为环境事实，不修改用户网络或 `.env`。
- Harness managed/source fingerprint drift 仍须通过 Harness 管理流程处理；在没有文件级 proposal 授权前不改 `.harness/`，阶段不写 `PASS`、不进入 Phase 6。

### 6. 本轮任务大白话讲解

n8n 的对照实验已经真正跑通，但它自己也提醒 HTTP 请求节点能力很宽。我们把这个提醒留下来，证明实验没有掩盖风险；结论是 n8n 只能帮助比较低代码体验，不能绕过 Synora 的权限、短期 capability 和只读 Gateway。

## 2026-08-27 16:28 · Phase 5 · 第 27 轮 · n8n LAB_ONLY 导入执行与审计

### 1. 本轮任务列表

- 固化最小 recorded Gateway fixture：仅监听 `127.0.0.1:18081`，限制 POST 路径、请求体大小、固定 `PLAN_EXECUTE`/只读工具字段，并返回脱敏 observation digest。
- 在固定 `n8nio/n8n:2.37.2-arm64@sha256:c087fd5b55f790bdbe998d3e16f27882031b2178fb574abcc4039fbcd96f5029` 中用临时 SQLite 完成 import、execute 和官方全类别 audit。
- 保持 n8n 为 `LAB_ONLY`；不使用真实 capability、ERP credential、生产数据或任意外网。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- 镜像已按固定 digest 拉取并验证 `arm64/linux` 与版本 `2.37.2`。
- `import:workflow` 退出码 0；`execute --id=p5-n8n-recorded-readonly-v1` 退出码 0，HTTP 请求命中 loopback recorded Gateway，最终节点返回 `safe_result=recorded read succeeded`。
- 官方 `n8n audit --categories=credentials,database,nodes,instance,filesystem` 退出码 0；未出现 credential、database、filesystem、community node 或 custom node 记录。
- audit 明确报告 `n8n-nodes-base.httpRequest` 为官方风险节点，并报告默认实例能力（community packages、templates、version notifications、public API、diagnostics）；这些是审计发现，不隐藏、不声称“无风险”。实验仍限定为 `LAB_ONLY`，业务主线不采用 n8n。

### 4. 测试方式与实际结果

- `docker pull n8nio/n8n@sha256:c087fd5b55f790bdbe998d3e16f27882031b2178fb574abcc4039fbcd96f5029`：退出码 0，digest 完全匹配。
- `docker run ... --version`：退出码 0，`2.37.2`；架构 `arm64/linux`。
- 临时实例 import：退出码 0。
- 临时实例 execute：退出码 0；`Recorded Gateway`、`Gateway Result Is Readable`、`Safe Result` 均 success，`lastNodeExecuted=Safe Result`。
- 临时实例全类别 audit：退出码 0；官方风险节点/实例设置报告已保留在本轮原始输出中。
- `recorded_gateway.js`：仅 Node 标准库、loopback、固定只读校验；后续执行 `node --check` 和安全路径回归。

### 5. 风险预估、修复与剩余门禁

- n8n audit 对 HTTP Request 的通用风险提示不能通过改变 URL 文本消除；本轮没有删除 Phase 5 规定的 loopback HTTP 对照能力，也没有把该提示伪装成 PASS。后续 Adoption Card 明确这是低代码取舍证据，不进入 Runtime。
- n8n 原先的网络阻塞已定位为 Codex 沙箱不能访问宿主代理/Docker socket；宿主 Docker Desktop 代理链路正常，本轮已形成真实 pull/import/execute/audit 证据。
- 阶段严格出口仍受 Harness managed drift 流程约束；`.harness/` 不手工修改。

### 6. 本轮任务大白话讲解

n8n 现在能在一次性实验环境里跑通同一个只读任务：先准备固定输入，再请求只监听本机的记录网关，最后得到“读取成功”。官方安全检查也确实指出 HTTP 请求节点本身能力较宽，所以我们把它当作风险证据保留，只做对照，不把 n8n 接进 Synora 主线。

## 2026-08-27 16:14 · Phase 5 · 第 26 轮 · n8n JSON Body 兼容修复

### 1. 本轮任务列表

- 在固定 n8n 镜像、临时 SQLite 和 recorded Gateway 中执行已成功导入的 workflow。
- 记录真实失败：n8n 2.37.2 对 `JSON Body` 先求值再严格 `JSON.parse`，旧表达式求值后不是合法 JSON，执行返回 `The value in the "JSON Body" field is not valid JSON`。
- 将请求体表达式改为 `JSON.stringify(...)`，保持输入字段和只读 Gateway 目标不变。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- 已完成最小 n8n 版本兼容修复；下一轮重跑导入、执行和 audit，并检查无危险节点/凭证。
- 未修改 `.env`、README、上游或 `.harness/`。

### 4. 测试方式与实际结果

- 同一临时实例 `import:workflow`：退出码 0。
- `execute --id=p5-n8n-recorded-readonly-v1 --rawOutput`：退出码 1；失败证据为 JSON Body 严格解析错误。
- `audit` 尚未进入执行阶段，避免把失败运行误记为安全通过。

### 5. 风险预估、修复与剩余门禁

- 该问题只影响 LAB_ONLY n8n 表达式兼容性，不改变业务 Runtime、Frappe 权威状态或权限边界。
- 请求体仍为固定三字段，Gateway 仍限制 loopback、POST 和 PLAN_EXECUTE/read-only 工具；后续必须以完整执行和 audit 原始输出为证。

### 6. 本轮任务大白话讲解

n8n 的 HTTP 节点最后要把请求体当作标准 JSON 解析。原写法看起来像 JSON，但变量没有按 n8n 表达式规则生成合法 JSON；改成先用 `JSON.stringify` 生成完整 JSON 字符串，解析器才能安全发送。

## 2026-08-27 16:05 · Phase 5 · 第 25 轮 · n8n 导入契约修复

### 1. 本轮任务列表

- 在固定 n8n `2.37.2-arm64` 镜像的临时 SQLite 实例中执行官方 `import:workflow`。
- 记录真实失败：工作流 JSON 缺少 n8n 必需的稳定 `id`，导入返回 `SQLITE_CONSTRAINT: NOT NULL constraint failed: workflow_entity.id`。
- 为 LAB_ONLY 工作流补充固定、非敏感的 workflow id；未接入真实凭证、ERP 或生产数据。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- 已完成最小契约修复；后续将用同一临时实例重跑导入、执行和官方安全审计。
- 未修改 `.env`、README、上游或 `.harness/`。

### 4. 测试方式与实际结果

- `docker run ... import:workflow --input=/tmp/n8n-workflow.json`：退出码 1；失败证据为 workflow id 缺失。
- 固定镜像 CLI `--help`、`import:workflow --help`、`execute --help`、`audit --help`：均退出码 0。

### 5. 风险预估、修复与剩余门禁

- 导入失败属于实验契约缺口，不影响业务主线；修复只增加稳定标识，不扩大 n8n 能力。
- n8n 仍限定为 `LAB_ONLY`；完成 import/execute/audit 后，严格阶段出口还需按 Harness 管理流程处理剩余门禁。

### 6. 本轮任务大白话讲解

n8n 的导入器把工作流保存到自己的数据库时要求每个工作流有唯一 id。原实验文件没有这个字段，所以数据库拒绝保存；现在补上固定 id，再继续跑完整实验。

## 2026-08-27 15:35 · Phase 5 · 第 24 轮 · 最终独立对抗审查与阶段收口

### 1. 本轮任务列表

- 在最终代码、测试和运行证据形成后完成第二轮独立对抗审查；审查角色只读检查，没有修改代码或文档。
- 核对第一轮 `CHANGES_REQUIRED` 的 P1 已由 `00d285e` 修复、`62f1312` 加固测试，并确认最终审查返回 `PASS`。
- 同步 PLAN、Architecture、ADR-0006 和 Adoption Card 的真实出口证据；严格阶段状态仍按未关闭门禁保留为 `BLOCKED / P5.5`。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- 独立审查两轮结果：第一轮 `CHANGES_REQUIRED`（取消/失败后可能展示陈旧 Runtime checkpoint），修复并复验后第二轮 `PASS`。
- 最终 HEAD 已完成代码、测试、浏览器和 Runtime 重启证据收口；工作区干净，未修改 `.env`、README、上游或 `.harness/`。
- 业务引擎采用边界保持不变：手写 Plan-and-Execute 为主线，LangGraph 与 n8n 仍为 `LAB_ONLY`；Phase 6 未启动。

### 4. 测试方式与实际结果

- `make format-check`：退出码 0，146 files already formatted。
- `make lint`：退出码 0。
- `make type`：退出码 0，mypy 60 个源码文件无问题。
- `make unit`：退出码 0，225 passed，2 个既有 FastAPI `on_event` 弃用警告。
- `make integration`：退出码 0，真实 Bench migration 后 `Ran 86 tests ... OK`。
- `compileall`、Harness structure、`git diff --check`：均退出码 0；Fixed/ReAct/Plan-and-Execute recorded comparison 均 `PASS/SUCCEEDED`。

### 5. 风险预估、修复与剩余门禁

- P0/P1 代码风险：最终独立审查无开放 P0/P1；取消/失败/过期终态均不会读取旧 Runtime checkpoint。
- n8n 镜像/import/execute/audit 仍受外部网络/代理阻塞，按用户明确备注暂作为 `LAB_ONLY` 延后项，不影响本轮业务代码收口，但严格阶段出口仍为 `BLOCKED`。
- Harness managed/source fingerprint drift 只完成只读检查；`.harness/` 由 Harness 管理，需经 `harness-update` 的文件级 proposal 与后续授权才能更新，不能手工改写。

### 6. 本轮任务大白话讲解

最终复查证明：即使 Runtime 还留着旧缓存，Frappe 已经记下取消或失败后，页面也不会再把旧任务当成可恢复。业务代码和安全回归已收口；剩下的是 n8n 实验和 Harness 管理流程这两个外部门禁。

## 2026-08-27 15:29 · Phase 5 · 第 23 轮 · 取消清理失败回归加固

### 1. 本轮任务列表

- 按第一轮独立审查的 P1 修复要求，把取消用例扩展为 Runtime 清理通知明确返回 `UNAVAILABLE` 的场景。
- 断言 Frappe 取消仍然成功提交，随后读取工作流不访问 Runtime，也不暴露旧 clarification 或旧步骤。
- 只调整测试替身边界和导入，业务实现保持上一轮已验证的终态权威逻辑。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- 取消流程的清理失败路径现在有真实 Frappe app-test 覆盖；终态摘要仍为 `CANCELLED`、空 clarification、空步骤。
- 未修改 `.env`、README、上游或 `.harness/`；本轮只增强回归证据。

### 4. 测试方式与实际结果

- 受影响文件 Ruff format/check：退出码 0。
- `UV_CACHE_DIR=/tmp/synora-uv-cache make integration`：退出码 0，真实 Bench migration 后 `Ran 86 tests ... OK`。

### 5. 风险预估、修复与剩余门禁

- P1 取消后 Runtime 清理失败导致陈旧 checkpoint 被展示：取消/失败终态短路与清理失败回归均已覆盖；需在最终 HEAD 上完成第二轮独立审查。
- n8n import/execute/audit 仍按用户备注延期为 `LAB_ONLY` 对照实验；Harness managed drift 仍只保留只读检测结果，不手工更新。

### 6. 本轮任务大白话讲解

就算通知 Runtime 清理失败，Frappe 已经记下“取消”这件事，页面也只能显示取消。测试现在专门模拟通知失败，证明旧的“请回答”不会重新出现。

## 2026-08-27 15:20 · Phase 5 · 第 22 轮 · 终态工作流状态遮蔽与独立审查修复

### 1. 本轮任务列表

- 阶段出口独立对抗审查返回 `CHANGES_REQUIRED`：Frappe 已提交 `CANCELLED/FAILED` 后，`get_workflow_status` 仍可能读取旧 Runtime checkpoint，继续展示可恢复的 clarification。
- 先新增取消和失败的陈旧 checkpoint 回归用例，确认问题可复现；随后把状态接口改为以 Frappe 终态为权威，直接返回受限终态摘要，不再访问 Runtime。
- 保持取消流程的 best-effort Runtime 清理语义；即使清理通知失败，状态读取也不会恢复或暴露旧工作流输入。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- `CANCELLED`、`FAILED`、`EXPIRED` 三种 Frappe 终态均由 `_terminal_workflow_summary` 生成无步骤、无 clarification 的安全摘要，并保留受限停止原因。
- 新增取消/失败两条 Frappe 回归测试，断言 Runtime status 不会在终态读取中被调用；未修改 `.env`、README、上游或 `.harness/`。

### 4. 测试方式与实际结果

- 修复前真实 `make integration`：退出码 2，86 个测试中取消和失败陈旧 checkpoint 用例失败，复现独立审查问题。
- 修复后的第一次测试运行还暴露测试替身覆盖了取消清理调用；调整测试作用域后重跑。
- 最终 `UV_CACHE_DIR=/tmp/synora-uv-cache make integration`：退出码 0，真实 Bench migration 后 `Ran 86 tests ... OK`。

### 5. 风险预估、修复与剩余门禁

- P1 Frappe 权威状态被旧 Runtime checkpoint 覆盖：已修复并由取消/失败回归测试关闭；后续 resume 仍由 active/ANALYZING 状态守卫拒绝。
- 独立审查第一轮的 P1 已修复，需在最终全量证据后执行第二轮复审；若再次发现问题，最多按阶段规则再修复一轮。
- n8n import/execute/audit 仍按用户备注延期为 `LAB_ONLY` 对照实验；Harness managed drift 仍只保留只读检测结果，不手工更新。

### 6. 本轮任务大白话讲解

取消或失败一旦在 Frappe 落库，Runtime 里哪怕还留着旧的“请回答”也只能当作过期缓存。页面现在直接显示取消/失败终态，不能再把旧问题当成可恢复任务。

## 2026-08-27 14:57 · Phase 5 · 第 21 轮 · deadline helper 类型门禁收口

### 1. 本轮任务列表

- 运行最终 `make type` 时发现新增 `_workflow_deadline_for_runtime` 直接返回 Frappe 动态对象的 `Any`，触发 mypy `no-any-return`。
- 用显式 `str(...)` 固定 helper 的公共返回类型；不改变 deadline 内容、时区或精度语义。
- 重跑受影响的 Ruff、mypy 和真实 Frappe app-test，确认类型门禁与业务回归均恢复通过。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- deadline helper 的动态边界已显式收窄为 `str`，Runtime deadline 合同和 Frappe 站点时区转换保持不变。
- 本轮只改一行业务代码并记录真实失败/修复；未改 `.env`、README、上游或 `.harness/`。

### 4. 测试方式与实际结果

- 首次 `make type`：退出码 2，1 个 `no-any-return`；作为可复现门禁失败保留在本轮记录。
- 修复后 `ruff format --check`、`ruff check`：退出码 0。
- 修复后 `make type`：退出码 0，mypy 60 个源码文件无问题。
- 修复后 `bash env/dev/scripts/dev/env.sh app-test`：退出码 0，`Ran 84 tests ... OK`。

### 5. 风险预估、修复与剩余门禁

- P2 动态类型污染：已通过显式返回类型收口；无运行时行为变化。
- n8n import/execute/audit 仍是用户允许延期的 `LAB_ONLY` 对照实验；独立对抗审查需以本轮最终提交重新执行。
- Harness managed drift 仍只保留只读检测结果，不手工更新。

### 6. 本轮任务大白话讲解

Frappe 的时间函数没有完整类型信息，mypy 不允许把这种“未知类型”直接当成字符串交给 Runtime。现在在边界处明确转成字符串，既让类型检查放心，也不改变实际时间。

## 2026-08-27 14:52 · Phase 5 · 第 20 轮 · 浏览器复核 deadline 对齐

### 1. 本轮任务列表

- 在 deadline 时区修复后启动新的真实 `PLAN_EXECUTE` Run，完成一次只读工具工作流。
- 对比同一详情中的 Frappe Run 到期时间与 Runtime workflow deadline，确认两处显示完全一致：`2026-08-28 12:21:57`。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- 新 Run `dfda2548…` 的 workflow status 为 `SUCCEEDED / Revision 3`，只读 `material_request.open` 步骤完成并形成确定性采购结果；Frappe 与 Runtime deadline 同时刻，未再出现时区漂移。
- 页面仍明确区分采购结果表与“工作流计划（只读）”区域；没有 ERP 写按钮或写入声明。

### 4. 测试方式与实际结果

- 浏览器 New Run/Start/Details：退出流程正常；详情同时显示 Frappe 与 Runtime deadline `2026-08-28 12:21:57`。
- workflow 步骤显示 `material_request.open`、`已完成`、观察 digest 和完成时间；无 capability、Cookie 或原始 ERP 响应。

### 5. 风险预估、修复与剩余门禁

- P1 deadline 时区漂移：真实浏览器复核关闭。
- n8n import/execute/audit 仍是用户允许延期的 `LAB_ONLY` 对照实验；独立对抗审查尚待重新运行，Harness managed drift 仍不手工处理。

### 6. 本轮任务大白话讲解

修复后再用一条新 Run 对表：Frappe 和 Runtime 现在看的是同一个“明天中午到期”，不会因为时区不同让其中一方提前结束。

## 2026-08-27 14:50 · Phase 5 · 第 19 轮 · 统一 Frappe 与 Runtime 的 workflow deadline

### 1. 本轮任务列表

- 最终审查前复核真实浏览器详情，发现 Frappe 页面显示的 24 小时 deadline 与 Runtime checkpoint 的 deadline 不一致：Frappe 使用站点时区的无时区 datetime，而 Runtime 按 UTC 比较带时区时间。
- 先增加 payload 断言，确认 `start` 必须携带 Frappe 权威 deadline；随后发现直接传 `str(datetime)` 会被 Runtime v1 合同拒绝（缺少时区），改为按 Frappe `get_system_timezone()` 附加显式 offset 的 ISO-8601 毫秒值。
- 首次测试断言还暴露 Frappe 微秒与 Runtime 毫秒精度差异；将断言固定为安全的毫秒截断后重跑。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- Frappe 仍是 deadline 权威；Runtime 现在只接收同一时刻、带显式站点时区 offset 的副本，避免过早或过晚过期。
- 新增回归断言覆盖 start payload 的 deadline、显式 timezone 和毫秒精度；未放宽 Runtime 合同，也未改 `.env`、README、上游或 `.harness/`。
- 真实浏览器新建 Run 的 deadline 对齐复测将在提交后进行；既有 Runtime 重启/恢复证据不被本轮改动覆盖。

### 4. 测试方式与实际结果

- `ruff format --check` 与 `ruff check`（service/test）：退出码 0。
- 首次 deadline 断言：1 个 Frappe 测试失败，证实源 datetime 保留微秒而 wire 值按毫秒序列化；修正断言后重跑 `bash env/dev/scripts/dev/env.sh app-test`：退出码 0，`Ran 84 tests ... OK`。
- Runtime 合同探针：无 offset 的 Frappe datetime 被拒绝，带 offset 的 ISO-8601 值被接受；未访问真实 ERP 写路径。

### 5. 风险预估、修复与剩余门禁

- P1 Frappe/Runtime deadline 时区漂移：已由显式 offset 序列化和回归测试修复；等待新 Run 浏览器复核。
- P1 n8n import/execute/audit 仍是 `LAB_ONLY` 外部对照实验，按用户备注延期，不写成通过。
- 独立对抗审查需在本轮修复和最终全量证据之后重新执行；Harness managed drift 仍待单独 proposal 授权。

### 6. 本轮任务大白话讲解

同一个“明天几点过期”不能因为一个服务用印度时区、另一个服务用 UTC 就变成两个时间。现在发送给 Runtime 的时间带上了原来的时区信息，双方比较的是同一个瞬间。

## 2026-08-27 14:36 · Phase 5 · 第 18 轮 · 真实 Runtime 重启与浏览器权限验收

### 1. 本轮任务列表

- 使用本地测试账户完成浏览器权限验收：Buyer 创建的工作流在 Accountant 的 Runs 列表不可见；随后恢复 Buyer 会话继续验收。
- 创建真实 `PLAN_EXECUTE` Run，确认 Runtime 停止前 checkpoint 为 `INTERRUPTED / Revision 3`；停止本轮启动的 Runtime 进程，再用同一 SQLite checkpoint 重启。
- 重启后重新读取同一 Run，计划、澄清问题和 Revision 3 均保留；提交仓库答案后恢复到 `SUCCEEDED / Revision 6`，澄清步骤和一个只读工具步骤各完成一次。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- 真实进程 kill/restart 后从最近 durable checkpoint 恢复，未丢失澄清输入或步骤元数据；恢复后未重复执行已完成步骤。
- 浏览器已覆盖正常创建、clarification 中断、页面重载、恢复、取消、过期、Runtime unavailable 和不同用户隔离；页面未出现 ERP 写按钮或“已创建 MR/PO”声明。
- 未改 `.env` 内容、README、上游或 `.harness/`；本轮只使用环境变量注入短期 Runtime token，token 未写入代码、日志、checkpoint 或公共响应。

### 4. 测试方式与实际结果

- 浏览器 Buyer：`PLAN_EXECUTE` Run `ec3a5b17…` 在 Runtime 停止前显示 `等待澄清 / Revision 3`。
- Runtime 生命周期：收到 `SIGINT` 后正常退出；同一 `SYNORA_WORKFLOW_DB_PATH` 重启成功，`/workflow/status` 重新返回 `Revision 3` 的 clarification checkpoint。
- 浏览器恢复：提交 `SYNORA-P1 Stores - SP1` 后显示“工作流完成 / Revision 6”，`warehouse-clarification` 与 `open-material-requests` 均为“已完成”，观察 digest 可见。
- 浏览器 Accountant：登录后 Runs 列表不包含 Buyer 的 Run `f0f6e3e5…` 或 `ec3a5b17…`；页面仍正常显示自身可读列表。

### 5. 风险预估、修复与剩余门禁

- P1 真实进程重启丢 checkpoint：本轮已关闭；重启前后 Revision、步骤和 clarification 一致，恢复只读工具调用完成一次。
- P1 跨用户读取/恢复：真实浏览器隔离通过；服务端权限与状态回归仍由 84 个 Frappe app-test 覆盖。
- P2 n8n import/execute/audit 仍为 `LAB_ONLY` 外部对照实验，按用户备注延期，不写成通过。
- 独立对抗审查将在最终 diff、全量验证和本轮浏览器证据之后运行；Harness managed drift 仍只保留待授权 proposal，不手工改写。

### 6. 本轮任务大白话讲解

Runtime 重启就像搬家：只要已经把“搬到哪一步、还要问用户什么”写进安全 checkpoint，服务回来后就能接着做，而不是重新调用 ERP。权限验收则确保另一个用户连这张工作流清单都看不到。

## 2026-08-27 14:28 · Phase 5 · 第 17 轮 · 过期后不再暴露陈旧恢复点

### 1. 本轮任务列表

- 复测真实浏览器的过期 Run，发现 Frappe 已权威提交 `EXPIRED` 后，状态接口仍读取 Runtime 中旧的 clarification checkpoint，页面继续显示“等待澄清”和恢复输入。
- 先增加真实 Frappe 回归用例，再在 `get_workflow_status` 边界优先读取 Frappe 的 24 小时 workflow deadline；对已过期 Run 直接生成受限终态摘要，不再请求或信任陈旧 Runtime checkpoint。
- 保留 capability 的 5 分钟生命周期分离：该修复不延长 capability，也不改变 Run/权限权威，只防止过期工作流重新打开人工输入。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- Frappe 过期状态现在会遮蔽旧 Runtime checkpoint，返回 `EXPIRED`、空 clarification、空步骤和安全 stop reason；Runtime 不可用时也不伪造可恢复状态。
- 业务代码、回归测试和本轮日志保持同一小步提交；未改 `.env`、README、上游或 `.harness/`。
- 浏览器复测通过：过期 Run 详情显示 `workflow status=EXPIRED`、`workflow deadline expired`、空步骤，且没有“澄清答案”输入或“提交并恢复”按钮。

### 4. 测试方式与实际结果

- `bash env/dev/scripts/dev/env.sh app-test`：退出码 0，真实 Bench migration 后 `Ran 84 tests ... OK`，包含陈旧 checkpoint 遮蔽回归。
- 浏览器复测：重新加载并打开 `phase5-browser-expiry-20260827`，页面显示“状态: 已过期”“工作流已过期”“workflow deadline expired”，无恢复控件；Frappe Run 列表也只保留“详情”。

### 5. 风险预估、修复与剩余门禁

- P1 过期 Run 仍暴露恢复输入：已用 Frappe 权威状态短路修复，并由真实浏览器复核关闭。
- P1 真实进程 kill/restart、20 并发恢复、同 invocation 不重放和独立对抗审查仍需阶段出口证据；已有单元/集成测试不替代这些证据。
- P2 n8n import/execute/audit 仍为 `LAB_ONLY` 外部对照实验，按用户备注延期，不写成通过。

### 6. 本轮任务大白话讲解

工作流过期后，Runtime 里可能还留着上一次暂停时的画面。现在由 Frappe 这个权威方先说“已经过期”，页面只展示终态，不再让旧画面把用户带回可恢复状态。

## 2026-08-27 14:20 · Phase 5 · 第 16 轮 · 保留合法 workflow step 元数据

### 1. 本轮任务列表

- 在真实 Runtime 下完成 `PLAN_EXECUTE` clarification interrupt；页面详情显示状态、版本、revision、图版本、澄清问题和两个步骤。
- 发现 Frappe 边界的通用深度清洗把合法的 `step_id/order/status/type` 截为 `[TRUNCATED]`，导致计划无法调试。
- 先增加失败断言，再让 `_safe_trace_value` 支持受控 `max_depth`；工作流响应仍经过完整严格 schema、DAG、状态和 JSON 校验，只有这条已验证的 bounded summary 路径使用更深一层，参数与原始观察仍不进入公共摘要。
- 复测真实 Bench app-test，确认步骤字段保留；浏览器随后成功提交澄清并恢复到 `SUCCEEDED`，Run 进入确定性收口前的 `ANALYZING` 展示流程。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- 工作流步骤可调试字段已恢复；敏感 key/value 过滤、长度、类型、公共摘要字段裁剪和 capability 生命周期边界未放宽。
- 浏览器实证：页面关闭/重开后 checkpoint 仍能读取；提交澄清后 revision 从 3 变为 6，两个步骤均显示“已完成”，观察 digest 和完成时间可见。
- 本轮变更按小步规则与回归测试、日志单独提交；未改 `.env`、README、上游或 `.harness/`。

### 4. 测试方式与实际结果

- `UV_CACHE_DIR=/tmp/synora-uv-cache uv run --python 3.14 ruff format synora_agentic_erp/agent/service.py synora_agentic_erp/tests/test_workflow_run.py`：退出码 0。
- `UV_CACHE_DIR=/tmp/synora-uv-cache uv run --python 3.14 ruff check synora_agentic_erp/agent/service.py synora_agentic_erp/tests/test_workflow_run.py`：退出码 0。
- `bash env/dev/scripts/dev/env.sh app-test`：退出码 0，真实 Bench migration 后 `Ran 83 tests ... OK`。
- Runtime HTTP：`/workflow/start`、`/workflow/status`、`/workflow/resume` 均返回 200；Frappe web 日志显示 `api.execute` 只读 Gateway 调用返回 200，`resume_run` 返回 200。
- 浏览器详情：`INTERRUPTED` 的澄清输入、step 元数据、`SUCCEEDED` 后的 stop reason 和 observation digest 均可见；没有 secret 或原始 Prompt。

### 5. 风险预估、修复与剩余门禁

- P1 公共摘要字段截断：已修复；通用 Trace 深度默认仍为 4，工作流只在已验证 schema 后使用 max depth 8，且最终仍只返回受限字段。
- P1 实际 kill/restart 后安全点恢复和同 invocation 不重放：本轮浏览器完成跨请求恢复，但真实进程 kill/restart 证据仍待独立复验。
- P2 n8n import/execute/audit 仍是 `LAB_ONLY` 延后项，不作为业务代码推进阻塞，但不写成通过。
- 独立对抗审查仍安排在最终 diff、全量证据和浏览器证据之后。

### 6. 本轮任务大白话讲解

安全清洗不能为了防止深层脏数据而把正常的计划步骤也抹掉。现在先严格验证整份工作流，再只把用户需要调试的有限字段展示出来；敏感内容和大块 ERP 原始响应依旧不会穿过边界。

## 2026-08-27 14:11 · Phase 5 · 第 15 轮 · Runtime 不可用分支的浏览器安全反馈

### 1. 本轮任务列表

- 用本地测试账户在 Runs 页面重新提交 `PLAN_EXECUTE` Run，并验证到期语义修复后的新 Run 显示为“已创建”，而不是因 capability TTL 被标成过期。
- 在 Runtime 未配置的真实浏览器环境点击“开始分析”，发现旧 UI 只弹出“分析失败”，没有安全原因或关联标识。
- 在 Runs 页面增加受限错误解析：兼容 Frappe `responseJSON` 包装，只显示 `UNAVAILABLE/CONFLICT` 等错误码对应的安全文案和 correlation；不显示原始异常、URL、token 或 ERP 响应。
- 重新加载页面并复测，浏览器显示“分析失败（UNAVAILABLE）：Runtime 当前不可用，工作流状态未被伪造成完成。关联标识: …”，Run 仍保持 Frappe 权威的“分析中”。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- Runtime unavailable 分支现在满足“明确安全原因 + correlation + 不伪造完成状态”的 UI 要求。
- 真实 Runtime 中断/恢复链路仍需 Runtime token 和同进程配置；当前环境的 Bench web 进程没有 token，未通过重启或修改 `.env` 绕过该边界。
- 本轮 UI 修复与日志按小步规则单独提交，不与最终出口文档混合。

### 4. 测试方式与实际结果

- `node --check synora_agentic_erp/synora_agentic_erp/page/runs/runs.js`：退出码 0。
- `git diff --check`：退出码 0。
- 浏览器登录：本地测试账户成功；不输出密码。
- 浏览器 New Run：`PLAN_EXECUTE` 入口、公司范围和只读提示可见；唯一验收 Run 已创建。
- 浏览器 Runtime unavailable：安全错误文案和 correlation 可见；没有 secret、原始异常或虚假完成状态。

### 5. 风险预估、修复与剩余门禁

- P3 Runtime unavailable 可观测性：已修复并完成浏览器复测。
- P1 真实进程 Runtime 的 interrupt/resume、kill/restart、20 并发恢复和 completed invocation 不重放：已有单元/Bench 证据，但本地 Runtime 未接入浏览器，仍未关闭阶段硬门禁。
- n8n 镜像 import/execute/audit 仍为 `LAB_ONLY` 外部对照实验，按用户备注不阻塞业务代码推进，但不写成通过。
- 独立对抗审查仍安排在最终 diff、全量证据和可完成的浏览器证据之后。

### 6. 本轮任务大白话讲解

Runtime 暂时不可用时，页面不能装作工作流已经结束，也不能把内部堆栈、地址或密钥弹给用户。现在页面只告诉用户“当前不可用”、给一个可以和日志对应的关联标识，并保留 Run 的分析中状态，方便安全重试或人工处理。

## 2026-08-27 14:02 · Phase 5 · 第 14 轮 · 浏览器验收发现并修复 workflow/capability 到期混淆

### 1. 本轮任务列表

- 使用项目提供的本地测试账户完成 Frappe Sign In；未输出或记录密码。
- 浏览器 Runs 页面发现 `PLAN_EXECUTE` Run 被显示为“已过期”，根因是展示层把单次 5 分钟 capability TTL 错当成了 24 小时 workflow deadline。
- 修复 `_run_summary`：`PLAN_EXECUTE` 只由 `workflow_expires_at` 决定工作流到期；缺失 deadline 也 fail closed，其他模式继续按 capability TTL 归一化。
- 增加 Frappe 回归用例，证明 capability 已过期但 workflow deadline 仍有效时，Run 保持 `CREATED` 且不显示 `expired`。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；阶段内仅记录业务开发日志，不创建学习笔记，也不自动记录普通对话。

### 3. 本轮任务完成度

- workflow deadline 与短期 capability TTL 的生命周期边界已在 API 摘要层和测试层固定。
- 未改变 Frappe 的 Run/权限权威边界，也未新增任何 ERP 写工具或业务事实。
- 修复尚未与最终阶段出口文档混合提交；按小步规则单独形成代码/回归测试/本轮日志提交。

### 4. 测试方式与实际结果

- `UV_CACHE_DIR=/tmp/synora-uv-cache make format-check`：退出码 0，146 个文件已格式化。
- `UV_CACHE_DIR=/tmp/synora-uv-cache make lint`：退出码 0。
- `UV_CACHE_DIR=/tmp/synora-uv-cache make type`：退出码 0，mypy 60 个源码文件无问题。
- `UV_CACHE_DIR=/tmp/synora-uv-cache make unit`：退出码 0，225 passed，保留 2 个既有 FastAPI `on_event` 弃用警告。
- `python3 -m compileall -q synora_agentic_erp labs tests services/agent_runtime/src`：退出码 0。
- `bash env/dev/scripts/dev/env.sh app-test`：退出码 0，真实 Bench migration 后 `Ran 83 tests ... OK`，包含本轮到期语义回归。

### 5. 风险预估、修复与剩余门禁

- P1 workflow/capability 到期混淆：已修复并由 83 个真实 Frappe 测试覆盖；仍需刷新页面完成浏览器可见状态复核。
- n8n 仍是 `LAB_ONLY` 对照实验，镜像 import/execute/audit 受外部网络阻塞；按用户确认不作为业务代码推进阻塞，但不把未运行证据写成通过。
- 独立对抗审查安排在最终 diff、全量证据和浏览器验收完成后执行；本轮不以 Execute 自评替代独立结论。

### 6. 本轮任务大白话讲解

一次工作流片段用完 capability，不等于整个 24 小时工作流过期。前者只是这一次请求的短钥匙，后者才是长任务的业务有效期；页面必须把两件事分开显示，否则用户会看到“明明还能恢复却已过期”的错误状态。

## 2026-08-27 13:41 · Phase 5 · 第 13 轮 · 取消竞态门禁与阶段出口证据

### 1. 本轮任务列表

- 复核 P5.3/P5.4 的取消、过期、capability 和 invocation 去重边界，发现 Gateway 在首次解析 capability 后只重检业务权限/范围，未再次确认 Run 生命周期与 `state_version`。
- 在 `recheck_run_scope` 增加当前 Run 行锁、ACTIVE/撤销/版本/5 分钟有效期/24 小时 workflow deadline 重检；取消或过期已提交后，新的只读 Handler 调用在 Gateway 边界 fail closed。
- 增加真实 Frappe 回归用例，模拟 capability 已解析后取消已提交的竞态，确认请求返回 `CONFLICT` 且不开始工具分发。
- 按用户要求保持小步提交：本轮安全修复单独提交为 `c8a941a fix(phase5): recheck run before gateway dispatch`；最终文档、日志、ADR 和 Adoption Card 另作一笔出口提交，不混入代码修复。

### 2. 用户 Assignment 与导师引导

本轮及 Phase 5 全程无 Assignment。用户已明确授权 Agent 接管代码编写；阶段内只记录业务开发日志，不创建学习笔记，也不自动记录普通对话。阶段出口仅标记“答疑待用户明确触发”。

### 3. 本轮任务完成度

- P5.1 typed plan/state、P5.2 引擎/低代码对照工件、P5.3 checkpoint/interrupt/resume/crash-recovery、P5.4 并发/取消/过期/内部认证/不重放、P5.5 Runs 可视化与取舍材料的实现增量均已落库。
- 本轮可见资源：耗时与 Token 宿主不可见；子 Agent 数为 `0`；本轮安全修复提交改动 1 个业务安全模块（28 行）和 1 个 Frappe 回归测试（25 行），出口文档与日志改动待本轮文档提交。
- `DETERMINISTIC` 仍为默认，`AGENT` 保留，`PLAN_EXECUTE` 显式且只读；没有 ERP 写工具、ProposedAction、Approval、Draft 或 Receipt。
- Frappe 仍是 Run、权限、24 小时 deadline、取消/过期和 capability 的权威；Runtime SQLite 仅保存非权威 checkpoint，Frappe invocation ledger 负责完成结果去重。
- 文档出口仍为 `BLOCKED / P5.5`，不得进入 Phase 6；n8n 可运行审计、登录态浏览器验收、独立对抗审查和 Harness managed drift proposal 仍缺证据。

### 4. 测试方式与实际结果

- `UV_CACHE_DIR=/tmp/synora-uv-cache make format-check`：退出码 0，146 个文件已格式化。
- `UV_CACHE_DIR=/tmp/synora-uv-cache make lint`：退出码 0，Ruff 全部通过。
- `UV_CACHE_DIR=/tmp/synora-uv-cache make type`：退出码 0，mypy 60 个源码文件无问题。
- `UV_CACHE_DIR=/tmp/synora-uv-cache make unit`：退出码 0，225 passed；保留 2 个既有 FastAPI `on_event` 弃用警告。
- `bash env/dev/scripts/dev/env.sh app-test`（允许访问本地 Docker daemon）：退出码 0，真实 Bench migration 后 `Ran 82 tests ... OK`，包含本轮取消竞态回归。
- `python3 -m compileall -q synora_agentic_erp labs tests services/agent_runtime/src`：退出码 0。
- `python3 .agents/skills/harness-build/scripts/validate_harness_structure.py .`：退出码 0，manifest 有效，引用检查 324/324，broken 0。
- Runtime workflow/contract/engine 定向测试：最近一轮 `24 passed, 2 warnings`；P5-G01～G10 的固定输入对照中 Fixed/ReAct/Plan-and-Execute 均 `PASS/SUCCEEDED`，每种完成 2 个只读步骤、2 次工具调用、观察 digest 一致、Trace complete。
- `node --check` 已通过 `new_run.js` 与 `runs.js`；n8n JSON `python3 -m json.tool`、labs Ruff check/format check 已通过。
- Harness `validate_manifest.py` 与 `check_references.py` 均通过；`detect_drift.py` 仍报告历史 managed/source fingerprint drift，`score_harness_health.py` 为 71.0/C。以上脚本均只读，未手工改写 `.harness/`。
- Secret 深层扫描只命中代码中的预期字段名和 n8n `N8N_SECURE_COOKIE` 配置名，未发现 secret 值；checkpoint 单元断言 capability/token/cookie/Authorization/密码/Prompt 不入 payload。
- `ponytail-review`/`ponytail-audit` 只读结论：无必须阻断的过度设计；可选瘦身为引擎比较 runner 的未使用 `label` 类属性和既有测试中的恒等断言，约 5 行，未在出口阶段扩大范围。`ponytail-debt` 排除 skill 自身目录后仅有 1 个已有 `ponytail:` 延后标记。

### 5. 未运行或未通过的外部验收

- 浏览器验收未运行：in-app Browser 只有 Frappe Sign In 页面，没有可用登录态；未读取或输入任何用户秘密。
- n8n import/execute/audit 未运行：固定镜像拉取受外部网络/代理阻塞；只保留不含凭证的 `LAB_ONLY` JSON 和复跑说明，不能把导出文件当成运行证据。
- 独立对抗审查未运行：当前上下文没有可独立运行的 Test/Review 角色，不能把 Execute 自评写成独立 `PASS`。
- Harness 文件级 proposal 未提交：`harness-update` 识别出漂移，但该管理动作需要单独授权；遵守边界不手工修改 `.harness/`。

### 6. 风险预估、修复与剩余门禁

- P1 取消后新工具调用：本轮已用行锁、版本 CAS 和每次 Gateway 重检修复；82 个真实 Frappe 测试通过，仍需真实 Runtime 进程取消竞态和独立审查证据关闭阶段门禁。
- P1 crash 后重复调用、并发 resume、capability 泄漏、cancel/expiry 顺序：代码和定向/集成测试已有保护；真实子进程 kill/restart、20 并发 resume、深层外部审查证据仍未完整形成。
- P2 SQLite 单实例扩展、n8n 安全审计、依赖/Harness 漂移：ADR-0006 已记录 owner/复验门禁，当前不声称生产可扩展或 n8n 通过。
- P3 工作流计划与采购结果计划混淆：前端分区、术语和无写入说明已实现，浏览器登录态验收待补。
- 当前无已知 P0；因外部证据缺失，不能把 P1/P2 风险登记写成阶段已关闭。

### 7. Rubric 自评（Execute，非独立审查）

| 维度 | 分数/10 | 证据与限制 |
| --- | ---: | --- |
| 业务价值 | 8 | 可暂停/恢复的只读 workflow 与 Runs 摘要已实现；浏览器真实路径未验收 |
| 正确性 | 8 | 225 个 unit + 82 个真实 Frappe 测试；真实 crash/restart 尚未运行 |
| 安全性 | 8 | token/capability、权限、只读注册表和取消重检有测试；独立审查和外部 UI 缺失 |
| 恢复与幂等 | 7 | SQLite CAS/lease、STARTED 不重放和 manual recovery 已实现；进程 kill/restart 未取证 |
| 可观测性 | 7 | Trace 类型、落库和 UI 摘要已实现；浏览器和长期运维证据缺失 |
| 可维护性 | 7 | format/lint/type 全通过；ponytail 有约 5 行可选瘦身项 |
| 互操作性 | 5 | Frappe 真实通过，LangGraph 仅 Spike/适配器，n8n 未运行 |
| 证据与复现 | 6 | 命令、原始对照和集成证据齐全；浏览器/n8n/独立/Harness proposal 缺失 |
| 采用与运维 | 4 | 手写引擎保留主线、SQLite 仅单实例；生产存储与扩展未决 |

合计 `60/90`（Execute 自评）。硬门禁未满足，不据此宣称 Phase 5 `PASS`。

### 8. 下轮建议与阶段停止条件

- 下轮仅在获得外部环境/授权后补齐 n8n import/execute/audit、登录态浏览器、Harness 文件级 proposal，并安排独立对抗审查；随后重跑受影响测试和阶段全量命令。
- 独立对抗审查：`0` 轮；原始结论 `BLOCKED / 未运行`；最终 `PASS` 尚未产生，Execute 自评不替代独立结论。
- 本轮完成代码、测试、文档和取舍材料后停止在 `BLOCKED / P5.5`；不触发 Phase 6。只有上述证据全部补齐且独立审查为 `PASS`，才可更新阶段出口。

### 9. 本轮任务大白话讲解

之前系统拿到一次有效 capability 后，如果用户刚好在另一条请求里取消，极短窗口里还可能继续把旧上下文送进工具。现在每次真正读 ERP 前都会锁住 Run 行，再确认它仍有效、没被取消、版本没变化；取消已经提交后，新的读取会被挡住。Runtime 的工作流位置仍只是“下一步做到哪”，不能替 Frappe 说“用户授权还有效”或替 ERP 说“事实已经改变”。

### 10. 面试官可能追问的技术细节以及如何回答（待练习）

- 为什么 capability 校验一次还不够？（待练习）答题提示：解析和 Handler 分发跨事务，取消/过期可能在中间提交；每次分发前还要做 Run/version/lifecycle 重检。
- 为什么要用 `STARTED` 账本而不是失败后直接重试？（待练习）答题提示：ERP 已完成但 checkpoint 未保存时结果不确定，自动重放会造成重复事实或审计歧义。
- 为什么 checkpoint 不能当作 ERP 事实？（待练习）答题提示：checkpoint 只描述编排进度，用户权限、Run 状态和业务数据必须由 Frappe/ERP 当前查询确认。
- 为什么 LangGraph 这轮没有进入主线？（待练习）答题提示：Spike 可运行不等于同任务安全、恢复、Trace、维护和运维证据有支配优势；先保留透明手写基线。
- 为什么 n8n 只能是 `LAB_ONLY`？（待练习）答题提示：实验只允许 recorded Gateway 和受限节点，未完成镜像、import、execute、audit 就不能声称低代码安全或可运行。
- 为什么阶段状态是 `BLOCKED` 而不是“代码已完成所以 PASS”？（待练习）答题提示：阶段出口需要外部验收与独立证据；缺证据必须停止，不能用 mock、导出或自评替代。

## 2026-08-27 13:52 · Phase 5 · 第 12 轮 · Python 3 异常语法修复

### 1. 本轮任务列表

- 运行阶段 `compileall`，发现 `agent/invocation.py` 和 `gateway/security.py` 的多异常 `except A, B` 写法在 Python 3.13/3.14 下无法解析。
- 把调用账本竞态处理改为保留原语义的显式异常类型判断，把 deadline 解析改为两个单独的 `except` 分支。
- 重新运行全仓静态编译和受影响文件的 Ruff check/format check，确保 Frappe 代码真正可编译。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；不写学习笔记。

### 3. 本轮任务完成度

- 两个语法错误已修复；这是阶段门禁发现的独立代码缺陷，未修改前端、实验材料或阶段文档。
- 通过保留异常过滤语义，未改变 invocation 竞态回读或过期 fail-closed 行为；准备单独修复提交。

### 4. 测试方式

- 首次 `python3 -m compileall -q synora_agentic_erp labs tests services/agent_runtime/src`。
- 修复后重新运行同一 compileall 命令。
- 针对 `synora_agentic_erp/agent/invocation.py`、`synora_agentic_erp/gateway/security.py` 运行 Ruff check/format check。

### 5. 测试结果

- 首次 compileall：退出码 1，报告两个文件各一处未加括号的多异常 `except` 语法错误。
- 修复后 compileall：通过，退出码 0。
- 修复后 Ruff check/format check：通过，退出码 0；后续仍需真实 Frappe integration 复验。

### 6. 用户如何进行手工测试

1. 并发创建相同 invocation，确认获胜请求返回/落账，竞态请求回读同一行而不重复进入 Handler。
2. 读取非法 workflow deadline，确认安全地判定为过期，不因异常语法路径而放行。

### 7. 风险预估与规避

- 语法修复采用显式单异常分支/类型判断，避免依赖 formatter 对异常元组的错误重写，同时保留未知异常继续上抛。
- 在继续阶段文档提交前必须重跑真实 Frappe app-test，因为这两个模块依赖 Frappe runtime，单纯 compileall 不足以证明行为。
- 该问题暴露出此前 Ruff/mypy 解析覆盖不足，后续把 compileall 作为阶段出口独立门禁，不再只看 lint/type。

### 8. 下轮建议

- 单独提交两个 Python 3 语法修复和日志，随后重跑完整 Frappe integration。
- 全量静态/业务命令通过后，再提交最终文档出口；若出现新问题继续保持一问题一提交。

### 9. 本轮任务大白话讲解

代码之前写了一种 Python 2 风格的“同时捕获两个错误”，在 Python 3 根本无法启动。现在改成 Python 3 明确支持的写法：账本竞态只处理预期的重复键错误，其他真正异常继续暴露；日期解析的两类错误分别安全地按过期处理。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么 compileall 能发现 lint 没发现的问题？答：阶段门禁使用不同工具链；Ruff/mypy 的路径/解析覆盖可能与 Python 编译器不同，最终交付必须让目标 Python 实际编译全部源码。
- 为什么未知异常不能一起吞掉？答：调用账本和权限边界出错时应 fail closed 并保留证据，宽泛吞异常会把系统错误伪装成竞态成功。
- 过期解析为什么分别捕获 TypeError 和 ValueError？答：这两类都是时间输入不合法的可预期失败，显式分支更符合 Python 3 语法和静态检查，任何其他异常仍然不会被隐藏。

## 2026-08-27 13:45 · Phase 5 · 第 11 轮 · Frappe workflow 类型门禁修复

### 1. 本轮任务列表

- 运行全量 `make type`，确认 `synora_agentic_erp/agent/service.py` 有两个 mypy 错误：异常变量 `error` 离开 except 作用域后复用，以及事件循环变量 `step_id` 与前文的 `str` 类型冲突。
- 只重命名参数校验的序列化异常、步骤错误和 Trace 事件步骤变量，保持 workflow response 校验与 Trace 落库语义不变。
- 复跑该模块的 Ruff 和 mypy，确认局部修复有效。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；不写学习笔记。

### 3. 本轮任务完成度

- 两个类型门禁问题已修复，未修改前端、实验材料或阶段权威文档；文档出口改动仍保持未暂存。
- 本轮只包含局部变量重命名和本日志，准备独立修复提交。

### 4. 测试方式

- 首次 `UV_CACHE_DIR=/tmp/synora-uv-cache make type`。
- 修复后针对 `synora_agentic_erp/agent/service.py` 运行 Ruff check/format check 和 mypy。
- 提交前运行 `git diff --check` 与 staged 范围审查。

### 5. 测试结果

- 首次 `make type`：退出码 2，`service.py:479` 报异常变量复用，`service.py:769` 报 `str | None` 与 `str` 赋值冲突。
- 修复后 Ruff check/format check：通过，退出码 0。
- 修复后 `mypy synora_agentic_erp/agent/service.py`：`Success: no issues found in 1 source file`，退出码 0。

### 6. 用户如何进行手工测试

1. 提交一份合法和一份非法 Runtime workflow response，确认参数校验仍返回相同的安全结果。
2. 读取一次 workflow Trace，确认每个事件的 `step_id` 仍对应正确步骤，且不会因为局部变量名称冲突而丢失。

### 7. 风险预估与规避

- 只改变局部变量名，不改变校验分支、事件类型或持久化字段，避免状态机行为回归。
- 通过 mypy 直接检查受影响文件，先关闭确定的类型缺陷，再继续阶段宽检查。
- 其他全量命令尚未因本轮修复重新执行，不能提前写成通过。

### 8. 下轮建议

- 单独提交该类型修复后，重新执行全量 type/unit/integration 和 compileall/Harness 检查。
- 若全量命令继续发现问题，继续按一个问题一个提交处理；全部门禁完成后再提交文档出口。

### 9. 本轮任务大白话讲解

这次不是业务规则错了，而是代码里两个不同含义的变量用了同一个名字，静态检查无法确认它们是不是同一种东西。把它们分别命名后，检查器能看懂“步骤错误”和“Trace 当前步骤”是两件事，运行逻辑不变。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么类型检查也要在阶段出口前修？答：类型歧义可能掩盖真实的 `None` 状态，尤其是 Trace/状态机路径；先清零静态错误才能相信后续运行证据。
- 为什么不直接关闭 mypy 规则？答：关闭规则会放宽安全边界，不能证明 response 校验和事件落库的类型完整性；局部重命名更小且可审查。
- `event_step_id` 为什么允许为空？答：终态事件可能没有当前步骤，schema 和 DocType 都把它作为可选值；事件生成时仍只引用经过校验的步骤或空值。

## 2026-08-27 13:38 · Phase 5 · 第 10 轮 · 崩溃恢复文案 lint 修复

### 1. 本轮任务列表

- 继续阶段全量门禁，定位 `workflow/runtime.py` 崩溃恢复 clarification 文案的 Ruff `RUF001` 全角标点和 `E501` 超长问题。
- 仅拆分该用户可见文案并将分号改为 ASCII，保持 `inspect` 选择和“新 plan version 才可重试”的安全语义不变。
- 复跑 Runtime workflow API/contract 相邻测试，确认中断、恢复和错误映射没有回归。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；不写学习笔记。

### 3. 本轮任务完成度

- lint 缺陷已修复，未修改前端、实验材料或阶段权威文档；文档出口改动仍保持未暂存。
- 本轮只包含一条文案格式修复和本日志，准备独立小提交。

### 4. 测试方式

- 首次 `UV_CACHE_DIR=/tmp/synora-uv-cache make lint`。
- 修复后针对 `workflow/runtime.py` 运行 Ruff check/format check。
- `pytest services/agent_runtime/tests/test_workflow_contracts.py services/agent_runtime/tests/test_workflow_api.py -q`。

### 5. 测试结果

- 首次 `make lint`：退出码 2，3 个问题均集中在崩溃恢复文案一行（RUF001 两项、E501 一项）。
- 修复后 Ruff check 与 format check：通过，退出码 0。
- Runtime targeted pytest：`21 passed, 2 warnings`，退出码 0；警告仍为既有 FastAPI `on_event` 弃用提示。

### 6. 用户如何进行手工测试

1. 让 Runtime 在只读工具步骤中退出，再启动 Runtime；确认工作流显示“结果不确定”，可选项只有 `inspect`。
2. 选择 `inspect` 后确认步骤不会被自动标记成功；若要重试，必须创建新计划版本和新 invocation。

### 7. 风险预估与规避

- 只改显示文案的换行/标点，不改 crash-recovery 状态转换，避免误把格式修复变成恢复策略变化。
- 用相邻 contract/API 测试覆盖 interruption、revision 和错误响应，确保安全提示与机器状态一致。
- FastAPI 弃用项继续单列，避免在阶段出口混入未经评估的生命周期重构。

### 8. 下轮建议

- 重新执行全量 lint/type/unit/integration 和静态编译/Harness 检查。
- 完成该单独修复提交后，再将最终架构、规范、PLAN、ADR 和 Adoption Card 作为一个文档出口小提交。

### 9. 本轮任务大白话讲解

系统崩溃后最危险的是“不知道工具到底有没有完成”。这轮只是把提示文字排版成统一规则，同时保留严格答案：只能先检查，不能把不确定结果冒充成功，更不能自动再读一次 ERP。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么崩溃恢复不能直接重放？答：外部只读调用的完成与 checkpoint 保存可能不在同一事务，自动重放会产生重复读取或审计歧义；先人工检查，再用新计划版本重试。
- 为什么把文案 lint 当作独立修复？答：全量门禁发现的是可复现、局部、无业务语义变化的问题，独立提交让审查和回滚边界清晰。
- `inspect` 为什么是唯一选择？答：它只能确认状态，不向 Runtime 授予工具参数或额外权限，符合 fail-closed 原则。

## 2026-08-27 13:34 · Phase 5 · 第 9 轮 · 阶段格式门禁修复

### 1. 本轮任务列表

- 运行阶段出口首个全量门禁，确认上一轮提交的 `workflow/engine.py` 有一处 Ruff 多行条件格式漂移。
- 仅按 Ruff 建议合并该条件表达式，保持执行语义、状态转换和依赖选择逻辑不变。
- 用相邻 Runtime 工作流 contract/API/engine 测试复验格式修复没有改变行为。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；不写学习笔记。

### 3. 本轮任务完成度

- 格式问题已修复，未修改前端、实验材料或阶段权威文档；后续文档仍保持未暂存。
- 本轮只包含一个机械格式修复和本日志，准备独立小提交。

### 4. 测试方式

- 首次 `UV_CACHE_DIR=/tmp/synora-uv-cache make format-check`。
- 修复后针对 `engine.py` 运行 Ruff check/format check。
- `pytest services/agent_runtime/tests/test_workflow_contracts.py services/agent_runtime/tests/test_workflow_api.py services/agent_runtime/tests/test_workflow_engines.py -q`。

### 5. 测试结果

- 首次 `make format-check`：退出码 2，唯一问题是 `engine.py:134` 条件换行格式。
- 修复后 Ruff check 与 format check：通过，退出码 0。
- Runtime 相邻 targeted pytest：`24 passed, 2 warnings`，退出码 0；警告是 FastAPI `on_event` 弃用提示，与本轮无关。

### 6. 用户如何进行手工测试

1. 查看 `engine.py` 的 ready-step 选择，确认只把依赖闭包已完成且状态为 PENDING/READY 的步骤选为下一步。
2. 运行一条包含两个依赖步骤的工作流，确认顺序、revision 和最终状态与格式修复前一致。

### 7. 风险预估与规避

- 只接受 Ruff 的机械格式变更，不借机重构状态机，降低行为回归风险。
- 通过 contract/API/engine 三组相邻测试确认 DAG、恢复和 Runtime 路由仍保持原有约束。
- 将 FastAPI 弃用提示记录为后续维护项，不把无关警告误判为本轮失败。

### 8. 下轮建议

- 重新执行阶段全量 format/lint/type/unit/integration 和 compileall/Harness 命令。
- 完成只包含 `engine.py` 与本轮日志的提交后，再处理最终文档出口提交。

### 9. 本轮任务大白话讲解

阶段总检查像出厂检验，先发现代码排版不符合统一规则。这个修复只把条件写成 Ruff 要求的格式，没有改变工作流怎么挑下一步；再用三组相邻测试确认“该做哪一步、能否恢复、API 是否安全”都没有被动到。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么格式问题也要单独提交？答：它是阶段门禁发现的独立缺陷，单独提交便于回滚和审查，不把无关修复混进文档或功能提交。
- ready-step 依赖闭包为什么重要？答：只有依赖已成功完成的步骤才可运行，稳定顺序能避免 DAG 中跨分支或悬空步骤产生不确定行为。
- 弃用警告为什么不在本轮顺手修？答：它不是本轮业务失败；扩大范围会混入生命周期重构，应单独安排并重新评估风险。

## 2026-08-27 13:27 · Phase 5 · 第 8 轮 · 同任务引擎对照与 n8n LAB_ONLY 工件

### 1. 本轮任务列表

- 用同一 `P5-G01-dependency-order` 输入和同一组 recorded Observation 运行 Fixed Workflow、ReAct 子图和手写 Plan-and-Execute。
- 输出受限比较数据：最终状态、完成步骤、工具次数、观察 digest、revision、恢复标记和 Trace 完整性；不保存 ERP 原始响应、Prompt、capability 或凭证。
- 添加 n8n `LAB_ONLY` 无凭证 workflow JSON 和复跑说明，节点只包含 Manual Trigger、Set、If 与 loopback recorded Gateway 的 HTTP Request。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；不写学习笔记。

### 3. 本轮任务完成度

- 三种本地代码模式在相同输入下均成功完成两个依赖步骤，形成可审查的原始 JSON 输出。
- n8n 导出文件可解析且边界说明完整；由于镜像拉取受网络/代理阻塞，没有把 n8n 标成已运行或已审计。
- 本轮只提交 `labs/workflow_comparison/` 与本日志，不混入前端或权威架构文档。

### 4. 测试方式

- `PYTHONPATH=services/agent_runtime/src UV_CACHE_DIR=/tmp/synora-uv-cache uv run --offline --no-sync --python 3.14 python -c '...run_recorded_comparison...'`。
- `python3 -m json.tool labs/workflow_comparison/n8n/n8n-workflow.json`。
- `uv run ... ruff check labs/workflow_comparison` 与 `ruff format --check labs/workflow_comparison`。
- `git diff --check` 和提交范围审查。

### 5. 测试结果

- comparison：Fixed/ReAct/Plan-and-Execute 均 `PASS/SUCCEEDED`，完成 `material-request -> stock`，每种 2 次只读工具调用，revision `5`，Trace complete，观察 digest 一致。
- n8n JSON 解析成功；Ruff check/format check 均通过。
- `docker pull`、n8n import/execute/audit 未通过：本地没有固定镜像且网络代理无法完成拉取；该限制已写入 n8n README 和后续出口风险。

### 6. 用户如何进行手工测试

1. 按 README 命令运行 comparison，比较三行的状态、步骤顺序、调用次数和 digest 是否一致。
2. 在隔离 n8n 实例中只导入 `n8n-workflow.json`，将 HTTP 节点保持为 loopback recorded Gateway，再运行同一固定输入。
3. 导入和执行后运行官方 security audit；若出现 credential、危险节点或非 loopback URL，删除能力并重新导入，不能以 LAB_ONLY 放行。

### 7. 风险预估与规避

- 对照程序只消费 recorded Observation，不能替代真实 ERP integration，也不宣称框架性能或生产可用性。
- n8n workflow 不带 credential、写节点、任意外网或 Execute Command；镜像 digest 固定，实际拉取失败时保持 `UNAVAILABLE`。
- LangGraph 和 n8n 的未运行行不填成功数据，Adoption Card 必须保留证据缺口。

### 8. 下轮建议

- 只提交 ADR/Adoption Card 与 Architecture/SPEC/PLAN 的阶段边界和阻塞状态，并在同一轮补齐 Rubric、风险和独立审查结论。
- 不进入 Phase 6；先按出口门禁处理浏览器、n8n、Harness 和独立审查阻塞。

### 9. 本轮任务大白话讲解

为了公平比较，三种代码模式拿到完全一样的任务和“已经记录好的观察结果”，所以它们都走出同样的两步、调用两次工具。n8n 只是一张受限的实验流程图，当前机器没能拉到固定镜像，就只留下可审查的导出和失败边界，不把实验文件冒充运行结果。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么比较时要固定 Observation？答：如果每个引擎各自访问 ERP，输入事实会漂移，比较结果无法归因到执行模型。
- 为什么 n8n 不进入主线？答：它能表达低代码流程，但当前没有真实安全审计和恢复证据；Phase 5 只把它作为 LAB_ONLY 取舍证据。
- 为什么 digest 可以展示而不能展示原始响应？答：digest 能证明观察绑定和重复性，原始 ERP 响应可能含敏感字段，Trace/UI 只显示受限摘要。

## 2026-08-27 13:24 · Phase 5 · 第 7 轮 · Runs 工作流可视化

### 1. 本轮任务列表

- 在 New Run 中增加显式 `PLAN_EXECUTE` 选项，保留 `DETERMINISTIC` 默认，并标明该模式是条件式、持久、只读工作流。
- 在 Runs 详情按需加载工作流 checkpoint，展示工作流状态、plan/revision/graph 版本、24 小时 deadline、步骤顺序/依赖/状态、观察 digest/完成时间、重规划原因、停止原因和崩溃恢复标记。
- 增加 clarification 输入与恢复按钮，处理 Runtime unavailable、checkpoint 不兼容、权限拒绝、旧 revision 冲突等安全文案；继续把用户内容全部 HTML escape。
- 将工作流计划与现有采购结果计划、Agent Trace 分区呈现，不增加任何 ERP 写操作入口。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；不写学习笔记。

### 3. 本轮任务完成度

- New Run/Runs 两个页面已接通 PLAN_EXECUTE 的展示和恢复请求；服务端仍是权限、revision 和 capability 的权威校验方。
- 只改页面脚本与本 Phase 日志，未混入 comparison、n8n 或架构出口文档。

### 4. 测试方式

- `node --check synora_agentic_erp/synora_agentic_erp/page/new_run/new_run.js`。
- `node --check synora_agentic_erp/synora_agentic_erp/page/runs/runs.js`。
- `git diff --check` 和提交范围审查。

### 5. 测试结果

- 两个页面 `node --check` 均通过，退出码 0。
- 浏览器真实验收未运行：当前 in-app Browser 只有 Frappe Sign In 页面，没有可使用的测试登录会话；未读取或输入任何凭证。
- 前端代码未把 Runtime 返回错误伪造成成功；后端跨用户权限测试已在上一轮真实 app-test 通过。

### 6. 用户如何进行手工测试

1. Buyer 打开 New Run，确认 `PLAN_EXECUTE` 可选且 `DETERMINISTIC` 仍为默认；创建后进入 Runs。
2. 打开详情，确认工作流区块与采购结论区块分开；等待 clarification 时输入答案并点击“提交并恢复”。
3. 关闭并重新打开详情，检查步骤、revision、deadline 和中断原因仍存在；用旧 revision 再提交应看到冲突反馈。
4. 取消或让工作流过期后刷新页面，确认终态和安全原因显示，且没有“创建 MR/PO”的按钮或声明。

### 7. 风险预估与规避

- 所有动态目标、步骤、错误和 digest 在插入 HTML 前 escape；恢复请求携带服务端返回的 revision/interrupt id，不能由页面自行扩权。
- 页面只按需读取 `get_run_workflow`，Runtime 不可用时显示明确失败，不猜测 checkpoint 已完成。
- `PLAN_EXECUTE` 只增加只读入口；取消按钮沿用唯一 Frappe 取消 API，避免 UI 绕过权威状态机。

### 8. 下轮建议

- 单独提交固定输入的 workflow comparison 与 n8n `LAB_ONLY` 工件，并明确 n8n 尚未完成 pull/import/audit 的证据边界。
- 最后再集中提交 ADR、Adoption Card、Architecture/SPEC/PLAN 和阶段出口日志。

### 9. 本轮任务大白话讲解

页面现在能把一条长任务的“路线图”显示出来：每一步做什么、依赖什么、做到哪、为什么停。需要用户补充信息时，答案只会送回当前版本的只读工作流；如果页面过期或有人抢先恢复，页面会告诉用户状态冲突，而不是重复调用 ERP。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么 workflow panel 要按需加载？答：列表只展示轻量 Run 生命周期；详细 checkpoint 可能更大且需要额外权限，按需读取减少暴露面。
- 前端为什么不能自己判断“恢复成功”？答：Frappe 的 revision、权限、deadline 和 capability 是权威；浏览器只能提交不可信答案并展示已验证响应。
- 如何防止工作流计划和采购结果计划混淆？答：页面使用独立区块和术语，前者是 Runtime checkpoint，后者是 Frappe 确定性收口事实，二者不共享状态语义。

## 2026-08-27 13:20 · Phase 5 · 第 6 轮 · Frappe 工作流边界测试落库

### 1. 本轮任务列表

- 把此前未追踪的 `synora_agentic_erp/tests/test_workflow_run.py` 纳入版本控制，覆盖 PLAN_EXECUTE 的 24 小时 workflow deadline 与 5 分钟 capability 分离。
- 覆盖真实 Frappe site 上的过期、取消、clarification 中断/恢复、revision 绑定、跨用户隔离、调用账本完成缓存、STARTED 不重放和参数 digest 冲突。
- 以真实 Bench migration/app-test 重新验证新增 DocType 与公共 API，而不是只依赖 Runtime mock。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5；不写学习笔记。

### 3. 本轮任务完成度

- 后端 Frappe 边界测试已形成可追踪文件并覆盖 8 个关键场景；没有增加 ERP 写工具或凭证路径。
- 本轮只提交测试证据与本日志，不混入前端、实验材料或出口文档。

### 4. 测试方式

- `UV_CACHE_DIR=/tmp/synora-uv-cache make integration`：迁移真实 `dev.localhost` 后运行 Synora 全部 Frappe app-test。
- 提交前检查待提交范围和 `git diff --check`。

### 5. 测试结果

- `make integration`：`Ran 81 tests in 1.354s`，`OK`，退出码 0。
- 真实测试包含 `TestWorkflowRun` 的 8 项新增用例；迁移、权限和账本 DocType 均加载成功。
- 浏览器、n8n 和独立对抗审查不属于本轮，尚未写成通过。

### 6. 用户如何进行手工测试

1. 以 Buyer 创建 `PLAN_EXECUTE` Run，确认响应没有 workflow capability，但保存了 24 小时到期时间。
2. 令 Runtime 返回 clarification，重复提交旧 revision；第一次恢复成功，旧 revision 只能得到冲突。
3. 以另一个用户读取同一 Run，确认返回拒绝且不泄露计划；对同一 invocation 重试，确认账本缓存结果而不再次进入 Handler。

### 7. 风险预估与规避

- 过期/取消先由 Frappe 权威 CAS，再撤销 capability，避免 Runtime 或 checkpoint 重新赋权。
- `STARTED` 无结果被视为不确定窗口，测试明确要求冲突而不是自动重放。
- 测试 fixture 使用固定只读商品与 Buyer，避免把测试 double 误当作生产工具；生产工具注册表仍由独立架构测试约束。

### 8. 下轮建议

- 单独提交 New Run/Runs 页面可视化，不与本轮测试混合。
- 随后再提交同任务 comparison/n8n LAB_ONLY 工件和最终文档状态。

### 9. 本轮任务大白话讲解

这轮把“工作流不会偷偷越权或重复读 ERP”的关键规则放到了真实 Frappe 环境里验证。页面创建长任务时只拿到普通 Run 信息；真正短期授权只在一次调用里存在。遇到过期、取消、旧答案或上次调用还没落结果，系统宁可停下来，也不会猜着再调用 ERP。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么要在真实 Bench app-test 中测？答：Runtime 单测看不到 Frappe DocType 的字段校验、权限和 migration；真实 site 才能证明持久化边界。
- 为什么 `STARTED` 不能自动重试？答：Gateway 可能已经完成而 checkpoint 尚未保存，状态不确定；自动重试可能重复副作用，必须让人工检查或新 plan version 形成新 invocation。
- 为什么 capability 不出现在 issue 响应？答：workflow deadline 与单次 5 分钟 capability 是不同权威，公共 API 不应把可调用秘密暴露给浏览器。

## 2026-08-27 13:17 · Phase 5 · 第 5 轮 · P5.4 并发恢复与只读工具架构测试

### 1. 本轮任务列表

- 增加 20 个并发 resume 竞态测试，证明 revision/lease 只有一个获胜者。
- 增加 Runtime 已分类工具失败的 durable stop 测试，确认不自动 replay。
- 用架构/安全测试机械断言生产工具注册表仍只有六类 READ 工具；排除测试用 `contract.*` double。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已授权 Agent 全程接管 Phase 5；不写学习笔记。

### 3. 本轮任务完成度

- P5.4 测试覆盖完成：并发请求 1 成功/19 冲突；工具失败后步骤为 FAILED、replan reason 为 TOOL_ERROR。
- 首次真实 integration 因测试夹具把三个 `contract.*` double 当生产工具而失败；已保留失败证据并将断言限定到生产注册表。

### 4. 测试方式

- Runtime workflow API/contract targeted pytest。
- Frappe `make integration`（含架构安全断言和新 DocType migration）。
- `git diff --check`。

### 5. 测试结果

- 修复前 `make integration`：`80 passed, 1 failed`，失败为测试断言范围错误，不是业务路径失败。
- 修复后 `make integration`：`81 tests OK`。
- Runtime targeted：并发/失败路径通过；阶段出口仍需全量命令。

### 6. 用户如何进行手工测试

1. 创建一个处于 clarification 的 workflow，复制同一 revision/interrupt 请求并并发提交 20 次。
2. 确认只有一次 200，其余都是 409，checkpoint revision 只递增一次。
3. 让只读 Gateway 返回分类失败，确认步骤停在 FAILED，重新调用 start/resume 不会再次进入 Handler。

### 7. 风险预估与规避

- SQLite `BEGIN IMMEDIATE`、expected revision 和 lease 共同防止双恢复；失败者只拿 typed conflict。
- 工具注册表测试避免被测试 double 污染，仍严格检查六类业务只读工具和版本 `1`。
- 该轮未启用任何 ERP 写工具或外部凭证。

### 8. 下轮建议

- 单独提交 Runs 页面与 New Run 模式选择，再提交同任务 comparison/n8n lab 工件。
- 最后集中提交 ADR/Adoption Card、PLAN 阻塞状态和出口日志，跑完整阶段门禁。

### 9. 本轮任务大白话讲解

两个浏览器同时点击“恢复”时，系统必须像抢一把锁：只有一个请求能拿到当前版本，其他请求看到版本已变就停止。工具失败也要记成明确的“这里停了”，不能因为用户再次刷新就偷偷再读 ERP。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么要 20 并发而不是单测一次？答：单线程通过不能证明数据库竞态；并发矩阵直接验证 CAS/lease 的唯一获胜者。
- 测试 double 为什么不能计入生产工具？答：测试为了覆盖错误分支动态注册了 `contract.*`，架构断言必须限定业务工具集合，否则会把测试夹具误报成权限扩大。
- 失败状态为什么还允许未来 replan？答：当前 plan 的失败步骤不可重放；只有显式新 plan version 才能在人工检查后引入新 invocation。

## 2026-08-27 13:13 · Phase 5 · 第 4 轮 · P5.3/P5.4 Frappe 工作流生命周期、Trace 与公共 API

### 1. 本轮任务列表

- 接入 `PLAN_EXECUTE` 的 Frappe Run 生命周期、24 小时 workflow deadline 和 5 分钟 capability 轮换。
- 新增 `resume_run`、`get_run_workflow`，并让 `cancel_run` 先完成 Frappe 权威取消/撤销，再 best-effort 通知 Runtime。
- 新增受保护的 `Synora Workflow Trace`，只保存计划/步骤/摘要 digest/停止原因等脱敏事件。
- 在 Runtime 返回边界做严格版本、DAG、状态、澄清、预算、时间和 UUID 校验。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已授权 Agent 全程接管 Phase 5；不写学习笔记。

### 3. 本轮任务完成度

- Frappe 能识别 `PLAN_EXECUTE`，issue response 不公开 capability；analyze 在 Runtime 中断时保持 `ANALYZING`，完成后才进入现有确定性采购分析收口。
- 取消/过期的顺序固定为 Frappe CAS、撤销 capability、再通知 Runtime；Runtime 不可用时不回滚 Frappe 权威状态。
- 跨用户 workflow status 被隐藏为 `RUN_REJECTED`；非法 Runtime response 不进入 Trace 或业务状态。

### 4. 测试方式

- Frappe workflow integration：issue deadline/capability 分离、expiry、cancel、interrupt/resume、Trace 和跨用户读取。
- 现有全量 Frappe app-test/integration。
- 相关 Python format/lint/type 和 diff 检查。

### 5. 测试结果

- Frappe integration 最近一次：`81 tests OK`。
- Runtime targeted 继续保持通过；详细全量复验留阶段出口。
- n8n pull/import/audit、浏览器新鲜登录验收和独立对抗审查仍未完成，不能写成通过。

### 6. 用户如何进行手工测试

1. Buyer 创建 `PLAN_EXECUTE` Run，确认公开响应没有 capability 且有 24 小时 deadline。
2. 触发分析，确认工作流中断时 Run 仍为 `ANALYZING`，页面可读取脱敏计划和 clarification。
3. 提交当前 revision/interrupt answer，确认 Runtime 恢复并由确定性代码收口为 `PROPOSED`。
4. 取消或过期后刷新页面，确认状态是终态且不再启动工具调用。

### 7. 风险预估与规避

- Runtime checkpoint 不得改变 Frappe Run；所有公共状态变化都经过 Frappe 状态机和 capability 撤销。
- Runtime response 被视为不可信网络输入，严格拒绝未知字段、版本、工具、依赖、状态、UUID、digest 和非有限 JSON。
- Trace 只保存 bounded safe summary，不保存 Prompt、隐藏思维链、capability、Cookie 或原始敏感 ERP 响应。

### 8. 下轮建议

- 单独提交 Runtime 20 并发 resume/工具失败测试和六类只读工具架构断言。
- 再提交 Runs 页面、n8n/对照 lab，并在阶段文档中明确真实证据缺口。

### 9. 本轮任务大白话讲解

Frappe 像总账：它决定谁能看、Run 是否还活着、授权是否过期和是否已取消。Runtime 像进度板：它只记“做到哪一步、正在等什么”，每次需要读 ERP 时再拿短期通行证。这样即使进度板坏了，也不能把它当成 ERP 已经发生的事实。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么中断不新增 Frappe Run 状态？答：`ANALYZING` 仍是生命周期事实，Runtime `INTERRUPTED` 是编排视图，避免双重状态权威。
- 为什么取消必须 Frappe 先提交？答：只有先撤销 capability 并提交，后续 Gateway 才会拒绝新工具；Runtime 通知失败不能复活权限。
- 为什么 Trace 不直接保存 Runtime 原文？答：网络响应不可信且可能含敏感信息，只允许固定字段、digest 和 bounded safe summary。

## 2026-08-27 13:12 · Phase 5 · 第 3 轮 · P5.4 Gateway 调用账本与确定性去重

### 1. 本轮任务列表

- 为 typed Gateway 请求增加可选的 workflow invocation metadata。
- 以 `run_id + plan_version + step_id + tool/version + canonical args digest` 生成稳定 invocation id。
- 新增 Frappe `Synora Workflow Tool Invocation` 账本，区分 `STARTED` 不确定窗口和 `SUCCEEDED` 可缓存结果。
- 在 Gateway dispatch 前重检工具、Run scope 和权限；完成缓存返回前仍执行当前授权校验。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已授权 Agent 全程接管 Phase 5；不写学习笔记。

### 3. 本轮任务完成度

- P5.4 幂等基础完成：旧的 Deterministic/AGENT 请求保持兼容，只有完整四元 metadata 才启用账本。
- `STARTED` 行在 Handler dispatch 前提交；成功响应完成账本后才返回；异常窗口保持 `STARTED`，禁止自动重放。
- 业务代码、Runtime Gateway adapter、Frappe DocType 和定向测试已形成一个可回滚小步。

### 4. 测试方式

- Runtime Gateway HTTP contract pytest。
- Frappe integration 中 completed cache、STARTED 不重放、digest conflict 和真实只读 Handler 路径。
- 相关 Ruff check/format 和 `git diff --check`。

### 5. 测试结果

- Runtime `test_gateway.py` 定向测试：通过（含 invocation metadata 发送断言）。
- Frappe integration 当前累计 `81 tests OK`，含调用账本场景。
- 尚未在本轮重复阶段全量命令；n8n、浏览器和独立对抗审查仍未运行。

### 6. 用户如何进行手工测试

1. 用合法 capability 和完整 metadata 发起一次只读 Gateway 调用，确认账本先出现 `STARTED`、成功后变为 `SUCCEEDED`。
2. 用完全相同 invocation 重试，确认返回缓存、不会再次进入 ERP Handler。
3. 修改同一 key 的参数，确认 args digest conflict；人为留下 `STARTED` 后重试，确认只得到不确定窗口提示。

### 7. 风险预估与规避

- invocation ledger 是幂等证据，不是 ERP 事实；每次读取仍由 Frappe 权限、scope 和 capability 重检。
- 成功前进程崩溃保留 `STARTED`，宁可人工检查也不盲目重读，避免重复审计和时间漂移。
- DocType 只读权限归 System Manager；受控完成字段由 Gateway completion flag 守卫，用户不能直接改状态。

### 8. 下轮建议

- 单独提交 Frappe PLAN_EXECUTE API、24 小时 expiry、Trace 和状态接入，再提交前端。
- 增加 cancel/expiry 与在途工具竞态的真实证据，并完成全量回归。

### 9. 本轮任务大白话讲解

每一步读 ERP 前先拿一张“调用收据”：收据由 Run、计划版本、步骤、工具和参数摘要共同决定。收据已经成功，就拿原结果；收据只写了开始却没有结果，就不能假装没发生过再调用一次。这样 Runtime 重启不会把同一只读步骤重复送进 ERP。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么 digest 必须规范化？答：JSON key 排序、无空格和禁止非有限数字，才能让语义相同的参数产生相同 key。
- 为什么 `STARTED` 不重放？答：跨进程调用存在“ERP 已完成、Runtime 未收到”的不确定窗口，账本必须保守地要求人工恢复。
- 为什么缓存命中还要重检权限？答：Run 可能被取消、过期、禁用或 scope 变化，历史成功不能延长当前授权。

## 2026-08-27 13:11 · Phase 5 · 第 2 轮 · P5.3 崩溃恢复不确定窗口修复

### 1. 本轮任务列表

- 修复 Runtime 重启后将未确认的工具调用错误标成成功的状态漏洞。
- 将 crash recovery 的用户动作收紧为 `inspect`；真实重试必须先形成新 plan version/new invocation。
- 让恢复后的终态保存释放 SQLite lease，避免失败确认短暂占用恢复锁。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已授权 Agent 全程接管 Phase 5；不写学习笔记。

### 3. 本轮任务完成度

- P5.3 安全修复完成：`WorkflowEngine.resume` 对 `crash_recovered` 只接受 `inspect`，将不确定步骤置为 `FAILED`，不生成 observation digest，也不自动 replay。
- `WorkflowRuntime.recover` 只展示安全的 inspect 选择；新的重试需要后续 planner 生成新版本。
- 业务代码 2 个文件、测试 1 个文件、日志 1 个文件；本轮仅准备一个恢复语义提交。

### 4. 测试方式

- Runtime workflow contract targeted pytest。
- 新增人工恢复断言：inspect 后步骤必须 FAILED，且 observation digest 为空。

### 5. 测试结果

- `PYTHONPATH=services/agent_runtime/src UV_CACHE_DIR=/tmp/synora-uv-cache uv run --offline --no-sync --python 3.14 pytest services/agent_runtime/tests/test_workflow_contracts.py -q`：`16 passed`。
- 尚未在本轮重跑全量 unit/integration；阶段出口仍需重新执行。

### 6. 用户如何进行手工测试

1. 让 Runtime 在工具 step 已保存为 `RUNNING` 后退出并重启。
2. 调 `/workflow/status`，确认出现 `INTERRUPTED`、`crash_recovered=true` 和唯一 `inspect` 选项。
3. 提交当前 revision 的 `inspect`，确认工作流为 `FAILED`，没有伪造的完成 digest，也没有再次进入 Gateway Handler。

### 7. 风险预估与规避

- 主要风险是响应已到达 ERP 但 checkpoint 未保存；修复后不猜结果、不重放，转人工检查终态。新计划版本和 invocation 账本仍是后续安全重试的唯一入口。
- 该修复不改变 Frappe Run 权威、capability 生命周期或 ERP 写入边界。

### 8. 下轮建议

- 分别提交 Gateway invocation ledger、Frappe workflow API、UI/lab 工件和阶段文档，保持小步可回滚。
- 在阶段出口补充真实 Runtime kill/restart、浏览器和 n8n 证据；缺失时保持 BLOCKED。

### 9. 本轮任务大白话讲解

如果 Runtime 调 ERP 后刚好崩溃，我们无法知道“读到了但没来得及记账”还是“根本没读到”。因此恢复页面不能擅自说成功，也不能直接再读一次；现在只能让用户确认检查，后续若要重试必须生成一张新计划和新调用凭证。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么不自动重试？答：外部读取虽是只读，重复调用仍会造成审计重复和事实时间漂移；`STARTED` 无结果是显式不确定窗口。
- 为什么新 plan version 才能重试？答：用新 invocation key 把“人工确认后的新意图”与旧不确定调用分开，禁止悄悄重放旧步骤。
- 为什么 inspect 要落成 FAILED？答：失败表示“无法证明结果”，不是 ERP 业务失败；它让用户看到安全停止原因，并阻止后续步骤消费未知观察。

## 2026-08-27 12:15 · Phase 5 · 第 1 轮 · P5.0 基线、P5.1 契约与 P5.2 框架 Spike

### 1. 本轮任务列表

- 核对 Phase 4 最终证据、当前 HEAD、工作区和 Harness 漂移。
- 修复启动基线中已存在的 `service.py` 格式与类型错误。
- 更新 Phase 5 所需的 Agent Run 状态边和 checkpoint/Run 分离语义。
- 实现严格 `PlanStep`、DAG、WorkflowState、Clarification、replan 和版本边界。
- 实现手写固定 Workflow/ReAct/Plan-and-Execute 共用协议、P5-G01～G10 数据集和评测入口。
- 实现 SQLite WAL checkpoint 基础、Runtime workflow start/resume/cancel/status 路由及定向测试。
- 完成 Python 3.14 下 LangGraph/SQLite checkpointer 临时兼容性 Spike，仅锁定可选 lab 依赖。

### 2. 用户 Assignment 与导师引导

本轮无 Assignment。用户已明确授权 Agent 全程接管 Phase 5 代码编写，因此不创建 Assignment、不等待用户练习，也不写学习笔记；本文件只记录业务开发结果、真实验证和风险。

### 3. 本轮任务完成度

- P5.0：基线事实已核对；`docs/PLAN.md` 已同步 Phase 4 `COMPLETED / PASS` 和 Phase 5 `IN_PROGRESS / P5.1`。Harness 清单有效，但既有来源指纹漂移仍需按管理流程收敛，未手工改 `.harness/`。
- P5.1：完成。新增严格契约、DAG/状态检查、澄清一次性恢复、completed step 不可变和 replan 版本递增。
- P5.2：完成当前增量。手写三种引擎共用 `WorkflowState`/`WorkflowResult`；新增 10 个固定场景；LangGraph 1.2.11 + `langgraph-checkpoint-sqlite` 3.1.1 在 Python 3.14 临时环境通过 import/interrupt/resume/SQLite 验证，保持 `workflow-lab` 可选组。
- P5.3/P5.4/P5.5：未完成，下一轮继续接入 Frappe、调用账本、完整 UI/E2E 和阶段出口证据。
- 业务代码/测试/文档改动：本轮新增 Runtime 工作流模块、测试和数据集，更新状态契约/计划/依赖；未改 `.env*`、README、上游 Frappe/ERPNext 或 `.harness/`。

### 4. 测试方式

- Phase 4 启动基线：`make format-check`、`make lint`、`make type`、`make unit`、Harness 结构检查和 `git diff --check`；uv 默认缓存路径受权限限制，改用 `UV_CACHE_DIR=/tmp/synora-uv-cache` 重跑。
- P5.1 定向：`pytest services/agent_runtime/tests/test_workflow_contracts.py`。
- P5.2 定向：`pytest services/agent_runtime/tests/test_workflow_engines.py`。
- Runtime API 定向：`pytest services/agent_runtime/tests/test_workflow_api.py services/agent_runtime/tests/test_health.py`。
- 静态检查：相关 Runtime 模块与测试的 Ruff format/check、mypy。
- LangGraph Spike：`langgraph==1.2.11`、`langgraph-checkpoint-sqlite==3.1.1`，Python 3.14，临时 `/tmp` 虚拟环境。

### 5. 测试结果

- 初始 uv 缓存路径的四个基线命令因 `/Users/qilong.lu/.cache/uv` 权限退出码 2；这是环境问题，不改写为代码通过。
- 缓存切换后 `make unit`：200 passed；format/lint/type 暴露 `service.py` 既有一处格式和类型错误，已最小修复并定向复验通过。
- P5.1：12 passed；P5.2：3 passed；Runtime API/health：5 passed（FastAPI `on_event` 产生弃用警告，行为未失败）。
- LangGraph import/interrupt/resume/SQLite Spike：`PASS`；无项目 lockfile/Runtime 主依赖变更之外的环境残留。
- 尚未运行 Frappe integration、浏览器、n8n audit、Runtime kill/restart E2E、阶段全量命令和独立对抗审查。

### 6. 用户如何进行手工测试

1. 设置独立的 `SYNORA_RUNTIME_TOKEN` 和临时 `SYNORA_WORKFLOW_DB_PATH`，用 Runtime `/workflow/start` 提交只读 `CLARIFICATION` + `material_request.open` 计划。
2. 确认响应为 `INTERRUPTED`，记录 `revision`/`interrupt_id`；关闭并重新启动 Runtime 后用原 revision 重复提交，应该得到冲突。
3. 用当前 revision 和合法答案调用 `/workflow/resume`，确认工作流继续到 `SUCCEEDED`，再调用 `/workflow/status` 查看相同 checkpoint。
4. 用另一个 revision 调 `/workflow/cancel`，确认状态为 `CANCELLED`，之后不能恢复。

### 7. 风险预估与规避

- checkpoint 被误当 ERP 权威：当前只保存严格 orchestration state，Runtime 没有 Frappe 状态写权限；P5.3 仍需每次 Gateway/Frappe 重检。
- LangGraph 版本漂移：只锁定经过 Python 3.14 Spike 的精确版本和 lockfile，保持 lab-only；未通过安全/恢复矩阵前不进 Runtime。
- SQLite 单实例边界：启用 WAL/foreign keys/busy timeout/0600，但尚未声称生产扩展能力。
- 本轮未触碰用户维护文件和秘密；真实调用账本、cancel/expiry 先 CAS 再通知 Runtime 的顺序留给 P5.3/P5.4。

### 8. 下轮建议

- 增加 `Synora Agent Run.workflow_expires_at` 和只读 `Synora Workflow Tool Invocation` DocType/migration。
- 接入 `analyze_run` 的 PLAN_EXECUTE 分支、`resume_run`/`get_run_workflow` API 和 Runtime 调用验证。
- 先写 cancel/expiry、ledger digest 冲突和 crash 不重放失败用例，再实现最小修复。

### 9. 本轮任务大白话讲解

以前 Agent 请求结束，运行进度就只在内存里；现在给每个只读长任务一张严格的“进度卡”：卡上有版本、步骤顺序、依赖、完成摘要和等待用户回答的位置。Runtime 重启只能从这张卡的安全点继续，卡里没有 ERP 事实、用户密码或 capability。Frappe 仍是 Run、权限和有效期的唯一权威，下一轮会把每次只读工具调用也登记到 Frappe 账本，保证崩溃恢复不重复进入 ERP Handler。

本轮最重要的文件：

- `services/agent_runtime/src/agent_runtime/workflow/contracts.py`：严格计划/状态/澄清契约；
- `services/agent_runtime/src/agent_runtime/workflow/checkpoint.py`：SQLite checkpoint/CAS/lease；
- `services/agent_runtime/src/agent_runtime/workflow/runtime.py`：Runtime 工作流入口和恢复语义。

### 10. 面试官可能追问的技术细节以及如何回答

- 为什么 checkpoint 不能代表 ERP 状态？答：它只记录 Runtime 是否走到某个编排节点，工具事实和授权每次都要回到 Frappe/Gateway 重查。
- 为什么 completed step 要不可变？答：恢复和重规划只能替换尚未开始的节点，否则同一 run 的证据会被悄悄改写，无法判断是否重复调用。
- 为什么 LangGraph 先留在 lab？答：Python 3.14 Spike 已通过，但主线采用还必须通过与手写引擎同任务的安全、恢复、不重放、Trace 和运维比较。
